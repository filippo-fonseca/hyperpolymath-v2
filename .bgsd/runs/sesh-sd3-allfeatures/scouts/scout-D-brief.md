You are Scout D (recon, STRICTLY READ-ONLY — you must not edit, write, create, or delete any file; no git commands that mutate state) working in /Users/filippofonseca/Developer/Projects/hyperpolymath-v2-sd-restyle (branch bgsd/sd-all-features).

Context: app just shipped a Spacedrive-register redesign (docs/DESIGN-SYSTEM.md; dimensional icons in apps/web/components/ui/icons; Space Grotesk app-wide; EB Garamond only via components/ui/Logotype.tsx). Three areas to scout (be very thorough):

1. LANDING PAGE: The public landing (apps/web/app/page.tsx? apps/web/components/landing/*) is reportedly still the OLD style. Map its full composition (hero, sections, header, footer, pricing?, screenshots?), which components carry old-register styling (glass, gradients, serif, old accents), size of the lift, and which parts already got touched (LandingHeader was recently edited). Note: a previous PR described the landing hero as a "theatrical near-black plate with canonical ambient + brand orb" — reconcile that with what's actually there now.

2. THE FLOATING JARVIS ORB: The user says there's a floating "Jarvis orb" in the app and wants a small kiwi BIRD (the brand logo) inside it. Find: which component renders the floating orb (JarvisSidePanel? a floating action button? presence orb on lifeos? check apps/web/components/shell/* and apps/web/components/jarvis/*), how it's drawn (CSS? SVG? canvas?), and where the kiwi bird logo asset lives (search apps/web/public for svg/png logos, favicon, components rendering a bird mark, Logotype.tsx, Wordmark.tsx). Report exact file paths + how the orb is layered/animated.

3. MOTION + SOUND INFRA: What animation tooling exists (is `motion`/`motion/react` installed? check apps/web/package.json; existing keyframes/utilities in apps/web/app/globals.css; transition conventions in docs/DESIGN-SYSTEM.md incl. the "zero-jank motion law"). Any EXISTING sound/audio infrastructure (use-sound, howler, AudioContext usage, sound files under public/, the voice/TTS pipeline components — note if audio playback utilities exist that a UI sound-effects layer could reuse). Also check reduced-motion handling conventions.

Your final response IS the deliverable and is captured to a file. Output ONLY the compact markdown report in three sections with file paths, per-section "state + recommended approach + estimated lift". No preamble, under ~120 lines, no file dumps.
