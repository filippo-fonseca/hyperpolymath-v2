```markdown
# Scout D — Recon Report (branch bgsd/sd-all-features)

## 1. LANDING PAGE
**Entry:** `apps/web/app/page.tsx` → renders `<LandingPage/>` only when logged OUT (logged in → `redirect("/lifeos")`).
**Composition** (`components/landing/LandingPage.tsx`): `CursorSpotlight` (cyan) → `LandingHeader` + `LandingSideNav` → main sections: §01 `ThesisSection` (hero) · §02 `BioSection` · §03 `MeetKiwiSection` · §04 `JarvisDemo` (+`JarvisDemoButton`,`VoiceInputCard`,`HeroJarvisLine`) · §05 `PrimitivesTable` · §06 `EngineSection` · §07 `ChoiceSection` · §08 `BuildLog` → `LandingFooter`. Also imported: `SurfaceSection`, `MCPSection`, `FrameworkSection`, `DiagramBannerCard`, `SectionDivider/Eyebrow`, `LifeosCanvasPreview`, `WaitlistForm`. No pricing; "screenshots" = `LifeosCanvasPreview` + `lifeos-hero.png`.
**Hero is ALREADY restyled** (`ThesisSection.tsx`): `.dark` near-black plate (`radial-gradient #0d1017→#060709`), `AmbientGlow intensity="bold"`, ONE `FocalOrb size={176}`, forces `--sd-accent: var(--hud-cyan)`, EB-Garamond-flavored `font-serif` headline with heavy drop-shadow, pill CTAs, `motion`+`useReducedMotion` fade-up. This IS the "theatrical near-black plate + canonical ambient + brand orb" from the prior PR — **done, not old**. `LandingHeader` also lightly touched (only 2 register markers).
**What's still old (the lift):** the 14 body sections. Signal = **78 `font-serif` usages** across landing vs the design-system rule (Space Grotesk app-wide, EB Garamond only via `Logotype`). NOTE: `--font-serif` already resolves to Space Grotesk in `globals.css:33`, so text renders Grotesk *already* — the real gap is these sections were composed for editorial-serif rhythm and **consume none of the new register**: no `sd-*` tokens/utilities, no dimensional `components/ui/icons`, no `entity-card`/`explorer` primitives, no two-tier selection. Almost no glass/gradient (backdrop-blur×1, bg-gradient×0) — so it's a token/composition migration, not a glass-strip. Heaviest: `WaitlistForm`, `SurfaceSection`, `PrimitivesTable`, `BioSection`, `MeetKiwiSection`.
**State:** hero + header done; ~14 sections on old register. **Approach:** section-by-section reskin onto `sd-*` tokens + dimensional icons, mirroring the ThesisSection pass; keep copy verbatim. **Lift: MEDIUM-LARGE** (14 files, mostly mechanical since font/token plumbing already resolves).

## 2. FLOATING JARVIS ORB
Two distinct "orbs" — clarify which the user means:
- **The floating HUD (most likely "floating orb"):** `components/voice/FloatingJarvisStatus.tsx`, mounted `app/(app)/layout.tsx:108`. `fixed bottom-4 right-4 z-50`, a `rounded-full` cyan pill (not a sphere) showing a **Lucide icon** (`Mic`/`Radio`/`Loader2`/`Volume2`) + label; cyan `--hud-cyan` border/glow, `jarvis-pulse`, and a wake-burst radial ring. Only visible while recording/thinking/speaking. → Put a kiwi bird "inside it" by swapping the `Icon` const for `KiwiIcon` (trivial).
- **The glossy presence sphere:** `components/ui/ambient/FocalOrb.tsx` — pure CSS/SVG cyan sphere (layered radial gradients, rim + specular, under-glow; **no canvas**), 8s bob, `useTabHidden` pause + `prefers-reduced-motion` guard, `aria-hidden`+`pointer-events-none`. Rendered in `LifeOsHero.tsx:241` (36px "presence lamp") and the landing hero. Decorative, **no icon slot** — a bird needs a new absolutely-centered layer above the sphere.
**Kiwi bird asset:** `apps/web/public/icons/kiwi-bird.svg` (24×24, single filled path) + `components/shared/KiwiIcon.tsx` (Lucide-compatible React wrapper, `currentColor`, size prop). Drop-in ready.
**State:** both orbs exist; neither shows the bird. **Approach:** if HUD pill → replace `Icon` with `KiwiIcon` + tune size; if presence sphere → add centered `<KiwiIcon>` layer inside `.sd-orb` (white/currentColor over cyan). **Lift: SMALL** (single-component edit either way).

## 3. MOTION + SOUND INFRA
**Motion:** `motion` **12.38.0** installed (`package.json`), used as `motion/react` (`ThesisSection` imports `motion`,`useReducedMotion`). `globals.css` holds a large keyframe library: `hud-*` (corner/focus-breathe, thinking-sweep, streaming-caret, submit-ignite/scan, scan-reveal, receipt-trace/shimmer, edge-tick, undo-wipe, error-glitch, core-rotate/breathe), plus `shimmer`, `collapsible-*`, `explorer-*`, `jarvis-pulse`, `jarvis-wake-burst`. Ease tokens `--ease-out-quart / -in-out-circ / -out-back / -in-fast`. **Zero-jank law** (DESIGN-SYSTEM §14): animate only opacity/transform/filter, 120–150ms micro (80ms tab, color-only), `useReducedMotion`-guarded, never on first paint, no hover-scale.
**Reduced-motion:** JS via `useReducedMotion()`; CSS blanket `@media (prefers-reduced-motion: reduce)` disabling keyframes at `globals.css:651`; `FocalOrb`/`AmbientGlow` self-guard + `useTabHidden` pause.
**Sound:** a reusable UI-SFX layer already exists — `lib/ui/play-send.ts`, `play-reply.ts`, `play-pop.ts` are singleton `new Audio()` chime players for `/message-sent.mp3`, `/jarvis-reply.mp3`, `/pop.mp3` (all in `public/`). Full TTS/voice audio stack under `lib/voice/`: `audio-context.ts` (shared, gesture-unlockable `AudioContext` singleton), `audio-queue.ts`, `use-tts-player.ts`, `turn-playback-controller.ts`, `encode-wav.ts`, `sentence-splitter.ts`; VAD assets in `public/voice/`.
**State:** both infra layers are mature and idiomatic. **Approach:** a UI sound-effects layer should copy the `lib/ui/play-*.ts` singleton-`Audio` pattern (autoplay-safe, no AudioContext gesture needed for simple cues) and add new mp3s to `public/`; new motion should reuse the `hud-*` keyframes + ease tokens and stay inside the zero-jank law. **Lift: SMALL** to extend (patterns exist; near-zero net-new infra).
```
