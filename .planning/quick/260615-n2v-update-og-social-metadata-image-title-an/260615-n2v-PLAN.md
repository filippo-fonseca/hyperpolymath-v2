---
quick_id: 260615-n2v
slug: update-og-social-metadata-image-title-an
date: 2026-06-15
status: planned
---

# Quick Task 260615-n2v: Update OG/social metadata image, title, and description

## Goal

Make the site's social share card use the README hero artwork, and align the
share title + description with the brand pitch. Live in production.

## Tasks

### 1. Swap the OG/Twitter card image to the README hero

- **Files:** `apps/web/app/opengraph-image.png` (new), `apps/web/app/twitter-image.png` (new), `apps/web/app/opengraph-image.alt.txt` (new), `apps/web/app/twitter-image.alt.txt` (new), `apps/web/app/opengraph-image.tsx` (delete), `apps/web/app/twitter-image.tsx` (delete)
- **Action:** Replace the dynamically-generated `next/og` ImageResponse routes with static PNGs copied from `.github/assets/readme-hero.png` (the README hero). Add matching `.alt.txt` files so the previous `alt` text survives. Next.js file conventions auto-attach these and emit `og:image` / `twitter:image` with correct dimensions and type.
- **Verify:** `next build` succeeds; no dangling imports of the deleted routes.
- **Done:** Static `opengraph-image.png` + `twitter-image.png` present; `.tsx` routes gone; `lib/og/fonts` retained (still used by `apple-icon.tsx`, `api/branding/asset/route.tsx`, `lib/branding/svg.tsx`).

### 2. Update title + description metadata

- **Files:** `apps/web/app/layout.tsx`
- **Action:** Set `metadata.title.default`, `openGraph.title`, `twitter.title` to "Hyperpolymath | A personal life-OS for people who refuse to specialize." (keep the `%s · Hyperpolymath` template). Set the three descriptions to a blend of the brand lines that also flags open source: "I brought back the Renaissance Human, and gave them JARVIS from Tony Stark, all in one. A platform and a framework, fully open source. Use mine, or build your own."
- **Verify:** Typecheck clean (pre-existing jarvis-tts test errors excepted); copy has no em/en dashes and no comma splices.
- **Done:** All three titles + three descriptions updated.

## Notes

- Done on a fresh branch `chore/og-social-metadata` off `origin/main` (not the
  current `feat/kiwi-autodev-worker`) so prod gets a clean, isolated commit.
- Push strategy confirmed with user before any push.
