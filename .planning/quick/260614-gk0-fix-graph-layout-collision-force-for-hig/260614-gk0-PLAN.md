---
phase: quick-260614-gk0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/app/(app)/graph/GraphExplorer.tsx
autonomous: false
requirements: [GRAPH-LAYOUT-FIX]

must_haves:
  truths:
    - "Clicking an Area with many Projects (e.g. Yale, ~20+ Projects) spreads its children into a clean radial layout — no overlapping/crumpled ring"
    - "Edges are clearly visible against the #0a0c10 canvas at default zoom"
    - "pnpm --filter web typecheck passes"
  artifacts:
    - path: "apps/web/app/(app)/graph/GraphExplorer.tsx"
      provides: "Collision/charge/link force config on fgRef + brighter edge accessors"
      contains: "forceCollide"
    - path: "apps/web/package.json"
      provides: "d3-force dependency + @types/d3-force devDependency"
      contains: "d3-force"
  key_links:
    - from: "apps/web/app/(app)/graph/GraphExplorer.tsx"
      to: "d3-force"
      via: 'import { forceCollide } from "d3-force"'
      pattern: "from \"d3-force\""
    - from: "GraphExplorer useEffect"
      to: "fgRef.current.d3Force"
      via: "imperative force config keyed on graphData/dims"
      pattern: "d3Force\\('collide'"
---

<objective>
Fix the /graph force-directed layout so high-degree nodes no longer crumple their children into an overlapping ring, and make relational edges clearly visible against the dark canvas.

Purpose: A node like Area "Yale" with ~20+ child Projects currently collapses its children into a tight overlapping cluster because the layout runs only the default charge + link forces — there is NO collision force. Edges are also nearly invisible (base linkColor `rgba(148,163,184,0.18)`, base linkWidth `0.5`).

Output:
- `d3-force` added as a dependency (+ `@types/d3-force` devDep) to `apps/web` (force-graph bundles d3-force but does not re-export it, so it is not resolvable today).
- Imperative collision/charge/link force configuration on the existing `fgRef`, run once the ref exists, keyed on `graphData`/`dims`, followed by a simulation reheat.
- Brighter base edge color/width while keeping the highlighted (selected-connected) branch readable above the new base.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@apps/web/app/(app)/graph/GraphExplorer.tsx
@apps/web/package.json

Key existing facts (from reading the file):
- `fgRef` is declared at ~line 114 with the `// biome-ignore lint/suspicious/noExplicitAny: force-graph ref has no exported type` convention — reuse that convention for any new untyped accessor args.
- `<ForceGraph2D>` is rendered at ~lines 300-331; `nodeRelSize={4}` is at ~line 326, so each node's render radius is `4 * Math.sqrt(n.val)`.
- A resize-driven `useEffect` already exists (~lines 119-127) populating `dims`. `graphData` is a memo (~lines 131-187).
- Base `linkColor` (~lines 317-321) and base `linkWidth` (~lines 323-325) are the edge-visibility targets. The highlighted branch (selected node connected) uses `rgba(255,255,255,0.55)` @ width `1.4`.
- The graph loads via `next/dynamic` with `ssr: false`, so all force config is client-only and safe.
- `pnpm-lock.yaml` lives at the monorepo root; `pnpm --filter web add` updates that root lockfile plus `apps/web/package.json`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add d3-force dep, configure collision/charge/link forces, brighten edges</name>
  <files>apps/web/package.json, pnpm-lock.yaml, apps/web/app/(app)/graph/GraphExplorer.tsx</files>
  <action>
PART A — dependency (run these exact commands from the repo root; they update apps/web/package.json + the root pnpm-lock.yaml):
- `pnpm --filter web add d3-force`
- `pnpm --filter web add -D @types/d3-force`

PART B — layout forces (GraphExplorer.tsx):
- Add `import { forceCollide } from "d3-force";` near the existing top-of-file imports (the `react`/`next/dynamic`/`next/link` group).
- Add a new `useEffect` keyed on `[graphData, dims]` that returns early when `fgRef.current` is null (client-only guard). Inside, configure forces imperatively on the existing engine via `fgRef.current`:
  - Collision (the core fix): `fgRef.current.d3Force("collide", forceCollide((n: any) => 4 * Math.sqrt(n.val) + 5));` — radius = nodeRelSize(4) * sqrt(node.val) + ~5px padding, deterministically preventing overlap for ANY high-degree node. Annotate the untyped arg with the same `// biome-ignore lint/suspicious/noExplicitAny: untyped accessor args` convention used elsewhere in the file.
  - Stronger charge: `fgRef.current.d3Force("charge")?.strength(-110);` (default ≈ -30; target band ~-90..-140 — this value is VISUAL and may need in-browser tuning).
  - Larger link distance: `fgRef.current.d3Force("link")?.distance(48);` (base ~40-60; VISUAL).
  - Reheat after configuring: `fgRef.current.d3ReheatSimulation();`.

PART C — edge visibility (existing accessors, ~lines 317-325):
- Base (non-highlighted) `linkColor`: `rgba(148,163,184,0.18)` → `rgba(148,163,184,0.40)` (same slate hue, higher opacity).
- Base `linkWidth`: `0.5` → `1.0`.
- Keep the highlighted (selected-connected) branch readable ABOVE the new base: keep `rgba(255,255,255,0.55)` (acceptable to nudge to `0.7`) and width `1.4` (acceptable to nudge to `1.6`). No directional particles, no other changes.

CONSTRAINTS: only touch the three files above. No data-pipeline changes, no dead code, no backwards-compat shims.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
  </verify>
  <done>
- `d3-force` present in apps/web/package.json dependencies; `@types/d3-force` in devDependencies; root pnpm-lock.yaml updated.
- GraphExplorer.tsx imports `forceCollide` from `d3-force` and configures `collide`/`charge`/`link` forces + `d3ReheatSimulation()` in a `useEffect` guarded against a null `fgRef`.
- Base edge accessors bumped (color 0.40, width 1.0); highlighted branch still distinct above the new base.
- `pnpm --filter web typecheck` passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Collision force (radius = 4·√val + 5px) plus stronger charge (-110) and longer link distance (48) on the /graph force engine, with a post-config reheat; brighter base edges (slate 0.40 @ width 1.0).</what-built>
  <how-to-verify>
This is a VISUAL change and cannot be verified headlessly. In the browser:
1. Run the dev server (`pnpm --filter web dev`) and open `/graph`.
2. Click the Area "Yale" (or any Area with ~20+ child Projects).
3. Confirm the children spread into a clean radial layout — NO overlapping/crumpled ring of project nodes.
4. Confirm edges are clearly visible against the `#0a0c10` canvas at default zoom (not the old near-invisible hairlines).
5. Confirm a selected node's connected edges are still visually distinct (brighter/thicker) above the new base.

If the spread looks too sparse/tight or edges are still faint, the charge (-110), link distance (48), and collision padding (+5) are the VISUAL knobs to tune — report back what you saw and they can be nudged within the noted bands (charge ~-90..-140, link distance ~40-60).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what still overlaps / what edge contrast is off.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web typecheck` passes (Task 1 automated gate).
- Browser check (Task 2): Yale's children spread radially without overlap; edges visible against #0a0c10; highlighted edges still distinct.
</verification>

<success_criteria>
- High-degree nodes (e.g. Yale) no longer crumple children into an overlapping ring — collision force enforces non-overlap for any node.
- Relational edges are clearly visible at default zoom against the dark canvas.
- Only GraphExplorer.tsx + apps/web/package.json + pnpm-lock.yaml changed; no dead code or shims.
- Typecheck green; visual behavior confirmed in-browser.
</success_criteria>

<output>
Create `.planning/quick/260614-gk0-fix-graph-layout-collision-force-for-hig/260614-gk0-SUMMARY.md` when done.
</output>
