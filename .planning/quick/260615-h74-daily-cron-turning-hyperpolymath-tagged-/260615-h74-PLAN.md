---
phase: quick-260615-h74
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/db/schema.ts
  - apps/web/drizzle/*.sql
  - apps/web/drizzle/meta/*
  - apps/web/lib/jarvis/issue-specer.ts
  - apps/web/app/api/cron/captures-to-issues/route.ts
  - vercel.json
  - apps/web/.env.local.example
  - CLAUDE.md
autonomous: true
requirements: [H74-SCHEMA, H74-CRON, H74-SPECER, H74-GITHUB, H74-WIRING, H74-SESSION]
user_setup:
  - service: github
    why: "Cron files issues against a public repo via a fine-grained PAT"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub > Settings > Developer settings > Fine-grained PATs (scope: Issues read/write on the target repo)"
      - name: GITHUB_ISSUE_USER_EMAIL
        source: "The owner-user's email as stored in the users table (e.g. the Supabase auth email)"
      - name: GITHUB_ISSUE_REPO
        source: "Optional. owner/repo slug; defaults to filippo-fonseca/hyperpolymath-v2"
  - service: database
    why: "The generated Drizzle migration must be applied by the user, never by the executor"
    dashboard_config:
      - task: "Apply the new migration to local and/or remote DB yourself (npm run db:migrate or via Supabase). The executor only generates the file."
        location: "apps/web (drizzle-kit) / Supabase dashboard"

must_haves:
  truths:
    - "captures table has nullable github_issue_url and github_evaluated_at columns; existing rows are untouched"
    - "cron_runs table exists with a UNIQUE (job_name, run_date) constraint backing a once-per-day lock"
    - "GET /api/cron/captures-to-issues rejects any request without a valid Bearer CRON_SECRET (401) before touching the DB, env user lookup, or Anthropic"
    - "A second invocation on the same UTC day returns {status:'already-ran'} and does no further work"
    - "Only the user whose email equals GITHUB_ISSUE_USER_EMAIL is ever processed; an unset env var or no match is a no-op"
    - "Captures with noExport===true are hard-skipped and never sent to Claude"
    - "At most 25 eligible captures are processed per run, oldest first"
    - "Each processed capture always gets github_evaluated_at set; github_issue_url is set only when an issue was created"
    - "Non-actionable or privacy-violating captures produce no GitHub issue"
    - "vercel.json has a captures-to-issues cron at '0 6 * * *' alongside the existing snapshot-context entry"
    - "CLAUDE.md has a session-start section instructing Claude Code to run gh issue list, rank, and ask before starting"
  artifacts:
    - path: "apps/web/lib/db/schema.ts"
      provides: "captures.githubIssueUrl, captures.githubEvaluatedAt columns + cronRuns table with unique (jobName, runDate)"
      contains: "cronRuns"
    - path: "apps/web/lib/jarvis/issue-specer.ts"
      provides: "specCaptureAsIssue(): one claude-sonnet-4-6 call returning Zod-validated {actionable,title,body,labels}"
      exports: ["specCaptureAsIssue"]
    - path: "apps/web/app/api/cron/captures-to-issues/route.ts"
      provides: "GET cron handler with 4 commented security layers, GitHub fetch creation, per-capture isolation, JSON summary"
      exports: ["GET", "runtime", "maxDuration"]
    - path: "vercel.json"
      provides: "captures-to-issues cron entry"
      contains: "captures-to-issues"
    - path: "CLAUDE.md"
      provides: "session-start gh issue list ranking block"
      contains: "gh issue list"
  key_links:
    - from: "apps/web/app/api/cron/captures-to-issues/route.ts"
      to: "apps/web/lib/jarvis/issue-specer.ts"
      via: "specCaptureAsIssue import + call per eligible capture"
      pattern: "specCaptureAsIssue"
    - from: "apps/web/app/api/cron/captures-to-issues/route.ts"
      to: "cron_runs unique constraint"
      via: "insert ... onConflictDoNothing returning to acquire the daily lock"
      pattern: "onConflictDoNothing"
    - from: "apps/web/app/api/cron/captures-to-issues/route.ts"
      to: "GitHub issues API"
      via: "native fetch POST with Bearer GITHUB_TOKEN"
      pattern: "api.github.com/repos"
---

<objective>
Build a daily Vercel cron that reads the owner-user's captures tagged "hyperpolymath", uses Claude to gate and spec the genuinely actionable platform ideas, and files them as GitHub issues on a public repo. Idempotent (once-per-day DB lock), bounded (25/run), and auditable (four clearly commented security layers).

Purpose: turn loose tagged captures into spec'd, publishable issues automatically without leaking anything private and without re-evaluating or double-filing.
Output: two new capture columns, a cron_runs idempotency table + migration FILE, an issue-spec'er helper, the cron route, vercel.json + env wiring, and a CLAUDE.md session-start ranking block.

LOCKED DECISIONS (do not revisit): two columns (not one); noExport hard skip; owner email via env var (never hardcoded); once-per-day DB lock via the (job_name, run_date) unique constraint; per-run cap N=25; native fetch (no Octokit / no new deps); schedule "0 6 * * *".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Schema + query conventions to mirror exactly
@apps/web/lib/db/schema.ts
@apps/web/lib/db/queries/captures.ts

# THE route pattern to mirror (runtime, maxDuration, Bearer auth, per-item isolation)
@apps/web/app/api/cron/snapshot-context/route.ts

# Anthropic client to reuse for the spec'er call
@apps/web/lib/jarvis/anthropic-client.ts

# Cron wiring
@vercel.json

# Drizzle config (schema glob, out dir); confirms `npm run db:generate` target
@apps/web/drizzle.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema columns + cron_runs table + migration FILE</name>
  <files>apps/web/lib/db/schema.ts, apps/web/drizzle/*.sql, apps/web/drizzle/meta/*</files>
  <action>
In apps/web/lib/db/schema.ts, extend the existing `captures` pgTable (the columns block, near the noExport line) with two NULLABLE columns, matching the file's column style and timestamp convention (`withTimezone: true`):
  - githubIssueUrl: text("github_issue_url"): nullable, no default. The created issue URL, or null.
  - githubEvaluatedAt: timestamp("github_evaluated_at", { withTimezone: true }): nullable, no default. Set whenever Claude has evaluated the capture (issued OR rejected) so it is never re-evaluated.
Add a short comment above both noting they are additive/nullable so existing rows are untouched, and that githubEvaluatedAt is the "already considered" marker.

Add a new pgTable `cronRuns` mapped to table name "cron_runs", mirroring the existing table style in this file (uuid pk via defaultRandom, snake_case column names, the import set already present at the top of the file (uuid/text/timestamp/date/uniqueIndex are all imported)):
  - id: uuid("id").primaryKey().defaultRandom()
  - jobName: text("job_name").notNull()
  - runDate: date("run_date").notNull()
  - startedAt: timestamp("started_at", { withTimezone: true }).defaultNow().notNull()
  - finishedAt: timestamp("finished_at", { withTimezone: true }), nullable
  - status: text("status"), nullable
  - table args callback returns: [ uniqueIndex("cron_runs_job_date_uniq").on(t.jobName, t.runDate) ]  // this UNIQUE constraint is the once-per-day lock
Place cronRuns near the other top-level tables (it is not capture-specific). Keep TypeScript strict-clean (no `any`).

Generate the migration FILE only with `npm run db:generate` (which runs `drizzle-kit generate`) from apps/web. Confirm exactly one new numbered .sql file appears under apps/web/drizzle/ and the meta snapshot updated.

CRITICAL SAFETY: DO NOT APPLY THE MIGRATION. Do NOT run `db:migrate`, `drizzle-kit migrate`, `drizzle-kit push`, or any command that touches a live database (local Docker, remote, or prod). The user applies migrations themselves. Generating the file requires DATABASE_URL to be readable by drizzle-kit for the schema diff; if drizzle-kit cannot run (missing DATABASE_URL or tool unavailable), STOP and tell the user rather than hand-writing SQL, unless drizzle-kit is genuinely unavailable, only then hand-write the .sql following the existing migrations' format and the meta journal.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -iE 'schema\.ts|cronRuns|github_(issue_url|evaluated_at)' | grep -c error | grep -qx 0 && echo SCHEMA_TYPECHECK_OK</automated>
  </verify>
  <done>
schema.ts compiles strict-clean with the two new nullable capture columns and the cronRuns table (unique on jobName+runDate). Exactly one new migration .sql file exists under apps/web/drizzle/ and the meta journal is updated. The migration was NEVER applied to any database (no db:migrate / push run). The new .sql adds the columns and table additively (no DROP/ALTER of existing data).
  </done>
</task>

<task type="auto">
  <name>Task 2: Issue spec'er helper (Claude gate + Zod validation)</name>
  <files>apps/web/lib/jarvis/issue-specer.ts</files>
  <action>
Create apps/web/lib/jarvis/issue-specer.ts exporting `async function specCaptureAsIssue(captureContent: string): Promise<{ actionable: boolean; title: string; body: string; labels: string[] }>`.

Reuse the existing client: import { getAnthropicClient, JARVIS_MODEL } from "@/lib/jarvis/anthropic-client". Make ONE non-streaming `getAnthropicClient().messages.create(...)` call per capture with model: JARVIS_MODEL and a tight max_tokens (e.g. 1024). Prefer Strict Tool Use: define a single tool (e.g. name "emit_issue_decision") whose input_schema is the JSON Schema for the result object, and pass tool_choice forcing that tool so the model must return structured input. Extract the tool_use block's `input` from the response content. (A plain JSON-in-text approach is acceptable as a fallback only if tool use is impractical; if so, parse defensively.)

Define a Zod schema (import { z } from "zod") for the result: actionable z.boolean(); title z.string(); body z.string(); labels z.array(z.string()). Parse the model output through it before returning. On Zod failure or a malformed/empty response, return a safe default of `{ actionable: false, title: "", body: "", labels: [] }` (fail closed; never emit an issue from unparseable output). Keep TypeScript strict (no `any`; type the tool input as unknown then z.parse).

The system/instruction prompt is a GATE with two HARD rules, stated explicitly:
  - ACTIONABILITY GATE: set actionable=true ONLY for explicit platform ideas, feature requests, bugs, or wish-list items for the Hyperpolymath app. Ranting, praise (e.g. "this app is great"), personal musings, or anything non-actionable -> actionable=false (the route then does NOTHING for it).
  - PRIVACY GATE: NEVER put anything sensitive or personal in title or body. The issue is published on a PUBLIC GitHub repo. If the capture contains sensitive or personal info, refuse by returning actionable=false. When actionable=true, the title is a concise issue title and the body is a clean spec (problem, proposed behavior, acceptance notes) written so it is safe to publish publicly.
  - LABELS: always include "kiwi-drafted". The model may add a small number of relevant labels (e.g. "bug", "enhancement"), but the route will ensure "kiwi-drafted" is present regardless.

Add a file header comment explaining this is the LLM gate for the captures-to-issues cron and that all output is Zod-validated and fail-closed.

NO em dashes or en dashes anywhere in the file (comments or prompt strings included): use commas, semicolons, colons, periods, or parentheses.
  </action>
  <verify>
    <automated>cd apps/web && test -f lib/jarvis/issue-specer.ts && grep -q 'specCaptureAsIssue' lib/jarvis/issue-specer.ts && grep -q 'kiwi-drafted' lib/jarvis/issue-specer.ts && grep -q 'from "zod"' lib/jarvis/issue-specer.ts && ! grep -nP '[\x{2013}\x{2014}]' lib/jarvis/issue-specer.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'issue-specer' | grep -c error | grep -qx 0 && echo SPECER_OK</automated>
  </verify>
  <done>
issue-specer.ts compiles strict-clean, exports specCaptureAsIssue, makes one claude-sonnet-4-6 call via getAnthropicClient, Zod-validates the result, fails closed to actionable:false on any parse/validation error, references "kiwi-drafted", and encodes both the actionability gate and the public-safe privacy gate in the prompt. No em/en dashes present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cron route with four audited security layers + GitHub create + isolation</name>
  <files>apps/web/app/api/cron/captures-to-issues/route.ts</files>
  <action>
Create apps/web/app/api/cron/captures-to-issues/route.ts. Mirror snapshot-context/route.ts conventions exactly:
  - export const runtime = "nodejs"; export const maxDuration = 60; export const dynamic = "force-dynamic";
  - Only a GET handler. Any other method file-exports are absent; additionally guard so a non-GET method returns 405 (export a small shared handler or rely on Next returning 405 for unexported verbs; if you add an explicit guard, comment it).
Import db from "@/lib/db", the needed tables (captures, capturesHashtags, hashtags, users, cronRuns) from "@/lib/db/schema", drizzle helpers (and, eq, isNull, asc, sql, inArray as needed) from "drizzle-orm", NextResponse from "next/server", timingSafeEqual from "node:crypto", and specCaptureAsIssue from "@/lib/jarvis/issue-specer".

Define named constants at the top: const PER_RUN_CAP = 25; const JOB_NAME = "captures-to-issues"; const HASHTAG_NAME = "hyperpolymath"; const DEFAULT_REPO = "filippo-fonseca/hyperpolymath-v2";

Implement the four layers IN THIS EXACT ORDER, each wrapped in a clearly delimited, commented block (e.g. a banner comment "// ===== LAYER 1: AUTH =====") so a human can audit each one in isolation:

  LAYER 1 AUTH (must run before any DB query, env user lookup, or Anthropic call):
    - Read process.env.CRON_SECRET. If unset/empty, FAIL CLOSED: return 401 (or 500); never proceed. Comment that missing secret never means open.
    - Read the Authorization header; expect "Bearer <CRON_SECRET>". Compare with a CONSTANT-TIME comparison using node:crypto timingSafeEqual. Guard against length mismatch first (timingSafeEqual throws on unequal Buffer lengths): if the provided token and the expected token differ in byte length, treat as unauthorized without calling timingSafeEqual; otherwise compare the two equal-length Buffers. On any failure return 401 immediately, before any other work.
    - If the request method is not GET, return 405.

  LAYER 2 ONCE-PER-DAY LOCK:
    - Compute today's date in UTC as a YYYY-MM-DD string (e.g. new Date().toISOString().slice(0,10)).
    - Atomically acquire the lock: db.insert(cronRuns).values({ jobName: JOB_NAME, runDate: today, status: "running" }).onConflictDoNothing({ target: [cronRuns.jobName, cronRuns.runDate] }).returning(). The (job_name, run_date) UNIQUE constraint IS the lock (race-safe at the DB, not an app-level check).
    - If returning() is empty (conflict = already ran today), return 200 { status: "already-ran" } and do NOTHING else. Capture the inserted row's id for finalization later.

  LAYER 3 PER-RUN CAP:
    - Will be enforced in the eligibility query via .limit(PER_RUN_CAP) ordered oldest-first; comment here that at most PER_RUN_CAP captures are processed per run and the rest wait for tomorrow.

  LAYER 4 ELIGIBILITY:
    - Resolve the target user AT RUNTIME: read process.env.GITHUB_ISSUE_USER_EMAIL. If unset/empty -> finalize the cron_runs row (status "skipped-no-user") and return a no-op summary; do NOT process any other user, ever. Look up users where email === that env value (limit 1). If no match -> same no-op (status "skipped-no-user"). NEVER hardcode the email (public/OSS repo).
    - Build the eligibility query for captures: WHERE captures.userId === targetUser.id AND captures.noExport !== true (HARD skip; use eq(captures.noExport, false) or NOT noExport so noExport rows are never even loaded/sent to Claude) AND captures.githubEvaluatedAt IS NULL, joined to the 'hyperpolymath' hashtag via capturesHashtags + hashtags using the canonical LOWERCASE name (eq(hashtags.name, HASHTAG_NAME) AND eq(hashtags.userId, targetUser.id), mirroring the innerJoin pattern in queries/captures.ts). Order oldest-first (asc(captures.createdAt)) and .limit(PER_RUN_CAP). Select at least id and content.

Processing loop (LAYER 4 results):
  - Read repo slug: const repo = process.env.GITHUB_ISSUE_REPO || DEFAULT_REPO; const token = process.env.GITHUB_TOKEN. If token is missing, finalize cron_runs (status "skipped-no-token") and return a no-op summary (do not call GitHub with no token).
  - For EACH eligible capture, wrap the whole unit in try/catch so one failure NEVER aborts the run:
      a. Call specCaptureAsIssue(capture.content).
      b. If actionable === true: POST https://api.github.com/repos/<repo>/issues via native fetch with headers { Authorization: `Bearer ${token}`, Accept: "application/vnd.github+json", "X-GitHub-Api-Version": "2022-11-28", "Content-Type": "application/json", "User-Agent": "hyperpolymath-cron" } and body JSON.stringify({ title, body, labels: ensure "kiwi-drafted" is in the labels array }). On a non-2xx response, throw so the catch records it as an error (still mark evaluated below). Parse the created issue's html_url from the response.
      c. PERSISTENCE: ALWAYS db.update(captures).set({ githubEvaluatedAt: <now> }).where(eq(captures.id, capture.id)) for BOTH issued and skipped captures (this prevents re-evaluation). Set githubIssueUrl to the created issue URL ONLY when an issue was actually created (combine into the same .set when issued). Use sql`now()` or a JS Date consistent with the column type.
      d. Tally: processed++, and either issued++ (with url) or skipped++ (actionable false). On catch: errors++ and log via console.error with the capture id; do NOT rethrow; still attempt to mark githubEvaluatedAt unless that mark itself failed.
  - FINALIZE: db.update(cronRuns).set({ finishedAt: <now>, status: errors > 0 ? "partial" : "ok" }).where(eq(cronRuns.id, <inserted id>)).
  - Return NextResponse.json({ processed, issued, skipped, errors }).

Add a file header comment summarizing the four layers and the locked decisions (cap=25, schedule 0 6 * * *, native fetch, email via env), pointing auditors to the LAYER banners.

Keep TypeScript strict (no `any`; type fetch JSON as unknown then narrow). NO em dashes or en dashes anywhere (comments included): use commas, semicolons, colons, periods, or parentheses.
  </action>
  <verify>
    <automated>cd apps/web && test -f app/api/cron/captures-to-issues/route.ts && grep -q 'runtime = "nodejs"' app/api/cron/captures-to-issues/route.ts && grep -q 'timingSafeEqual' app/api/cron/captures-to-issues/route.ts && grep -q 'onConflictDoNothing' app/api/cron/captures-to-issues/route.ts && grep -q 'PER_RUN_CAP = 25' app/api/cron/captures-to-issues/route.ts && grep -q 'GITHUB_ISSUE_USER_EMAIL' app/api/cron/captures-to-issues/route.ts && grep -q 'api.github.com/repos' app/api/cron/captures-to-issues/route.ts && grep -Eqc 'LAYER 1|LAYER 2|LAYER 3|LAYER 4' app/api/cron/captures-to-issues/route.ts && ! grep -nP '[\x{2013}\x{2014}]' app/api/cron/captures-to-issues/route.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i 'captures-to-issues' | grep -c error | grep -qx 0 && echo ROUTE_OK</automated>
  </verify>
  <done>
Route compiles strict-clean and mirrors snapshot-context conventions (nodejs runtime, maxDuration 60, force-dynamic). All four layers are present, ordered, and individually commented with auditable banners: (1) constant-time Bearer CRON_SECRET auth that fails closed and returns 401 before any DB/env/Anthropic work, 405 on non-GET; (2) once-per-day lock via onConflictDoNothing on the (job_name,run_date) unique constraint returning early with {status:'already-ran'} on conflict; (3) PER_RUN_CAP=25 oldest-first limit; (4) email-from-env user resolution (no hardcode, no-op if unset/unmatched), noExport hard skip, githubEvaluatedAt IS NULL, hyperpolymath hashtag join. Issues are created via native fetch (no new deps). Each capture is try/catch-isolated; githubEvaluatedAt is always set, githubIssueUrl only when issued; cron_runs is finalized; the handler returns {processed, issued, skipped, errors}. No em/en dashes present.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wiring (vercel.json cron, env example, CLAUDE.md session-start block)</name>
  <files>vercel.json, apps/web/.env.local.example, CLAUDE.md</files>
  <action>
1) vercel.json (repo root): add a SECOND entry to the existing "crons" array, keeping the snapshot-context entry intact: { "path": "/api/cron/captures-to-issues", "schedule": "0 6 * * *" }. Valid JSON, trailing comma rules respected.

2) apps/web/.env.local.example: append the new vars with brief one-line comments (NO em/en dashes; use periods/parentheses):
   - GITHUB_TOKEN= (fine-grained PAT with Issues read/write on the target repo; used by the captures-to-issues cron)
   - GITHUB_ISSUE_USER_EMAIL= (owner-user email; only this user's hyperpolymath captures are processed; never hardcoded)
   - GITHUB_ISSUE_REPO= (optional owner/repo slug; defaults to filippo-fonseca/hyperpolymath-v2)
   NOTE: this file currently sits in a directory the planner could not read due to permission settings; the executor must be granted access to edit it. If editing is genuinely blocked, surface that to the user rather than skipping.

3) CLAUDE.md (repo root): append a NEW prose section (not code). Title it clearly (e.g. "## Session start: triage open issues"). Instruct Claude Code that at the start of every session it should: run `gh issue list` to load the open issues, rank the pending issues by what is most tractable and highest-leverage to tackle next, briefly present that ranking to the user, and then ask whether to start on the top one or whether the user has their own prompt in mind. Write it in the project's voice. NO em dashes or en dashes anywhere: use commas, semicolons, colons, periods, or parentheses. Do not disturb existing sections (especially GSD Workflow Enforcement and Developer Profile).
  </action>
  <verify>
    <automated>grep -q 'captures-to-issues' vercel.json && grep -q 'snapshot-context' vercel.json && python3 -c "import json;json.load(open('vercel.json'))" && grep -q 'GITHUB_ISSUE_USER_EMAIL' apps/web/.env.local.example && grep -q 'gh issue list' CLAUDE.md && ! grep -nP '[\x{2013}\x{2014}]' CLAUDE.md && ! grep -nP '[\x{2013}\x{2014}]' apps/web/.env.local.example && echo WIRING_OK</automated>
  </verify>
  <done>
vercel.json is valid JSON containing both the existing snapshot-context cron and a new captures-to-issues cron at "0 6 * * *". apps/web/.env.local.example documents GITHUB_TOKEN, GITHUB_ISSUE_USER_EMAIL, and GITHUB_ISSUE_REPO with brief comments. CLAUDE.md has a new session-start section telling Claude Code to run gh issue list, rank by tractability/leverage, present the ranking, and ask before starting. No em/en dashes introduced in CLAUDE.md or the env example.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel cron scheduler -> GET route | Only Vercel (holding CRON_SECRET) should trigger the job; the public internet can hit the URL |
| capture content -> Claude -> public GitHub | Untrusted/personal capture text must not leak into a public issue |
| route -> GitHub API | A write-capable PAT crosses here; must not be logged or echoed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h74-01 | Spoofing | GET /api/cron/captures-to-issues | mitigate | Layer 1: constant-time Bearer CRON_SECRET check via node:crypto timingSafeEqual, length-guarded, fails closed (401) before any DB/env/Anthropic work; 405 on non-GET |
| T-h74-02 | Information disclosure | capture content -> public issue | mitigate | Layer + spec'er privacy gate: Claude refuses (actionable=false) on sensitive/personal content; body written to be public-safe; Zod-validated, fail-closed |
| T-h74-03 | Elevation/scope | target user resolution | mitigate | Layer 4: only the user matching GITHUB_ISSUE_USER_EMAIL is ever processed; unset/no-match is a no-op; email never hardcoded |
| T-h74-04 | Denial of service (spend) | Anthropic + GitHub calls | mitigate | Layer 2 once-per-day DB lock (unique constraint) + Layer 3 PER_RUN_CAP=25 bound per-run cost |
| T-h74-05 | Repudiation/double-action | re-evaluation + duplicate issues | mitigate | githubEvaluatedAt always set (issued or skipped) so captures are never re-evaluated; daily lock prevents same-day re-run |
| T-h74-06 | Tampering | secrets in logs | accept | console.error logs capture id + error only, never CRON_SECRET or GITHUB_TOKEN; low-value single-user app |
| T-h74-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies added (native fetch, existing zod/drizzle/anthropic SDK); nothing to audit |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` passes (whole web app strict-clean) after all four tasks.
- Exactly one new migration .sql under apps/web/drizzle/ and an updated meta journal; NO migration applied to any DB.
- `python3 -c "import json;json.load(open('vercel.json'))"` succeeds and both cron paths are present.
- `grep -nP '[\x{2013}\x{2014}]'` returns nothing for the new/edited route, spec'er, CLAUDE.md, and env example (no em/en dashes).
- Manual security audit by the user: each of the four LAYER banner blocks in the route is locatable and self-contained.
</verification>

<success_criteria>
- captures has nullable github_issue_url + github_evaluated_at; cron_runs exists with unique (job_name, run_date); migration file generated, never applied.
- specCaptureAsIssue makes one claude-sonnet-4-6 call, Zod-validates, fails closed, enforces the actionability + public-safe privacy gates, and tags "kiwi-drafted".
- GET /api/cron/captures-to-issues enforces the four ordered, commented layers; processes at most 25 oldest eligible captures for the env-resolved owner user only; hard-skips noExport; creates issues via native fetch; isolates per-capture failures; always marks evaluated; sets the URL only when issued; finalizes cron_runs; returns {processed, issued, skipped, errors}.
- vercel.json runs the job daily at 0 6 * * *; env example documents the three new vars; CLAUDE.md instructs the session-start gh issue triage + ranking + ask.
- TypeScript strict-clean throughout; no em/en dashes anywhere.
</success_criteria>

<output>
Create `.planning/quick/260615-h74-daily-cron-turning-hyperpolymath-tagged-/260615-h74-SUMMARY.md` when done.
</output>
