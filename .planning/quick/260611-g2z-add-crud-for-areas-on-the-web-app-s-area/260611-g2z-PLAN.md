---
quick_id: 260611-g2z
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/actions/areas.ts
  - apps/web/app/(app)/areas/page.tsx
  - apps/web/app/(app)/areas/[areaId]/page.tsx
  - apps/web/components/areas/AreasPageHeader.tsx
  - apps/web/components/areas/AreaCardMenu.tsx
  - apps/web/components/areas/AreaDetailHeader.tsx
  - apps/web/components/areas/AreaProjectCardMenu.tsx
  - apps/web/components/areas/MoveProjectDialog.tsx
autonomous: false

must_haves:
  truths:
    - "User can create a new Area from the /areas page without opening the sidebar"
    - "User can rename and edit the emoji of an Area from the /areas page (per-card menu)"
    - "User can delete an Area from /areas even if it has projects under it — child projects are reassigned to a 'No Area' bucket"
    - "User can create a new Project pre-scoped to the current Area from /areas/[areaId]"
    - "User can rename, edit, delete, and move a Project to a different Area from /areas/[areaId] (per-card menu)"
    - "A 'No Area' bucket card appears on /areas when any unassigned (sentinel-area) projects exist; clicking opens its detail page like any other area"
  artifacts:
    - path: "apps/web/app/actions/areas.ts"
      provides: "deleteArea reassigns child projects to a per-user 'No Area' sentinel area instead of blocking"
    - path: "apps/web/components/areas/AreasPageHeader.tsx"
      provides: "Standalone 'New Area' button + AreaCreateDialog wiring decoupled from Sidebar's optimistic dispatcher"
    - path: "apps/web/components/areas/AreaCardMenu.tsx"
      provides: "Per-area-card dropdown: Rename / Edit emoji / Delete (with reassign-to-No-Area confirmation copy)"
    - path: "apps/web/components/areas/AreaDetailHeader.tsx"
      provides: "Detail-page header with Edit (rename + emoji) action + 'New project in this area' button"
    - path: "apps/web/components/areas/AreaProjectCardMenu.tsx"
      provides: "Per-project-card dropdown on the area detail page: Rename / Edit / Move / Delete"
    - path: "apps/web/components/areas/MoveProjectDialog.tsx"
      provides: "Move-project-to-area picker reusing moveProjectToArea server action"
  key_links:
    - from: "apps/web/components/areas/AreaCardMenu.tsx"
      to: "apps/web/app/actions/areas.ts"
      via: "updateArea / deleteArea calls; router.refresh() on success (page is SSR, no Sidebar optimistic dispatcher in scope)"
      pattern: "(updateArea|deleteArea)\\("
    - from: "apps/web/components/areas/AreaProjectCardMenu.tsx"
      to: "apps/web/app/actions/projects.ts"
      via: "updateProject / deleteProject / moveProjectToArea via MoveProjectDialog"
      pattern: "(updateProject|deleteProject|moveProjectToArea)\\("
    - from: "apps/web/app/actions/areas.ts deleteArea"
      to: "apps/web/lib/db/schema.ts projects.areaId"
      via: "ensureNoAreaBucket(userId) → UPDATE projects SET area_id = <sentinel> WHERE area_id = <victim>"
      pattern: "ensureNoAreaBucket|No Area"
---

<objective>
Add full Areas-page and Area-detail-page CRUD that does NOT require the sidebar. This is a Web-UI-only quick task.

Purpose: Today the only way to create/rename/edit/delete Areas and Projects is via the sidebar's `AreaContextMenu` / `ProjectCreateDialog`. The /areas page and /areas/[areaId] page are read-only. Filippo wants both pages to be fully self-sufficient so he can manage the hierarchy without ever opening the sidebar.

Output:
- Areas page (/areas) gains a "New Area" button + per-card menu (Rename / Edit emoji / Delete).
- Area detail page (/areas/[areaId]) gains a header Edit + "New project in this area" button, plus per-project menu (Rename / Edit / Move to another area / Delete).
- `deleteArea` server action stops blocking on children and instead reassigns child projects to a per-user "No Area" sentinel row (locked decision #1).
- Tasks/captures already cascade their junction-table links on project delete (existing behavior satisfies locked decision #2 — verify and document; no code change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@apps/web/app/actions/areas.ts
@apps/web/app/actions/projects.ts
@apps/web/app/(app)/areas/page.tsx
@apps/web/app/(app)/areas/[areaId]/page.tsx
@apps/web/components/areas/AreaCreateDialog.tsx
@apps/web/components/areas/AreaContextMenu.tsx
@apps/web/components/areas/AreasTree.tsx
@apps/web/components/projects/ProjectCreateDialog.tsx
@apps/web/components/projects/ProjectHeader.tsx
@apps/web/lib/db/schema.ts

<interfaces>
<!-- Server actions already exported — DO NOT re-create. Wire UI to these. -->

From apps/web/app/actions/areas.ts:
- createArea({ id?, name, emoji? }) → { success, data: { id } } | { success: false, error }
- updateArea({ id, name?, emoji? }) → { success } | { success: false, error }
- archiveArea(id) / unarchiveArea(id)
- deleteArea(id) → currently BLOCKS if projectCount > 0 (line 138-143). This plan REWRITES it.
- getAreasForCurrentUser() → SidebarArea[] (auth-gated select)

From apps/web/app/actions/projects.ts:
- createProject({ id?, areaId, name, description?, icon?, bannerUrl?, startDate?, endDate?, isClass?, courseCode?, ... }) → { success, data: { id } } | { success: false, error }
- updateProject({ id, name?, description?, icon?, ...class fields }) → { success } | { success: false, error }
- archiveProject(id) / unarchiveProject(id)
- deleteProject(id) → DELETE FROM projects; junction tables (tasks_projects, captures_projects) ON DELETE CASCADE — tasks/captures themselves persist (matches locked decision #2)
- moveProjectToArea({ projectId, newAreaId }) → already exists, reused as-is

From apps/web/lib/db/schema.ts:
- areas: { id, userId, name, emoji nullable, orderIndex, archivedAt, ... } — NO color column. "Edit" = name + emoji.
- projects.areaId: uuid NOT NULL, onDelete: "restrict" — this is why deleteArea currently blocks. The fix is NOT a schema migration; we use a per-user sentinel "No Area" row.

From apps/web/components/projects/ProjectCreateDialog.tsx:
- export function ProjectCreateDialog({ open, onOpenChange, defaultAreaId, areas, graduationYear }) — ALREADY decoupled from sidebar. Pass `defaultAreaId={areaId}` and the area list to reuse on detail page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite deleteArea to reassign children to a per-user "No Area" sentinel + verify junction cascades</name>
  <files>apps/web/app/actions/areas.ts</files>
  <action>
Open `apps/web/app/actions/areas.ts`.

1) Add a private helper `ensureNoAreaBucket(userId: string): Promise<string>` that:
   - Selects the user's existing area where `name = 'No Area'` and `emoji IS NULL` (treat this combination as the sentinel signature).
   - If found, return its id.
   - Otherwise INSERT a new row: `{ userId, name: 'No Area', emoji: null, orderIndex: 9999, archivedAt: null }` and return the new id. Use a large orderIndex (9999) so the bucket sorts last in the sidebar tree without disrupting existing ordering.
   - Wrap in a single db.transaction to avoid race-on-double-delete.

2) REWRITE `deleteArea(id)`:
   - Auth check + uuid check as today.
   - Reject deletion of the sentinel itself: if the area's name === 'No Area' AND emoji is null AND there are no other 'No Area' rows for this user, return `{ success: false, error: "Can't delete the No Area bucket." }`. (Defense: prevents UX loop.)
   - Otherwise: count child projects. If > 0, call `ensureNoAreaBucket(userId)` to get the sentinel id, then `UPDATE projects SET area_id = <sentinel>, updated_at = now() WHERE area_id = <victim> AND user_id = <userId>`.
   - Then DELETE the victim area row.
   - Do ALL of the above (count → ensure bucket → reassign → delete) inside `db.transaction`. Return `{ success: true, data: null }`.

3) Remove the "Can't delete an area that has projects under it." early-return — it is replaced by the reassign path.

4) Add a JSDoc on `deleteArea` explaining: "Locked decision (Quick 260611-g2z #1): child projects are reassigned to a per-user 'No Area' bucket on delete, NOT blocked, NOT cascade-deleted. Tasks/captures linked to those projects survive untouched (junction tables already CASCADE their join rows on project delete per PROJ-04, but the projects themselves are not deleted here)."

5) Export `ensureNoAreaBucket` from the same file (used by the Areas page in Task 2 to label the sentinel as "No Area" in the UI without a separate lookup — actually, since the page already SELECTs all areas, we just need the sentinel to be queryable; export not strictly required, keep private unless Task 2 imports it).

6) Verify the locked-decision #2 path is satisfied by existing code:
   - Grep `apps/web/lib/db/schema.ts` for `tasks_projects` and `captures_projects` cascade clauses. They are `ON DELETE CASCADE` on the junction tables — tasks/captures rows themselves persist. No code change needed.
   - Add a comment block at the top of `deleteProject` reiterating: "Locked decision (Quick 260611-g2z #2): tasks/captures linked to this project lose ONLY their junction-table link; the task/capture row itself persists (acts as 'Unassigned' in the UI)."

Do NOT touch `createArea`, `updateArea`, `archiveArea`, `unarchiveArea`, `reorderAreas`, `getAreasForCurrentUser`.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; pnpm vitest run app/actions 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - `deleteArea` no longer returns the "Can't delete" error when projects exist.
    - On delete of an area with N projects, those N projects appear under a new (or existing) "No Area" area for the same user.
    - Re-running delete on a freshly-emptied area succeeds with no sentinel needed.
    - Typecheck green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CRUD UI to /areas page (New Area button + per-card menu)</name>
  <files>apps/web/app/(app)/areas/page.tsx, apps/web/components/areas/AreasPageHeader.tsx, apps/web/components/areas/AreaCardMenu.tsx, apps/web/components/areas/AreaCreateDialog.tsx</files>
  <action>
Goal: the /areas page becomes self-sufficient for area CRUD. It currently renders `AreasTree` (tree visualization, read-only) — do NOT replace the tree; ADD a header bar and per-area-node menu affordances. The tree component (`AreasTree`) is large (25KB) and reused; the cleanest move is to introduce two new small components and update the page wrapper.

1) **Refactor `AreaCreateDialog` to be sidebar-agnostic** (`apps/web/components/areas/AreaCreateDialog.tsx`):
   - Make the `addOptimisticArea` and `currentAreaCount` props OPTIONAL.
   - When `addOptimisticArea` is absent, skip the optimistic dispatch entirely and rely on a `router.refresh()` (Next.js) after successful create to re-fetch the SSR page.
   - Import `useRouter` from `next/navigation` and call `router.refresh()` after the toast success branch.
   - This preserves the sidebar's existing optimistic UX (still gets dispatcher passed) while allowing the /areas page to use the same dialog without a fake dispatcher.

2) **Create `apps/web/components/areas/AreasPageHeader.tsx`** (new client component):
   - Renders a right-aligned "New Area" button (use shadcn `Button` with default variant, lucide `Plus` icon).
   - Wraps `AreaCreateDialog` as the dialog target; manages local `open` state via `useState` (the dialog will be opened by clicking the button).
   - Props: `{ userId: string }`.
   - DOES NOT pass `addOptimisticArea` or `currentAreaCount` (the refactored dialog handles their absence).
   - Aesthetic: match existing /areas page header — `font-mono text-[11px] uppercase tracking-[0.14em]` style is used elsewhere; keep the button minimal (existing JARVIS × Notion restraint per locked decision #5).

3) **Create `apps/web/components/areas/AreaCardMenu.tsx`** (new client component):
   - A `⋯` dropdown identical in shape to `AreaContextMenu` but stripped of the sidebar optimistic-dispatcher coupling.
   - Items: **Rename**, **Edit emoji**, **Delete**.
   - "Rename" opens a small dialog with an `Input` (existing pattern from `AreaContextMenu`). Calls `updateArea({ id, name })`.
   - "Edit emoji" opens a dialog with an `Input` (maxLength 8) prefilled with current emoji, calls `updateArea({ id, emoji: trimmedOrNull })`. (Decision: combine name + emoji into ONE "Edit" dialog instead of two if it reads cleaner — implementer's discretion, but DEFAULT to one combined "Edit area" dialog with both fields, since the schema only has those two editable fields. Matches AreaCreateDialog's shape.)
   - "Delete" opens a confirmation dialog. The body copy MUST be:
     - "Delete this area? Any projects under it will be moved to the **No Area** bucket. Tasks and captures stay intact."
     - (Reflects locked decision #1 + #2 plainly.)
   - On Delete confirm: call `deleteArea(id)`, toast result, then `router.refresh()` on success.
   - Props: `{ areaId: string; areaName: string; areaEmoji: string | null }`.
   - DO NOT use `AreaOptimisticDispatch` — this menu lives on an SSR page; `router.refresh()` is the canonical "settle" path.
   - Reuse shadcn `DropdownMenu`, `Dialog`, `Button`, `Input`, `Label`, `sonner` toast — same imports as `AreaContextMenu.tsx`.

4) **Update `apps/web/app/(app)/areas/page.tsx`**:
   - Render `<AreasPageHeader userId={user.id} />` in the right column of the existing `<header>` block (or just above the AreasTree if simpler — implementer's discretion; restraint).
   - Pass an additional optional prop to `AreasTree` if needed to surface a per-node `⋯` menu — but DO NOT modify `AreasTree.tsx` heavily. Instead: place per-area `AreaCardMenu` controls in the page itself via a simple horizontal list under the tree, e.g. a small "Manage areas" section showing `{emoji} {name}  ⋯` rows. This avoids invasive surgery to the 25KB tree component. Render this list using the same `areas` array already in scope. Style with the existing `font-mono text-[11px] uppercase tracking-[0.14em] text-[var(--ink-muted)]` heading + a simple `<ul>` of rows matching the existing serif/edge token palette.
   - The "Manage" section MUST also include the "No Area" sentinel if present so it can be visually distinguished (small italic label "(auto-created bucket)" beside it) — but its menu hides Delete (handled by the server-action defense already).

5) Aesthetic constraints (locked decision #5): no new visual language. Reuse `var(--ink)`, `var(--ink-muted)`, `var(--edge)`, `var(--surface)`, `font-serif`, `font-mono`. No new icons beyond existing lucide imports already present in the components folder.

Do NOT touch `AreaContextMenu.tsx` (sidebar still uses it). Do NOT modify the global `Sidebar.tsx`.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; pnpm vitest run components/areas 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - /areas page renders a "New Area" button that opens AreaCreateDialog and creates an area without sidebar interaction.
    - "Manage areas" section under the tree shows each area with a ⋯ menu offering Rename / Edit / Delete.
    - Deleting an area that has projects succeeds and the projects appear under a "No Area" entry in the same list on next refresh.
    - Typecheck green; existing sidebar still works (AreaContextMenu untouched, AreaCreateDialog backwards-compatible).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CRUD UI to /areas/[areaId] page (edit area, create/rename/edit/delete/move project)</name>
  <files>apps/web/app/(app)/areas/[areaId]/page.tsx, apps/web/components/areas/AreaDetailHeader.tsx, apps/web/components/areas/AreaProjectCardMenu.tsx, apps/web/components/areas/MoveProjectDialog.tsx</files>
  <action>
1) **Create `apps/web/components/areas/AreaDetailHeader.tsx`** (new client component):
   - Props: `{ area: { id: string; name: string; emoji: string | null }; allAreas: { id: string; name: string }[]; graduationYear: number | null }`.
   - Renders the existing serif H1 + emoji header block (move the JSX from the current `page.tsx` `<header>` into this component so the page becomes a thin server wrapper).
   - Adds a right-aligned button row: **Edit area** (opens a dialog with name + emoji inputs → `updateArea`) and **New project** (opens `ProjectCreateDialog` with `defaultAreaId={area.id}`, `areas={allAreas}`, `graduationYear={graduationYear}` — this dialog already exists and is decoupled).
   - On any successful mutation: `router.refresh()`.
   - Reuse shadcn `Dialog`, `Button`, `Input`, `Label`, `sonner`. Match the JARVIS × Notion restraint — buttons should be `variant="outline"` size `sm`, no flashy icons (a small lucide `Pencil` and `Plus` are fine — both are already imported elsewhere in components/projects).

2) **Create `apps/web/components/areas/MoveProjectDialog.tsx`** (new client component):
   - Props: `{ projectId: string; projectName: string; currentAreaId: string; allAreas: { id: string; name: string }[]; open: boolean; onOpenChange: (open: boolean) => void }`.
   - Renders a `Dialog` containing a shadcn `Select` of areas (filter out `currentAreaId`).
   - On confirm: calls `moveProjectToArea({ projectId, newAreaId })`, toast result, on success `router.refresh()` AND close.
   - Title: "Move project". Description: small serif italic line "{projectName} → another area".

3) **Create `apps/web/components/areas/AreaProjectCardMenu.tsx`** (new client component):
   - A `⋯` dropdown anchored to each project card on the detail page.
   - Items: **Rename**, **Edit details**, **Move to another area**, **Delete**.
   - "Rename" opens a small dialog with an `Input` → `updateProject({ id, name })`.
   - "Edit details" opens a richer dialog: for MVP scope of this quick task, include only `description` (Textarea) and `icon` (text input — reuse the existing `IconPicker` if it can be embedded without a heavy lift; if not, fall back to a plain text `Input` accepting a lucide icon name). Submit → `updateProject({ id, description, icon })`.
     - NOTE: Full class-field editing is NOT in this quick's scope — class projects already have `ProjectEditClassDialog` reachable from the project's own detail page. Add a small footer link in the "Edit details" dialog: "Edit class fields on the project page →" linking to `/projects/{id}` when `isClass=true`.
   - "Move to another area" opens `MoveProjectDialog`.
   - "Delete" opens a confirmation dialog. Body copy:
     - "Delete '{projectName}'? Linked tasks and captures stay in your library — they just lose this project link."
     - On confirm: `deleteProject(id)`, `router.refresh()` on success.
   - Props: `{ projectId: string; projectName: string; projectDescription: string | null; projectIcon: string | null; isClass: boolean; currentAreaId: string; allAreas: { id: string; name: string }[] }`.

4) **Update `apps/web/app/(app)/areas/[areaId]/page.tsx`**:
   - Additionally SELECT all of the user's active areas (`id`, `name`) for use as `allAreas` (passed into the new components).
   - Additionally SELECT the user's `graduationYear` from `users` table (use existing helper if one exists in `lib/auth/get-user.ts` — `requireOnboarded` already returns a user; check whether it includes graduationYear and use it if so, else add a minimal select).
   - Move the existing `<header>` JSX into `<AreaDetailHeader area={area} allAreas={allAreas} graduationYear={gradYear} />`.
   - Wrap each `<li>` project entry with `<AreaProjectCardMenu ... />` positioned absolutely in the top-right of the card (use existing `relative` + `absolute top-2 right-2` pattern). Keep the existing `<Link>` to `/projects/{id}` working — the menu button must `e.stopPropagation()` + `e.preventDefault()` on click so it doesn't navigate.

5) Aesthetic constraints (locked decision #5): reuse existing tokens; no new visual language; menu buttons follow the same "hover-revealed `⋯`" pattern as `AreaContextMenu` (opacity-0 → group-hover:opacity-100).

Do NOT touch `ProjectCreateDialog.tsx` (it already supports `defaultAreaId` + `open` + `onOpenChange`).
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -30 &amp;&amp; pnpm vitest run components/areas 2>&amp;1 | tail -30 &amp;&amp; pnpm build 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - /areas/[areaId] page has "Edit area" and "New project" buttons in the header.
    - Each project card has a hover ⋯ menu with Rename / Edit details / Move / Delete.
    - Move dialog lists all other areas and reassigns on confirm.
    - Delete project succeeds and removes the card on refresh; linked tasks/captures remain queryable (junction CASCADE only).
    - Build green; sidebar + project detail pages still work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual verification of full CRUD loop on /areas and /areas/[areaId]</name>
  <what-built>
    - Sidebar-free area + project CRUD on the two Areas pages.
    - deleteArea now reassigns children to a per-user "No Area" sentinel instead of blocking.
  </what-built>
  <how-to-verify>
    Start the dev server (`pnpm dev` from repo root or `apps/web`). Sign in.

    On **/areas**:
    1. Click "New Area" → create "Quick Test Area" with emoji 🧪. Verify it appears in the tree and in the "Manage areas" list.
    2. On that area's row, click ⋯ → Rename → "Quick Test Area Renamed". Verify title updates after the page refresh settles.
    3. ⋯ → Edit → change emoji to 🔬. Verify update.
    4. Navigate into the area, create one or two projects (next page test).
    5. Return to /areas → ⋯ → Delete on the area with projects. Confirm. Verify the area disappears AND a "No Area" entry appears in the list containing the orphaned projects.
    6. Try to delete the "No Area" bucket from its ⋯ menu → must show the "Can't delete the No Area bucket." toast.

    On **/areas/[areaId]**:
    1. Click "New project" → create a basic non-class project. Verify card appears.
    2. Hover the card → ⋯ → Rename. Verify name updates.
    3. ⋯ → Edit details → change description and icon. Verify card reflects changes.
    4. Create a second area from /areas. Return to the detail page of the first area.
    5. ⋯ → Move to another area → pick the new area. Verify the card disappears from the source area and appears in the destination.
    6. ⋯ → Delete on a project. Confirm. Verify card removal. Confirm that any task previously linked to that project still exists on /tasks (its project link is just gone).

    Aesthetic check: confirm no new visual language was introduced — fonts, edges, surface tokens all match the rest of the app. No flashy color accents, no neumorphic shadows, no HUD glow.

    If anything misroutes, copies the wrong tone, or breaks the sidebar, list the issues and reject.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm tsc --noEmit` → 0 errors.
- `cd apps/web && pnpm vitest run` → green (no new failing tests; existing area/project tests still pass).
- `cd apps/web && pnpm build` → builds clean.
- Manual loop in Task 4 passes all bullets including the "No Area" bucket flow.
</verification>

<success_criteria>
- Every locked decision is implemented and traceable in the code:
  - #1: `deleteArea` reassigns to "No Area" sentinel (Task 1).
  - #2: existing junction CASCADE preserved + documented (Task 1 comment on `deleteProject`).
  - #3: zero changes under `apps/web/lib/jarvis/` or `packages/jarvis-core/` (grep proof).
  - #4: `MoveProjectDialog` wired to `moveProjectToArea` (Task 3).
  - #5: only existing shadcn primitives + CSS tokens used (grep should find no new `bg-gradient`, no new color hex literals in new files).
  - #6: Areas page exposes create / rename / edit emoji / delete (Task 2).
  - #7: Detail page exposes create-in-area / rename / edit / delete / move (Task 3).
- /areas and /areas/[areaId] pages no longer require the sidebar for any of the above operations.
- Sidebar `AreaContextMenu` and `Sidebar` itself remain untouched and functional.
- No new DB migration introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-g2z-add-crud-for-areas-on-the-web-app-s-area/260611-g2z-SUMMARY.md` documenting:
- Files touched and why
- The "No Area" sentinel pattern (when it's created, how it's identified, how the page treats it)
- Any deferred ideas (e.g., per-area icon picker, per-area color — both intentionally not in scope since the schema has neither)
- Test commands run and their results
</output>
