{
  "unit_id": "unit-u5-area-detail-timeline-by-date--b485",
  "run_id": "sesh-1784257742502",
  "scale": "feature",
  "title": "u5-area-detail-timeline: by-date view on /areas/[areaId]",
  "scope": "Bring the timeline to the area detail page. Widen the area-detail RSC data path to include start_date (and whatever TimelineProjectInput needs), extend the AreaProject interface accordingly, add a grid|timeline view toggle in the AreaProjectList filter row consistent with u3's TREE|TIMELINE toggle grammar, render ProjectsTimeline with scope=\"area\" (single pre-filtered area, no group headers per u3's API NOTES), sensible persistence (shared zoom key areas:timeline-zoom; page-scoped view key), realtime invalidation for date mutations mirroring u3's alsoInvalidate wiring, reconcile with the page's existing show-archived idiom, responsive per the DESIGN.md amendment, tests. Consumes u3's components strictly - NEVER edits components/projects/timeline/** (u4 owns that surface concurrently).",
  "criteria": [
    "/areas/[areaId] offers a grid|timeline toggle in the filter row, grammar-consistent with u3's toggle; grid view remains byte-for-byte today's behavior",
    "Timeline renders via ProjectsTimeline scope=\"area\" with the single area pre-filtered (no areaId prop exists - pass filtered arrays per u3 API NOTES); no area group header shown",
    "Area-detail data path includes start_date and satisfies TimelineProjectInput without changing existing consumers' semantics",
    "Zoom persists via the SHARED areas:timeline-zoom key; view mode persists via a page-scoped key following the lifeos:view idiom exactly",
    "Date edits from the popover work on the detail page with undo toast and archive-trap intact; realtime invalidation wired so external date changes refresh the timeline",
    "Show-archived: detail page's existing idiom governs; ghosts render when shown",
    "Responsive per DESIGN.md amendment (no page-body overflow; toolbar wraps); both themes; reduced motion",
    "Scoped tests green plus npx tsc --noEmit clean; zero edits under components/projects/timeline/"
  ],
  "touched": ["apps/web/app/(app)/areas/[areaId]/page.tsx", "apps/web/components/areas/AreaProjectList.tsx", "apps/web/tests"],
  "model": "claude-opus-4-8",
  "model_assignment": {
    "tier": "heavy",
    "effort": "xhigh",
    "reason": "fixed routing: user contract pins every build lane to Opus 4.8"
  },
  "advisor_path": "/Users/filippofonseca/Developer/Projects/hyperpolymath-v2-projects-timeline/.bgsd/runs/sesh-1784257742502/advisor/unit-u5-area-detail-timeline-by-date--b485.md",
  "control_report_path": "/Users/filippofonseca/Developer/Projects/hyperpolymath-v2-projects-timeline/.bgsd/runs/sesh-1784257742502/control/u5-area-detail-timeline.report.md",
  "advisor_checkpoints": [
    "before implementation",
    "after planning",
    "after every commit",
    "on blocker or assumption",
    "before verification",
    "after every verification result"
  ]
}
