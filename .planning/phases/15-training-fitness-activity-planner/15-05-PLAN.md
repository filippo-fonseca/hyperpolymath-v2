---
phase: 15-training-fitness-activity-planner
plan: 05
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - apps/web/app/(app)/training/stats/page.tsx
  - apps/web/components/training/stats/TrainingStatsClient.tsx
  - apps/web/components/training/stats/TrainingHeatmap.tsx
  - apps/web/components/training/stats/HeatmapDayPopover.tsx
  - apps/web/components/training/stats/BatchTotalsTable.tsx
  - apps/web/components/training/stats/AdherenceCard.tsx
  - apps/web/components/training/stats/DurationTrendChart.tsx
  - apps/web/components/training/stats/TimeWindowToggle.tsx
  - apps/web/components/training/PlannerHeader.tsx
autonomous: true
requirements: [TRN-09, TRN-10, TRN-11]
must_haves:
  truths:
    - "Visiting /training/stats renders a GitHub-style heatmap of last 12 months with OKLCH-blended day cells"
    - "Empty days render as the muted neutral token; non-empty days render as the blended (duration-weighted) OKLCH average of that day's activity-type colors"
    - "Hovering a cell shows a tooltip listing the day's activities (types + durations + distances)"
    - "Clicking a cell opens a popover with the full day composition and links to activities"
    - "Time-window toggle (week / month / all-time) re-aggregates the stat numbers"
    - "Stats include batch-grouped per-type duration + distance totals, planned-vs-actual adherence card, and an over-time bar chart"
  artifacts:
    - path: "apps/web/app/(app)/training/stats/page.tsx"
      provides: "Server Component loading all-time activities + types + batches"
      contains: "getAllActivities"
    - path: "apps/web/components/training/stats/TrainingHeatmap.tsx"
      provides: "365-day grid with OKLCH-blended cells"
      contains: "blendOklchStrings"
    - path: "apps/web/components/training/stats/AdherenceCard.tsx"
      provides: "Planned-vs-actual headline + breakdown"
      contains: "adherence"
  key_links:
    - from: "TrainingHeatmap cell color"
      to: "blendOklchStrings (lib/training/color-blend)"
      via: "useMemo per-day map"
      pattern: "blendOklchStrings"
    - from: "HeatmapDayPopover"
      to: "shadcn Popover anchored to clicked cell"
      via: "Popover from components/ui/popover"
      pattern: "Popover"
---

<objective>
Build the stats surface: heatmap with OKLCH-blended day colors as the headline visual, hover/click drilldowns, time-window toggle, batch-grouped totals table, adherence card, and a duration trend bar chart.

Purpose: Surface 2 of 3. The heatmap is the moment the design earns the rest of the surface (per CONTEXT specifics).

Output: /training/stats route with heatmap + 3-4 supporting stat cards/tables, all reading from existing tables via TanStack Query + Realtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-training-fitness-activity-planner/15-CONTEXT.md
@.planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md
@apps/web/lib/training/color-blend.ts
@apps/web/lib/training/palette.ts
@apps/web/lib/training/distance.ts
@apps/web/lib/db/queries/training.ts
@apps/web/lib/realtime/useTableSubscription.ts
@apps/web/components/training/PlannerHeader.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Heatmap + DayPopover (the load-bearing visual)</name>
  <files>apps/web/components/training/stats/TrainingHeatmap.tsx, apps/web/components/training/stats/HeatmapDayPopover.tsx</files>
  <read_first>
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Pattern 5 (TrainingHeatmap), Pattern 3 (color blend), Pitfall 1 (circular hue), Pitfall 5 (memoize per-day)
    - apps/web/lib/training/color-blend.ts
    - apps/web/lib/training/palette.ts (EMPTY_DAY_COLOR)
    - apps/web/lib/training/distance.ts
  </read_first>
  <action>
1. Create apps/web/components/training/stats/TrainingHeatmap.tsx:
   - Props: activities: ActivityWithType[], distanceUnit.
   - Compute viewport: today back 365 days. Align grid so first column starts on the most-recent Monday before earliest day.
   - useMemo dayMap: Map<string (YYYY-MM-DD), ActivityWithType[]> keyed by scheduledDate. Memo on [activities].
   - useMemo dayColors: Map<string, string> — for each ISO date with activities, compute blendOklchStrings(types.map(a => a.type.color), weights = a.actualDurationMin ?? a.plannedDurationMin ?? 30). Empty days use EMPTY_DAY_COLOR. Memo on [dayMap].
   - Layout: CSS Grid — grid-template-rows: repeat(7, 12px); grid-auto-flow: column; grid-auto-columns: 12px; gap: 3px.
   - Render one HeatmapCell per day. Wrap cell rendering in React.memo for performance per Pitfall 5.
   - Cell: small rounded-sm div with backgroundColor = dayColors.get(iso) ?? EMPTY_DAY_COLOR. Wrap in shadcn Tooltip showing day composition (types + durations + distance summary). Click triggers Popover via lifted state in parent.
   - Month labels: row above the grid, positioned every ~4 columns.
   - Day-of-week labels: column left of the grid (Mon, Wed, Fri only — standard heatmap convention).
   - Color performance: do NOT render 365 React components with heavy work — the cell sub-component reads from the memoized dayColors map only. Verified by Pitfall 5 guidance.

2. Create apps/web/components/training/stats/HeatmapDayPopover.tsx:
   - Props: dateISO, activities (for that day), distanceUnit, anchorRef or trigger pattern.
   - shadcn Popover. Content: date heading (formatted "Sat, Jun 8"), then a list of activities grouped by status, each showing color chip, title, type name, duration, distance (formatted via formatDistance).
   - Empty state: "No activities logged this day."

Per TRN-09, TRN-10, D-12.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "stats/(Training|Heatmap)" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep "blendOklchStrings" apps/web/components/training/stats/TrainingHeatmap.tsx matches
    - grep "EMPTY_DAY_COLOR" apps/web/components/training/stats/TrainingHeatmap.tsx matches
    - grep "useMemo" apps/web/components/training/stats/TrainingHeatmap.tsx matches at least twice
    - grep "React.memo\|memo(" apps/web/components/training/stats/TrainingHeatmap.tsx matches (Pitfall 5)
    - grep "Tooltip" apps/web/components/training/stats/TrainingHeatmap.tsx matches
    - grep "Popover" apps/web/components/training/stats/HeatmapDayPopover.tsx matches
    - No use of CSS color-mix in this file (RESEARCH anti-pattern): grep -c "color-mix" apps/web/components/training/stats/TrainingHeatmap.tsx returns 0
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    365-day heatmap with perceptually-blended day cells; hover tooltips and click popovers reveal day composition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Supporting stats — BatchTotalsTable + AdherenceCard + DurationTrendChart + TimeWindowToggle</name>
  <files>apps/web/components/training/stats/BatchTotalsTable.tsx, apps/web/components/training/stats/AdherenceCard.tsx, apps/web/components/training/stats/DurationTrendChart.tsx, apps/web/components/training/stats/TimeWindowToggle.tsx</files>
  <read_first>
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Stats Architecture (D-13 / TRN-11)
    - apps/web/lib/training/distance.ts
    - apps/web/lib/training/week.ts
  </read_first>
  <action>
1. Create apps/web/components/training/stats/TimeWindowToggle.tsx:
   - Props: value: "week" | "month" | "all", onChange.
   - shadcn ToggleGroup with three options labeled "Week", "Month", "All time".
   - Per TRN-11 (week / month / all-time only; custom range deferred per Deferred Ideas).

2. Create apps/web/components/training/stats/AdherenceCard.tsx:
   - Props: activities (filtered by current time window), windowLabel.
   - Compute: total planned (status in 'planned' or 'done'), total done, adherence % = done / (planned + done). Skipped + cancelled are tracked separately.
   - Layout: headline number (big "{done}/{planned} done" + "{pct}% adherence"), secondary line ({skipped} skipped, {cancelled} cancelled).
   - Per TRN-12, D-14.

3. Create apps/web/components/training/stats/BatchTotalsTable.tsx:
   - Props: activities, types, batches, distanceUnit.
   - For each batch (in user-defined order via batches.orderIndex), render a section:
     * Batch name header + total batch duration + (sum of distance only for distance-enabled types)
     * Per-type rows: color chip, type name, total duration (only completed activities — actualDurationMin if set, else plannedDurationMin for planned-but-not-done counted separately), total distance (formatDistance from km canonical).
   - After all batches, render "Ungrouped" section for types with batchId=null.
   - Per TRN-11.

4. Create apps/web/components/training/stats/DurationTrendChart.tsx:
   - Props: activities, windowLabel.
   - Compute: bucket completed activities by week (ISO week, last 12 weeks for any window — or scale to window). Sum actualDurationMin per bucket.
   - Render as a CSS-flexbox bar chart (no chart lib — keep dependencies clean; one component, ~80 LOC). Each bar: relative height as percentage of max bucket; tooltip showing week and total minutes.
   - X-axis labels: week-start dates.
   - Per TRN-11 (≥1 over-time chart). No pie charts per D-13.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "stats/(Batch|Adherence|Duration|TimeWindow)" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep "ToggleGroup\|Tabs" apps/web/components/training/stats/TimeWindowToggle.tsx matches
    - grep "formatDistance" apps/web/components/training/stats/BatchTotalsTable.tsx matches
    - grep "adherence\|done\|planned" apps/web/components/training/stats/AdherenceCard.tsx matches (case-insensitive ok)
    - grep -r "pie\|PieChart" apps/web/components/training/stats/ returns no matches (D-13 no pies)
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    Three supporting stat components ship; all aggregations correct; no pie charts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Stats route + TrainingStatsClient orchestration + PlannerHeader stats-link</name>
  <files>apps/web/app/(app)/training/stats/page.tsx, apps/web/components/training/stats/TrainingStatsClient.tsx, apps/web/components/training/PlannerHeader.tsx</files>
  <read_first>
    - apps/web/lib/db/queries/training.ts (getAllActivities, getActivityTypes, getBatches)
    - apps/web/lib/realtime/useTableSubscription.ts
    - apps/web/components/training/PlannerHeader.tsx (from Plan 15-03; needs a stats link added)
  </read_first>
  <action>
1. Create apps/web/app/(app)/training/stats/page.tsx as a Server Component:
   - Get userId via getClaims.
   - Call getAllActivities(userId), getActivityTypes(userId), getBatches(userId), getDistanceUnit(userId).
   - Render TrainingStatsClient with the initial data.

2. Create apps/web/components/training/stats/TrainingStatsClient.tsx with "use client":
   - useState for timeWindow: "week" | "month" | "all" (default "month").
   - Three useTableSubscription calls identical to TrainingClient (training_activities, training_activity_types with alsoInvalidate, training_batches with alsoInvalidate).
   - useQuery keyed ["training_activities", userId, "all"] with queryFn calling a Server Action listAllActivities. initialData = initialActivities prop.
   - useQuery for types and batches.
   - filteredActivities = useMemo: filter activities by timeWindow (week → current ISO week; month → current ISO month; all → no filter).
   - Layout (top to bottom): TimeWindowToggle, AdherenceCard, TrainingHeatmap (always all-time / 12 months regardless of toggle — heatmap is the headline 12-month view), BatchTotalsTable (filtered), DurationTrendChart (filtered).
   - Critical Pattern 3: NEVER setQueryData from realtime callers.

3. Edit apps/web/components/training/PlannerHeader.tsx (from Plan 15-03):
   - Add a "Stats" link button (lucide BarChart3) next to "Manage types". Link to /training/stats via next/link.

Per TRN-09, TRN-10, TRN-11.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "stats/page|TrainingStatsClient" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(app)/training/stats/page.tsx exists
    - grep "getAllActivities" apps/web/app/(app)/training/stats/page.tsx matches
    - grep "useTableSubscription" apps/web/components/training/stats/TrainingStatsClient.tsx matches at least 3 times
    - grep -r setQueryData apps/web/components/training/ returns no matches
    - grep "/training/stats" apps/web/components/training/PlannerHeader.tsx matches
    - grep "getClaims" apps/web/app/(app)/training/stats/page.tsx matches
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    /training/stats route renders heatmap + adherence + batch totals + trend chart; time window toggles correctly; Realtime invalidations refresh stats live; stats reachable from planner header.
  </done>
</task>

</tasks>

<verification>
- pnpm --filter web exec tsc --noEmit passes
- Manual smoke: visit /training/stats — see 12-month heatmap with blended cells where activities exist; hover shows tooltip; click opens popover; toggle window changes the supporting cards
- grep -r setQueryData apps/web/components/training/ returns no matches
- grep -r getSession apps/web/app/(app)/training/ apps/web/components/training/ returns no matches
- grep -r "color-mix" apps/web/components/training/stats/ returns no matches (anti-pattern)
</verification>

<success_criteria>
- Heatmap is performant (memoized per-day, ≤ 5ms compute), perceptually correct (circular hue average), duration-weighted
- Supporting stats answer real questions (no vanity numbers)
- All Realtime + Query patterns clean
</success_criteria>

<output>
Create .planning/phases/15-training-fitness-activity-planner/15-05-SUMMARY.md summarizing: heatmap algorithm, stats list shipped, time-window semantics.
</output>
