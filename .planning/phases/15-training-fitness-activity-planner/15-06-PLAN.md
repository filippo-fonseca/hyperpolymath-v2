---
phase: 15-training-fitness-activity-planner
plan: 06
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - apps/web/components/lifeos/TodayTrainingWidget.tsx
  - apps/web/components/lifeos/LifeOsWidgetGrid.tsx
  - apps/web/components/training/settings/DistanceUnitToggle.tsx
  - apps/web/app/(app)/settings/page.tsx
  - apps/web/app/(app)/settings/actions.ts
autonomous: true
requirements: [TRN-13, TRN-14]
must_haves:
  truths:
    - "LifeOS shows a TodayTrainingWidget alongside the existing widgets"
    - "If today has zero planned activities, the widget renders a positive Rest day state"
    - "If today has planned activities, the widget lists them (color chip + title + duration)"
    - "Tapping an activity in the widget routes to /training (or opens a contextual action)"
    - "Settings page has a distance_unit toggle that persists km|mi to users.distance_unit"
    - "All distance-displaying surfaces respect the user's preference"
  artifacts:
    - path: "apps/web/components/lifeos/TodayTrainingWidget.tsx"
      provides: "Today's training widget for LifeOS"
      contains: "Rest day"
    - path: "apps/web/components/training/settings/DistanceUnitToggle.tsx"
      provides: "km / mi toggle for /settings"
      contains: "distance_unit"
    - path: "apps/web/app/(app)/settings/actions.ts"
      provides: "updateDistanceUnit Server Action"
      contains: "updateDistanceUnit"
  key_links:
    - from: "TodayTrainingWidget realtime"
      to: "useTableSubscription('training_activities', userId)"
      via: "lib/realtime/useTableSubscription"
      pattern: "useTableSubscription"
    - from: "DistanceUnitToggle change"
      to: "updateDistanceUnit Server Action"
      via: "app/(app)/settings/actions.ts"
      pattern: "updateDistanceUnit"
---

<objective>
Add the cross-surface touch points: the TodayTrainingWidget on LifeOS (with positive Rest day state per CONTEXT specifics), and the distance_unit toggle on /settings.

Purpose: TRN-13 + TRN-14 — completes the integration scope of Phase 15. Cross-surface integration beyond LifeOS is explicitly out of scope per D-16.

Output: Widget mounted in LifeOsWidgetGrid, settings toggle wired to users.distance_unit, Server Action shipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-training-fitness-activity-planner/15-CONTEXT.md
@.planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md
@apps/web/components/lifeos/TodayHabitsWidget.tsx
@apps/web/components/lifeos/UpcomingTasksWidget.tsx
@apps/web/components/lifeos/LifeOsWidgetGrid.tsx
@apps/web/lib/realtime/useTableSubscription.ts
@apps/web/lib/db/queries/training.ts
@apps/web/app/(app)/settings/page.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodayTrainingWidget + LifeOsWidgetGrid wiring</name>
  <files>apps/web/components/lifeos/TodayTrainingWidget.tsx, apps/web/components/lifeos/LifeOsWidgetGrid.tsx</files>
  <read_first>
    - apps/web/components/lifeos/TodayHabitsWidget.tsx (the analog widget — copy the shape, replace data layer)
    - apps/web/components/lifeos/UpcomingTasksWidget.tsx (additional widget reference)
    - apps/web/components/lifeos/LifeOsWidgetGrid.tsx (where to mount the new widget)
    - apps/web/lib/realtime/useTableSubscription.ts
    - .planning/phases/15-training-fitness-activity-planner/15-CONTEXT.md specifics block (rest-day tone)
  </read_first>
  <action>
1. Create apps/web/components/lifeos/TodayTrainingWidget.tsx with "use client":
   - Props: userId, initialActivities (today's activities, server-loaded), distanceUnit.
   - useTableSubscription("training_activities", userId).
   - todayISO = format(new Date(), "yyyy-MM-dd").
   - useQuery keyed ["training_activities", userId, todayISO, todayISO] with queryFn calling the same listActivitiesInRange Server Action used by TrainingClient. initialData = initialActivities.
   - Layout (match TodayHabitsWidget shell — card with header "Today's training"):
     * If activities.length === 0: render Rest day state — small positive copy ("Rest day. Recover well.") and a subtle illustration or icon (lucide Moon or Coffee). Make it feel intentional, NOT empty.
     * Else: list each activity row with color chip (left dot, style backgroundColor = type.color), title, type.name, duration; distance summary when type.hasDistance. A small Check button on the right that calls completeActivity (or routes to /training for the full completion dialog — Claude's Discretion; simpler: route to /training so user lands in the planner with the activity surface).
   - Tapping a row navigates to /training via next/link (or could trigger the completion modal — but the modal lives in /training; routing is simpler).

2. Edit apps/web/components/lifeos/LifeOsWidgetGrid.tsx:
   - Import TodayTrainingWidget.
   - Add it to the widget grid layout next to TodayHabitsWidget. Match the existing chrome/spacing.
   - The Server-Component parent (LifeOS page) needs to load today's training activities + distance_unit and pass them in — locate that parent file (likely apps/web/app/(app)/lifeos/page.tsx) and extend it to call getActivitiesInRange(userId, todayISO, todayISO) + getDistanceUnit, then pass through props.

Per TRN-13, D-15.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "TodayTrainingWidget|LifeOsWidgetGrid" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/lifeos/TodayTrainingWidget.tsx exists
    - grep "Rest day" apps/web/components/lifeos/TodayTrainingWidget.tsx matches
    - grep "useTableSubscription" apps/web/components/lifeos/TodayTrainingWidget.tsx matches
    - grep -c setQueryData apps/web/components/lifeos/TodayTrainingWidget.tsx returns 0
    - grep "TodayTrainingWidget" apps/web/components/lifeos/LifeOsWidgetGrid.tsx matches
    - grep "getActivitiesInRange\|TodayTrainingWidget" apps/web/app/(app)/lifeos/page.tsx matches
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    Widget mounted on LifeOS; positive Rest day state on zero activities; live invalidation via Realtime.
  </done>
</task>

<task type="auto">
  <name>Task 2: DistanceUnitToggle + settings Server Action + page wiring</name>
  <files>apps/web/components/training/settings/DistanceUnitToggle.tsx, apps/web/app/(app)/settings/page.tsx, apps/web/app/(app)/settings/actions.ts</files>
  <read_first>
    - apps/web/app/(app)/settings/page.tsx (current shape, where to insert new toggle)
    - apps/web/app/(app)/settings/actions.ts (existing Server Action pattern — getClaims usage)
    - apps/web/lib/db/schema.ts users.distanceUnit column (added in 15-01)
  </read_first>
  <action>
1. Edit apps/web/app/(app)/settings/actions.ts:
   - Add export async function updateDistanceUnit(unit: "km" | "mi"): Promise<ActionResult>. Pattern: getClaims (NEVER getSession) → Zod validate (z.enum(["km","mi"])) → db.update(users).set({ distanceUnit: unit }).where(eq(users.id, userId)) → return { success: true }.

2. Create apps/web/components/training/settings/DistanceUnitToggle.tsx with "use client":
   - Props: value: "km" | "mi".
   - shadcn ToggleGroup (or RadioGroup) with two options: "Kilometers (km)" / "Miles (mi)".
   - useTransition for the action call; show pending state. On change call updateDistanceUnit. Toast on success/error via sonner.

3. Edit apps/web/app/(app)/settings/page.tsx:
   - Locate the Server Component's section list. Add a new "Units" section near display preferences.
   - Render <DistanceUnitToggle value={currentUserDistanceUnit} />. Currently-saved unit comes from selecting users.distanceUnit on initial load via Drizzle.

Per TRN-14, D-10.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "settings/(page|actions)|DistanceUnitToggle" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep "export async function updateDistanceUnit" apps/web/app/(app)/settings/actions.ts matches
    - grep "getClaims" apps/web/app/(app)/settings/actions.ts matches
    - grep -c "getSession" apps/web/app/(app)/settings/actions.ts returns 0
    - grep "z.enum" apps/web/app/(app)/settings/actions.ts matches (Zod validation)
    - grep "DistanceUnitToggle" apps/web/app/(app)/settings/page.tsx matches
    - grep "updateDistanceUnit" apps/web/components/training/settings/DistanceUnitToggle.tsx matches
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    Settings exposes km/mi toggle; preference persists to users.distance_unit; all training surfaces (planner, dialog, widget, stats) read it via the prop passed from each Server Component.
  </done>
</task>

</tasks>

<verification>
- pnpm --filter web exec tsc --noEmit passes
- Manual smoke: switch to mi in /settings → revisit /training → distance inputs and displays show "mi"; create activity with distance "3 mi" → reload → still shows "3.0 mi"; switch back to km → shows "4.83 km" (= 3 mi)
- grep -r setQueryData apps/web/components/lifeos/TodayTrainingWidget.tsx returns no matches
- grep -r getSession apps/web/components/training/settings/ apps/web/app/(app)/settings/actions.ts returns no matches
</verification>

<success_criteria>
- TodayTrainingWidget shipped with both populated and Rest-day states
- LifeOsWidgetGrid + lifeos/page.tsx updated to load + pass data
- /settings exposes the distance_unit toggle; Server Action shipping with Zod + getClaims
- Distance values roundtrip correctly between mi and canonical km storage
</success_criteria>

<output>
Create .planning/phases/15-training-fitness-activity-planner/15-06-SUMMARY.md summarizing: widget contract, settings toggle wiring, distance unit flow.
</output>
