---
phase: 15-training-fitness-activity-planner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/db/schema.ts
  - apps/web/lib/db/enums.ts
  - apps/web/supabase/migrations/0021_training.sql
  - apps/web/lib/realtime/query-keys.ts
autonomous: true
requirements: [TRN-14, TRN-15, TRN-16, TRN-17]
must_haves:
  truths:
    - "Three training tables exist in Postgres with RLS enforcing user_id = auth.uid()"
    - "users table has distance_unit column with CHECK (km|mi)"
    - "Writing to any training table bumps users.state_version via BEFORE trigger"
    - "Supabase Realtime broadcasts changes on all three training tables"
    - "RealtimeTable union includes the three new tables so useTableSubscription typechecks"
  artifacts:
    - path: "apps/web/lib/db/schema.ts"
      provides: "Drizzle trainingBatches, trainingActivityTypes, trainingActivities tables + users.distanceUnit column"
      contains: "trainingBatches"
    - path: "apps/web/supabase/migrations/0021_training.sql"
      provides: "Schema, RLS policies, realtime publication, state_version triggers"
      contains: "CREATE TRIGGER bump_state_version_on_training_activities"
    - path: "apps/web/lib/realtime/query-keys.ts"
      provides: "RealtimeTable union expanded with training_* members"
      contains: "training_activities"
  key_links:
    - from: "training_activities BEFORE trigger"
      to: "bump_user_state_version()"
      via: "FOR EACH ROW EXECUTE FUNCTION"
      pattern: "bump_state_version_on_training_activities"
---

<objective>
Lay the database foundation for Phase 15 Training: three new tables (`training_batches`, `training_activity_types`, `training_activities`), a `distance_unit` column on `users`, RLS policies, Realtime publication membership, and `state_version` BEFORE triggers per CACHE-03. Also extend the `RealtimeTable` union in `lib/realtime/query-keys.ts` so downstream plans can subscribe via `useTableSubscription` with typesafety.

Purpose: All UI and Server Action work in later waves depends on the schema and the typed query-key surface. Adding state_version triggers now (D-19) means future JARVIS training tools just slot in.

Output: Drizzle table definitions + raw SQL migration + RealtimeTable union update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-training-fitness-activity-planner/15-CONTEXT.md
@.planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md
@apps/web/lib/db/schema.ts
@apps/web/lib/db/enums.ts
@apps/web/lib/realtime/query-keys.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Drizzle schema with three training tables + distance_unit column</name>
  <files>apps/web/lib/db/schema.ts, apps/web/lib/db/enums.ts</files>
  <read_first>
    - apps/web/lib/db/schema.ts (existing pgTable conventions, users table, habit tables)
    - apps/web/lib/db/enums.ts (existing pgEnum usage)
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Pattern 1 (lines ~205-291)
  </read_first>
  <action>
Append to `apps/web/lib/db/schema.ts` after the habits-related tables:

1. Add `distanceUnit: text("distance_unit").notNull().default("km")` column to the existing `users` pgTable declaration. (If users table is split across files, locate it via grep `pgTable("users"`.)

2. Add three new pgTable declarations exactly per 15-RESEARCH.md Pattern 1:
   - `trainingBatches` — columns: id (uuid PK defaultRandom), userId (uuid notNull FK users.id ON DELETE CASCADE), name (text notNull), description (text), orderIndex (integer notNull default 0), archivedAt (timestamptz), createdAt (timestamptz defaultNow notNull), updatedAt (timestamptz defaultNow notNull). Index: `training_batches_user_order_idx` on (userId, orderIndex).
   - `trainingActivityTypes` — columns: id, userId (cascade), batchId (uuid nullable FK trainingBatches.id ON DELETE SET NULL), name (text notNull), color (text notNull), hasDistance (boolean notNull default false), orderIndex (integer notNull default 0), archivedAt, createdAt, updatedAt. Indexes: `training_activity_types_user_idx` on (userId), `training_activity_types_batch_order_idx` on (batchId, orderIndex) WHERE `batch_id IS NOT NULL`.
   - `trainingActivities` — columns: id, userId (cascade), activityTypeId (uuid notNull FK trainingActivityTypes.id ON DELETE RESTRICT), scheduledDate (date notNull), title (text notNull), description (text), plannedDurationMin (integer), actualDurationMin (integer), plannedDistanceKm (numeric precision 8 scale 3), actualDistanceKm (numeric precision 8 scale 3), status (text notNull default 'planned'), dayOrderIndex (integer notNull default 0), completedAt (timestamptz), createdAt, updatedAt. Indexes: `training_activities_user_date_idx` on (userId, scheduledDate), `training_activities_user_type_idx` on (userId, activityTypeId), `training_activities_user_status_idx` on (userId, status) WHERE `status = 'done'`.

Imports to add as needed: `numeric`, `date`, `boolean`, `index`, `sql` from `drizzle-orm/pg-core` / `drizzle-orm`.

3. No new enum required (status is `text` with CHECK in migration; distanceUnit is `text` with CHECK). Skip enums.ts unless project convention demands enums for these — repo convention per habits/tasks uses `text + check` for similar statuses.

Per D-17 / D-19 / TRN-15 / TRN-16.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "(error|schema\.ts)" | head -20 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const trainingBatches = pgTable(" apps/web/lib/db/schema.ts` returns 1
    - `grep -c "export const trainingActivityTypes = pgTable(" apps/web/lib/db/schema.ts` returns 1
    - `grep -c "export const trainingActivities = pgTable(" apps/web/lib/db/schema.ts` returns 1
    - `grep -c "distanceUnit: text(\"distance_unit\")" apps/web/lib/db/schema.ts` returns 1
    - `grep "scheduledDate: date(" apps/web/lib/db/schema.ts` matches
    - `grep "hasDistance: boolean(\"has_distance\")" apps/web/lib/db/schema.ts` matches
    - `grep "activityTypeId.*onDelete.*restrict" apps/web/lib/db/schema.ts` matches
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Schema compiles; three tables exported with correct columns, indexes, FKs; users.distanceUnit column added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write migration 0021_training.sql (DDL + RLS + Realtime publication + state_version triggers)</name>
  <files>apps/web/supabase/migrations/0021_training.sql</files>
  <read_first>
    - apps/web/supabase/migrations/0015_habits.sql (RLS + Realtime publication template)
    - apps/web/supabase/migrations/0019_user_state_version.sql (state_version function + BEFORE trigger pattern)
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Pattern 2 (lines ~296-389)
  </read_first>
  <action>
Create `apps/web/supabase/migrations/0021_training.sql` (rename to next sequential number if 0021 is taken; check `ls apps/web/supabase/migrations/` first and use highest+1).

The migration must contain, in order:

1. `ALTER TABLE public.users ADD COLUMN IF NOT EXISTS distance_unit text NOT NULL DEFAULT 'km'; ALTER TABLE public.users ADD CONSTRAINT users_distance_unit_check CHECK (distance_unit IN ('km','mi'));` (wrap the CONSTRAINT add in a DO block that catches duplicate_object for re-runnability).

2. `CREATE TABLE IF NOT EXISTS public.training_batches (...)` with columns per Drizzle schema and CHECK `training_batches_name_not_blank CHECK (length(btrim(name)) > 0)`. Add index `training_batches_user_order_idx`. Enable RLS. Create four policies:
   - `training_batches_select`: FOR SELECT USING (`user_id = auth.uid()`)
   - `training_batches_insert`: FOR INSERT WITH CHECK (`user_id = auth.uid()`)
   - `training_batches_update`: FOR UPDATE USING (`user_id = auth.uid()`) WITH CHECK (`user_id = auth.uid()`)
   - `training_batches_delete`: FOR DELETE USING (`user_id = auth.uid()`)

3. Same for `training_activity_types` with FK `batch_id REFERENCES training_batches(id) ON DELETE SET NULL`, CHECK name_not_blank, indexes per Pattern 1, RLS + 4 policies.

4. Same for `training_activities` with FK `activity_type_id REFERENCES training_activity_types(id) ON DELETE RESTRICT`, CHECK `status IN ('planned','done','cancelled','skipped')`, CHECK title_not_blank, three indexes per Pattern 1, RLS + 4 policies.

5. Realtime publication: a DO $$ block that ALTER PUBLICATION supabase_realtime ADD TABLE for each of the three tables, catching duplicate_object exceptions per the 0015_habits.sql pattern.

6. state_version triggers — three BEFORE INSERT OR UPDATE OR DELETE triggers calling `public.bump_user_state_version()` (function was created in 0019). Drop-if-exists each first, then create. Names: `bump_state_version_on_training_batches`, `bump_state_version_on_training_activity_types`, `bump_state_version_on_training_activities`.

Migration must be idempotent (IF NOT EXISTS / DROP IF EXISTS everywhere).
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE IF NOT EXISTS public.training_" apps/web/supabase/migrations/0021_training.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/supabase/migrations/0021_training.sql` exists (or next-available number; if so update this criterion accordingly)
    - `grep -c "CREATE TABLE IF NOT EXISTS public.training_" file` returns 3
    - `grep -c "ENABLE ROW LEVEL SECURITY" file` returns 3
    - `grep -c "CREATE POLICY" file` returns 12 (4 per table × 3)
    - `grep "ALTER TABLE public.users ADD COLUMN IF NOT EXISTS distance_unit" file` matches
    - `grep "CONSTRAINT training_activities_status_check" file` matches and includes `'planned'`, `'done'`, `'cancelled'`, `'skipped'`
    - `grep -c "CREATE TRIGGER bump_state_version_on_training_" file` returns 3
    - `grep "ALTER PUBLICATION supabase_realtime ADD TABLE public.training_activities" file` matches
    - `grep "ON DELETE RESTRICT" file` matches (for activity_type_id FK)
  </acceptance_criteria>
  <done>
    Migration created and idempotent; 3 tables with RLS, indexes, CHECK constraints, Realtime publication membership, and state_version triggers per D-17/D-19.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend RealtimeTable union for training tables</name>
  <files>apps/web/lib/realtime/query-keys.ts</files>
  <read_first>
    - apps/web/lib/realtime/query-keys.ts (existing union)
    - apps/web/lib/realtime/useTableSubscription.ts (how the union is consumed)
  </read_first>
  <action>
Add three string literals to the `RealtimeTable` union in `apps/web/lib/realtime/query-keys.ts`:
- `"training_batches"`
- `"training_activity_types"`
- `"training_activities"`

Place them after `"habit_completions"`, each on its own line. Add a short comment grouping them under Phase 15. No other changes to this file.

Per TRN-17.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit 2>&amp;1 | grep "realtime/query-keys" | head -5 || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"training_batches"' apps/web/lib/realtime/query-keys.ts` returns 1
    - `grep -c '"training_activity_types"' apps/web/lib/realtime/query-keys.ts` returns 1
    - `grep -c '"training_activities"' apps/web/lib/realtime/query-keys.ts` returns 1
    - `pnpm --filter web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Union updated; downstream `useTableSubscription("training_activities", userId)` typechecks.
  </done>
</task>

</tasks>

<verification>
- TS typecheck on `apps/web` exits 0
- Migration file passes `psql --dry-run` (or visual review): all 3 tables, 12 policies, 3 triggers, publication adds present
- After running `pnpm --filter web exec drizzle-kit push` (manually verified by user; not part of plan), tables exist in Supabase and RLS denies cross-user reads
</verification>

<success_criteria>
- Three Drizzle tables exported and compile
- Migration file ready to apply (idempotent, complete)
- RealtimeTable union covers all three training tables
- No regressions in other typecheck paths
</success_criteria>

<output>
Create `.planning/phases/15-training-fitness-activity-planner/15-01-SUMMARY.md` summarizing: tables created, migration filename, RLS pattern used, state_version trigger names.
</output>
