---
phase: 15-training-fitness-activity-planner
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - apps/web/app/(app)/training/page.tsx
  - apps/web/components/training/TrainingClient.tsx
  - apps/web/components/training/TrainingBoard.tsx
  - apps/web/components/training/TrainingDayColumn.tsx
  - apps/web/components/training/ActivityCard.tsx
  - apps/web/components/training/ActivityCreateInline.tsx
  - apps/web/components/training/PlannerHeader.tsx
  - apps/web/components/shell/SidebarTree.tsx
autonomous: true
requirements: [TRN-04, TRN-05, TRN-06, TRN-12, TRN-17, TRN-18]
must_haves:
  truths:
    - "Visiting /training renders a Mon-Sun week with 7 day columns"
    - "User can navigate prev/next week"
    - "User can create an activity in a column with title and (if hasDistance) distance"
    - "User can drag an activity card to a different day; on drop, scheduled_date updates and Realtime broadcasts the change"
    - "Current-week planned-vs-actual adherence is visible in the planner header"
    - "Training nav entry appears in the sidebar"
  artifacts:
    - path: "apps/web/app/(app)/training/page.tsx"
      provides: "Server-Component route loading initial week + types + batches + distance_unit"
      contains: "getActivitiesInRange"
    - path: "apps/web/components/training/TrainingBoard.tsx"
      provides: "DndContext wrapper over 7 day columns"
      contains: "DndContext"
    - path: "apps/web/components/training/ActivityCard.tsx"
      provides: "Compact card with type color chip, title, duration, kebab menu"
      contains: "useDraggable"
  key_links:
    - from: "TrainingClient subscribes to Realtime"
      to: "useTableSubscription('training_activities', userId)"
      via: "lib/realtime/useTableSubscription"
      pattern: "useTableSubscription"
    - from: "TrainingBoard onDragEnd"
      to: "moveActivity Server Action"
      via: "import from app/actions/training"
      pattern: "moveActivity"
---

<objective>
Build the weekly planner: route, server-component shell, client island with Realtime + TanStack Query, the @dnd-kit-powered TrainingBoard with 7 day columns, compact ActivityCard, inline create UI, header with week nav + adherence, and the sidebar nav entry.

Purpose: Surface 1 of 3 — the core planning experience users come to /training to use.

Output: Functional Mon-Sun planner with drag-between-days, inline create, prev/next week, adherence indicator, and live updates via Realtime.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-training-fitness-activity-planner/15-CONTEXT.md
@.planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md
@apps/web/components/tasks/KanbanBoard.tsx
@apps/web/components/tasks/KanbanColumn.tsx
@apps/web/components/tasks/KanbanDayHeader.tsx
@apps/web/components/tasks/TaskCard.tsx
@apps/web/components/tasks/TasksClient.tsx
@apps/web/components/shell/SidebarTree.tsx
@apps/web/lib/realtime/useTableSubscription.ts
@apps/web/app/actions/training.ts
@apps/web/lib/training/week.ts
@apps/web/lib/db/queries/training.ts
@CLAUDE.md

<interfaces>
From apps/web/lib/db/queries/training.ts (Wave 1):
```typescript
export type ActivityWithType = {
  id: string;
  userId: string;
  activityTypeId: string;
  scheduledDate: string; // YYYY-MM-DD
  title: string;
  description: string | null;
  plannedDurationMin: number | null;
  actualDurationMin: number | null;
  plannedDistanceKm: string | null;
  actualDistanceKm: string | null;
  status: "planned" | "done" | "cancelled" | "skipped";
  dayOrderIndex: number;
  type: { id: string; name: string; color: string; hasDistance: boolean; batchId: string | null };
};
export function getActivitiesInRange(userId: string, fromISO: string, toISO: string): Promise<ActivityWithType[]>;
export function getActivityTypes(userId: string): Promise<TypeWithBatch[]>;
export function getBatches(userId: string): Promise<Batch[]>;
export function getDistanceUnit(userId: string): Promise<"km" | "mi">;
```

From apps/web/app/actions/training.ts (Wave 1):
```typescript
export function createActivity(input): Promise<ActionResult<{ id: string }>>;
export function moveActivity(input: { id: string; scheduledDate: string; dayOrderIndex?: number }): Promise<ActionResult>;
export function deleteActivity(input: { id: string }): Promise<ActionResult>;
export function completeActivity(input): Promise<ActionResult>;
export function cancelActivity(input): Promise<ActionResult>;
export function skipActivity(input): Promise<ActionResult>;
```

From apps/web/lib/realtime/useTableSubscription.ts (existing):
```typescript
useTableSubscription(table: RealtimeTable, userId: string, options?: { alsoInvalidate?: ReadonlyArray<readonly [RealtimeTable, string]> }): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route + Server Component + TrainingClient shell with Realtime + Query</name>
  <files>apps/web/app/(app)/training/page.tsx, apps/web/components/training/TrainingClient.tsx, apps/web/components/training/PlannerHeader.tsx</files>
  <read_first>
    - apps/web/app/(app)/tasks/page.tsx (route + initial data load pattern)
    - apps/web/components/tasks/TasksClient.tsx (client island + Realtime + Query pattern)
    - apps/web/lib/realtime/useTableSubscription.ts (subscription API + alsoInvalidate)
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Client Realtime + Query block (lines around 675-710)
  </read_first>
  <action>
1. Create apps/web/app/(app)/training/page.tsx as a Server Component:
   - Get userId via getClaims().
   - Compute current week range with getWeekRange(new Date()) from lib/training/week.
   - Call getActivitiesInRange, getActivityTypes, getBatches, getDistanceUnit from lib/db/queries/training.
   - Render TrainingClient with userId, initialActivities, initialTypes, initialBatches, distanceUnit props.

2. Create apps/web/components/training/TrainingClient.tsx with "use client":
   - Props: userId, initialActivities, initialTypes, initialBatches, distanceUnit.
   - useState for currentWeek: Date (defaults to today). Derive start/end/days/startISO/endISO via getWeekRange.
   - Three useTableSubscription calls per RESEARCH client block:
     * useTableSubscription("training_activities", userId)
     * useTableSubscription("training_activity_types", userId, { alsoInvalidate: [["training_activities", userId]] })
     * useTableSubscription("training_batches", userId, { alsoInvalidate: [["training_activity_types", userId]] })
   - useQuery keyed ["training_activities", userId, startISO, endISO] with initialData. The queryFn calls a "use server" thin wrapper added to actions/training.ts named listActivitiesInRange(fromISO, toISO).
   - useQuery for types and batches similarly keyed via tableKey.
   - Render PlannerHeader (currentWeek, onWeekChange, adherence numbers, onManageTypesClick lifting a manageOpen boolean state per Pitfall 8).
   - Render TrainingBoard (days, activities, types, distanceUnit, userId).
   - First-time empty state: when types.length === 0 on mount, setManageOpen(true) auto (per D-07).

3. Create apps/web/components/training/PlannerHeader.tsx:
   - Layout: prev arrow, formatted date range "Mon Jun 8 – Sun Jun 14", next arrow, adherence pill, Manage types button.
   - Adherence calc: count(done) over count(non-cancelled-non-skipped planned) for the active week. Per TRN-12 / D-14.
   - Use date-fns format for the date range string.
   - lucide icons: ChevronLeft, ChevronRight, Settings.

Critical Pattern 3 enforcement: NEVER call queryClient.setQueryData from realtime handlers — useTableSubscription already handles invalidation correctly.

Per TRN-04, TRN-12, TRN-17, TRN-18 (header part).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "training/page|TrainingClient|PlannerHeader" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/app/(app)/training/page.tsx exists and references getActivitiesInRange
    - grep useTableSubscription apps/web/components/training/TrainingClient.tsx matches at least 3 times
    - grep -r setQueryData apps/web/components/training/ returns no matches (Critical Pattern 3)
    - grep useQuery apps/web/components/training/TrainingClient.tsx matches
    - grep -r getSession apps/web/app/(app)/training/ apps/web/components/training/ returns no matches (Critical Pattern 1)
    - grep getClaims apps/web/app/(app)/training/page.tsx matches
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    /training route renders; types-empty triggers Sheet auto-open; header shows week + adherence; live Realtime invalidations land.
  </done>
</task>

<task type="auto">
  <name>Task 2: TrainingBoard + TrainingDayColumn + ActivityCard + ActivityCreateInline with @dnd-kit</name>
  <files>apps/web/components/training/TrainingBoard.tsx, apps/web/components/training/TrainingDayColumn.tsx, apps/web/components/training/ActivityCard.tsx, apps/web/components/training/ActivityCreateInline.tsx</files>
  <read_first>
    - apps/web/components/shell/SidebarTree.tsx (the @dnd-kit precedent in this repo — DndContext, useDraggable, useDroppable, sensors)
    - apps/web/components/tasks/KanbanBoard.tsx + KanbanColumn.tsx + TaskCard.tsx (structural pattern reference ONLY — do NOT copy native HTML5 DnD)
    - .planning/phases/15-training-fitness-activity-planner/15-RESEARCH.md Anti-Patterns + visual density notes
    - apps/web/lib/training/distance.ts (formatDistance helper)
  </read_first>
  <action>
1. Create apps/web/components/training/TrainingBoard.tsx:
   - Props: days Date[], activities ActivityWithType[], types TypeWithBatch[], distanceUnit, userId.
   - Wrap in DndContext from @dnd-kit/core with PointerSensor (activation distance 4px) + KeyboardSensor per SidebarTree precedent.
   - handleDragEnd({ active, over }): if dropped over a different day column, call moveActivity({ id: active.id, scheduledDate: over.id }). The day column droppable id is the ISO date. Sonner toast on success; revert on error.
   - Render 7 TrainingDayColumn children, passing that day's filtered activities.
   - Grid: grid grid-cols-7 gap-2 (tighter than tasks).

2. Create apps/web/components/training/TrainingDayColumn.tsx:
   - Props: dateISO string, dayLabel string, activities, types, distanceUnit, userId.
   - useDroppable({ id: dateISO }) from @dnd-kit/core.
   - Column header: short day label, small text. Today gets an accent ring.
   - Activity list: map to ActivityCard.
   - Bottom: ActivityCreateInline.
   - Tighter density: px-2 py-1.5 text-xs per D-01.

3. Create apps/web/components/training/ActivityCard.tsx:
   - Props: activity ActivityWithType, distanceUnit, onCheckOff (callback that TrainingClient wires to the CompleteActivityDialog in Plan 15-04).
   - useDraggable({ id: activity.id }).
   - Layout: small rounded card. Left edge: 3px color stripe using style={{ backgroundColor: activity.type.color }}. Title (truncate). Subline: "{type.name} · {duration}min" plus "· {formatDistance(km, unit)}" when hasDistance.
   - Status visuals:
     * planned: default
     * done: subtle line-through + ~60% opacity + Check glyph
     * cancelled: strikethrough + muted + X glyph
     * skipped: italic + muted + MinusCircle glyph
   - Click on card body invokes onCheckOff(activity) (the dialog itself ships in 15-04).
   - Kebab DropdownMenu items: Mark done (invokes onCheckOff), Mark cancelled (cancelActivity), Mark skipped (skipActivity), Edit (callback wired in 15-04), Delete (deleteActivity with confirmation).
   - Use Motion motion.div with layoutId={activity.id} for slide-on-rerank polish.

4. Create apps/web/components/training/ActivityCreateInline.tsx:
   - Props: dateISO, types, distanceUnit, userId.
   - Default state: small "+ Add activity" ghost button that expands to a form on click.
   - Form fields: type Select (ordered by orderIndex, grouped by batch name, required), title input (required, autoFocus on expand), planned duration min input (optional, number), planned distance input (only when selected type.hasDistance, label reflects distanceUnit).
   - Submit: convert displayed distance to km via displayToKm. Call createActivity({ id: crypto.randomUUID(), activityTypeId, scheduledDate: dateISO, title, plannedDurationMin, plannedDistanceKm }). Optimistic UUID per RT-05.
   - Esc collapses form; Enter submits.

Per TRN-04, TRN-05, TRN-06, TRN-08 (status kebab).
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&amp;1 | grep -E "training/(Training|Activity)" | head -10 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep "from \"@dnd-kit/core\"" apps/web/components/training/TrainingBoard.tsx matches
    - grep DndContext apps/web/components/training/TrainingBoard.tsx matches
    - grep useDroppable apps/web/components/training/TrainingDayColumn.tsx matches
    - grep useDraggable apps/web/components/training/ActivityCard.tsx matches
    - grep moveActivity apps/web/components/training/TrainingBoard.tsx matches
    - grep createActivity apps/web/components/training/ActivityCreateInline.tsx matches
    - grep crypto.randomUUID apps/web/components/training/ActivityCreateInline.tsx matches (RT-05)
    - grep displayToKm apps/web/components/training/ActivityCreateInline.tsx matches
    - No native HTML5 DnD attrs: grep -E "onDragStart|onDrop=|draggable=" apps/web/components/training/ returns no matches
    - grep -r setQueryData apps/web/components/training/ returns no matches
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    Drag-between-days works via @dnd-kit; cards render with status visuals; inline create produces optimistic rows; no HTML5 DnD leakage.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Training entry to sidebar nav</name>
  <files>apps/web/components/shell/SidebarTree.tsx</files>
  <read_first>
    - apps/web/components/shell/SidebarTree.tsx (locate the top-level surface list — look for existing /tasks, /captures, /habits, /calendar, /lifeos entries)
    - apps/web/components/shell/Sidebar.tsx if SidebarTree doesn't own nav entries
  </read_first>
  <action>
Locate the top-level nav entry list. Insert a "Training" entry alongside the other top-level surfaces (Tasks, Captures, Habits, Calendar, LifeOS).

- Icon: lucide Dumbbell (or Activity if Dumbbell is already used elsewhere)
- Label: "Training"
- href: "/training"
- Position: after Habits (verify by reading current ordering before inserting).

Per TRN-18.
  </action>
  <verify>
    <automated>grep -r "/training" apps/web/components/shell/ | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "/training" apps/web/components/shell/ matches at least once
    - grep -r "Training" apps/web/components/shell/ matches the label
    - TS typecheck passes
  </acceptance_criteria>
  <done>
    Sidebar shows Training entry linking to /training.
  </done>
</task>

</tasks>

<verification>
- pnpm --filter web exec tsc --noEmit passes
- Manual smoke: visit /training, see 7 columns, create activity, drag to another day, observe Realtime echo in second tab
- grep -r setQueryData apps/web/components/training/ returns no matches (Critical Pattern 3)
- grep -r getSession apps/web/app/(app)/training/ apps/web/components/training/ returns no matches (Critical Pattern 1)
</verification>

<success_criteria>
- Planner ships, drag-drop works, header adherence visible, sidebar nav wired
- Empty-types state auto-opens Manage sheet (sheet itself in 15-04)
- All Realtime + Query patterns conform to Critical Pattern 3
</success_criteria>

<output>
Create .planning/phases/15-training-fitness-activity-planner/15-03-SUMMARY.md summarizing: routes, components, props contracts, lifted state for the Sheet (so 15-04 can wire it).
</output>
