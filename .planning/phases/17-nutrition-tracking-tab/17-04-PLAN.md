---
phase: 17-nutrition-tracking-tab
plan: 04
type: execute
wave: 4
depends_on: ["17-03"]
files_modified:
  - apps/web/components/nutrition/FoodSearch.tsx
  - apps/web/components/nutrition/FoodSearchResult.tsx
  - apps/web/components/nutrition/ServingPicker.tsx
  - apps/web/components/nutrition/ManualEntryForm.tsx
  - apps/web/components/nutrition/MealsManagerSheet.tsx
  - apps/web/components/nutrition/MealSlot.tsx
  - apps/web/components/nutrition/NutritionClient.tsx
  - apps/web/components/nutrition/QuickAddComposer.tsx
  - apps/web/app/(app)/settings/nutrition/page.tsx
  - apps/web/components/nutrition/NutritionTargetsForm.tsx
autonomous: true
requirements:
  - NUTR-SEARCH-01
  - NUTR-LOG-01
  - NUTR-MANUAL-01
  - NUTR-MEALS-01
  - NUTR-TARGETS-UI-01
  - NUTR-QUICKADD-01
must_haves:
  truths:
    - "User can search foods: history results appear instantly; OFF results appear after 300ms debounce + 2+ chars"
    - "User can select a food → pick serving option + quantity → see live macro preview → click Log → row appears in correct meal slot"
    - "When OFF returns no results, 'Enter manually' fallback opens a form that creates a food + log in one flow (D-03)"
    - "User can save a meal grouping (name + items with quantities) and log it whole into a slot (D-07 reusable meals)"
    - "Global keyboard shortcut 'n' opens FoodSearch with time-of-day meal slot pre-selected (D-07 dual entry)"
    - "Settings → Nutrition lets user set targetKcal + protein/carbs/fat % with auto-adjust (sum=100) + live gram preview (D-09)"
  artifacts:
    - path: "apps/web/components/nutrition/FoodSearch.tsx"
      provides: "Sheet/Popover with search input + grouped Recents + OFF results + manual fallback"
    - path: "apps/web/components/nutrition/ServingPicker.tsx"
      provides: "Select (serving unit) + Input (quantity multiplier) + live macro preview"
    - path: "apps/web/components/nutrition/ManualEntryForm.tsx"
      provides: "Manual food creation: name + macros + serving info → upsertFoodAction → logFoodAction"
    - path: "apps/web/components/nutrition/MealsManagerSheet.tsx"
      provides: "Create/list/edit saved meals + log-meal-into-slot affordance"
    - path: "apps/web/app/(app)/settings/nutrition/page.tsx"
      provides: "Settings → Nutrition page (NUTR-TARGETS-UI-01 D-09)"
    - path: "apps/web/components/nutrition/QuickAddComposer.tsx"
      provides: "Global 'n' shortcut handler + sheet that pre-selects time-of-day slot"
  key_links:
    - from: "FoodSearch.tsx onSelect"
      to: "logFoodAction"
      via: "ServingPicker confirm → Server Action → Realtime invalidation"
      pattern: "logFoodAction\\("
    - from: "ManualEntryForm.tsx onSubmit"
      to: "upsertFoodAction → logFoodAction"
      via: "two-step: create food then log it"
      pattern: "upsertFoodAction"
    - from: "NutritionTargetsForm.tsx onSubmit"
      to: "upsertNutritionTargetsAction"
      via: "Zod sum=100 refine"
      pattern: "upsertNutritionTargetsAction"
---

<objective>
Make the day view interactive: food search, manual entry, serving picker, meals manager, global quick-add, and the targets settings page. After this plan, the core MyFitnessPal-style loop works end-to-end.

Purpose: Closes D-02, D-03, D-05, D-07, D-08, D-09 at the UI layer.
Output: Full logging loop usable; targets settable; meals reusable; quick-add via keyboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-nutrition-tracking-tab/17-CONTEXT.md
@.planning/phases/17-nutrition-tracking-tab/17-UI-SPEC.md
@.planning/phases/17-nutrition-tracking-tab/17-RESEARCH.md
@apps/web/components/nutrition/NutritionClient.tsx
@apps/web/components/nutrition/MealSlot.tsx
@apps/web/components/ui/sheet.tsx
@apps/web/components/ui/popover.tsx
@apps/web/components/ui/input.tsx
@apps/web/components/ui/select.tsx
@apps/web/components/ui/dialog.tsx
@apps/web/app/actions/nutrition.ts
@apps/web/lib/nutrition/macro-math.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: FoodSearch + FoodSearchResult + ServingPicker + ManualEntryForm</name>
  <files>apps/web/components/nutrition/FoodSearch.tsx, apps/web/components/nutrition/FoodSearchResult.tsx, apps/web/components/nutrition/ServingPicker.tsx, apps/web/components/nutrition/ManualEntryForm.tsx, apps/web/components/nutrition/MealSlot.tsx, apps/web/components/nutrition/NutritionClient.tsx</files>
  <read_first>
    - apps/web/components/nutrition/MealSlot.tsx (Plan 03 stub — wire onAddFood to open FoodSearch)
    - apps/web/components/nutrition/NutritionClient.tsx (Plan 03 — add FoodSearch state, currentSlot, isSearchOpen)
    - apps/web/components/ui/sheet.tsx, popover.tsx, input.tsx, select.tsx (shadcn primitives — props + Tailwind class conventions)
    - .planning/phases/17-nutrition-tracking-tab/17-UI-SPEC.md (Food Search Surface, Serving Picker, Interaction Contracts: D-07, D-08, copy strings)
    - apps/web/lib/nutrition/macro-math.ts (computeMacros for live preview)
  </read_first>
  <action>
**File 1: `FoodSearch.tsx`** — `"use client"`. Props: `{ open, onOpenChange, mealSlot, date, foodHistory, userId, onLogged }`.
- Render as a shadcn `Sheet` (side="bottom" on small screens) — UI-SPEC line 187.
- Inside: shadcn `Input` autoFocus, placeholder `"Search foods or enter a name…"` (exact UI-SPEC copy), `--ring-hud` focus ring (className `focus-visible:ring-[var(--ring-hud)]`).
- Debounce: `const deferredQ = useDeferredValue(q)` + `useEffect` calling `/api/nutrition/search?q=...` after 300ms when deferredQ.length >= 2 (React 19 idiomatic per UI-SPEC).
- Sections:
  - `"Recent"` header (uppercase mono 10.5px, `Clock` icon) → filter `foodHistory` by q (client-side ilike) → list as `<FoodSearchResult />`.
  - `"From Open Food Facts"` header → OFF results from fetch.
  - When OFF result count is 0 and q.length >= 2: render `<button>Can't find it? Enter it manually.</button>` (exact UI-SPEC copy) → opens `ManualEntryForm` inline.
  - When OFF still loading: skeleton row with `hud-receipt-shimmer` class.
- Keyboard: ArrowUp/ArrowDown to navigate; Enter to select; Escape closes (Sheet handles Esc).
- On select → swap sheet content to `<ServingPicker food={selected} onConfirm={handleLog} />`.

`handleLog({ servingOptionId, quantity })`:
1. If food is from OFF (no DB id yet): call `upsertFoodAction(normalizedOff)` → returns `{ food, servingOptions }` → pick the user-chosen servingOption from returned set.
2. Call `logFoodAction({ date, mealSlot, foodId: food.id, servingOptionId, quantity })`.
3. Close sheet, fire sonner success toast (no specific copy mandated; brief).
4. Realtime invalidates the food_logs query → row appears.

**File 2: `FoodSearchResult.tsx`** — props: `{ result, isFocused, onSelect }`. Renders one row:
- Food name (serif 16px), brand (serif 16px `var(--ink-muted)`), kcal/100g (mono 10.5px ink-muted on right).
- `aria-selected={isFocused}`, on click → `onSelect(result)`.
- Hover: `bg-[var(--surface)]` tint (no glass per UI-SPEC).

**File 3: `ServingPicker.tsx`** — props: `{ food, servingOptions, onConfirm, onCancel }`.
- shadcn `Select`: lists servingOptions (default option first per `isDefault`); fallback "100 g" always present.
- shadcn `Input` type=number step=0.1 min=0, default value `1`.
- Live macro preview line below: `"→ {kcal} kcal · P {p}g · C {c}g · F {f}g"` in mono 10.5px ink-muted. Updates on every keystroke via `useMemo` over `computeMacros(quantity * servingOption.gramsOrMl, food)`.
- Confirm: `<button className="glass-button rounded-md">Log</button>` OR Enter key.

**File 4: `ManualEntryForm.tsx`** — props: `{ initialName?, onCreated }`. Form fields (react-hook-form + zodResolver):
- name (text, required)
- brand (text, optional)
- kcalPer100g, proteinPer100g, carbsPer100g, fatPer100g (number, required ≥ 0)
- fiberPer100g (number, optional ≥ 0)
- baseUnit (radio: "g" | "ml")
- servingLabel (text, e.g., "1 cup") + servingGramsOrMl (number) — optional set; if provided, seed as product serving on the food.
- Submit → `upsertFoodAction({ ...manualInput, isManual: true })` → on success, callbacks `onCreated(food)`.
- Heading: "Add a food manually" (exact UI-SPEC copy).

**File 5: Update `MealSlot.tsx`** (from Plan 03) — wire the "Log food" button: `onClick={() => onAddFood?.(slot)}`. The parent (`NutritionClient`) passes `onAddFood` that toggles search state.

**File 6: Update `NutritionClient.tsx`** — add:
- `const [searchState, setSearchState] = useState<{ open: boolean; slot: MealSlot } | null>(null);`
- Pass `onAddFood={(slot) => setSearchState({ open: true, slot })}` down to NutritionDayView → MealSlot.
- Render `<FoodSearch open={searchState?.open ?? false} onOpenChange={(o) => o ? null : setSearchState(null)} mealSlot={searchState?.slot ?? "breakfast"} date={date} foodHistory={foodHistory} userId={userId} />`

Implements NUTR-SEARCH-01, NUTR-LOG-01, NUTR-MANUAL-01.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep -iE "FoodSearch|ServingPicker|ManualEntry" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"Search foods or enter a name…"' apps/web/components/nutrition/FoodSearch.tsx` matches verbatim
    - `grep "Can't find it" apps/web/components/nutrition/FoodSearch.tsx` matches
    - `grep "/api/nutrition/search" apps/web/components/nutrition/FoodSearch.tsx` matches
    - `grep "useDeferredValue" apps/web/components/nutrition/FoodSearch.tsx` matches (React 19 debounce per UI-SPEC)
    - `grep "Add a food manually" apps/web/components/nutrition/ManualEntryForm.tsx` matches
    - `grep "computeMacros" apps/web/components/nutrition/ServingPicker.tsx` matches (live preview)
    - `grep "upsertFoodAction" apps/web/components/nutrition/FoodSearch.tsx` matches
    - `grep "logFoodAction" apps/web/components/nutrition/FoodSearch.tsx` matches
    - `grep "Log" apps/web/components/nutrition/ServingPicker.tsx` matches (confirm button)
    - `pnpm exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>End-to-end log flow works from search → serving → log → row in slot.</done>
</task>

<task type="auto">
  <name>Task 2: MealsManagerSheet + QuickAddComposer (keyboard 'n')</name>
  <files>apps/web/components/nutrition/MealsManagerSheet.tsx, apps/web/components/nutrition/QuickAddComposer.tsx, apps/web/components/nutrition/NutritionClient.tsx</files>
  <read_first>
    - apps/web/lib/nutrition/nutrition-service.ts (createMeal, logMeal signatures)
    - apps/web/app/actions/nutrition.ts (createMealAction, logMealAction)
    - apps/web/components/nutrition/FoodSearch.tsx (Task 1 — reused inside meals manager for adding items)
    - .planning/phases/17-nutrition-tracking-tab/17-UI-SPEC.md (D-07 dual entry, time-of-day slot defaulting)
    - apps/web/components/jarvis/* or any existing global hotkey hook (look for useEffect on window keydown for 'n' or similar — match pattern; alternative: use Phase 5 Cmd+K pattern as reference)
  </read_first>
  <action>
**File 1: `MealsManagerSheet.tsx`** — Sheet with two modes:
- **List mode** (default): list saved meals from `useQuery({ queryKey: ["meals", userId], ... })` → each meal row shows name + item count + total kcal + button "Log into Breakfast/Lunch/Dinner/Snacks" (Select).
- **Create mode**: form with name, description, and a dynamic list of items. Each item row: food search (reuse FoodSearch inline / collapsed picker) + serving option + quantity. Add/remove rows with + / Trash2.
- Submit (Create mode): `createMealAction({ name, description, items: items.map((it, i) => ({ ...it, orderIndex: i })) })`.
- Log-into-slot (List mode): `logMealAction({ mealId, date: currentDate, mealSlot })`.

Mount as a Sheet from a "Meals" button in NutritionClient header (alongside DayNavigator).

**File 2: `QuickAddComposer.tsx`** — global keyboard shortcut handler.
- `useEffect` registers `keydown` listener on window. When key === "n" AND not inside an input/textarea/contenteditable AND no modifier keys → open a global FoodSearch sheet.
- On open, compute default meal slot from `new Date().getHours()`:
  - `<10` → "breakfast"
  - `10..<14` → "lunch"
  - `17..<21` → "dinner"
  - else → "snacks"
- Sheet content reuses `<FoodSearch>` with the computed slot pre-selected (allow user to change via a small slot pill bar above the input).
- Cleanup listener on unmount.

**File 3: Update `NutritionClient.tsx`** — mount `<QuickAddComposer userId={userId} date={date} foodHistory={foodHistory} />` and add a "Meals" button next to the DayNavigator that opens `<MealsManagerSheet />`.

Implements NUTR-MEALS-01, NUTR-QUICKADD-01.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep -iE "MealsManager|QuickAdd" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "createMealAction" apps/web/components/nutrition/MealsManagerSheet.tsx` matches
    - `grep "logMealAction" apps/web/components/nutrition/MealsManagerSheet.tsx` matches
    - `grep -E 'key\s*===?\s*"n"|e\.key\s*===?\s*"n"' apps/web/components/nutrition/QuickAddComposer.tsx` matches
    - `grep -E "getHours\\(\\)" apps/web/components/nutrition/QuickAddComposer.tsx` matches (time-of-day defaulting)
    - `grep -E "\"breakfast\"|\"lunch\"|\"dinner\"|\"snacks\"" apps/web/components/nutrition/QuickAddComposer.tsx | wc -l` returns at least 4
    - `grep "QuickAddComposer" apps/web/components/nutrition/NutritionClient.tsx` matches (mounted)
    - `pnpm exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>Meals reusable; 'n' opens global quick-add with time-of-day default.</done>
</task>

<task type="auto">
  <name>Task 3: Settings → Nutrition page + NutritionTargetsForm (D-09)</name>
  <files>apps/web/app/(app)/settings/nutrition/page.tsx, apps/web/components/nutrition/NutritionTargetsForm.tsx</files>
  <read_first>
    - apps/web/app/(app)/settings/page.tsx OR existing settings subpages (e.g., settings/memory/page.tsx) for the Server Component + section layout precedent
    - apps/web/components/settings/SettingsSectionNav.tsx (to see how to register a new settings section — may require adding a "Nutrition" entry to the nav array if applicable)
    - apps/web/lib/nutrition/nutrition-service.ts (getNutritionTargets)
    - apps/web/app/actions/nutrition.ts (upsertNutritionTargetsAction)
    - .planning/phases/17-nutrition-tracking-tab/17-UI-SPEC.md (Target % Auto-Adjust section)
  </read_first>
  <action>
**File 1: `apps/web/app/(app)/settings/nutrition/page.tsx`** — Server Component:
```typescript
import { requireOnboarded } from "@/lib/auth/get-user";
import { getNutritionTargets } from "@/lib/nutrition/nutrition-service";
import { NutritionTargetsForm } from "@/components/nutrition/NutritionTargetsForm";

export default async function NutritionSettingsPage() {
  const user = await requireOnboarded();
  const targets = await getNutritionTargets(user.id);
  return (
    <section className="mx-auto max-w-2xl px-4 py-12">
      <h1 className="font-[var(--font-serif)] text-[28px]">Nutrition targets</h1>
      <p className="text-[var(--ink-muted)] mt-2">Set your daily calorie target and the macro split.</p>
      <NutritionTargetsForm initialTargets={targets} />
    </section>
  );
}
```

If `SettingsSectionNav.tsx` items array drives the settings section list, add a "Nutrition" entry pointing to `/settings/nutrition`.

**File 2: `NutritionTargetsForm.tsx`** — `"use client"`, react-hook-form + zodResolver:
- Fields: `targetKcal` (number, integer, ≥ 1), `proteinPct`, `carbsPct`, `fatPct` (numbers 0–100, with one decimal).
- Auto-adjust: when user edits one of the three % fields, scale the OTHER TWO proportionally so they sum to 100. Pattern:
  ```typescript
  function rebalance(changed: "p"|"c"|"f", newVal: number, current: {p,c,f}) {
    const remainingTotal = 100 - newVal;
    const otherSum = changed === "p" ? current.c + current.f : changed === "c" ? current.p + current.f : current.p + current.c;
    if (otherSum <= 0) {
      // split remaining evenly between the other two
      return { ...current, [changed]: newVal, /* assign evenly */ };
    }
    const scale = remainingTotal / otherSum;
    // scale the other two proportionally
  }
  ```
- Live gram preview using `deriveTargetGrams` from macro-math.ts — render below each macro % field: "= {g}g".
- Submit → `upsertNutritionTargetsAction({ targetKcal, proteinPct, carbsPct, fatPct })`. On Zod error from server (sum ≠ 100), show the exact UI-SPEC copy: "Percentages must add up to 100. Adjust the values to continue."
- Save button: `<button className="glass-button rounded-md">Save targets</button>`.

Implements NUTR-TARGETS-UI-01.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit 2>&1 | grep -iE "settings/nutrition|NutritionTargetsForm" || echo "clean"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/app/(app)/settings/nutrition/page.tsx` exists with `getNutritionTargets` import
    - `grep "Nutrition targets" apps/web/app/(app)/settings/nutrition/page.tsx` matches
    - `grep "upsertNutritionTargetsAction" apps/web/components/nutrition/NutritionTargetsForm.tsx` matches
    - `grep "deriveTargetGrams" apps/web/components/nutrition/NutritionTargetsForm.tsx` matches
    - `grep "Percentages must add up to 100" apps/web/components/nutrition/NutritionTargetsForm.tsx` matches (exact UI-SPEC copy)
    - `pnpm exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>User can set + save targets; auto-adjust keeps sum=100; gram preview live.</done>
</task>

</tasks>

<verification>
- Manual: type 'banana' in search → see OFF results → click → adjust serving to 1 medium (118g) → see live preview → Log → row appears in selected meal slot with correct kcal.
- Press 'n' anywhere → quick-add opens with time-of-day meal slot.
- Visit /settings/nutrition → edit protein % → carbs/fat scale → save → returns to day view → macro summary updated.
</verification>

<success_criteria>
Full MFP-style logging loop usable. D-02/03/05/07/08/09 closed at UI layer.
</success_criteria>

<output>
After completion, create `.planning/phases/17-nutrition-tracking-tab/17-04-SUMMARY.md` documenting: components shipped, decisions implemented, copy verbatim, manual test results.
</output>
