---
phase: 17
slug: nutrition-tracking-tab
status: draft
shadcn_initialized: true
preset: detected from components.json (tailwind v4 + react 19)
created: 2026-06-12
---

# Phase 17 — UI Design Contract

> Visual and interaction contract for the nutrition tracking tab.
> Generated by gsd-ui-researcher. All decisions pre-populated from CONTEXT.md, RESEARCH.md, and live codebase scan.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (already initialized — components.json present) |
| Preset | Tailwind 4 / React 19 branch (detected from project) |
| Component library | Radix UI primitives via shadcn |
| Icon library | Lucide React 0.460.x — `UtensilsCrossed` for nav, `Plus` for add food, `ChevronLeft`/`ChevronRight` for day navigation, `Trash2` for delete log, `Search` for search input, `Clock` for recents, `Star` for frequents |
| Font (serif / body) | EB Garamond via `next/font/google` — `var(--font-serif)` |
| Font (mono / labels) | JetBrains Mono via `next/font/local` — `var(--font-mono)` |

Source: CONTEXT.md D-13; `apps/web/app/globals.css` `@theme` block.

---

## Spacing Scale

Declared values (multiples of 4 — matches Tailwind 4 default scale):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, chip inline padding |
| sm | 8px | Food log row padding, macro label spacing |
| md | 16px | Section padding (px-4), card body padding, dialog content padding |
| lg | 24px | Meal slot header-to-list gap, section breaks |
| xl | 32px | Day view top padding, stats section gaps |
| 2xl | 48px | Major section breaks (day view ↔ heat map) |
| 3xl | 64px | Page-level top padding (matching training tab pattern) |

**Grid exceptions — established production precedents, exempt from the 4pt grid rule:**

- **Pill bar geometry (`px-3.5 py-1.5` = 14px / 6px) and pill rail (`px-2 py-1.5`):** Lifted verbatim from `apps/web/components/settings/SettingsSectionNav.tsx` lines 66–70. This component was user-locked as the visual reference in CONTEXT.md D-13. These values are exempt from the 4pt grid rule as established production precedents.
- **Heat map cells (10px × 10px with 2px gap):** Non-interactive data-visualization element. Cell geometry is sized for readability at year scale (52 weeks × 7 days in a compact grid) and is not a touch target. Exempt from the 4pt grid rule as a data-visualization carve-out. `grid-template-rows: repeat(7, 10px); gap: 2px` applied exactly.
- **Food log row minimum height:** 44px — touch target minimum, unchanged.

Source: `apps/web/components/settings/SettingsSectionNav.tsx` lines 66–70; heat map pattern from RESEARCH.md Pattern 5.

---

## Typography

Exactly 4 distinct sizes. No exceptions.

| Role | Family | Size | Weight | Line Height | Usage |
|------|--------|------|--------|-------------|-------|
| Mono label | `var(--font-mono)` (JetBrains Mono) | 10.5px | 400 | 1 | Meal slot pill bar labels (`BREAKFAST`, `LUNCH`, `DINNER`, `SNACKS`), macro stat labels, serving unit selector label, heat map day-of-week label, "consumed / target" progress label, food log quantity display, search result brand text, stats chart x-axis |
| Body | `var(--font-serif)` (EB Garamond) | 16px (1rem) | 400 | 1.5 | Food item names, brand subtext (at `var(--ink-muted)`), dialog body text, notes, day navigator date label |
| Heading / Stat display | `var(--font-serif)` + `var(--font-mono)` `.font-mono-stats` | 20px | 400 | 1.2 | Meal slot section headings ("Breakfast") and page title use `var(--font-serif)` at 20px; calorie total (consumed, remaining, target) and macro gram totals use `var(--font-mono)` `.font-mono-stats` at 20px. Same size slot, different family by semantic role. |
| Stat large | `var(--font-mono)` `.font-mono-stats` | 28px | 400 | 1 | Calorie total in the daily macro summary bar only — the primary focal point number. All other stat displays remain at 20px. |

**Rules:**
- Brand subtext (food product brand names in search results and log rows) renders in `var(--font-serif)` at 16px with `color: var(--ink-muted)` — NOT a separate size.
- Macro percentages (protein / carbs / fat split) rendered in `.font-mono-stats` at 20px for tabular alignment.
- All pill tab labels: uppercase, `tracking-[0.14em]` — exactly matching SettingsSectionNav.tsx line 98.
- 10.5px is the established production value from `SettingsSectionNav.tsx` (`text-[10.5px]`). 11px is NOT used anywhere in this phase.

**Primary visual anchor:** The calorie total in the macro summary bar is the primary visual focal point of the day view. It renders at 28px in `font-mono-stats` — the only element in this phase at that size. Everything else in the stats hierarchy descends from it.

Source: `apps/web/app/globals.css` `--font-serif`, `--font-mono`, `.font-mono-stats`; SettingsSectionNav.tsx line 98.

---

## Color

| Role | Token | Light mode OKLCH | Dark mode OKLCH | Usage |
|------|-------|-----------------|-----------------|-------|
| Dominant (60%) | `--canvas` | oklch(97% 0.005 75) | oklch(15% 0.005 240) | Page background, day view background |
| Secondary (30%) | `--surface` | oklch(94% 0.008 75) | oklch(19% 0.006 240) | Meal slot cards, food log rows, pill rail background |
| Raised surface | `--surface-raised` | oklch(99% 0.003 75) | oklch(23% 0.007 240) | Search result popover, dialogs, tooltips |
| Accent (10%) | `--hud-cyan` | oklch(72% 0.13 210) | oklch(72% 0.13 210) | Reserved elements only — see below |
| Body text | `--ink` | oklch(22% 0.01 60) | oklch(92% 0.01 90) | All primary readable text |
| Muted text | `--ink-muted` | oklch(50% 0.01 60) | oklch(65% 0.01 90) | Brand names, quantity labels, placeholder text, empty state description |
| Borders | `--edge` | oklch(86% 0.008 75) | oklch(28% 0.008 240) | Card borders, row separators, pill rail border |
| Destructive | `--ink-coral` | oklch(63% 0.16 25) | oklch(68% 0.15 25) | Delete log confirmation only |
| Calorie success | `--ink-sage` | oklch(62% 0.09 145) | oklch(68% 0.10 145) | "Target met" toast, heat map level-4 cell glow, over-target warning suppressed |
| Calorie warn | `--ink-amber` | oklch(70% 0.13 75) | oklch(72% 0.12 75) | Focus rings (journal mode), optional "over target" callout |

**Accent (`--hud-cyan`) is reserved strictly for:**
- Active pill indicator in the meal slot pill bar (the `motion.span` layoutId pill)
- Heat map level-4 cells (adherence ≥ 100% of calorie target)
- Input focus ring in food search (`--ring-hud`)
- Progress bar fill when calories approach target (≥ 70%)
- Scrollbar thumb (global — already applied)

**Accent (`--hud-cyan`) is NOT used on:**
- Passive food log rows
- Meal slot section headings
- Stats charts (use `--ink-sage` for protein, `--ink-amber` for carbs, `--ink-coral` for fat — three academic inks)
- Call-to-action buttons (use `--ink` on `--surface`, glass-button pattern)

Source: `apps/web/app/globals.css` — all tokens. D-13 (glass on interactive surfaces, document discipline elsewhere).

### Heat Map Color Scale

5-level adherence encoding (daily kcal logged ÷ target_kcal, clamped [0, 1.0+]):

| Level | Adherence | CSS |
|-------|-----------|-----|
| 0 (empty) | 0% logged | `var(--surface)` + `border: 1px solid var(--edge)` |
| 1 | 1–39% | `oklch(30% 0.08 210)` — very dim cyan |
| 2 | 40–69% | `oklch(45% 0.13 210)` — mid cyan |
| 3 | 70–99% | `oklch(60% 0.18 210)` — near-full cyan |
| 4 | 100%+ | `var(--hud-cyan)` — full JARVIS signature |

Tooltip on hover: "{date} — {kcal_logged} kcal ({adherence_pct}% of target)"

Rationale: adherence % is more meaningful than raw calories when the target changes. Encoding is Claude's discretion per D-12.

---

## Layout Contract

### Tab Route

`/nutrition` — registered in `PersistentNav.tsx` and `TopTabBar.tsx`.
Icon: `UtensilsCrossed` from `lucide-react`.
Label: "Nutrition".

### Page Structure

```
/nutrition (Server Component shell — mirrors /training/page.tsx)
├── NutritionClient.tsx (client island — TanStack Query + Realtime)
│   ├── Day Navigator (← {date} →)  [header row]
│   ├── Daily Macro Summary Bar      [sticky below header, collapses on scroll]
│   ├── Meal Slot Pill Bar           [glass pill nav — SettingsSectionNav pattern]
│   └── Active Meal Slot View
│       ├── FoodLogRow[]             [logged items]
│       ├── MealSlot Totals Row      [macro subtotals for slot]
│       └── "+ Add Food" button      [inline, opens FoodSearch]
└── FoodSearch (popover/sheet)
    ├── Search Input (search-as-you-type, 300ms debounce)
    ├── Recents / Frequents Section  [personal history, sorted by lastUsedAt then useCount]
    ├── OFF Search Results Section   [appears after 2+ chars typed]
    └── "Enter manually" fallback    [appears when no results or user chooses]

/nutrition/stats (Server Component shell — mirrors /training/stats)
├── NutritionStatsClient.tsx
│   ├── NutritionHeatMap             [year grid, CSS-only, date-fns]
│   ├── 7-day Macro Trend            [recharts LineChart, three academic ink colors]
│   └── Personal Bests strip        [longest streak, best adherence day — mono display]
```

### Meal Slot Pill Bar

Mirrors `SettingsSectionNav.tsx` exactly:
- Rail: `rounded-full px-2 py-1.5 backdrop-blur-md bg-[color-mix(in_oklch,var(--surface)_88%,transparent)] shadow-[inset_0_1px_0...] border border-[color-mix(in_oklch,var(--edge)_60%,transparent)]`
- Active pill: `motion.span` with `layoutId="nutrition-slot-pill"` spring animation (`stiffness: 360, damping: 32`)
- Labels: `font-mono text-[10.5px] uppercase tracking-[0.14em]` — values are "BREAKFAST", "LUNCH", "DINNER", "SNACKS"
- Inactive label color: `var(--ink-muted)`, active: `var(--ink)`
- Pill background: `var(--surface)` with neumorphic inset shadow matching SettingsSectionNav.tsx line 89

### Daily Macro Summary Bar

Sticky below the day navigator. Shows:
- Large calorie display: consumed kcal in `font-mono-stats` at 28px (primary focal point), "/ {target}" in `var(--ink-muted)` at 16px mono
- Three macro progress rows (protein, carbs, fat): label (uppercase mono 10.5px) + progress bar + gram count (mono 20px)
- Progress bar fill: `var(--hud-cyan)` at 0–99%, `var(--ink-sage)` at 100%+ (target met signal)
- Collapses to a single-line thin strip on downward scroll via `motion/react` layout animation

### Food Log Row

- Height: 44px minimum (touch target)
- Structure: [Food name (serif 16px)] [quantity + unit (mono 10.5px, ink-muted)] [kcal (mono-stats 20px)] [kebab menu]
- Hover: `glass-button` pressed inset subtly
- Swipe-to-delete on touch: expose red `Trash2` icon button (via `pointer-fine` variant to hide swipe UI on mouse)
- Delete: fires `use-undo-toast.ts` pattern — 5s undo window with sage-accent left-border toast

### Food Search Surface

Opens as a `Sheet` (shadcn) from bottom on first load; on subsequent opens uses a `Popover` anchored to the triggering "+ Add" button.

- Search input: full-width, autofocus, `--ring-hud` focus ring (it is the primary action surface — JARVIS-adjacent feel)
- Results grouped: "Recent" section header (clock icon, mono label) → personal history items; "From Open Food Facts" section header → OFF results
- Each result: food name (serif 16px) + brand (serif 16px, ink-muted) + kcal/100g (mono 10.5px, ink-muted)
- Keyboard: ↑/↓ navigate results, Enter selects, Escape dismisses
- On select: inline ServingPicker appears (shadcn `Select` for unit + shadcn `Input` for quantity multiplier), confirm with Enter or "Log" button

---

## Interaction Contracts

### D-07: Dual Entry Points

Both "+ Add Food" per-slot (inline) and a global quick-add (keyboard shortcut `n` anywhere on the page) open the same `FoodSearch` sheet/popover. The global version pre-selects the meal slot based on time of day (Breakfast before 10am, Lunch 10am–2pm, Dinner 5pm–9pm, Snacks otherwise) — Claude's discretion default.

### D-08: Search-as-you-type

- Debounce: 300ms (`useDeferredValue` + `useEffect` — React 19 idiomatic, no external dep)
- Min chars to trigger OFF search: 2
- Personal history results appear instantly (local data, no debounce)
- Loading state: single-line skeleton row with `hud-receipt-shimmer` animation during OFF fetch

### Serving Picker

After selecting a food:
- `Select` (shadcn) lists serving options: product serving first (if available), then "100 g" fallback
- `Input` (shadcn) for quantity multiplier — numeric, defaulting to 1
- Live computed macro preview: "→ {kcal} kcal · P {p}g · C {c}g · F {f}g" in mono 10.5px, ink-muted, updates on every keystroke
- Confirm: "Log" button (glass-button pattern) OR Enter key

### Day Navigator

- Left/right arrow buttons (ChevronLeft / ChevronRight, glass-button circle)
- Center: date in serif 16px ("Today", "Yesterday", or "Mon, Jun 10" format via `date-fns format`)
- "Copy from yesterday" button appears in header ONLY when today's logs are empty — label: "Copy yesterday" (mono 10.5px, ink-muted)

### Target % Auto-Adjust (Settings)

In the nutrition targets settings form:
- When user edits one macro %, the remaining percentage adjusts proportionally to keep sum = 100
- Validation: Zod refinement `Math.abs(sum - 100) < 0.5`
- Live "gram equivalent" display updates as % changes

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Nav tab label | "Nutrition" |
| Primary CTA (inline per-slot add) | "Log food" |
| Search placeholder | "Search foods or enter a name…" |
| Empty state heading (no logs today) | "Nothing logged yet" |
| Empty state body | "Add your first meal to start tracking today's macros." |
| Empty state CTA | "Log your first meal" |
| Manual entry prompt | "Can't find it? Enter it manually." |
| Manual entry dialog heading | "Add a food manually" |
| Log delete toast | "Food removed" |
| Log delete undo action | "UNDO" (mono uppercase — per toast pattern) |
| Destructive label (delete log) | "Remove" |
| Destructive confirmation (if dialog used) | "Remove this food log? This cannot be undone." |
| Target % validation error | "Percentages must add up to 100. Adjust the values to continue." |
| OFF incomplete data flag | "Nutrition data may be incomplete — verify before logging." |
| Copy yesterday CTA | "Copy yesterday" |
| Copy yesterday success toast | "Yesterday's meals copied to today" |
| Stats page: heat map section label | "LOGGING HISTORY" (mono uppercase) |
| Stats page: trend section label | "7-DAY MACRO TREND" (mono uppercase) |
| Stats page: empty heat map | "No logs yet. Your history will appear here as you log meals." |
| Search no results | "No matches found. Try a different name or add it manually." |

Note: the empty-state CTA ("Log your first meal") is intentionally distinct from the primary per-slot CTA ("Log food") to make the first-time action feel grounded rather than generic.

Tone: concise, direct, document-discipline. No emoji. No exclamation points. Food is a utility surface, not a celebratory one.

---

## Glass Surface Application

The `.glass-tile` and `.glass-button` utility classes (defined in `globals.css`) are the ONLY mechanism for the glassy/neumorphic look per D-13.

| Surface | Class | Notes |
|---------|-------|-------|
| Meal slot pill rail | Custom (SettingsSectionNav pattern — not `.glass-tile`) | Exact Tailwind classes from SettingsSectionNav.tsx |
| Meal slot pill (active) | `motion.span` with inset shadow | See SettingsSectionNav.tsx line 88–89 |
| Day navigator arrow buttons | `.glass-button rounded-full` | 32px circle buttons |
| "Log food" primary button | `.glass-button rounded-md` | Inline to meal slot |
| Food log row (hover state) | `.glass-button` hover shadow via `pointer-fine:` | Not applied at rest |
| Search input | shadcn `Input` + `--ring-hud` focus ring | HUD ring because it is the primary action surface |
| Search result (hover) | `bg-[var(--surface)]` tint | No glass — document discipline |
| Daily macro summary bar | `.glass-tile rounded-xl` | Sticky header surface |
| Serving picker confirm button | `.glass-button rounded-md` | |
| Stats heatmap container | No glass — plain `--surface` card | Document surface, not interactive |
| Stats chart container | No glass — plain `--surface` card | Same reason |

Do NOT apply `.agent-mode-scope` to nutrition pages. The JARVIS ambient background (hexagonal dot grid + cyan radial glows) is reserved for agent surfaces: `/`, `/insights`, `/health`, `/settings/memory`.

---

## Motion Contract

All animation via `motion/react` (`motion` package). Respect `useReducedMotion()` — if reduced-motion is active, disable all transition animations and use instant cuts.

| Element | Animation | Duration | Easing | Note |
|---------|-----------|----------|--------|------|
| Meal slot pill indicator | `layoutId="nutrition-slot-pill"` spring | stiffness 360, damping 32 | spring | Mirrors SettingsSectionNav.tsx |
| Macro summary bar collapse | `motion.div` height + opacity | 200ms | `--ease-out-quart` | On scroll direction change |
| Food log row add | `motion.div` initial `opacity:0, y:8` → `opacity:1, y:0` | 160ms | `--ease-out-quart` | Per-row enter |
| Food log row remove | `motion.div` exit `opacity:0, x:-16` | 120ms | `--ease-in-fast` | Then undo window opens |
| FoodSearch sheet open | shadcn Sheet default slide-up | 200ms | built-in | No override needed |
| Serving picker preview update | no animation — instant recompute | — | — | Feels responsive |
| Heat map cells | no animation | — | — | Static grid |
| Progress bar fill | CSS `transition: width 300ms var(--ease-out-quart)` | 300ms | `--ease-out-quart` | Pure CSS, no Motion overhead |

---

## Accessibility Contract

- All interactive elements: `focus-visible` ring (`--ring-doc` journal mode; `--ring-hud` on the food search input which is the agent-adjacent surface)
- Meal slot pill bar: `aria-current="true"` on active pill (mirrors SettingsSectionNav.tsx line 81)
- Food log rows: `role="listitem"` within `role="list"`
- Progress bars: `role="progressbar"` with `aria-valuenow`, `aria-valuemin=0`, `aria-valuemax={target}`, `aria-label="{macro} progress"`
- Delete button: `aria-label="Remove {food name}"`
- Search input: `aria-label="Search foods"`, `aria-expanded` on result popover
- Heat map cells: `aria-label="{date}: {kcal} kcal"` on each cell; grid has `role="img"` with descriptive `aria-label="Nutrition logging history for the past year"`
- Day navigator: `aria-label="Previous day"` / `aria-label="Next day"` on arrow buttons
- Keyboard: Tab order follows visual order; Escape closes FoodSearch; ↑/↓ navigate search results; Enter confirms

---

## Stats Page Contract (Claude's Discretion, D-12)

Three sections only — no dashboard bloat:

1. **LOGGING HISTORY** — full-year heat map (365 days, CSS grid, adherence encoding)
2. **7-DAY MACRO TREND** — `recharts` LineChart with three lines: protein (`--ink-sage`), carbs (`--ink-amber`), fat (`--ink-coral`). Y-axis in grams. X-axis: day abbreviation (mono 10.5px).
3. **PERSONAL BESTS** — compact strip: longest logging streak (days), highest single-day kcal, best adherence day. Values in `font-mono-stats` 20px, labels in mono uppercase 10.5px.

No additional charts. No sodium/fiber breakdown on the stats page (targets are macros-only per D-09; secondary fields stored but not surfaced).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `button`, `dialog`, `input`, `select`, `sheet`, `popover`, `tooltip`, `scroll-area`, `command` (existing), `separator` (existing) | not required — all already installed |
| No third-party registries | — | not applicable |

`react-github-calendar` (already in package.json) is NOT used — plain CSS grid preferred per RESEARCH.md new dependency assessment. No new runtime dependencies.

---

## Pre-Population Source Map

| Decision | Source |
|----------|--------|
| Glass pill bar pattern (exact Tailwind classes) | `apps/web/components/settings/SettingsSectionNav.tsx` |
| Design tokens (all --canvas, --surface, --hud-cyan, etc.) | `apps/web/app/globals.css` |
| `.glass-tile`, `.glass-button` utility classes | `apps/web/app/globals.css` (Phase 6.1) |
| Font families | `apps/web/app/globals.css` `@theme` block |
| Motion easing tokens | `apps/web/app/globals.css` `--ease-*` |
| Toast pattern (undo, intent borders) | `apps/web/app/globals.css` `[data-sonner-toast]` |
| Focus ring tokens | `apps/web/app/globals.css` `--ring-doc`, `--ring-hud` |
| Tab layout, TanStack Query + Realtime pattern | `apps/web/components/training/TrainingClient.tsx` |
| Nav registration pattern | `apps/web/components/shell/PersistentNav.tsx` |
| Meal slot decisions (D-07, D-08) | CONTEXT.md |
| Heat map encoding (adherence %) | RESEARCH.md Pattern 5 + Open Question 2 |
| Copy yesterday shortcut | RESEARCH.md Open Question 4 (Claude's discretion) |
| Recents-first ordering | RESEARCH.md Open Question — `lastUsedAt DESC` then `useCount DESC` |
| Macro chart colors (three academic inks) | `apps/web/app/globals.css` `--ink-sage`, `--ink-amber`, `--ink-coral` |
| Stats page scope | D-12 (Claude's discretion — 3 sections maximum) |
| No .agent-mode-scope on nutrition | Phase 6.1 directional anchors + CONTEXT.md D-13 |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
