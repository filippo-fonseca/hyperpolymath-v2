---
phase: 19-tasks-redesign-inbox-universal-day-view-glass-cards
plan: 04
type: execute
wave: 4
depends_on:
  - 19-02
files_modified:
  - apps/web/components/tasks/TaskOverviewView.tsx
  - apps/web/components/tasks/TasksClient.tsx
  - apps/web/components/tasks/KanbanDayHeader.tsx
  - apps/web/lib/ui/useTasksExpanded.ts
  - apps/web/components/shell/AppShell.tsx
autonomous: true
requirements:
  - TASK-DAY-04
  - TASK-UI-04
  - TASK-UI-05
must_haves:
  truths:
    - "The view toggle offers kanban | list | overview (the old standalone 'day' mode is retired) (D-07)"
    - "Overview shows day-toggle rows (today + next 6 days) each expanding to that day's tasks; clicking a day header sets the universal day and drills into kanban (D-07)"
    - "A universal day switcher above the view toggle re-scopes all views from one shared dateYmd (D-05)"
    - "An expand/fullscreen toggle hides the app sidebar and is persisted in localStorage (D-08)"
  artifacts:
    - path: "apps/web/components/tasks/TaskOverviewView.tsx"
      provides: "Day-toggle overview view"
      exports: ["TaskOverviewView"]
      min_lines: 40
    - path: "apps/web/lib/ui/useTasksExpanded.ts"
      provides: "App-wide expand/fullscreen flag (localStorage + window event)"
      exports: ["useTasksExpanded"]
  key_links:
    - from: "apps/web/components/shell/AppShell.tsx"
      to: "Sidebar visibility"
      via: "useTasksExpanded() hides Sidebar when expanded"
      pattern: "useTasksExpanded"
    - from: "apps/web/components/tasks/TaskOverviewView.tsx day header"
      to: "TasksClient setDateYmd + setView('kanban')"
      via: "onSelectDay callback"
      pattern: "onSelectDay"
---

<objective>
Add the overview (day-toggle) view, lift the day switcher to a universal control, retire the old standalone "day" mode, and add the expand/fullscreen toggle. Three locked decisions: D-07 (tasks overview / home with days-as-toggles), D-08 (expandable/fullscreen surface), and the universal day-switcher half of D-05. Runs last because it shares `TasksClient.tsx` with Plans 01 and 02.

Purpose: Give the user a glance-oriented "what do I need to do" home with day toggles, a single day control shared by every view, and a roomier full-width mode.

Output: New `TaskOverviewView` component, a universal DaySwitcher rendered above the view toggle in `TasksClient`, the view toggle changed to `kanban | list | overview`, a `useTasksExpanded` hook, and `AppShell` hiding the sidebar when expanded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-CONTEXT.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-PATTERNS.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-UI-SPEC.md
@apps/web/components/tasks/TasksClient.tsx
@apps/web/components/tasks/TaskDayView.tsx
@apps/web/components/tasks/KanbanDayHeader.tsx
@apps/web/components/shell/AppShell.tsx
@apps/web/lib/ui/useSplitScreen.ts
</context>

<artifacts_this_phase_produces>
- New file `apps/web/components/tasks/TaskOverviewView.tsx` exporting `TaskOverviewView`. Props: `tasks: TaskWithProjects[]`, `onTaskClick: (id: string) => void`, `onSelectDay: (ymd: string) => void`.
- New file `apps/web/lib/ui/useTasksExpanded.ts` exporting `useTasksExpanded()` (localStorage key `tasks-expanded` + window CustomEvent, mirroring `useSplitScreen`); also export a `readTasksExpanded()` helper if needed by AppShell.
- `TasksClient.tsx`: universal `DaySwitcher` block (lifted day-nav markup) rendered above the view toggle; view toggle array changed from `["kanban","list","day"]` to `["kanban","list","overview"]`; `view === "day"` branch + `TaskDayView` import removed; `view === "overview"` branch renders `TaskOverviewView`; expand toggle button in the header wired to `useTasksExpanded`.
- `AppShell.tsx`: reads `useTasksExpanded()` and hides `<Sidebar>` (Motion layout width→0, 200ms) when expanded.
- Requirement IDs introduced: `TASK-DAY-04` (overview day-toggle view + universal day switcher, retire 'day' mode), `TASK-UI-04` (expand/fullscreen toggle), `TASK-UI-05` (sidebar collapse on expand).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskOverviewView (day-toggle rows) + useTasksExpanded hook (D-07, D-08)</name>
  <read_first>apps/web/components/tasks/TaskDayView.tsx (the day-section grouping pattern + date-fns imports), apps/web/components/tasks/TaskCard.tsx (props), apps/web/lib/ui/useSplitScreen.ts (the localStorage + CustomEvent hook pattern to mirror exactly), apps/web/lib/tasks/date-shortcuts (toYmd/fromYmd), 19-PATTERNS.md (TaskOverviewView section + localStorage pattern), 19-UI-SPEC.md (S-6, S-7, motion budget)</read_first>
  <files>apps/web/components/tasks/TaskOverviewView.tsx, apps/web/lib/ui/useTasksExpanded.ts</files>
  <action>Per D-07 / UI-SPEC S-6: create `TaskOverviewView` — a vertical stack of collapsible day-group rows for today + next 6 days. Generate days with `Array.from({ length: 7 }, (_, i) => toYmd(addDays(today, i)))` (`addDays` from date-fns, `toYmd` from `@/lib/tasks/date-shortcuts`). For each day, filter the `tasks` prop by `t.dueDate === <dayYmd>` (YMD string equality — never `new Date()` round-trip; this is load-bearing per the aesthetic guardrails). Closed row markup per S-6: `flex items-center justify-between px-4 py-2.5 rounded-lg cursor-pointer-always border border-[var(--edge)] hover:border-[var(--edge-hud)] transition-colors duration-150`, day label `font-serif text-base text-[var(--ink)]` using `format(dayDate, "EEEE, MMMM d")`, count badge `font-mono text-[11px] text-[var(--ink-muted)] tabular-nums`, chevron `ChevronDown`/`ChevronRight size={14}`. Expanded body via `AnimatePresence` (initial=false, `transition={{ duration: 0.16, ease: [0.25, 1, 0.5, 1] }}` per motion budget) wrapping `glass-tile rounded-xl p-3 mt-1 mb-2 space-y-1` containing the day's `TaskCard`s. Clicking the day-row HEADER calls `onSelectDay(dayYmd)` (per S-6: sets the universal day state and the caller will switch to kanban so the user drills in). Track open/closed per-day in local `useState<Set<string>>`. Empty day body copy is fine to omit (the row count communicates emptiness) — but if a day has zero tasks, still render the row.

Per D-08 / UI-SPEC S-7: create `useTasksExpanded.ts` mirroring `useSplitScreen.ts` EXACTLY (localStorage key `"tasks-expanded"`, a distinct CustomEvent name e.g. `"tasks-expanded-change"`, value `"1"`/`"0"`). Export `useTasksExpanded(): { expanded: boolean; setExpanded: (next: boolean) => void; toggle: () => void }`. Also export a `readTasksExpanded(): boolean` imperative helper. This keeps the toggle in sync across `TasksClient` (button) and `AppShell` (sidebar) without adding zustand/context — same rationale as `useSplitScreen`.</action>
  <verify>
    <automated>cd apps/web && test -f components/tasks/TaskOverviewView.tsx && test -f lib/ui/useTasksExpanded.ts && grep -q "export function TaskOverviewView" components/tasks/TaskOverviewView.tsx && grep -q "onSelectDay" components/tasks/TaskOverviewView.tsx && grep -q "tasks-expanded" lib/ui/useTasksExpanded.ts && grep -q "export function useTasksExpanded" lib/ui/useTasksExpanded.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "TaskOverviewView|useTasksExpanded" || echo "no new-file type errors"</automated>
  </verify>
  <done>`TaskOverviewView` exists, renders 7 collapsible day rows using YMD string filtering, expands a glass body of `TaskCard`s with the 160ms motion, and calls `onSelectDay` from the row header; `useTasksExpanded` exists mirroring the `useSplitScreen` localStorage+event pattern with key `tasks-expanded`; tsc clean for both new files.</done>
</task>

<task type="auto">
  <name>Task 2: Universal day switcher + overview wiring + retire 'day' mode in TasksClient (D-05, D-07)</name>
  <read_first>apps/web/components/tasks/TasksClient.tsx (lines 102-105 view/date state; lines 479-498 view toggle pill; lines 520-527 list/day/overview branches; lines 528-536 kanban branch + KanbanDayHeader render), apps/web/components/tasks/KanbanDayHeader.tsx (the day-nav markup to lift — arrows/Today/date-picker), 19-UI-SPEC.md (S-2, I-5)</read_first>
  <files>apps/web/components/tasks/TasksClient.tsx, apps/web/components/tasks/KanbanDayHeader.tsx</files>
  <action>Per UI-SPEC S-2 / D-05: render a universal day switcher in `TasksClient` ABOVE the view toggle toolbar (currently the toolbar block at lines ~450-498), visible in kanban / list / overview alike. Implement it by extracting the day-nav markup (prev/next arrows, "Today" button, native date-picker label) from `KanbanDayHeader.tsx` into either a small local sub-component or inline JSX in `TasksClient`, wired to `dateYmd` + `setDateYmd`. Container per S-2: `flex items-center gap-2 px-1 pb-3`. Once lifted, remove the day-nav markup from `KanbanDayHeader.tsx` and either delete `KanbanDayHeader` usage from the kanban branch or reduce it to nothing — the kanban branch should no longer render its own day header (the switcher is now global). If `KanbanDayHeader` becomes empty, remove the component file usage/import; if you prefer to keep a thin wrapper, ensure no duplicate day controls render.

Per D-07 / UI-SPEC I-5: change the view toggle array from `["kanban", "list", "day"] as const` (line ~480) to `["kanban", "list", "overview"] as const`. Remove the `view === "day"` branch (lines ~524-527) and the `TaskDayView` import (line 29). Add a `view === "overview"` branch rendering `<TaskOverviewView tasks={filtered} onTaskClick={setOpenTaskId} onSelectDay={(ymd) => { void setDateYmd(ymd); void setView("kanban"); }} />` inside the existing scroll container. Also update the localStorage view-restore effect (lines 159-165) which currently restores `"day"` — change it to restore `"overview"` (or drop "day" from the accepted stored values) so a stale `tasks-view=day` does not break.</action>
  <verify>
    <automated>cd apps/web && grep -q '"kanban", "list", "overview"' components/tasks/TasksClient.tsx && grep -q "TaskOverviewView" components/tasks/TasksClient.tsx && ! grep -q "TaskDayView" components/tasks/TasksClient.tsx && ! grep -q 'view === "day"' components/tasks/TasksClient.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "TasksClient|KanbanDayHeader" || echo "no client/header type errors"</automated>
  </verify>
  <done>View toggle reads `kanban | list | overview`; the `day` branch + `TaskDayView` import are gone; overview branch renders `TaskOverviewView` whose `onSelectDay` sets `dateYmd` and switches to kanban; a single universal day switcher renders above the toolbar (no duplicate day header in the kanban branch); stale `tasks-view=day` no longer restores a missing mode; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Expand/fullscreen toggle + sidebar collapse (D-08, S-7/I-6)</name>
  <read_first>apps/web/components/tasks/TasksClient.tsx (header block lines 427-444 where the toggle button goes), apps/web/components/shell/AppShell.tsx (the Sidebar render + main layout, lines ~55-77), apps/web/lib/ui/useTasksExpanded.ts (the hook from Task 1), apps/web/lib/ui/useSplitScreen.ts (Motion-free pattern reference), 19-UI-SPEC.md (S-7, I-6, motion budget)</read_first>
  <files>apps/web/components/tasks/TasksClient.tsx, apps/web/components/shell/AppShell.tsx</files>
  <action>Per D-08 / UI-SPEC S-7: add an expand toggle button to the tasks page header row (top-right, near the h1 — wrap the existing `<header>` content in a flex row so the title sits left and the toggle sits right). Icon `Maximize2` when collapsed / `Minimize2` when expanded (lucide-react, `size={16} strokeWidth={1.5}`), styled `text-[var(--ink-muted)] hover:text-[var(--ink)] p-1 rounded cursor-pointer-always transition-colors duration-150 ease-out`. Aria-label per copy contract: "Expand tasks to fullscreen" / "Exit fullscreen". Wire onClick to `toggle()` from `useTasksExpanded()`. State is ephemeral (localStorage only via the hook) — do NOT put it in the URL.

Per UI-SPEC I-6: in `AppShell.tsx`, consume `useTasksExpanded()` and hide the `<Sidebar>` when `expanded` is true. Animate the collapse with Motion `layout` on the sidebar wrapper (width → 0, 200ms `--ease-out-quart`) per the motion budget — wrap `<Sidebar>` in a `motion.div` with `layout` and conditionally render/collapse it; respect `useReducedMotion`. The main column already uses `flex-1` so content expands to fill automatically. Because the expand toggle only makes sense on /tasks, guard with `usePathname()` if the sidebar should only collapse there — or simply let the flag drive it globally (the toggle only exists on /tasks, so the flag is only ever set there). Keep the existing JARVIS side-panel logic untouched.</action>
  <verify>
    <automated>cd apps/web && grep -qE "Maximize2|Minimize2" components/tasks/TasksClient.tsx && grep -q "useTasksExpanded" components/tasks/TasksClient.tsx && grep -q "useTasksExpanded" components/shell/AppShell.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "TasksClient|AppShell" || echo "no client/shell type errors"</automated>
  </verify>
  <done>The tasks header shows a Maximize2/Minimize2 toggle wired to `useTasksExpanded().toggle`; `AppShell` hides/collapses the `Sidebar` via Motion `layout` (200ms) when expanded and respects reduced motion; the flag persists in localStorage (`tasks-expanded`); JARVIS side panel logic unchanged; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is presentation + client-only UI state. Overview filters already-fetched RLS-scoped rows; the expand flag is a localStorage boolean. No new server actions, no new input parsing, no data egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Tampering | localStorage `tasks-expanded` flag | accept | A user editing their own localStorage only toggles their own sidebar visibility; no security-relevant state, no server trust placed in it. |
| T-19-08 | Information Disclosure | overview day filtering | accept | Renders only rows already present in the user's `tableKey("tasks", userId)` cache; client-side string filtering cannot widen the data set. |
| T-19-09 | Denial of Service | overview 7-day render | accept | Bounded to today+6 days; per-day counts are small (CONTEXT note). No unbounded loops or fetches. |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` passes with no new errors across the 5 modified/new files.
- Manual smoke: view toggle shows kanban|list|overview; overview lists 7 day rows that expand to show that day's cards; clicking a day header jumps to that day in kanban; the universal day switcher above the toolbar re-scopes kanban + list + overview from one control; the Maximize2 toggle hides the sidebar (animated) and persists across reload; old `?view=day` URLs no longer crash.
</verification>

<success_criteria>
- Overview view with day toggles ships; standalone "day" mode retired (D-07 / TASK-DAY-04).
- Universal day switcher re-scopes every view from shared `dateYmd` (D-05).
- Expand/fullscreen toggle hides the sidebar and persists (D-08 / TASK-UI-04, TASK-UI-05).
- YMD string comparison preserved throughout.
</success_criteria>

<output>
Create `.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-04-SUMMARY.md` when done.
</output>
