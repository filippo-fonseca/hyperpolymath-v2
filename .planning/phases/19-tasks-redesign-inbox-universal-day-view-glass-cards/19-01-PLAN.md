---
phase: 19-tasks-redesign-inbox-universal-day-view-glass-cards
plan: 01
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/web/components/tasks/TasksClient.tsx
autonomous: true
requirements:
  - TASK-DAY-01
  - TASK-DAY-02
  - TASK-DAY-03
must_haves:
  truths:
    - "Switching the day re-scopes the list view, not just kanban (D-05)"
    - "Completed (lesno) tasks for the selected day stay visible in day-scoped views (D-06)"
    - "Day filtering uses YMD string equality and never round-trips through new Date()"
  artifacts:
    - path: "apps/web/components/tasks/TasksClient.tsx"
      provides: "Universal day-scoped filtering shared by kanban + list; day-scoped lesno visibility"
      contains: "dayFilteredTasks"
  key_links:
    - from: "apps/web/components/tasks/TasksClient.tsx (list branch)"
      to: "TaskList tasks prop"
      via: "pass dayFilteredTasks instead of filtered"
      pattern: "<TaskList tasks=\\{dayFilteredTasks\\}"
    - from: "filtered useMemo lesno predicate"
      to: "day-scoped completed visibility"
      via: "bypass showLesno gate when t.dueDate === dateYmd"
      pattern: "t\\.dueDate === dateYmd"
---

<objective>
Consolidate day-scoping into shared state so EVERY view respects the selected day, and stop hiding that day's completed work. Two locked decisions: D-05 (universal day-switching — list view currently ignores dates, an explicit bug) and D-06 (done/lesno tasks show for the day instead of vanishing).

Purpose: The list view ignoring the day model and completed tasks disappearing are the two correctness bugs the user called out. Fixing them here, before the Inbox/overview work, gives later plans a single trustworthy day-scoped slice to build on.

Output: `TasksClient.tsx` passes `dayFilteredTasks` to the list view, and the lesno filter is loosened so the selected day's completed tasks render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-CONTEXT.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-PATTERNS.md
@.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-UI-SPEC.md
@apps/web/components/tasks/TasksClient.tsx
@apps/web/components/tasks/TaskList.tsx
</context>

<artifacts_this_phase_produces>
- Modified `dayFilteredTasks` consumption: list view branch in `TasksClient.tsx` now consumes `dayFilteredTasks` (no new symbol; behavioral change to an existing memo).
- New derived value `dayLesnoTasks` (or inline predicate) in `TasksClient.tsx`: tasks where `t.status === "lesno" && t.dueDate === dateYmd`, shown regardless of the global `showLesno` toggle.
- Requirement IDs introduced: `TASK-DAY-01` (universal day-scoping across views), `TASK-DAY-02` (list view respects selected day), `TASK-DAY-03` (per-day completed visibility).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Make list view day-scoped (D-05, TASK-DAY-02)</name>
  <read_first>apps/web/components/tasks/TasksClient.tsx (lines 176-233 filter memos; lines 520-527 view branches), apps/web/components/tasks/TaskList.tsx (confirm it renders whatever tasks prop it receives — no internal date logic)</read_first>
  <files>apps/web/components/tasks/TasksClient.tsx</files>
  <action>Per D-05: in the `view === "list"` branch (currently line ~522), change the `TaskList` tasks prop from `filtered` to `dayFilteredTasks` so the list respects the universal `dateYmd` exactly like kanban does. Do NOT touch `TaskList.tsx` internals — the day-scoping is enforced at the call site (PATTERNS S-5). The `dayFilteredTasks` memo (lines 226-229) already uses `t.dueDate === dateYmd` string equality; preserve it. Do NOT reintroduce `new Date()` round-trips for day comparison (load-bearing — see PATTERNS "YMD Date Comparison (non-negotiable)"). Note the existing "day" view branch (`view === "day"`, line ~524) and `TaskDayView` import stay in place for now — Plan 04 retires them when overview ships.</action>
  <verify>
    <automated>grep -n "TaskList tasks={dayFilteredTasks}" apps/web/components/tasks/TasksClient.tsx && cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "TasksClient" || echo "no TasksClient type errors"</automated>
  </verify>
  <done>The list view branch passes `dayFilteredTasks` (not `filtered`) to `TaskList`; grep confirms the literal `TaskList tasks={dayFilteredTasks}`; no new `new Date(t.dueDate)` comparison was introduced in the day-filter path; tsc reports no new errors in `TasksClient.tsx`.</done>
</task>

<task type="auto">
  <name>Task 2: Show the selected day's completed (lesno) tasks (D-06, TASK-DAY-03)</name>
  <read_first>apps/web/components/tasks/TasksClient.tsx (line 181 the lesno predicate inside the `filtered` useMemo; lines 226-233 day/inbox memos; lines 138-145 showLesno localStorage pattern)</read_first>
  <files>apps/web/components/tasks/TasksClient.tsx</files>
  <action>Per D-06: dragging a card to "Lesno"/Done must not make it vanish from the day. The disappearance is caused by the predicate at line 181 (`if (!showLesno && t.status === "lesno" && !filters.status.includes("lesno")) return false;`) which drops ALL lesno globally. Loosen it so a lesno task whose `t.dueDate === dateYmd` is NOT dropped from the day-scoped pipeline. Implement by adding the day-match escape to the predicate: a lesno task survives the gate when `t.dueDate === dateYmd` (the selected day), even if `showLesno` is false. Keep the global hide behavior intact for non-day-scoped contexts: lesno tasks that do NOT match `dateYmd` still obey the `showLesno` toggle. Per UI-SPEC S-8, prefer the `dueDate === dateYmd` check (consistent with the YMD comparison pattern) over `completedAt` local-date matching. This makes `dayFilteredTasks` (already `filtered.filter(t => t.dueDate === dateYmd)`) include that day's done tasks automatically, so kanban + list both surface them. Do not alter `inboxTasks` (it already excludes lesno via `t.status !== "lesno"`). Do not change the `showLesno` localStorage persistence.</action>
  <verify>
    <automated>cd apps/web && grep -n "dateYmd" components/tasks/TasksClient.tsx | grep -i "lesno\|status === \"lesno\"" ; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "TasksClient" || echo "no TasksClient type errors"</automated>
  </verify>
  <done>The line-181 lesno predicate no longer unconditionally drops lesno tasks that match the selected day; a lesno task with `t.dueDate === dateYmd` passes the filter with `showLesno=false`; lesno tasks for OTHER days still respect `showLesno`; `inboxTasks` remains lesno-free; tsc reports no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client filter logic only | No new untrusted input crosses any boundary. This plan changes which already-fetched, already-RLS-scoped rows are displayed client-side. No server action signatures change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | day-scoped lesno visibility predicate | accept | Tasks shown are already in the user's RLS-scoped `tasks` query result (`tableKey("tasks", userId)`); loosening a client display filter cannot surface another user's rows. No new data path. |
| T-19-02 | Tampering | dateYmd URL param (nuqs) | accept | `dateYmd` is a display-only filter key; it selects among already-authorized rows via string equality and is not used in any query/where clause. No injection surface. |
</threat_model>

<verification>
- `npm run build` (or `cd apps/web && npx tsc --noEmit`) passes with no new errors in `TasksClient.tsx`.
- Manual smoke (executor may run dev server): switching days in LIST view changes the visible tasks (previously list ignored the day). Marking a task lesno on the selected day keeps it visible (in the day-scoped views) instead of vanishing.
</verification>

<success_criteria>
- List view is day-scoped to `dateYmd` (D-05 / TASK-DAY-02).
- The selected day's completed tasks remain visible without flipping the global `showLesno` toggle (D-06 / TASK-DAY-03).
- YMD string comparison preserved everywhere; no `new Date()` day-filter round-trips introduced (TASK-DAY-01 invariant).
</success_criteria>

<output>
Create `.planning/phases/19-tasks-redesign-inbox-universal-day-view-glass-cards/19-01-SUMMARY.md` when done.
</output>
