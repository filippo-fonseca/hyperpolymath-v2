---
context: phase
phase: 19-tasks-redesign-inbox-universal-day-view-glass-cards
task: 5
total_tasks: 5
status: paused
last_updated: 2026-06-14T00:24:26.674Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No failure-derived blocking constraints this session. One important advisory below (no-date policy contradiction)._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| UTC date drift | `new Date("YYYY-MM-DD")` is UTC midnight → renders the PREVIOUS day in negative-UTC zones. Hit twice now (tasks page guards it; JARVIS receipt did not). | advisory | For date-only DATE-column strings, parse as LOCAL (`new Date(y, m-1, d)`) and compare YMD strings, never via `Date` objects. |
| Static overflow-hidden on an animation wrapper | The fullscreen-collapse `motion.div` clipped the sidebar's hover overlay and trapped its z-index. | advisory | Clip only while animating (state flag); keep `overflow-visible` + raised z at rest. |

<current_state>
Phase 19 is fully executed, verified (9/9 automated PASS), committed, and DEPLOYED. HEAD = `bbc1ec6` on `main`, pushed to origin → Vercel production auto-deploys from `apps/web`. Four post-verification fixes landed this session on top of the phase work. The only open work is browser verification and one product decision.
</current_state>

<completed_work>
- Phase 19 execution: 5 plans / 4 waves — Inbox-first, universal day-switching, glass cards/panel, JARVIS no-date routing, overview view, fullscreen. (93a51cd and prior)
- STATE.md + ROADMAP.md finalized; 19-VERIFICATION.md committed (0a2aa39).
- Inbox now persistent across ALL views; DaySwitcher moved into the central column to scope it visually to the day-area, not the Inbox (93a51cd).
- Sidebar collapsed-mode hover-overlay fix — clip only mid-animation, wrapper `relative z-40` (bbc1ec6).
- JARVIS receipt date-drift fix (local parse of date-only strings) + explicit "morning dump" day-planning mode in the system prompt (bbc1ec6).
</completed_work>

<remaining_work>
- Browser-verify (user away): glass visual quality, drag-to-Inbox E2E, JARVIS undated→Inbox receipt, sidebar overlay, a real live "morning dump".
- DECISION: no-date dating policy (see anti-pattern/advisory below).
- Housekeeping: untracked `phases/18-...`, malformed `999.17-{...}` dir, `JARVIS.Desktop_0.0.0_aarch64.dmg`.
- NEW (just requested, in progress): fix dark-mode `#` (hashtag) styling in JARVIS + captures.
</remaining_work>

<decisions_made>
- Inbox in every view + DaySwitcher scoped to central column.
- Morning-dump tasks get explicit due dates (planned day) rather than the default-due path, to sidestep the policy contradiction below.
</decisions_made>

<blockers>
- User away from laptop → cannot browser-verify. Code is deployed; verify on return.
</blockers>

## Required Reading (in order)
1. `.planning/phases/19-.../19-CONTEXT.md` — phase scope + the 9 decisions (esp. D-02).
2. `apps/web/lib/jarvis/executor.ts:148` and `packages/jarvis-core/src/personality.ts:116` — the contradicting no-date policy (see below).

## ⚠️ Open product decision — no-date dating policy (ADVISORY, do not silently "fix")
Phase 19 D-02 shipped **no-date → Inbox (NULL)** in `executor.ts:148`. But `personality.ts:116` (`DUE-DATE DEFAULT`) still tells the model **no-date → today**. They contradict. The user's receipt-bug message implied they actually want **no-date → today** ("it correctly put today"). This was left as-shipped (Inbox) pending a deliberate decision — resolving it affects JARVIS task creation and morning-dump behavior. Decide direction with the user before changing the DB policy.

## Infrastructure State
- Local dev stack running in background: `node tools/hyperpolymath/hyperpolymath.mjs --only=supabase,web` → web on localhost:3000, **local-docker Supabase (NOT prod)**.
- `tsc` clean except 6 pre-existing out-of-scope errors in `apps/web/tests/api-jarvis-tts.test.ts` (Request vs NextRequest).

<context>
Post-verification polish + deploy session. User stacked several quick asks and said "just finish it all" / "deploy" while away from laptop, so everything was pushed autonomously. Latest incoming request: dark-mode `#` styling in JARVIS and captures — being picked up now.
</context>

<next_action>
Finish the dark-mode `#` (hashtag) styling fix in JARVIS receipts + captures. Then on user's return: browser-verify the 5 items and resolve the no-date policy decision.
</next_action>
