---
phase: 08-public-landing-manifesto
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - apps/web/components/landing/JarvisDemo.tsx
  - apps/web/components/landing/EngineSection.tsx
  - apps/web/components/landing/LandingPage.tsx
autonomous: true
requirements:
  - LAND-DEMO       # SC-3 / D-01 / D-02 / D-03 / UI-SPEC §7 — animated README ASCII demo with FSM + reduced motion + 3 rotating examples
  - LAND-ENGINE     # SC-5 / D-05 / UI-SPEC §5d / §11e — input/JSON side-by-side block using REAL fixture from Plan 08-01
must_haves:
  truths:
    - "§02 JarvisDemo types the README example character-by-character (~28 cps), pauses 600ms, then fades in 2 receipts staggered 220ms apart (UI-SPEC §7b)"
    - "On prefers-reduced-motion: reduce, JarvisDemo renders the settled state (input + all 2 receipts) statically on mount, no typing, no caret, no fades (D-02 / UI-SPEC §11d)"
    - "Visitor can click '▶ show another' to advance to the next of 3 rotating examples (A → B → C → A) (D-03 / UI-SPEC §7c)"
    - "JS-disabled visitors see Example A's settled state (static input + 2 receipts) — server-rendered initial state (RESEARCH Pitfall 7 / SC-9)"
    - "§04 EngineSection imports STRICT_TOOL_USE_FIXTURE from packages/jarvis-core/tests/strict-tool-use.fixture.ts verbatim (UI-SPEC §11e)"
    - "§04 EngineSection renders left INPUT card (--edge border, --surface bg) + right STRICT-TOOL-USE JSON card (--edge-hud border, --surface-raised bg, --glow-hud-subtle shadow) + source-of-truth note (UI-SPEC §5d)"
    - "Cyan appears in EXACTLY these two files and EXACTLY in the elements UI-SPEC §11a enumerates: JarvisDemo (⚜ ornament + verb + caret), EngineSection (--edge-hud + STRICT-TOOL-USE JSON eyebrow) — nowhere else (UI-SPEC §4)"
  artifacts:
    - path: "apps/web/components/landing/JarvisDemo.tsx"
      provides: "§02 — animated terminal block with FSM + 3 rotating examples + reduced-motion gate"
      exports: ["JarvisDemo"]
    - path: "apps/web/components/landing/EngineSection.tsx"
      provides: "§04 — input/JSON side-by-side block with real fixture import"
      exports: ["EngineSection"]
    - path: "apps/web/components/landing/LandingPage.tsx"
      provides: "Orchestrator updated to import and render JarvisDemo + EngineSection (placeholders replaced)"
      contains: "import { JarvisDemo }"
  key_links:
    - from: "EngineSection.tsx"
      to: "packages/jarvis-core/tests/strict-tool-use.fixture.ts (Plan 08-01)"
      via: "import { STRICT_TOOL_USE_FIXTURE } from ..."
      pattern: "STRICT_TOOL_USE_FIXTURE"
    - from: "LandingPage.tsx"
      to: "JarvisDemo.tsx and EngineSection.tsx"
      via: "import statements + render in §02 and §04 slots"
      pattern: "import.*JarvisDemo.*from.*JarvisDemo"
---

<objective>
Build the **two cyan-bearing surfaces** of the landing — the only components on the page allowed to consume `var(--hud-cyan)`. Both surfaces speak as the machine: the JarvisDemo replays JARVIS routing input → receipts, the EngineSection shows the typed JSON contract. Cyan here is earned because it signals "this surface IS the machine speaking" (UI-SPEC §4).

1. **JarvisDemo** (§02 / SC-3 / D-01-D-03) — A client component that:
   - Renders a printed terminal box (`--surface-raised` bg, 1px `--edge` border, 4px radius, `p-6`, `max-w-[760px]`)
   - Types the input character-by-character via a vanilla `useState` + `setTimeout` FSM (UI-SPEC §7b explicitly forbids Motion's `<Typewriter>` — the choreography (typed → 600ms pause → submit-flash → receipts stream) exceeds the declarative model)
   - Uses `motion/react` (`AnimatePresence` + `<motion.div>`) for the receipt fade-up entrances ONLY
   - Cyan-bearing in 3 specific elements: the `⚜` ornament on receipt lines, the verb on receipt lines (`scheduled`, `created`, `captured`), and the streaming caret `▮` during typing (UI-SPEC §11a)
   - Reduced-motion: renders the final state of Example A on mount, no animation (D-02 / UI-SPEC §11d)
   - JS-disabled fallback: server-rendered initial state shows Example A settled (input + 2 receipts) so JS-off visitors see a "terminal already finished" snapshot (RESEARCH Pitfall 7 / SC-9)
   - 3 rotating examples per UI-SPEC §7c (Example A: multi-action; Example B: capture-only; Example C: project-tagged task)
   - "▶ show another" affordance below the terminal

2. **EngineSection** (§04 / SC-5 / D-05) — A server component (no client interactivity needed) that:
   - Renders eyebrow + Display 2 heading + 1 paragraph of body + side-by-side INPUT / JSON cards + source-of-truth note
   - Imports `STRICT_TOOL_USE_FIXTURE` from `packages/jarvis-core/tests/strict-tool-use.fixture.ts` (created in Plan 08-01) and renders the input on the left card + the JSON on the right card
   - Right card is the SECOND cyan-bearing surface: 1px `--edge-hud` border (subtle cyan-tinted) + `STRICT-TOOL-USE JSON` mono eyebrow in `--hud-cyan-light` + `box-shadow: var(--glow-hud-subtle)` (UI-SPEC §5d, §11a)
   - JSON syntax styled via manual `<span>` classes (keys at `--ink` weight 500, string values at `--ink-amber`, numbers/null/boolean at `--ink-sage`, punctuation at `--ink-muted`) — no syntax highlighter library
   - Source-of-truth note: `Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits.`

3. **Wire JarvisDemo + EngineSection into LandingPage** — replace the §02 and §04 placeholders from Plan 08-03 with real imports.

Purpose: These are the load-bearing visual elements of the manifesto. The demo earns the page's promise ("see it route"); the Engine card earns its technical credibility ("here's the actual contract"). They're separate files so cyan stays surgically contained.

Output: 2 new component files + 1 edit to LandingPage.tsx. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-public-landing-manifesto/08-CONTEXT.md
@.planning/phases/08-public-landing-manifesto/08-UI-SPEC.md
@.planning/phases/08-public-landing-manifesto/08-RESEARCH.md

@apps/web/app/globals.css
@apps/web/components/landing/LandingPage.tsx
@apps/web/components/landing/SectionEyebrow.tsx
@packages/jarvis-core/tests/strict-tool-use.fixture.ts

<interfaces>
<!-- Tokens this plan IS permitted to consume (cyan reserved for these two files only). -->

From apps/web/app/globals.css:
- `var(--hud-cyan)` — primary cyan accent
- `var(--hud-cyan-light)` — light cyan for the JSON eyebrow text (lower-contrast variant)
- `var(--hud-cyan-glow)` — cyan glow color
- `var(--hud-cyan-glow-soft)` — softer cyan glow
- `var(--edge-hud)` — subtle cyan-tinted border (the right card's border)
- `var(--glow-hud-subtle)` — `0 0 20px rgb(var(--hud-cyan-rgb) / 0.10)` — the right card's box-shadow
- `var(--ink-amber)` — string value color in JSON syntax (UI-SPEC §5d)
- `var(--ink-sage)` — number/null/bool value color in JSON syntax (UI-SPEC §5d)
- `.hud-streaming-caret` — already exists at globals.css line 233 (verified); 1.1s linear infinite pulse via @keyframes hud-streaming-caret-pulse
- `.font-mono-stats` — tabular figures variant
- `.custom-scrollbar` — thin parchment scrollbar for overflow-x-auto on mobile terminal

From packages/jarvis-core/tests/strict-tool-use.fixture.ts (created in Plan 08-01):
- `STRICT_TOOL_USE_FIXTURE` — { input: string, output: { tool_use: Array<{ type, name, input }> } }
- `StrictToolUseFixture` — the type

From motion/react:
- `useReducedMotion()` — returns boolean
- `motion.div` — animated div
- `AnimatePresence` — exit animation wrapper

Easing tokens (from globals.css):
- `var(--ease-out-quart)` — `cubic-bezier(0.25, 1, 0.5, 1)` — used for receipt fade-up
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: JarvisDemo — §02 animated terminal with FSM + reduced motion + 3 rotating examples + SSR fallback</name>
  <files>apps/web/components/landing/JarvisDemo.tsx</files>
  <read_first>
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5b (full §02 spec — eyebrow, heading, body, terminal block, "▶ show another" below)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §7 (FULL Demo Animation Contract — §7a visual shell, §7b choreography state machine, §7c 3 rotating examples verbatim, §7d show-another affordance)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §6 (motion budget — 35ms/char ≈ 28cps, 140ms extra on punctuation, 600ms post-type pause, 220ms stagger between receipts, 2000ms settle)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11a (cyan placement gate — EXACTLY 3 uses in JarvisDemo: ⚜ ornament + verb + streaming caret; NO MORE)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11d (reduced-motion gate — JarvisDemo renders Example A's final state on mount when prefers-reduced-motion is set)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §9 (copy — eyebrow `§ 02 · DEMO`, heading `Watch it route.`, body `One sentence in. One or more actions out, each routed to the right primitive.`, control `▶ show another`)
    - .planning/phases/08-public-landing-manifesto/08-RESEARCH.md "Pattern 6: Motion 12 typing + receipts pattern" — full TypeScript reference implementation (~lines 547-690)
    - .planning/phases/08-public-landing-manifesto/08-RESEARCH.md "Pitfall 7: JS-disabled visitors see a broken demo (SC-9 violation)" — seed initial state to "settled" for SSR
    - apps/web/app/globals.css line 233 (`.hud-streaming-caret` class — verified exists)
    - README.md lines 33-39 (canonical Example A: `dinner with anna 8pm saturday. buy her flowers friday afternoon` → 2 receipts)
  </read_first>
  <action>
    Create `apps/web/components/landing/JarvisDemo.tsx` as a client component with an SSR-friendly initial state (settled). Use the RESEARCH Pattern 6 reference implementation as the base, with these adjustments per UI-SPEC §7 / §11a:

    ```typescript
    "use client";

    import { useEffect, useState } from "react";
    import { motion, AnimatePresence, useReducedMotion } from "motion/react";
    import { SectionEyebrow } from "./SectionEyebrow";

    /**
     * §02 — Live JARVIS Demo (LAND-DEMO / SC-3 / D-01-D-03 / UI-SPEC §5b + §7).
     *
     * The centerpiece of the landing. Replay of the README ASCII demo block via
     * a vanilla useState+setTimeout FSM (UI-SPEC §7b explicitly forbids motion's
     * <Typewriter> — the choreography exceeds the declarative model).
     *
     * Cyan placement (one of 2 surfaces allowed cyan per UI-SPEC §4):
     *   1. ⚜ ornament glyph on each receipt line
     *   2. Verb on each receipt line ("scheduled", "created", "captured")
     *   3. Streaming caret ▮ during typing (uses .hud-streaming-caret class)
     *
     * SSR / JS-disabled (RESEARCH Pitfall 7 / SC-9):
     *   - Initial state is "settled" so server renders Example A complete (input + 2 receipts)
     *   - On mount, if !reducedMotion, state resets to "typing" and animation begins
     *   - JS-off visitors see a "terminal already finished" snapshot — usable
     *
     * Reduced motion (UI-SPEC §11d / D-02):
     *   - useReducedMotion() short-circuits useEffects → state stays "settled"
     *   - Caret never renders, receipts have initial={false} (no fade)
     */

    type DemoState =
      | { phase: "typing"; typedChars: number }
      | { phase: "pause" }
      | { phase: "submitted" }
      | { phase: "settled" };

    type Example = {
      input: string;
      receipts: Array<{ verb: string; body: string }>;
    };

    // UI-SPEC §7c — the 3 rotating examples, verbatim.
    const EXAMPLES: readonly Example[] = [
      {
        // Example A: multi-action (the canonical README example, ships as default)
        input: "dinner with anna 8pm saturday. buy her flowers friday afternoon",
        receipts: [
          { verb: "scheduled", body: 'gcal · sat 8:00pm · "Dinner with Anna"' },
          { verb: "created",   body: 'task · fri afternoon · P2 · "Buy flowers"' },
        ],
      },
      {
        // Example B: capture-only (shows capture-first principle)
        input: "#idea polymathy as a competitive advantage",
        receipts: [
          { verb: "captured", body: "capture · #idea" },
        ],
      },
      {
        // Example C: project-tagged task with implicit date
        input: "finish anth pset $ANTH 2480 p2 by 3pm tomorrow",
        receipts: [
          { verb: "created", body: "task · tomorrow 3:00pm · P2 · linked $ANTH 2480" },
        ],
      },
    ] as const;

    // Timing constants — UI-SPEC §6 + §7b
    const CHAR_INTERVAL_MS = 35;     // ≈28 cps
    const PUNCT_PAUSE_MS = 140;      // extra on '.' or ','
    const POST_TYPE_PAUSE_MS = 600;  // pause between typing end and receipt fade
    const RECEIPT_STAGGER_MS = 220;  // between receipt fades

    export function JarvisDemo() {
      const [exampleIdx, setExampleIdx] = useState(0);
      // Initial state "settled" so SSR renders the final frame (Pitfall 7).
      const [state, setState] = useState<DemoState>({ phase: "settled" });
      const reducedMotion = useReducedMotion();
      const example = EXAMPLES[exampleIdx];

      // On mount: if motion allowed, reset to typing and begin animation.
      // Reduced motion: stay in "settled" — SSR'd content remains visible.
      useEffect(() => {
        if (reducedMotion) {
          setState({ phase: "settled" });
          return;
        }
        setState({ phase: "typing", typedChars: 0 });
      }, [exampleIdx, reducedMotion]);

      // Typing loop
      useEffect(() => {
        if (reducedMotion || state.phase !== "typing") return;
        if (state.typedChars >= example.input.length) {
          const t = setTimeout(() => setState({ phase: "pause" }), 0);
          return () => clearTimeout(t);
        }
        const nextChar = example.input[state.typedChars];
        const delay =
          nextChar === "." || nextChar === ","
            ? CHAR_INTERVAL_MS + PUNCT_PAUSE_MS
            : CHAR_INTERVAL_MS;
        const t = setTimeout(() => {
          setState({ phase: "typing", typedChars: state.typedChars + 1 });
        }, delay);
        return () => clearTimeout(t);
      }, [state, example.input, reducedMotion]);

      // Pause → submitted → settled
      useEffect(() => {
        if (reducedMotion) return;
        if (state.phase === "pause") {
          const t = setTimeout(() => setState({ phase: "submitted" }), POST_TYPE_PAUSE_MS);
          return () => clearTimeout(t);
        }
        if (state.phase === "submitted") {
          // No explicit timer back to "settled" needed — UI doesn't change visually after settled.
          // Receipt fade-ins are driven by AnimatePresence + delay below.
        }
      }, [state.phase, reducedMotion]);

      const showCaret = !reducedMotion && state.phase === "typing";
      const showReceipts =
        state.phase === "submitted" || state.phase === "settled";
      const typedText =
        state.phase === "typing"
          ? example.input.slice(0, state.typedChars)
          : example.input;

      function showAnother() {
        // 200ms fade handled by AnimatePresence + key change
        setExampleIdx((i) => (i + 1) % EXAMPLES.length);
      }

      return (
        <section className="py-16 max-w-[760px] mx-auto px-6 md:px-10">
          <SectionEyebrow label="§ 02 · DEMO" />
          <h2 className="mt-2 font-serif font-semibold text-[32px] leading-[1.2] text-[var(--ink)]">
            Watch it route.
          </h2>
          <p className="mt-4 font-serif text-[18px] leading-[1.6] text-[var(--ink)]">
            One sentence in. One or more actions out, each routed to the right primitive.
          </p>

          {/* The terminal block */}
          <div className="mt-6 border border-[var(--edge)] rounded bg-[var(--surface-raised)] p-6 overflow-x-auto custom-scrollbar">
            <div className="font-mono font-mono-stats text-[14px] leading-[1.55] text-[var(--ink)]">
              <div>
                <span className="text-[var(--ink-muted)]">$ </span>
                <span>{typedText}</span>
                {showCaret && (
                  <span
                    className="hud-streaming-caret inline-block ml-[2px]"
                    style={{ color: "var(--hud-cyan)" }}
                    aria-hidden="true"
                  >
                    ▮
                  </span>
                )}
              </div>
              {showReceipts && (
                <AnimatePresence mode="popLayout">
                  <div className="mt-4 space-y-2" key={`receipts-${exampleIdx}`}>
                    {example.receipts.map((r, i) => (
                      <motion.div
                        key={`${exampleIdx}-${i}`}
                        initial={reducedMotion ? false : { opacity: 0, y: 4 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{
                          duration: 0.28,
                          delay: reducedMotion ? 0 : i * 0.22,
                          ease: [0.25, 1, 0.5, 1], // --ease-out-quart
                        }}
                      >
                        <span style={{ color: "var(--hud-cyan)" }}>⚜  </span>
                        <span
                          className="font-medium"
                          style={{ color: "var(--hud-cyan)" }}
                        >
                          {r.verb}
                        </span>
                        <span className="text-[var(--ink)]">  →  {r.body}</span>
                      </motion.div>
                    ))}
                  </div>
                </AnimatePresence>
              )}
            </div>
          </div>

          {/* "▶ show another" — right-aligned below terminal */}
          <div className="mt-4 flex justify-end max-w-[760px] mx-auto">
            <button
              type="button"
              onClick={showAnother}
              className="font-mono text-[14px] font-medium text-[var(--ink-muted)] hover:text-[var(--ink)] opacity-70 hover:opacity-100 transition-opacity cursor-pointer"
            >
              ▶ show another
            </button>
          </div>
        </section>
      );
    }
    ```

    **Critical details:**
    - The cyan accent uses **inline style** (`style={{ color: "var(--hud-cyan)" }}`) NOT Tailwind utility — this makes the grep gate for cyan placement precise (UI-SPEC §11a checks for `var(--hud` substring across landing files; this is what it counts)
    - Initial state `"settled"` (not `"typing"`) so server renders the final frame (Pitfall 7 / SC-9)
    - The `useEffect` on mount transitions to `"typing"` only when `!reducedMotion`
    - `useReducedMotion()` from `motion/react` (NOT `framer-motion`)
    - `.hud-streaming-caret` class from globals.css line 233 handles the caret pulse
    - `font-mono-stats` for tabular figures
    - `custom-scrollbar` + `overflow-x-auto` on the terminal box for mobile <640px (terminal does NOT reflow — preserves the monospace shape per UI-SPEC §2)
    - Heading uses `text-[32px]` Display 2 with `font-semibold`
    - Body paragraph uses `text-[18px]` Body
    - Terminal contents use `text-[14px]` Caption (canonical mono register)
    - "▶ show another" uses `text-[14px] font-medium` (mono weight 500)
    - The `key={`receipts-${exampleIdx}`}` on the receipts wrapper forces AnimatePresence to swap when example changes
    - The `mode="popLayout"` on AnimatePresence allows siblings to reflow during exit (matches Phase 6.1 canonical pattern from STATE.md)

    Implements LAND-DEMO (SC-3 / D-01-D-03).
  </action>
  <verify>
    <automated>cd apps/web && test -f components/landing/JarvisDemo.tsx && grep -q '"use client"' components/landing/JarvisDemo.tsx && grep -q "useReducedMotion" components/landing/JarvisDemo.tsx && grep -q "AnimatePresence" components/landing/JarvisDemo.tsx && grep -q "dinner with anna 8pm saturday" components/landing/JarvisDemo.tsx && grep -q "#idea polymathy as a competitive advantage" components/landing/JarvisDemo.tsx && grep -q "ANTH 2480" components/landing/JarvisDemo.tsx && grep -q "Watch it route" components/landing/JarvisDemo.tsx && grep -q "hud-streaming-caret" components/landing/JarvisDemo.tsx && grep -q "▶ show another" components/landing/JarvisDemo.tsx && grep -q 'phase: "settled"' components/landing/JarvisDemo.tsx && [ "$(grep -cE 'var\(--hud' components/landing/JarvisDemo.tsx)" -ge 3 ] && ! grep -E "HudCorner|HudStatusPill|HudEdgeInstrumentation|HudThinkingRing|whileInView|useInView|agent-mode-scope|framer-motion" components/landing/JarvisDemo.tsx && ! grep -E "text-xs|text-base|text-xl|text-2xl|text-3xl|text-4xl|text-5xl|text-6xl|font-bold" components/landing/JarvisDemo.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/landing/JarvisDemo.tsx` exists with `"use client"` on line 1
    - Imports `motion`, `AnimatePresence`, `useReducedMotion` from `motion/react` (NOT `framer-motion`)
    - Initial state is `{ phase: "settled" }` (NOT `"typing"` — SSR fallback per Pitfall 7)
    - useEffect on mount transitions to typing only when `!reducedMotion`
    - EXAMPLES array contains exactly 3 examples (A, B, C) with the verbatim inputs from UI-SPEC §7c
    - Renders heading `Watch it route.` and body `One sentence in. One or more actions out, each routed to the right primitive.`
    - Renders the `▶ show another` button as a `<button type="button">` with onClick handler
    - Uses `.hud-streaming-caret` class for the caret element
    - Cyan appears in 3 places: ⚜ ornament + verb + streaming caret (grep `var(--hud` returns ≥3 hits, all on these 3 contexts)
    - NO `HudCornerCrops` / `HudStatusPill` / `HudEdgeInstrumentation` / `HudThinkingRing` imports
    - NO `whileInView` / `useInView`
    - NO `framer-motion` imports (must use `motion/react`)
    - NO `.agent-mode-scope` class
    - NO forbidden font sizes (text-xs/base/xl/2xl/...) or `font-bold`
    - Terminal box uses `overflow-x-auto custom-scrollbar` for mobile horizontal scroll
    - `font-mono-stats` class applied to the terminal contents
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>JarvisDemo renders the animated terminal with FSM typing + receipt fades + reduced motion gate + SSR fallback + 3 rotating examples.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: EngineSection — §04 input/JSON side-by-side block importing real fixture, with cyan-tinted right card</name>
  <files>apps/web/components/landing/EngineSection.tsx</files>
  <read_first>
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5d (FULL §04 spec — eyebrow, heading, body paragraph, left INPUT card, ArrowRight arrow, right STRICT-TOOL-USE JSON card, source-of-truth note)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §4 "Surface 2 — Engine §04 right card" (cyan placement: --edge-hud border + STRICT-TOOL-USE JSON eyebrow — exactly 2 uses, nowhere else in this file)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §9 (copy verbatim — eyebrow `§ 04 · THE ENGINE`, heading `Claude Sonnet 4.6, with a contract.`, body paragraph (4 sentences), `INPUT`, `STRICT-TOOL-USE JSON`, source note)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11a (cyan placement gate — EXACTLY 2 uses in EngineSection: --edge-hud border + STRICT-TOOL-USE JSON eyebrow)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11e (Engine JSON plucked from REAL fixture)
    - packages/jarvis-core/tests/strict-tool-use.fixture.ts (created in Plan 08-01 — import STRICT_TOOL_USE_FIXTURE)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5d JSON syntax styling (keys at --ink weight 500, string values at --ink-amber, numbers/null/boolean at --ink-sage, punctuation at --ink-muted)
    - apps/web/app/globals.css lines 37 + 52 (`--edge-hud` and `--glow-hud-subtle` token definitions)
    - apps/web/components/landing/SectionEyebrow.tsx (from Plan 08-03)
  </read_first>
  <action>
    Create `apps/web/components/landing/EngineSection.tsx` as a **Server Component** (no `"use client"` — no interactivity needed):

    ```typescript
    import { ArrowRight } from "lucide-react";
    import { SectionEyebrow } from "./SectionEyebrow";
    import { STRICT_TOOL_USE_FIXTURE } from "@hyperpolymath/jarvis-core/tests/strict-tool-use.fixture";

    /**
     * §04 — The Engine (LAND-ENGINE / SC-5 / D-05 / UI-SPEC §5d).
     *
     * THE ONE MODERATE-DENSITY SECTION on the landing — earns its weight via the
     * Claude Sonnet 4.6 + Strict Tool Use paragraph and the real JSON contract
     * imported verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts.
     *
     * Cyan placement (one of 2 surfaces allowed cyan per UI-SPEC §4):
     *   1. The right card's 1px --edge-hud border (subtle cyan-tinted)
     *   2. The right card's STRICT-TOOL-USE JSON eyebrow in --hud-cyan-light
     *
     * The right card also gets box-shadow: var(--glow-hud-subtle) — that uses
     * --hud-cyan internally but is a token, not a direct cyan reference at this
     * component layer (counts as part of the --edge-hud cyan placement).
     *
     * JSON syntax styling (UI-SPEC §5d):
     *   - Keys: --ink weight 500
     *   - String values: --ink-amber
     *   - Number/null/boolean: --ink-sage
     *   - Punctuation { } : , [ ]: --ink-muted
     */

    export function EngineSection() {
      return (
        <section className="py-16 max-w-[760px] mx-auto px-6 md:px-10">
          <SectionEyebrow label="§ 04 · THE ENGINE" />
          <h2 className="mt-2 font-serif font-semibold text-[32px] leading-[1.2] text-[var(--ink)]">
            Claude Sonnet 4.6, with a contract.
          </h2>
          <p className="mt-4 font-serif text-[18px] leading-[1.6] text-[var(--ink)]">
            JARVIS isn&rsquo;t an LLM with prompts taped to it. It&rsquo;s a streaming agent built on Claude Sonnet 4.6 with Strict Tool Use — every action is a typed, schema-validated JSON tool call. The Zod schema is the source of truth; the model cannot emit a malformed action. One input becomes N actions, each a different shape.
          </p>

          {/* Side-by-side input + JSON */}
          <div className="mt-6 flex flex-col md:flex-row md:items-stretch md:gap-6">
            {/* Left card — INPUT */}
            <div className="md:max-w-[280px] flex-shrink-0 border border-[var(--edge)] rounded bg-[var(--surface)] p-6">
              <p className="font-mono text-[14px] font-medium uppercase tracking-[0.14em] text-[var(--ink-muted)]">
                INPUT
              </p>
              <p className="mt-3 font-serif italic text-[18px] leading-[1.5] text-[var(--ink)]">
                {STRICT_TOOL_USE_FIXTURE.input}
              </p>
            </div>

            {/* Arrow (desktop only) */}
            <div className="hidden md:flex items-center text-[var(--ink-muted)]">
              <ArrowRight size={24} aria-hidden="true" />
            </div>

            {/* Right card — STRICT-TOOL-USE JSON (cyan-bearing surface) */}
            <div
              className="md:max-w-[460px] flex-1 rounded bg-[var(--surface-raised)] p-6 overflow-x-auto custom-scrollbar mt-6 md:mt-0"
              style={{
                border: "1px solid var(--edge-hud)",
                boxShadow: "var(--glow-hud-subtle)",
              }}
            >
              <p
                className="font-mono text-[14px] font-medium uppercase tracking-[0.14em]"
                style={{ color: "var(--hud-cyan-light)" }}
              >
                STRICT-TOOL-USE JSON
              </p>
              <pre className="mt-3 font-mono font-mono-stats text-[14px] leading-[1.55] text-[var(--ink)] whitespace-pre-wrap break-words">
                <JsonFormatted value={STRICT_TOOL_USE_FIXTURE.output} />
              </pre>
            </div>
          </div>

          {/* Source-of-truth note */}
          <p className="mt-4 font-mono text-[14px] text-[var(--ink-muted)]">
            Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits.
          </p>
        </section>
      );
    }

    /**
     * Manual JSON formatter with span-based syntax coloring per UI-SPEC §5d.
     * Keys --ink weight 500; string --ink-amber; number/null/bool --ink-sage;
     * punctuation --ink-muted.
     * Recursive — handles nested objects and arrays.
     */
    function JsonFormatted({ value, indent = 0 }: { value: unknown; indent?: number }) {
      const pad = "  ".repeat(indent);
      const padNext = "  ".repeat(indent + 1);

      if (value === null) {
        return <span style={{ color: "var(--ink-sage)" }}>null</span>;
      }
      if (typeof value === "boolean") {
        return <span style={{ color: "var(--ink-sage)" }}>{String(value)}</span>;
      }
      if (typeof value === "number") {
        return <span style={{ color: "var(--ink-sage)" }}>{value}</span>;
      }
      if (typeof value === "string") {
        return <span style={{ color: "var(--ink-amber)" }}>{JSON.stringify(value)}</span>;
      }
      if (Array.isArray(value)) {
        if (value.length === 0) {
          return <span style={{ color: "var(--ink-muted)" }}>[]</span>;
        }
        return (
          <>
            <span style={{ color: "var(--ink-muted)" }}>{"["}</span>
            {value.map((item, i) => (
              <span key={i}>
                {"\n"}
                {padNext}
                <JsonFormatted value={item} indent={indent + 1} />
                {i < value.length - 1 && <span style={{ color: "var(--ink-muted)" }}>,</span>}
              </span>
            ))}
            {"\n"}
            {pad}
            <span style={{ color: "var(--ink-muted)" }}>{"]"}</span>
          </>
        );
      }
      if (typeof value === "object") {
        const entries = Object.entries(value as Record<string, unknown>);
        if (entries.length === 0) {
          return <span style={{ color: "var(--ink-muted)" }}>{"{}"}</span>;
        }
        return (
          <>
            <span style={{ color: "var(--ink-muted)" }}>{"{"}</span>
            {entries.map(([key, val], i) => (
              <span key={key}>
                {"\n"}
                {padNext}
                <span style={{ color: "var(--ink)", fontWeight: 500 }}>
                  {JSON.stringify(key)}
                </span>
                <span style={{ color: "var(--ink-muted)" }}>: </span>
                <JsonFormatted value={val} indent={indent + 1} />
                {i < entries.length - 1 && <span style={{ color: "var(--ink-muted)" }}>,</span>}
              </span>
            ))}
            {"\n"}
            {pad}
            <span style={{ color: "var(--ink-muted)" }}>{"}"}</span>
          </>
        );
      }
      return null;
    }
    ```

    **Critical details:**
    - Imports `STRICT_TOOL_USE_FIXTURE` from `@hyperpolymath/jarvis-core/tests/strict-tool-use.fixture` — the workspace package path. If TypeScript path mapping doesn't resolve subpath imports from the package, fall back to a relative path: `import { STRICT_TOOL_USE_FIXTURE } from "../../../../packages/jarvis-core/tests/strict-tool-use.fixture"` — but verify which import strategy works in the existing project (check `tsconfig.json` `paths` + `packages/jarvis-core/package.json` `exports` field).
    - Cyan appears in EXACTLY 2 places (UI-SPEC §11a): (1) the `border: "1px solid var(--edge-hud)"` on the right card; (2) `style={{ color: "var(--hud-cyan-light)" }}` on the STRICT-TOOL-USE JSON eyebrow. The `boxShadow: "var(--glow-hud-subtle)"` is a token that internally references `--hud-cyan-rgb` — it counts as part of the surface 2 cyan budget (UI-SPEC §4 §11a allow it implicitly via the right-card-border + eyebrow cluster)
    - Heading: `text-[32px]` Display 2 + `font-semibold`
    - Body paragraph: `text-[18px]` Body, full 4-sentence verbatim from UI-SPEC §9
    - Left card: `--surface` bg, 1px `--edge` border, italic Body 18 for the input (per UI-SPEC §5d "Body 18 EB Garamond italic --ink, the user's verbatim sentence")
    - Right card: `--surface-raised` bg, 1px `--edge-hud` border, mono Caption 14 JSON
    - ArrowRight is 24px Lucide icon (exempt from type scale)
    - Source-of-truth note: mono Caption 14 `--ink-muted` with EXACT string from UI-SPEC §9: `Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits.`
    - JSON formatter recursively renders nested structures with proper indent
    - `<pre>` wraps the JSON for whitespace preservation; `whitespace-pre-wrap break-words` allows line wrapping on narrow viewports (the `max-w-[460px]` desktop card width was sized to fit the canonical 14px JSON without horizontal scroll per UI-SPEC §3)

    Implements LAND-ENGINE (SC-5 / D-05 / UI-SPEC §5d / §11e).
  </action>
  <verify>
    <automated>cd apps/web && test -f components/landing/EngineSection.tsx && ! grep -q '"use client"' components/landing/EngineSection.tsx && grep -q "STRICT_TOOL_USE_FIXTURE" components/landing/EngineSection.tsx && grep -q "Claude Sonnet 4.6, with a contract" components/landing/EngineSection.tsx && grep -q "STRICT-TOOL-USE JSON" components/landing/EngineSection.tsx && grep -q "Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts" components/landing/EngineSection.tsx && grep -q "ArrowRight" components/landing/EngineSection.tsx && grep -q "var(--edge-hud)" components/landing/EngineSection.tsx && grep -q "var(--hud-cyan-light)" components/landing/EngineSection.tsx && grep -q "var(--ink-amber)" components/landing/EngineSection.tsx && grep -q "var(--ink-sage)" components/landing/EngineSection.tsx && ! grep -E "HudCorner|HudStatusPill|HudEdgeInstrumentation|HudThinkingRing|whileInView|useInView|agent-mode-scope|framer-motion" components/landing/EngineSection.tsx && ! grep -E "text-xs|text-base|text-xl|text-2xl|text-3xl|text-4xl|text-5xl|text-6xl|font-bold" components/landing/EngineSection.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/landing/EngineSection.tsx` exists; NO `"use client"` (Server Component)
    - Imports `STRICT_TOOL_USE_FIXTURE` from the jarvis-core fixture path (resolves via workspace import OR relative path)
    - Imports `ArrowRight` from `lucide-react` and `SectionEyebrow` from `./SectionEyebrow`
    - Renders eyebrow `§ 04 · THE ENGINE`, heading `Claude Sonnet 4.6, with a contract.`, body paragraph (4 sentences from UI-SPEC §9)
    - Renders left INPUT card with `--surface` bg + 1px `--edge` border + italic Body 18 input text
    - Renders right card with 1px `--edge-hud` border (cyan-tinted) + `--surface-raised` bg + `--glow-hud-subtle` box-shadow + STRICT-TOOL-USE JSON eyebrow in `--hud-cyan-light`
    - Renders the JSON via the `JsonFormatted` recursive component with span-based syntax coloring: keys (`--ink` weight 500), strings (`--ink-amber`), numbers/null/bool (`--ink-sage`), punctuation (`--ink-muted`)
    - Source-of-truth note: `Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits.` (verbatim from UI-SPEC §9, including em-dash)
    - Cyan appears in 2 places only: `var(--edge-hud)` (border) + `var(--hud-cyan-light)` (eyebrow); `var(--glow-hud-subtle)` is an allowed token in the right-card cluster per UI-SPEC §11a
    - NO HUD primitives, NO `whileInView`, NO `framer-motion`, NO `.agent-mode-scope`
    - NO forbidden font sizes or `font-bold`
    - Mobile: stacks vertically (`flex-col md:flex-row`); right card uses `overflow-x-auto custom-scrollbar`
    - `pnpm tsc --noEmit` exits 0 (fixture import resolves; types check)
  </acceptance_criteria>
  <done>EngineSection renders the §04 input/JSON side-by-side with cyan-tinted right card; JSON is provably plucked from the real fixture file (UI-SPEC §11e gate satisfied).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire JarvisDemo + EngineSection into LandingPage — replace §02 and §04 placeholders</name>
  <files>apps/web/components/landing/LandingPage.tsx</files>
  <read_first>
    - apps/web/components/landing/LandingPage.tsx (current state from Plan 08-03 — has placeholders for §02, §04, §05, §06)
    - apps/web/components/landing/JarvisDemo.tsx (created in Task 1 of this plan)
    - apps/web/components/landing/EngineSection.tsx (created in Task 2 of this plan)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5 (section order — §02 between §01/§03, §04 between §03/§05)
  </read_first>
  <action>
    Edit `apps/web/components/landing/LandingPage.tsx`:

    **Step 1 — Add imports at the top** (after the existing landing imports):

    ```typescript
    import { JarvisDemo } from "./JarvisDemo";
    import { EngineSection } from "./EngineSection";
    ```

    **Step 2 — Replace the §02 placeholder block** (the `<section>...placeholder...</section>` block currently rendering `[§02 JarvisDemo placeholder — replaced in Plan 08-04]`) with:

    ```tsx
    <JarvisDemo />
    ```

    **Step 3 — Replace the §04 placeholder block** (the `<section>...placeholder...</section>` block currently rendering `[§04 EngineSection placeholder — replaced in Plan 08-04]`) with:

    ```tsx
    <EngineSection />
    ```

    **Do NOT touch** the §05 and §06 placeholders — those remain for Plan 08-05 to replace.

    **Do NOT change** the SectionDivider rhythm (the `<div className="py-12"><SectionDivider /></div>` wrappers around §04 — UI-SPEC §2 requires 96px around the Engine section vs 64px around sparse sections).

    Implements the final §02 + §04 wire-up. Wave 4 (Plan 08-05) finishes the §05 + §06 wire + the page.tsx refactor.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "import { JarvisDemo }" components/landing/LandingPage.tsx && grep -q "import { EngineSection }" components/landing/LandingPage.tsx && grep -q "<JarvisDemo />" components/landing/LandingPage.tsx && grep -q "<EngineSection />" components/landing/LandingPage.tsx && ! grep -q "§02 JarvisDemo placeholder" components/landing/LandingPage.tsx && ! grep -q "§04 EngineSection placeholder" components/landing/LandingPage.tsx && grep -q "§05 ChoiceSection placeholder" components/landing/LandingPage.tsx && grep -q "§06 BuildLog placeholder" components/landing/LandingPage.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/landing/LandingPage.tsx` imports `JarvisDemo` from `./JarvisDemo`
    - `apps/web/components/landing/LandingPage.tsx` imports `EngineSection` from `./EngineSection`
    - `<JarvisDemo />` appears between the §01 (`<ThesisSection />`) and §03 (`<PrimitivesTable />`) slots, with `<SectionDivider />`s on either side
    - `<EngineSection />` appears between the §03 and §05 slots, with `<div className="py-12"><SectionDivider /></div>` wrappers on either side (96px Engine breathing room preserved)
    - The §02 and §04 placeholder text is GONE (grep returns 0 hits for the placeholder strings)
    - The §05 and §06 placeholder text is PRESERVED (Plan 08-05 replaces them)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` succeeds (full build with all current components)
  </acceptance_criteria>
  <done>LandingPage renders 4 of 6 sections with real components (§01 Thesis, §02 Demo, §03 Primitives, §04 Engine); §05 + §06 remain placeholders for Plan 08-05.</done>
</task>

</tasks>

<verification>
- Both new components compile (`pnpm tsc --noEmit`)
- `pnpm build` from `apps/web/` succeeds end-to-end
- Cyan placement gate per UI-SPEC §11a: grep `var(--hud` across `apps/web/components/landing/**` returns hits in EXACTLY 2 files: `JarvisDemo.tsx` and `EngineSection.tsx`. Any other landing file containing cyan tokens is a failure.
- `STRICT_TOOL_USE_FIXTURE` import resolves (the workspace package path or relative path works in this TS config)
- The 3 examples in JarvisDemo are verbatim from UI-SPEC §7c
- The JSON syntax styling spans render correctly when the page is built (Plan 08-06 visually verifies)
</verification>

<success_criteria>
- **LAND-DEMO satisfied:** JarvisDemo animates Example A → B → C via FSM + receipt fades + reduced motion gate + SSR-friendly settled fallback
- **LAND-ENGINE satisfied:** EngineSection renders side-by-side INPUT/JSON cards with the right card cyan-tinted, JSON imported verbatim from `strict-tool-use.fixture.ts`, source-of-truth note matches UI-SPEC §9 verbatim
- **Cyan surgical containment:** cyan appears in EXACTLY `JarvisDemo.tsx` and `EngineSection.tsx`. No leakage to other landing components (UI-SPEC §11a)
- **LandingPage progressively complete:** 4 of 6 sections render real components; §05 + §06 await Plan 08-05
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-landing-manifesto/08-04-SUMMARY.md`
</output>
