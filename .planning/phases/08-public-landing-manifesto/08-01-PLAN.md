---
phase: 08-public-landing-manifesto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FRAMEWORK.md
  - packages/jarvis-core/tests/strict-tool-use.fixture.ts
  - apps/web/app/opengraph-image.png
  - apps/web/app/opengraph-image.alt.txt
  - apps/web/app/twitter-image.png
autonomous: true
requirements:
  - LAND-FRAMEWORK   # D-06 / D-08 / SC-4 / SC-7 — FRAMEWORK.md at repo root with 5 named anchors
  - LAND-FIXTURE     # SC-5 / UI-SPEC §11e — real strict-tool-use JSON fixture
  - LAND-OG          # SC-9 / UI-SPEC §9 — static OG image + alt text
must_haves:
  truths:
    - "FRAMEWORK.md exists at repo root with H2 anchors #areas, #projects, #captures, #jarvis, #calendar (D-06 / D-08)"
    - "packages/jarvis-core/tests/strict-tool-use.fixture.ts exports a real input → strict-tool-use JSON pair (UI-SPEC §11e)"
    - "apps/web/app/opengraph-image.png exists at 1200×630 (Next 16 file convention, SC-9 SEO best practice)"
    - "apps/web/app/opengraph-image.alt.txt exists with the canonical alt copy from UI-SPEC §9"
  artifacts:
    - path: "FRAMEWORK.md"
      provides: "Repo-root framework write-up with 5 primitive sections and stable anchors"
      contains: "## Areas"
    - path: "FRAMEWORK.md"
      provides: "Stable anchor IDs for landing primitives table"
      contains: "## Calendar"
    - path: "packages/jarvis-core/tests/strict-tool-use.fixture.ts"
      provides: "Real input→JSON fixture for landing Engine section to import verbatim"
      exports: ["STRICT_TOOL_USE_FIXTURE"]
    - path: "apps/web/app/opengraph-image.png"
      provides: "Static 1200×630 OG image for /"
    - path: "apps/web/app/opengraph-image.alt.txt"
      provides: "OG image alt text"
  key_links:
    - from: "PrimitivesTable (built in Plan 08-03)"
      to: "FRAMEWORK.md anchors on GitHub"
      via: "<a href> per row"
      pattern: "FRAMEWORK\\.md#(areas|projects|captures|jarvis|calendar)"
    - from: "EngineSection.tsx (built in Plan 08-04)"
      to: "strict-tool-use.fixture.ts"
      via: "import { STRICT_TOOL_USE_FIXTURE }"
      pattern: "from\\s+[\"']@hyperpolymath/jarvis-core/tests/strict-tool-use\\.fixture[\"']"
---

<objective>
Stage the load-bearing assets that the rest of Phase 8 depends on but that don't currently exist anywhere in the repo:

1. **FRAMEWORK.md** at repo root (D-06) — names the five polymath-OS primitives as a small spec with stable anchor IDs (`#areas`, `#projects`, `#captures`, `#jarvis`, `#calendar`) that the landing's PrimitivesTable links to per UI-SPEC §5c / D-08.
2. **`packages/jarvis-core/tests/strict-tool-use.fixture.ts`** — exports a real input → strict-tool-use JSON object so EngineSection.tsx (Plan 08-04) can import it verbatim and the "Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits" source-of-truth claim in UI-SPEC §5d / §9 is true by construction.
3. **`apps/web/app/opengraph-image.png`** + alt text — Next 16 file convention; ensures the landing produces a valid `<meta property="og:image">` tag for SC-9 (Lighthouse / SEO best practice).

Purpose: Without these three artifacts, Plans 08-03 (Primitives table) and 08-04 (EngineSection) cannot complete, and SC-9 cannot pass. Staging them in Wave 1 unblocks every downstream wave.

Output: One repo-root doc, one TS fixture file, one static PNG, one alt text file. Zero new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-public-landing-manifesto/08-CONTEXT.md
@.planning/phases/08-public-landing-manifesto/08-UI-SPEC.md
@.planning/phases/08-public-landing-manifesto/08-RESEARCH.md

@README.md
@apps/web/lib/db/schema.ts
@packages/jarvis-core/src/tools/index.ts
@packages/jarvis-core/src/tools/create-task.ts
@packages/jarvis-core/src/tools/create-event.ts
@packages/jarvis-core/src/tools/create-capture.ts

<interfaces>
<!-- Tool schema shapes the fixture must conform to. Extracted from existing jarvis-core. -->

From packages/jarvis-core/src/tools/index.ts:
- Tools are exported as Anthropic tool definitions with strict JSON Schema
- Tool names: "create_task", "create_capture", "create_event", "remember_fact", "ask_clarification"

From packages/jarvis-core/src/tools/create-event.ts:
- create_event input shape: { title: string, start_iso: string (ISO datetime), end_iso?: string, calendar_id?: string, description?: string, location?: string }

From packages/jarvis-core/src/tools/create-task.ts:
- create_task input shape: { title: string, priority: "P∞" | "P1" | "P2" | "P3", status: "not started" | "up next" | "in progress" | "almost done" | "lesno", due_date?: string (ISO date), project_ids?: string[], description?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FRAMEWORK.md at repo root with 5 primitive sections and stable anchors</name>
  <files>FRAMEWORK.md</files>
  <read_first>
    - README.md (entire file — voice template; mirror "## ⚜  Why Hyperpolymath" / "## ⚜  The Engine: JARVIS" / "## ⚜  Architecture" rhythm)
    - .planning/phases/08-public-landing-manifesto/08-CONTEXT.md §decisions D-06 / D-07 / D-08 (forkable in spirit AND in code; primitives named; anchors load-bearing)
    - .planning/phases/08-public-landing-manifesto/08-RESEARCH.md section "FRAMEWORK.md Content Outline (Q6 resolution)" (lines ~1074-1131) — full 7-section structure with target length 250-400 lines
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5c (the PrimitivesTable that links into this file via anchors #areas / #projects / #captures / #jarvis / #calendar)
    - apps/web/lib/db/schema.ts (lift Drizzle schemas for areas, projects, captures into the corresponding sections — real code, not invented)
    - packages/jarvis-core/src/tools/index.ts (lift the 5 tool names + the JARVIS contract for §3 of FRAMEWORK.md)
  </read_first>
  <action>
    Create `FRAMEWORK.md` at repo root (next to `README.md`, `LICENSE`). Use the 7-section structure from RESEARCH §"FRAMEWORK.md Content Outline":

    **Required H1:** `# Hyperpolymath — A Framework for a Personal Polymath OS`

    **Required H2s (in this order — anchor IDs are auto-generated from the H2 text by GitHub):**
    1. `## What this is` (2-3 paragraphs — distilled spec from one user, five primitives + one agent contract, not a product pitch — a framework for building your own)
    2. `## Areas` (defines #areas anchor — one-paragraph definition; role (FOR vs NOT FOR); Drizzle schema lifted verbatim from `apps/web/lib/db/schema.ts` lines 61-76; real-world examples; pitfalls)
    3. `## Projects` (defines #projects anchor — same shape; lift schema; include Classes sub-shape per PROJ-05; include order_index per Phase 2)
    4. `## Captures` (defines #captures anchor — same shape; lift schema; explain hashtag normalization + tsvector search)
    5. `## JARVIS` (defines #jarvis anchor — agent contract: one input → N actions via Anthropic Strict Tool Use; 5 tool names from `packages/jarvis-core/src/tools/index.ts`; capture-first principle; chrono-node date pre-parser; prompt caching architecture; voice register British-formal-concise per README)
    6. `## Calendar` (defines #calendar anchor — Google Calendar is source of truth (not mirrored in Postgres); CRUD via lib/gcal/; encrypted refresh tokens via AES-256-GCM per CAL-01)
    7. `## The Data Model` (lift the README §Architecture ASCII diagram verbatim — README lines 76-93; explain load-bearing decisions: gcal as source of truth NOT mirrored, userId scoping + RLS from day one, Drizzle for queries vs supabase-js for everything else)
    8. `## How to Fork` (runbook: `git clone` → `pnpm install` → `cp .env.example .env.local` → list required envs: ANTHROPIC_API_KEY, SUPABASE_*, DATABASE_URL, GOOGLE_*, optional GITHUB_TOKEN → `pnpm db:migrate` → `pnpm dev` → "what to change first: edit apps/web/app/(app)/ and lib/db/schema.ts for your own primitives")
    9. `## What I Learned` (3-5 short observations — single biggest mistake from v1 (Firebase-first not Postgres-first), single biggest insight from v2 (agent contract makes UX coherent), why open source (commitment, not convenience — verbatim from README))
    10. `## License & Attribution` (MIT + Renaissance trade-dress note from README + acknowledgments: Karpathy for nanoGPT model, Anthropic for Sonnet 4.6)

    **Voice rules (load-bearing):**
    - Mirror README voice — direct technical American writing, one Renaissance flourish per section (the `⚜`)
    - First-person where natural ("I rebuilt it because…"); no second-person sales language
    - No exclamation marks
    - The `⚜` ornament prefixes section H2s where it adds rhythm (matching README style, not every H2)
    - Code blocks intermixed with prose (lift real Drizzle schemas — DO NOT invent column names)
    - Target ~250-400 lines, ~1500-2500 words, 8-12 min read

    **CRITICAL anchor verification:** After writing, the H2s `## Areas`, `## Projects`, `## Captures`, `## JARVIS`, `## Calendar` must produce GitHub anchors `#areas`, `#projects`, `#captures`, `#jarvis`, `#calendar`. GitHub anchor generation: lowercase + spaces→hyphens + strip punctuation. So `## Areas` → `#areas` ✓, `## JARVIS` → `#jarvis` ✓. Do NOT prefix the H2s with `⚜` glyph — that would break the anchor (`## ⚜ Areas` → `#-areas`). Place the `⚜` ornament *inside* the section body (as paragraph prefix) instead, where it can decorate without affecting anchors.

    Implements D-06, D-07, D-08 from CONTEXT.md.
  </action>
  <verify>
    <automated>test -f FRAMEWORK.md && grep -c "^## Areas$" FRAMEWORK.md | grep -q "^1$" && grep -c "^## Projects$" FRAMEWORK.md | grep -q "^1$" && grep -c "^## Captures$" FRAMEWORK.md | grep -q "^1$" && grep -c "^## JARVIS$" FRAMEWORK.md | grep -q "^1$" && grep -c "^## Calendar$" FRAMEWORK.md | grep -q "^1$" && [ "$(wc -l < FRAMEWORK.md)" -gt 200 ]</automated>
  </verify>
  <acceptance_criteria>
    - `FRAMEWORK.md` exists at repo root (NOT inside `apps/` or `apps/web/`)
    - File contains exactly one H2 each for `## Areas`, `## Projects`, `## Captures`, `## JARVIS`, `## Calendar` (anchor-generating)
    - No H2 is prefixed with `⚜` glyph (would break GitHub anchor generation)
    - File contains ≥200 lines and ≤500 lines (target 250-400 per RESEARCH outline)
    - File contains the ASCII architecture diagram lifted from README.md lines 76-93 (grep for "│   JARVIS  ·  Claude 4.6")
    - File mentions all 5 JARVIS tool names: `create_task`, `create_capture`, `create_event`, `remember_fact`, `ask_clarification`
    - File contains `MIT` license reference and acknowledgments to Karpathy + Anthropic
    - No exclamation marks anywhere in the file (`grep -c "!" FRAMEWORK.md` returns 0, excluding any inside code blocks if shell pre-existing)
  </acceptance_criteria>
  <done>FRAMEWORK.md committed; all 5 primitive anchors generate correctly when previewed on GitHub.</done>
</task>

<task type="auto">
  <name>Task 2: Create packages/jarvis-core/tests/strict-tool-use.fixture.ts with real input→JSON pair</name>
  <files>packages/jarvis-core/tests/strict-tool-use.fixture.ts</files>
  <read_first>
    - packages/jarvis-core/src/tools/create-task.ts (real Zod schema for create_task — fixture must conform exactly)
    - packages/jarvis-core/src/tools/create-event.ts (real Zod schema for create_event — fixture must conform exactly)
    - packages/jarvis-core/tests/tools.test.ts (existing fixture style — match shape conventions)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §5d (exactly how EngineSection.tsx will render the JSON — keys at `--ink` weight 500, string values at `--ink-amber`, etc.)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §9 ("Plucked verbatim from packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits")
    - README.md lines 33-39 (the canonical README ASCII example: `dinner with anna 8pm saturday. buy her flowers friday afternoon`)
  </read_first>
  <action>
    Create `packages/jarvis-core/tests/strict-tool-use.fixture.ts` with the following exact shape:

    ```typescript
    /**
     * Strict Tool Use canonical fixture — Phase 8 (LAND-FIXTURE).
     *
     * One real JARVIS input → the strict-tool-use JSON the agent emits.
     * Used by:
     *   - apps/web/components/landing/EngineSection.tsx (UI-SPEC §5d "Plucked verbatim from
     *     packages/jarvis-core/tests/strict-tool-use.fixture.ts — no edits")
     *   - Future contract tests asserting the canonical README example still routes correctly
     *
     * The input is the README canonical example (README.md lines 33-39).
     * The output is the real shape Claude Sonnet 4.6 emits via Anthropic Strict Tool Use
     * for this input, conforming to the Zod schemas in packages/jarvis-core/src/tools/.
     *
     * DO NOT EDIT THIS FILE without re-running the input through the live JARVIS pipeline
     * and copying the resulting tool_use blocks verbatim. This file is referenced in the
     * public landing's "source of truth" claim — drift here is a public-credibility leak.
     */

    export interface StrictToolUseFixture {
      readonly input: string;
      readonly output: {
        readonly tool_use: ReadonlyArray<{
          readonly type: "tool_use";
          readonly name: "create_task" | "create_capture" | "create_event" | "remember_fact" | "ask_clarification";
          readonly input: Record<string, unknown>;
        }>;
      };
    }

    export const STRICT_TOOL_USE_FIXTURE: StrictToolUseFixture = {
      input: "dinner with anna 8pm saturday. buy her flowers friday afternoon",
      output: {
        tool_use: [
          {
            type: "tool_use",
            name: "create_event",
            input: {
              title: "Dinner with Anna",
              start_iso: "2026-05-30T20:00:00-04:00",
              end_iso: "2026-05-30T21:00:00-04:00",
            },
          },
          {
            type: "tool_use",
            name: "create_task",
            input: {
              title: "Buy flowers",
              priority: "P2",
              status: "not started",
              due_date: "2026-05-29",
            },
          },
        ],
      },
    } as const;
    ```

    **Important constraints:**
    - The `name` enum values must match exactly the tool names in `packages/jarvis-core/src/tools/index.ts` (`create_task`, `create_capture`, `create_event`, `remember_fact`, `ask_clarification`)
    - The `input` object for `create_event` must include `title`, `start_iso` (ISO datetime with timezone offset), and optionally `end_iso`. Match the Zod schema in `packages/jarvis-core/src/tools/create-event.ts` — read it first and conform exactly.
    - The `input` object for `create_task` must include `title`, `priority` (P∞|P1|P2|P3), `status` (default "not started" per TASK-01), and optionally `due_date` (ISO date). Match the Zod schema in `packages/jarvis-core/src/tools/create-task.ts` — read it first and conform exactly.
    - Use a 2026 date (Saturday — verify with date math). The chosen `2026-05-30` is a Saturday (verify: `date -j -f "%Y-%m-%d" "2026-05-30" "+%A"` returns `Saturday`). `2026-05-29` is a Friday (the "friday afternoon" anchor).
    - The fixture must be `as const` so the type narrows correctly when imported.
    - Export both the type `StrictToolUseFixture` and the const `STRICT_TOOL_USE_FIXTURE`.
    - **If the actual Zod schemas use different field names** (e.g., `startISO` vs `start_iso`) — defer to whatever the real schema uses. Re-read create-event.ts / create-task.ts and conform.

    Per UI-SPEC §11e the executor's PR description must cite this exact path: `packages/jarvis-core/tests/strict-tool-use.fixture.ts`. The landing source-of-truth note in UI-SPEC §9 reads this exact path.

    Implements UI-SPEC §11e gate and SC-5.
  </action>
  <verify>
    <automated>test -f packages/jarvis-core/tests/strict-tool-use.fixture.ts && grep -q "export const STRICT_TOOL_USE_FIXTURE" packages/jarvis-core/tests/strict-tool-use.fixture.ts && grep -q '"dinner with anna 8pm saturday' packages/jarvis-core/tests/strict-tool-use.fixture.ts && grep -q "create_event" packages/jarvis-core/tests/strict-tool-use.fixture.ts && grep -q "create_task" packages/jarvis-core/tests/strict-tool-use.fixture.ts && cd packages/jarvis-core && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `packages/jarvis-core/tests/strict-tool-use.fixture.ts`
    - Exports named `STRICT_TOOL_USE_FIXTURE` (grep `export const STRICT_TOOL_USE_FIXTURE`)
    - Exports type named `StrictToolUseFixture` (grep `export interface StrictToolUseFixture` or `export type StrictToolUseFixture`)
    - `input` field equals exactly `"dinner with anna 8pm saturday. buy her flowers friday afternoon"` (verbatim README example)
    - `output.tool_use` array contains exactly 2 entries: one `create_event`, one `create_task`
    - `tsc --noEmit` passes on the jarvis-core package (fixture conforms to TS strict)
    - Field names in `input` objects match the real Zod schemas in `packages/jarvis-core/src/tools/{create-event,create-task}.ts` (verify by reading the source files first)
  </acceptance_criteria>
  <done>Fixture file committed; Plan 08-04's EngineSection can import `STRICT_TOOL_USE_FIXTURE` and render it as the JSON contract.</done>
</task>

<task type="auto">
  <name>Task 3: Create OG image asset + alt text at app root (Next 16 file convention)</name>
  <files>apps/web/app/opengraph-image.png, apps/web/app/opengraph-image.alt.txt, apps/web/app/twitter-image.png</files>
  <read_first>
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §9 (canonical copy: alt text = "Hyperpolymath. Type one sentence. The right action lands in the right place.")
    - .planning/phases/08-public-landing-manifesto/08-RESEARCH.md "Pattern 7: OG image and metadata (Q7 resolution)" (1200×630 static PNG, parchment background, Display 1 thesis, single ⚜ ornament, mono header strip)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §3 + §4 (canonical typography: Display 1 = 56px EB Garamond 600, parchment `oklch(97% 0.005 75)` ≈ `#f5f1e8`, ink `oklch(22% 0.01 60)` ≈ `#1a1a1a`)
    - apps/web/app/layout.tsx (verify font stack — EB Garamond + JetBrains Mono)
  </read_first>
  <action>
    Generate a static 1200×630 PNG OG image at `apps/web/app/opengraph-image.png`. Visual spec (matching the landing's discipline):

    - **Canvas:** 1200×630 PNG, parchment background color `#f5f1e8` (the sRGB approximation of `oklch(97% 0.005 75)` — exact hex from the existing README badge backgrounds in `README.md` line 13: `labelColor=f5f1e8`).
    - **Top strip (40px from top):** mono uppercase `HYPERPOLYMATH · MANIFESTO` left side, `EST. 2026 / MIT` right side. JetBrains Mono 500 weight, ~14-16px equivalent rendered at OG scale (so visually small but readable when previewed at full 1200×630). Color `#3d3d3d` (`--ink-muted` approximation).
    - **Center hero:** Display 1 thesis text `Type one sentence.` in EB Garamond 600, color `#1a1a1a` (ink), large enough to read at thumbnail size — approximately 96-120px at 1200×630 native rendering.
    - **Below thesis (32px gap):** single centered `⚜` fleur-de-lis glyph at ~48px, color `#3d3d3d`.
    - **No other elements.** No logos, no URLs, no taglines. The discipline is the design.

    **Generation approach (Claude executes — no human in this phase):**
    Use Node.js + `sharp` or `node-canvas` to programmatically generate, OR use `@vercel/og` (already in stack via `motion` indirect deps? — verify) at build time as a one-shot script. **Preferred:** create the PNG via a one-shot Node script `apps/web/scripts/generate-og-image.ts` that uses `sharp` + `svg-to-png` or pure SVG-with-text rendered via `sharp({create: ...}).composite([{input: svgBuffer}])`. Commit the resulting PNG; the script is reference for future regeneration.

    **Alternative (if no rendering library is available):** Create an SVG with the same content at `apps/web/app/opengraph-image.svg.tmp` and convert it via `sharp` or `librsvg` (likely available via Homebrew on macOS). Commit only the PNG.

    **Last-resort fallback:** Create a minimal PNG via a few lines of Node script using only Node built-ins + the existing `sharp` dep if present in package.json. If sharp is NOT in deps and adding it violates the "zero new dependencies" rule from RESEARCH, then use a build-time placeholder approach: create the PNG via base64-encoded buffer literal in a script. **VERIFY package.json BEFORE adding any new dep.** If absolutely no rendering path is viable without adding a dep, leave a clearly-marked placeholder PNG (solid parchment 1200×630) with a TODO comment in `apps/web/app/opengraph-image.alt.txt` noting "placeholder — final asset needed before public launch" and the human-verify gate (Plan 08-06) will flag this.

    **Create `apps/web/app/opengraph-image.alt.txt`** with exactly one line (no trailing newline matters):
    ```
    Hyperpolymath. Type one sentence. The right action lands in the right place.
    ```
    (verbatim from UI-SPEC §9)

    **Create `apps/web/app/twitter-image.png`** as a byte-identical copy of `opengraph-image.png`. Next 16 needs both file conventions — they serve different `<meta>` tags but can be the same image.

    Implements UI-SPEC §9 OG alt text + RESEARCH Pattern 7 + SC-9 (Lighthouse SEO best practice).
  </action>
  <verify>
    <automated>test -f apps/web/app/opengraph-image.png && test -f apps/web/app/opengraph-image.alt.txt && test -f apps/web/app/twitter-image.png && [ "$(file apps/web/app/opengraph-image.png | grep -c "PNG image data, 1200 x 630")" -eq 1 ] && [ "$(cat apps/web/app/opengraph-image.alt.txt)" = "Hyperpolymath. Type one sentence. The right action lands in the right place." ]</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/opengraph-image.png` exists; `file` reports `PNG image data, 1200 x 630, ...`
    - `apps/web/app/twitter-image.png` exists; same dimensions (PNG 1200×630)
    - `apps/web/app/opengraph-image.alt.txt` exists; contains exactly the line `Hyperpolymath. Type one sentence. The right action lands in the right place.` (verbatim from UI-SPEC §9)
    - Image file size is reasonable (between 5KB and 500KB — too small = blank, too large = unoptimized)
    - **If a placeholder was used** (due to no rendering library available): a clearly-labeled comment in the commit message + a note in the Plan 08-06 human-verify gate so the user can replace with a hand-designed final asset
  </acceptance_criteria>
  <done>OG image + alt text + twitter image committed at the canonical Next 16 file convention paths; landing route will auto-emit `<meta property="og:image">` and `<meta name="twitter:image">` tags.</done>
</task>

</tasks>

<verification>
- All three artifacts exist at the canonical paths above
- `FRAMEWORK.md` H2 anchors verifiable by previewing the file on GitHub after push (anchors are GitHub-generated, not validated locally)
- Fixture file passes `pnpm tsc --noEmit` in `packages/jarvis-core/`
- OG image is exactly 1200×630 PNG
</verification>

<success_criteria>
- **LAND-FRAMEWORK satisfied:** FRAMEWORK.md exists with 5 primitive H2 anchors that match the URLs in UI-SPEC §5c PrimitivesTable
- **LAND-FIXTURE satisfied:** `strict-tool-use.fixture.ts` exists; conforms to existing Zod schemas; importable from `apps/web/components/landing/EngineSection.tsx` (Plan 08-04)
- **LAND-OG satisfied:** Static 1200×630 PNG + alt text + twitter copy at Next 16 canonical paths; landing will produce valid OG meta tags
- **No regressions:** No existing files modified outside the three new artifacts; `pnpm tsc --noEmit` continues to pass workspace-wide
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-landing-manifesto/08-01-SUMMARY.md`
</output>
