---
phase: 08-public-landing-manifesto
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified: []
autonomous: false
requirements:
  - LAND-VERIFY     # SC-2 / SC-3 / SC-6 / SC-7 / SC-8 / SC-9 — full UI-SPEC §11 acceptance gate + Phase 6/6.1/6.2 rejection prevention
must_haves:
  truths:
    - "All UI-SPEC §11a restraint gates pass (cyan in exactly 2 files; zero HUD primitives; zero scroll reveals; zero hover shadows; zero arc-reactor)"
    - "All UI-SPEC §11b typography gates pass (4 sizes; 2+2 weights; zero forbidden Tailwind utilities)"
    - "All UI-SPEC §11c layout gates pass (max-w-[640px] for sparse, max-w-[760px] for Engine + demo; 40px header; 3-col footer)"
    - "All UI-SPEC §11d motion gates pass (reduced-motion respected; no scroll-triggered reveals; ↓ vanishes on first scroll)"
    - "All UI-SPEC §11e content gates pass (6 sections in order; Block 1 renders when GitHub down; FRAMEWORK.md anchors resolve; fixture file cited)"
    - "All UI-SPEC §11f three-rejection gates pass (landing reads as document not agent chrome; cyan reads as machine output not Iron Man; reader without Tony Stark context not confused)"
    - "User approves the landing live (the gate Phase 6 and Phase 6.1 failed)"
  artifacts: []
  key_links:
    - from: "Lighthouse audit on /"
      to: "perf ≥95, a11y ≥95, best-practices ≥95"
      via: "Chrome DevTools Lighthouse run"
      pattern: "Lighthouse score categories"
---

<objective>
**The gate.** Phase 6 (neumorphic) and Phase 6.1 (HUD-heavy) were both rejected by the user at the final human-verify checkpoint with the same words: "clunky and blah." Phase 6.2 (Anthropic-discipline rebuild) was also reverted. This plan exists to prevent a fourth rejection — by gating Phase 8 acceptance on the user before declaring it done.

This plan ships **zero code**. It runs the UI-SPEC §11 acceptance gates as a structured checkpoint:

1. **Automated grep gates** (Claude runs these first to catch obvious failures)
2. **Build + smoke checks** (Claude runs `pnpm build`, `pnpm typecheck`, optionally Lighthouse)
3. **Visual + functional walkthrough** (user does this — light + dark, mobile + desktop, JS enabled + disabled, reduced motion on + off)
4. **Waitlist live test** (user submits a real email; Claude verifies row written; user submits duplicate; Claude verifies idempotency)
5. **Build-log degradation test** (Claude simulates GitHub failure; user confirms graceful fallback renders)

Output: Either user approval ("approved — Phase 8 ships") or a structured rejection list (which becomes the input to `/gsd:plan-phase 08 --gaps`).

**This plan is `autonomous: false`** — Claude MUST pause and wait for the user at the verification checkpoint. Do not auto-close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/08-public-landing-manifesto/08-CONTEXT.md
@.planning/phases/08-public-landing-manifesto/08-UI-SPEC.md
@.planning/phases/08-public-landing-manifesto/08-RESEARCH.md

@.planning/phases/08-public-landing-manifesto/08-01-SUMMARY.md
@.planning/phases/08-public-landing-manifesto/08-02-SUMMARY.md
@.planning/phases/08-public-landing-manifesto/08-03-SUMMARY.md
@.planning/phases/08-public-landing-manifesto/08-04-SUMMARY.md
@.planning/phases/08-public-landing-manifesto/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated UI-SPEC §11 grep gates + build sweep</name>
  <files></files>
  <read_first>
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11a (restraint gates — cyan placement, hover shadows, HUD primitives, scroll reveals, agent-mode-scope, arc-reactor)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11b (typography gates — 4 sizes, 2+2 weights, forbidden classes)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11c (layout gates — container widths, header, footer, mobile)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11d (motion gates — reduced motion, no scroll reveals, ↓ vanishes)
    - .planning/phases/08-public-landing-manifesto/08-UI-SPEC.md §11e (content gates — 6 sections, fixture cited, FRAMEWORK.md anchors)
  </read_first>
  <action>
    Run the UI-SPEC §11 grep gates programmatically. Each command below MUST return the expected result. If ANY gate fails, STOP and report — do not proceed to Task 2 (human-verify).

    **Gate 11a-1 — Cyan appears in EXACTLY 2 landing component files:**
    ```bash
    cd apps/web && grep -rEl "var\(--hud-cyan|--edge-hud|--glow-hud" components/landing/ | grep -v "/lib/" | sort
    ```
    Expected output (exactly 2 lines):
    ```
    components/landing/EngineSection.tsx
    components/landing/JarvisDemo.tsx
    ```

    **Gate 11a-2 — Zero hover box-shadow:**
    ```bash
    cd apps/web && grep -rE "hover:shadow|:hover\s*\{[^}]*box-shadow" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11a-3 — Zero .agent-mode-scope on landing:**
    ```bash
    cd apps/web && grep -rE "agent-mode-scope" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11a-4 — Zero HUD primitive imports:**
    ```bash
    cd apps/web && grep -rE "HudCornerCrops|HudStatusPill|HudEdgeInstrumentation|HudThinkingRing|HudCoreBubble" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11a-5 — Zero scroll-triggered reveals (whileInView / useInView):**
    ```bash
    cd apps/web && grep -rE "whileInView|useInView" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11b-1 — Zero forbidden Tailwind text-size utilities:**
    ```bash
    cd apps/web && grep -rE "text-xs|text-base|text-xl|text-2xl|text-3xl|text-4xl|text-5xl|text-6xl" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11b-2 — Zero font-bold (any weight 700):**
    ```bash
    cd apps/web && grep -rE "font-bold" components/landing/ app/page.tsx 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11b-3 — Arbitrary text-[Npx] sizes only from canonical {14, 18, 32, 56}:**
    ```bash
    cd apps/web && grep -rEo "text-\[[0-9]+px\]" components/landing/ app/page.tsx 2>/dev/null | sort -u
    ```
    Expected: ONLY some combination of `text-[14px]`, `text-[18px]`, `text-[32px]`, `text-[56px]`. Plus possibly `text-[24px]` for the centered ⚜ glyph in LandingFooter (icon dimension exempt — verify by reading the file context). NO other sizes allowed. **If `text-[24px]` appears outside the LandingFooter ⚜ glyph context, it is a violation.**

    **Gate 11b-4 — Zero framer-motion (must use motion/react):**
    ```bash
    cd apps/web && grep -rE 'from\s+["'\'']framer-motion["'\'']' components/landing/ 2>/dev/null | wc -l
    ```
    Expected: `0`

    **Gate 11e-1 — FRAMEWORK.md exists at repo root:**
    ```bash
    test -f FRAMEWORK.md && echo "ok" || echo "MISSING"
    ```
    Expected: `ok`

    **Gate 11e-2 — All 5 FRAMEWORK.md primitive H2 anchors present:**
    ```bash
    for p in Areas Projects Captures JARVIS Calendar; do echo -n "## $p: "; grep -c "^## $p$" FRAMEWORK.md; done
    ```
    Expected: each line ends with `: 1`

    **Gate 11e-3 — strict-tool-use.fixture.ts exists and is imported by EngineSection.tsx:**
    ```bash
    test -f packages/jarvis-core/tests/strict-tool-use.fixture.ts && grep -q "STRICT_TOOL_USE_FIXTURE" apps/web/components/landing/EngineSection.tsx && echo "ok" || echo "FAIL"
    ```
    Expected: `ok`

    **Gate 11e-4 — OG image exists at the canonical Next 16 path:**
    ```bash
    test -f apps/web/app/opengraph-image.png && test -f apps/web/app/twitter-image.png && test -f apps/web/app/opengraph-image.alt.txt && echo "ok" || echo "FAIL"
    ```
    Expected: `ok`

    **Gate page.tsx-1 — page.tsx is dynamic (no revalidate export):**
    ```bash
    ! grep -q "export const revalidate" apps/web/app/page.tsx && echo "ok" || echo "FAIL — PITFALL 1"
    ```
    Expected: `ok`

    **Build sweep:**
    ```bash
    cd apps/web && pnpm tsc --noEmit && pnpm build
    ```
    Expected: both exit 0; build output shows the root route as compiled.

    **Test sweep (if existing tests cover landing — none expected at this point):**
    ```bash
    cd apps/web && pnpm test --run 2>&1 | tail -20
    ```
    Expected: all existing tests still pass (no regressions to Phase 5/5.1/6/6.1/7 test suites).

    **Compile a results table** with one row per gate (PASS / FAIL). If ANY gate FAILs, STOP and report the failure to the user; do NOT proceed to Task 2. The user decides whether to fix immediately (loop back to plan-phase --gaps) or override.

    If ALL gates PASS, write the results table to the conversation output (NOT to a file — the parent agent reads it) and proceed to Task 2.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm build 2>&1 | tail -10 | grep -qiE "compiled|^✓|ready"</automated>
  </verify>
  <acceptance_criteria>
    - All gates listed above either PASS or are explicitly waived with a user-acknowledged reason
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0
    - Existing test suites (`pnpm test --run`) continue to pass (no regressions)
    - Results table reported to user before proceeding to Task 2
  </acceptance_criteria>
  <done>All automated gates green; results documented; Task 2 unblocked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User walkthrough — visual + functional verification of the landing</name>
  <what-built>
    The complete public landing manifesto at `/`:
    - **§01 Thesis** (cold open, no eyebrow, pull-quote → 56px hero → sub-line, breathing ↓ affordance)
    - **§02 Live JARVIS Demo** (animated terminal box, types Example A, fades in 2 receipts; click `▶ show another` to rotate to Examples B and C)
    - **§03 The Primitives** (5-row spec table linking to FRAMEWORK.md anchors on GitHub)
    - **§04 The Engine** (the one moderate-density section — paragraph + side-by-side INPUT/JSON cards with the right card cyan-tinted)
    - **§05 The Choice** (two equally-weighted doors — waitlist form on left, fork links on right)
    - **§06 Build Log** (3 blocks — currently shipping parsed from ROADMAP.md, last 7 commits from GitHub API via ISR, shipped-this-week summary)
    - **Header** (40px sticky mono eyebrow strip), **Footer** (3-col mono + ⚜ ornament + "be goated. well." sign-off)
    - **Sign-in escape link** under Door 1 routes to `/sign-in` (D-13)
    - **Logged-in branch** preserved: visiting `/` while signed in redirects to `/today` (SC-1)
    - All cyan accent confined to JarvisDemo (3 elements: ⚜, verb, caret) and EngineSection (2 elements: right card border, JSON eyebrow). NOWHERE ELSE.
  </what-built>
  <how-to-verify>
    **Pre-flight (Claude does this — START THE DEV SERVER FOR THE USER):**
    1. From repo root, run: `cd apps/web && pnpm dev` (boots Next.js on `http://localhost:3000`)
    2. Confirm dev server started without errors. If error, STOP and report — do not ask the user to verify a broken build.
    3. Verify `.env.local` has `GITHUB_TOKEN=` set (if not, the build log will show the degraded variant by design — confirm with the user whether to set it before verification)

    **USER walkthrough (read each step carefully and respond):**

    ### A. Cold-load — sign out first
    1. Open `http://localhost:3000/` in your browser **while signed out** (or in a private/incognito window).
    2. **Verify**: You see the manifesto landing — NOT a redirect to `/sign-in`.
    3. **Verify**: First paint shows: header strip at top (`HYPERPOLYMATH · MANIFESTO` left / `EST. 2026 / MIT` right) + pull-quote + 56px "Type one sentence." hero + sub-line + a breathing ↓ glyph at the bottom of the viewport.
    4. **Verify**: The page is on a warm parchment background — NOT white, NOT pure grey. The typography is EB Garamond serif (not the OS default).

    ### B. Scroll through all 6 sections
    1. Scroll down slowly. The ↓ affordance should vanish after the first scroll and NOT come back.
    2. **§01 Thesis** — confirm 3-line thesis: `Type one sentence.` / `The right action lands in the right place.` / `Every time.`
    3. **Section divider** appears (three ⚜ glyphs centered).
    4. **§02 Demo** — heading "Watch it route." appears. Then **the animation starts**: cursor types `lunch with sam 8pm saturday. pick up groceries friday afternoon` character by character (~2-3 seconds), pauses briefly, then 2 receipts fade in below with cyan `⚜` ornaments and cyan verbs `scheduled` / `created`.
    5. **Click `▶ show another`** below the terminal. The terminal should fade out and replay with Example B (`#idea polymathy as a competitive advantage` → 1 capture receipt). Click again → Example C (`finish anth pset $ANTH 2480 p2 by 3pm tomorrow` → 1 task receipt). Click again → back to Example A.
    6. **§03 Primitives** — see the 5-row spec table (Areas, Projects, Captures, JARVIS, Calendar). Each row has a small ↗ arrow on the right.
    7. **Click any ↗ arrow** — should open the corresponding FRAMEWORK.md anchor on GitHub in a new tab (e.g., `#areas`). Verify the anchor actually resolves to the right H2 section.
    8. **§04 Engine** — paragraph about Claude Sonnet 4.6 + Strict Tool Use, then a side-by-side block: left card "INPUT" (the dinner-with-sam sentence in italic serif), right card "STRICT-TOOL-USE JSON" (with subtle cyan-tinted border + glow). The JSON shows the real `create_event` + `create_task` shapes.
    9. **§05 Choice** — heading "Two doors. Both open." Two columns: left "USE IT" with email input + dark `Join the waitlist` button; right "FORK IT" with two text-links (`▶ Read the framework` + `◆ View the repo`) + caption about MIT.
    10. Under the waitlist form there should be a quieter line: `Already have an account? Sign in →` linking to `/sign-in`.
    11. **§06 Build Log** — three blocks: CURRENTLY SHIPPING (shows the current in-progress phase from ROADMAP.md), LAST 7 COMMITS (real commit hashes with dates and subjects, hashes link to GitHub), SHIPPED THIS WEEK (a one-line summary).
    12. **Footer** — 3-column mono row (`MIT LICENSE` / `github.com/filippo-fonseca` / `filippofonseca.com →`), centered ⚜ ornament, italic centered `be goated. well.` sign-off.

    ### C. Discipline gates (visual restraint)
    1. **Verify**: Cyan accent appears ONLY in: (a) the JARVIS demo terminal (⚜ + verbs + caret), (b) the Engine §04 right card (border + JSON eyebrow). NOWHERE ELSE — not on header, not on footer, not on CTAs, not on links, not on the spec table.
    2. **Verify**: NO HUD chrome anywhere — no hex grids, no corner crops, no glowing orbs, no scan-lines, no edge instrumentation rails.
    3. **Verify**: The page reads like a printed broadside / manifesto, not like an AI app dashboard or a marketing page.
    4. **Verify**: Every section has generous breathing room — sparse, document-discipline density (except §04 which has the one moderate-density block earned by the contract content).

    ### D. Waitlist live test
    1. In Door 1, type a real test email (e.g., `test-yourname-{date}@example.com`).
    2. Click `Join the waitlist`. The button should briefly show `Submitting...` then the form swaps in-place to: serif italic `You're in. I'll write when there's something to log into.` plus an optional `what do you do? (optional)` field.
    3. Type a note (e.g., `testing the form`). Click `[submit]`. Should swap to `Got it. Thanks.`
    4. Now refresh the page. Submit the **same email again**. Should swap to the same success message (no error visible — idempotent).
    5. **Claude verifies the DB:** run `psql $DATABASE_URL -c "SELECT email, note, created_at FROM public.waitlist ORDER BY created_at DESC LIMIT 5;"` (or via Supabase Studio). Confirm exactly ONE row with that email (NOT duplicates).

    ### E. Sign-in escape + logged-in branch
    1. Click `Already have an account? Sign in →`. Should navigate to `/sign-in` (the existing route).
    2. Sign in with Google. After auth, visit `/` directly. Should redirect to `/today` (NOT render the landing).
    3. Sign out. Visit `/` again. Should render the landing again.

    ### F. JS-disabled fallback (SC-9)
    1. In DevTools → Settings → Debugger → "Disable JavaScript". Reload `/`.
    2. **Verify**: The JARVIS demo terminal shows the SETTLED state of Example A (input + 2 receipts visible) — NOT an empty `$` prompt. The page is otherwise readable.
    3. Re-enable JS.

    ### G. Reduced-motion (SC-3 / D-02)
    1. In OS settings, enable "Reduce motion" (macOS: System Settings → Accessibility → Display → Reduce motion).
    2. Reload `/`. **Verify**: The JARVIS demo shows Example A's settled state on mount — no typing animation, no caret pulse, no fade. The ↓ scroll affordance is static (no breath).
    3. Disable reduce motion.

    ### H. Mobile responsiveness (UI-SPEC §11c)
    1. DevTools → toggle device toolbar → iPhone SE (375px). Reload.
    2. **Verify**: The entire page renders without horizontal scroll EXCEPT the JARVIS demo terminal box, which can scroll horizontally (its mono shape is load-bearing — preserved per UI-SPEC §2).
    3. **Verify**: The 2 doors in §05 stack vertically.
    4. **Verify**: The Engine §04 input/JSON cards stack vertically.
    5. **Verify**: Padding stays consistent (`px-6` = 24px each side).

    ### I. Light + dark modes
    1. If a theme toggle is accessible from `/`, use it. Otherwise force via DevTools → toggle dark mode emulation.
    2. **Verify**: Both modes pass the parchment-discipline feel. Dark mode should be a deep-ink canvas (not pure black) with appropriate ink/edge token swaps. Cyan should still appear ONLY in the JARVIS demo + Engine right card.

    ### J. Build-log graceful degradation (D-10)
    1. **Claude does this** (NOT user): temporarily edit `apps/web/.env.local` to set `GITHUB_TOKEN=invalid_token_for_testing`. Restart dev server. Reload `/`.
    2. **Verify**: §06 Build Log shows Block 1 (Currently Shipping) correctly + Block 2/3 collapse to a single line `→ Commit feed unavailable. See the repo directly.` linked to the repo.
    3. **Claude does this:** restore the real `GITHUB_TOKEN`. Restart dev server. Reload. Verify the full commit feed returns.

    ### K. Lighthouse (SC-9)
    1. DevTools → Lighthouse → Mode: Navigation → Categories: Performance, Accessibility, Best Practices, SEO → Generate report.
    2. **Verify**: All 4 categories ≥95.
    3. **Verify**: Zero console errors during page load.
    4. If any score is <95, report which category and the top issues. May be acceptable to defer minor issues — user decides.

    ### L. The three-rejection gate (UI-SPEC §11f)
    1. Does the landing read as a **document / manifesto** (printed broadside), NOT as agent chrome?
    2. Does the cyan in §02 + §04 read as **machine output** (universally legible, "this is JSON / this is JARVIS routing"), NOT as "Iron Man HUD reference"?
    3. Would a reader who has never heard of Tony Stark / JARVIS find any part of this page confusing? (Should be NO — JARVIS is the agent's name; the cyan is "machine output color"; nothing else demands cultural context.)

    ---

    **If ALL of A through L pass — type `approved` to ship Phase 8.**

    **If ANY step fails — describe specifically:**
    - Which section / element
    - What you expected vs what you saw
    - Whether it's a blocker (must fix) or a defer (ship and follow up)

    Claude will use your feedback to either spawn `/gsd:plan-phase 08 --gaps` (for blockers) or to log to the backlog (for defers).

    **Specific Open Questions Claude needs your answer on during this walkthrough:**
    1. **Production URL for OG metadata** (RESEARCH Open Question 1) — what domain should `openGraph.url` be set to? (e.g., `hyperpolymath.com`, `hyperpolymath.app`, `hyperpolymath.io`, deferred?) — if deferred, the meta tag will use the request domain automatically.
    2. **`filippofonseca.com` footer link** (RESEARCH Open Question 2) — does this URL currently resolve to a working site? If not, what URL should replace it (or remove the column)?
    3. **OG image quality** (Plan 08-01 Task 3) — if a placeholder was used due to no rendering library available, please review the placeholder and decide whether to ship as-is or replace with a hand-designed version.
  </how-to-verify>
  <resume-signal>
    Type `approved` to declare Phase 8 complete and update STATE.md to milestone complete.

    OR type specific issues you observed (one per line, prefixed with `BLOCKER:` or `DEFER:`) and Claude will route them.

    OR type `escalate` if something fundamental is off and you want to re-discuss the phase before proceeding.
  </resume-signal>
  <files>N/A — no code changes; this task is a user verification walkthrough</files>
  <action>
    Claude facilitates the walkthrough described in `<what-built>` and `<how-to-verify>` above:
    1. Boot the dev server (`cd apps/web && pnpm dev`)
    2. Confirm boot success
    3. Verify .env.local has GITHUB_TOKEN set; if not, ask user before proceeding
    4. Wait for the user to walk through steps A through L and respond with `approved` or specific blockers/defers
    5. DO NOT mark the task complete until the user explicitly types `approved` (or routes blockers/escalates)
  </action>
  <verify>
    <automated>echo "Manual verification — Claude waits for user resume signal per <resume-signal> block above"</automated>
  </verify>
  <done>User typed `approved` (success path), OR user typed blockers/defers (route to Task 3 / gap-closure path), OR user typed `escalate` (re-open discuss-phase).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: On approval — commit human-verify checkpoint + update phase summary</name>
  <files></files>
  <read_first>
    - .planning/STATE.md (current state — must update milestone status after Phase 8 ships)
    - User's resume signal from Task 2
  </read_first>
  <action>
    **Only run this task if Task 2 resume signal is `approved`.** If the user typed blockers or escalated, route to the appropriate follow-up (gap-closure plan or discuss-phase re-open) and do NOT run this task.

    **On approval:**

    1. **Commit the human-verify approval marker** — write a short note to the conversation output confirming Phase 8 approved (the GSD orchestrator persists this to STATE.md / VERIFICATION.md per its normal pattern).

    2. **No file edits required** — Plan 08-06 ships zero code; this final task just acknowledges the approval and signals Phase 8 complete to the orchestrator.

    3. **Return the structured planning outcome** with:
       - Phase: 08-public-landing-manifesto
       - Status: APPROVED (live walkthrough passed — the gate Phase 6 + 6.1 + 6.2 failed)
       - Plans complete: 6 of 6
       - Any user-flagged DEFER items (logged for backlog)
       - Production URL decision from Open Question 1
       - `filippofonseca.com` decision from Open Question 2
       - OG image decision from Open Question 3

    **On any non-approval resume signal:** report back to the user and orchestrator with the structured rejection list; do NOT mark Phase 8 complete.
  </action>
  <verify>
    <automated>echo "Verification depends on user signal from Task 2 — manually inspect conversation outcome"</automated>
  </verify>
  <acceptance_criteria>
    - Task 2's resume signal received and recorded
    - If `approved`: Phase 8 marked complete; deferred items routed to backlog
    - If blockers: Phase 8 NOT marked complete; gap-closure plan-phase invoked
    - If escalate: discuss-phase re-opened
  </acceptance_criteria>
  <done>Phase 8 status finalized per user signal; STATE.md update handled by orchestrator.</done>
</task>

</tasks>

<verification>
- All UI-SPEC §11 grep gates pass (Task 1)
- `pnpm build` succeeds; no regressions to prior phase test suites
- User walkthrough completes all 12 steps A-L
- User explicitly types `approved` (or describes blockers/defers)
- Waitlist live test confirms one DB row written per unique email (idempotency)
- Build-log graceful degradation visually confirmed
- Lighthouse ≥95 across perf/a11y/best-practices
</verification>

<success_criteria>
- **LAND-VERIFY satisfied** (the gate that defines this plan):
  - All UI-SPEC §11a-§11e automated gates pass
  - User completes the manual walkthrough and types `approved`
  - Phase 6/6.1/6.2 rejection lesson NOT repeated — landing reads as document discipline, not agent chrome
  - All three open questions answered or explicitly deferred
- **All 9 ROADMAP success criteria proven live:**
  - SC-1: signed-out renders landing; signed-in redirects to /today
  - SC-2: 6 sections in correct order
  - SC-3: demo animates without layout shift; respects prefers-reduced-motion
  - SC-4: Primitives names Areas/Projects/Captures/JARVIS/Calendar as a spec table
  - SC-5: Engine explains Sonnet 4.6 + Strict Tool Use with real input→JSON
  - SC-6: build-log pulls live commits + current phase + shipped-this-week with graceful degradation
  - SC-7: Choice presents two equally-weighted doors
  - SC-8: passes Phase 6.1 restraint check (no HUD-heavy chrome, JARVIS as ATMOSPHERIC mood only)
  - SC-9: Lighthouse ≥95; no console errors; JS-disabled graceful degradation
</success_criteria>

<output>
After completion, create `.planning/phases/08-public-landing-manifesto/08-06-SUMMARY.md` documenting the verification outcome (approved / blockers list / deferrals routed to backlog).
</output>
