---
status: partial
phase: 05.1-jarvis-agentic-refactor
source: [05.1-VERIFICATION.md]
started: 2026-05-17T22:00:00Z
updated: 2026-05-17T22:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Live prose register smoke
prompt: "dinner 8pm saturday with sam + remind me to pick up groceries friday"
expected: One prose block (1-3 sentences, JARVIS register — opens with "Handled, sir." or "Very good." or similar) renders ABOVE two compact receipt cards. Queued placeholder animation visible before each receipt resolves to a checked state. Receipts show resolved fields (Saturday 8 PM dinner event, Friday flowers task). Optional witty observational aside is welcome but not mandatory.
result: [pending]

### 2. Memory round-trip smoke
prompt: "Sam is my partner"
expected: remember_fact receipt appears with 5s undo countdown. Fact persists to jarvis_facts table. Navigate to `/settings/memory` — Sam fact visible in MemoryTable, type=entity. Refresh browser tab — fact still visible (persistence across sessions confirmed).
result: [pending]

### 3. /ask mode memory read-back
prompt (after creating at least one fact via test 2): "/ask what do you remember about me?"
expected: Prose-only reply (no tool_use emitted) referencing the stored fact from the JARVIS MEMORY block injected into the system prompt. Should mention "Sam" or whatever fact was created.
result: [pending]

### 4. ask_clarification live trigger
prompt: An ambiguous project-linked request — pick a phrasing where multiple projects could match (e.g. typing `$ph` with several `$ph*` projects in the system, or a date that could mean two things).
expected: Violet QUESTION receipt with chip options renders inline. Clicking a chip OR typing a reply submits the next user message prefixed with `[CLARIFICATION REPLY] ...`. JARVIS does NOT ask another clarification on the same exchange (depth cap = 1).
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

(none yet — pending live testing)

## Notes

- Phase 5.1 automated verification scored 22/22 must-haves; these 4 items defer live smoke because they require ANTHROPIC_API_KEY + an authenticated Supabase session (not available in CI).
- Plan 03 SUMMARY noted MemoryTable.queryFn currently returns initialFacts instead of re-fetching — facts written mid-session don't appear in /settings/memory without a page reload. Hard refresh always shows current state. Test 2's "Refresh browser tab" step covers this acceptably for MVP; promote to a real re-fetch in Phase 6 polish if it bothers in practice.
- Plan 04 SUMMARY notes runScriptedTurn / toolsStructurallyEqual are CONCRETE (not stubs); JARVIS-22 live-mode pass rate is the model-quality acceptance bar — run `ANTHROPIC_LIVE=true pnpm --filter web test jarvis-implicit-intent` for the first baseline if/when desired (not a gating requirement here).
