---
phase: 05.1-jarvis-agentic-refactor
verified: 2026-05-18T02:15:00Z
status: human_needed
score: 22/22 must-haves verified
human_verification:
  - test: "Live JARVIS turn — type 'dinner 8pm saturday with sam + remind me to pick up groceries friday'"
    expected: "Prose block appears ABOVE compact receipts. Prose opens with 'Handled, sir.' or equivalent JARVIS register (1-3 sentences, dry observational aside is optional). Two receipts render below in compact variant (thinner border, smaller padding). Queued placeholder flashes briefly before each receipt resolves."
    why_human: "Prose register, JARVIS personality voice calibration, and queued animation are model-behavior + visual — not verifiable by static grep or unit tests."
  - test: "Live JARVIS turn — type 'what do you know about me?' in /ask mode"
    expected: "No tool_use emitted. Prose-only reply. If any jarvis_facts exist for the user, JARVIS references them from the JARVIS MEMORY block (injected in system prompt). If empty, JARVIS says plainly 'I haven't recorded anything yet, sir.' or equivalent."
    why_human: "Requires real Supabase session, live facts in DB, and live Anthropic call. Verifies D-R4 /ask mode memory read-back."
  - test: "Live JARVIS turn — create a fact via 'Sam is my partner' then navigate to /settings/memory"
    expected: "JARVIS emits remember_fact tool call with type=entity, key=Sam, value=my partner, source=user_explicit. Receipt shows the fact with 5s undo countdown. Settings > Memory page lists the fact under 'entity' group. Fact persists after browser refresh."
    why_human: "End-to-end persistent memory write-through: model -> tool_use -> executor -> Postgres -> settings page. Requires live Supabase session."
  - test: "Live JARVIS turn — ask an ambiguous multi-resolution question like 'what project should I work on today'"
    expected: "JARVIS emits ask_clarification (if multiple projects exist) OR routes to capture-first (if input is genuinely low-signal). ask_clarification receipt renders inline with chip options and a free-text reply input. Typing a reply and pressing Enter continues the turn as '[CLARIFICATION REPLY]...'; JARVIS does not ask again on that reply."
    why_human: "ask_clarification model routing depends on real project list + live model confidence heuristic. Chip UI and reply-correlation require visual inspection."
---

# Phase 5.1: jarvis-agentic-refactor Verification Report

**Phase Goal:** Reshape JARVIS into a high-efficiency agentic assistant: prose-first witty Tony-Stark response surface, persistent jarvis_facts memory layer, ask_clarification tool for medium-confidence intents, per-turn pipeline budget (≤2 DB roundtrips + p50 TTFA <800ms warm), implicit-intent fidelity from fragmented language. Built incrementally on Phase 5 jarvis-core / Node-runtime SSE route / Console UI.
**Verified:** 2026-05-18T02:15:00Z
**Status:** human_needed — all automated checks pass; 4 live-turn smoke items require manual verification
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 01 | Sidebar areas query no longer refetches when a JARVIS Server Action fires — staleTime Infinity + initialDataUpdatedAt | VERIFIED | `grep -c "initialDataUpdatedAt"` → 1; `grep -c "staleTime: Infinity"` → 1 in Sidebar.tsx |
| 02 | validate-references batches project + calendar checks into a single Promise.all roundtrip via validateTurnReferences | VERIFIED | `grep -c "validateTurnReferences"` → 2; `grep -c "Promise.all"` → 2 in validate-references.ts |
| 03 | tests/jarvis-perf-budget.test.ts asserts ≤2 DB roundtrips per single-action turn via Drizzle logger spy | VERIFIED | File exists; `grep -c "toBeLessThanOrEqual(2)"` → 1; test passes (4/4 tests) |
| 04 | tests/sidebar-no-refetch.test.tsx mounts Sidebar with initialActiveAreas and asserts queryFn fires 0 additional times | VERIFIED | File exists; `grep -c "no incidental refetch"` → 2; test passes |
| 05 | personality.ts TOOL_USE_RULES inverted: "emit tool calls only" GONE; "leading text block" rule present; "Handled, sir" calibration target verbatim | VERIFIED | `grep -c "Handled, sir"` → 5; `grep -c "leading text block"` → 2; `grep -c "emit tool calls only"` → 0 |
| 06 | personality.ts contains "JARVIS MEMORY" (D-R4 /ask mode forward-compat) | VERIFIED | `grep -c "JARVIS MEMORY"` → 1 |
| 07 | JarvisScrollback renders textDelta unconditionally on action turns — old gate removed | VERIFIED | `grep -c "actions.length === 0 && turn.textDelta"` → 0; turn.textDelta rendered at lines 72-75 |
| 08 | JarvisReceipt has compact variant and queued placeholder | VERIFIED | `grep -c "compact"` → 7; `grep -c "queued"` → 7 in JarvisReceipt.tsx |
| 09 | route.ts emits `event: queued` SSE before executor work; jarvis-stream-client handles onQueued | VERIFIED | `grep -c 'sse("queued"'` → 1 in route.ts; `grep -c "onQueued"` → 2 in jarvis-stream-client.ts |
| 10 | supabase/migrations/0011_jarvis_facts.sql exists with CREATE TABLE jarvis_facts, RLS policies (SELECT/INSERT/UPDATE/DELETE), UNIQUE(user_id,type,key), 2 indexes | VERIFIED | File present; migration contains table definition, UNIQUE constraint, 4 RLS policies, 2 CREATE INDEX statements |
| 11 | packages/jarvis-core/src/tools/remember-fact.ts exports zRememberFactFor; strict:true applied via tools/index.ts | VERIFIED | File exists; `grep -c "zRememberFactFor"` → 2; strict:true confirmed in index.ts for all 5 tools |
| 12 | prompt-builder.ts buildSystemPrompt accepts facts: JarvisFact[] and emits compiled facts block; cache_control boundary updated | VERIFIED | `grep "buildFactsBlock\|facts:"` in prompt-builder.ts returns 2 hits including export |
| 13 | personality.ts contains "five tools" after Plan 04 | VERIFIED | "five tools: create_task, create_capture, create_event, remember_fact, ask_clarification" present at line 64 |
| 14 | apps/web/lib/jarvis/executor.ts exports rememberFact + askClarification cases | VERIFIED | `grep -c "rememberFact\|askClarification"` → 2 in executor.ts |
| 15 | apps/web/app/(app)/settings/memory/page.tsx exists | VERIFIED | File present at path |
| 16 | JarvisReceipt contains jarvis_suggested branch with useUndoCountdown(10 for Keep/Discard | VERIFIED | `grep -c "useUndoCountdown(10"` → 2; `grep -c "jarvis_suggested"` → 10 in JarvisReceipt.tsx |
| 17 | tests/jarvis-facts-injection.test.ts contains ≥5 adversarial fixtures including "forget that" and "ignore all previous instructions" | VERIFIED | File is 364 lines; `grep -c "it("` → 5; both literal strings confirmed at lines 10-11, 320, 336 |
| 18 | packages/jarvis-core/src/tools/ask-clarification.ts exports zAskClarificationFor; 5th tool in index.ts | VERIFIED | File exists; `grep -c "zAskClarificationFor"` → 2; index.ts has 7 references to ask_clarification |
| 19 | personality.ts contains "five tools" AND "ask_clarification"; co-emit prohibition present ("alone in the turn") | VERIFIED | Line 64: "five tools"; line 71: "it must be alone in the turn"; co-emit prohibition confirmed |
| 20 | apps/web/components/jarvis/JarvisClarification.tsx exists; renders question + chip options + reply input; onReply fires [CLARIFICATION REPLY] prefix (via Console) | VERIFIED | File exists; `grep -c "CLARIFICATION REPLY"` → 3 in component; Console adds prefix |
| 21 | apps/web/tests/jarvis-implicit-intent.fixtures.ts exports IMPLICIT_INTENT_FIXTURES array of length 20 | VERIFIED | Export confirmed; 20 fixture entries (`grep -c "  {$"` → 20); `fragmented:` appears 21 times (one in interface def + 20 fixtures) |
| 22 | apps/web/tests/jarvis-implicit-intent.test.ts exists; mocked-mode exits 0; runScriptedTurn and toolsStructurallyEqual are NOT stubs | VERIFIED | `grep -c "runScriptedTurn\|toolsStructurallyEqual"` → 7; test passes 234/235 (1 skipped = ANTHROPIC_LIVE gate) |

**Score:** 22/22 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `apps/web/components/shell/Sidebar.tsx` | initialDataUpdatedAt + staleTime: Infinity on areas useQuery | VERIFIED | Both present |
| `apps/web/lib/jarvis/validate-references.ts` | validateTurnReferences batched helper | VERIFIED | Function exported, Promise.all inside |
| `apps/web/lib/db/client.ts` | Drizzle logger hook for test-time spy | VERIFIED | `logger` keyword present; production path is undefined |
| `apps/web/tests/jarvis-perf-budget.test.ts` | Perf budget test asserting ≤2 DB roundtrips | VERIFIED | File exists, assertion confirmed |
| `apps/web/tests/sidebar-no-refetch.test.tsx` | Sidebar refetch regression test | VERIFIED | File exists, "no incidental refetch" text present |
| `packages/jarvis-core/src/personality.ts` | Prose-first rewrite: "Handled, sir" + "leading text block" + "JARVIS MEMORY" | VERIFIED | All 3 patterns confirmed |
| `packages/jarvis-core/tests/personality.test.ts` | Personality contract tests (5 assertions) | VERIFIED | File created; 8 total tests pass |
| `apps/web/components/jarvis/JarvisScrollback.tsx` | Prose above receipts unconditionally | VERIFIED | Gate removed; textDelta rendered at line 72 |
| `apps/web/components/jarvis/JarvisReceipt.tsx` | compact variant + queued placeholder | VERIFIED | Both present |
| `apps/web/components/jarvis/jarvis-types.ts` | ScrollbackAction.status "queued"/"done"; clarification field | VERIFIED | Both fields added |
| `apps/web/app/api/jarvis/route.ts` | event: queued SSE emission + [CLARIFICATION REPLY] depth cap | VERIFIED | `sse("queued"` → 1; "CLARIFICATION REPLY" → 3 |
| `apps/web/tests/jarvis-prose-first.test.tsx` | 3 prose-first tests | VERIFIED | File exists; 3 tests pass |
| `apps/web/supabase/migrations/0011_jarvis_facts.sql` | jarvis_facts table + RLS + UNIQUE + indexes | VERIFIED | All 4 RLS policies present; UNIQUE constraint and 2 indexes confirmed |
| `packages/jarvis-core/src/tools/remember-fact.ts` | zRememberFactFor factory; strict:true | VERIFIED | Factory confirmed; strict:true via index.ts |
| `packages/jarvis-core/src/prompt-builder.ts` | buildSystemPrompt accepts facts; buildFactsBlock exported | VERIFIED | Both confirmed |
| `apps/web/lib/jarvis/executor.ts` | rememberFact + askClarification cases | VERIFIED | Both present |
| `apps/web/app/(app)/settings/memory/page.tsx` | Settings Memory page | VERIFIED | File present |
| `apps/web/tests/jarvis-facts-injection.test.ts` | ≥5 adversarial fixtures including "forget that" + "ignore all previous instructions" | VERIFIED | 5 it() blocks; both literal strings confirmed |
| `packages/jarvis-core/src/tools/ask-clarification.ts` | zAskClarificationFor; strict:true | VERIFIED | Factory confirmed; strict:true via index.ts (all 5 tools) |
| `packages/jarvis-core/src/tools/index.ts` | 5 tools total; cache_control on ask_clarification | VERIFIED | 7 references to ask_clarification; cache_control moved to 5th/last tool |
| `apps/web/components/jarvis/JarvisClarification.tsx` | Question + chips + reply input; onReply via Console prefixes [CLARIFICATION REPLY] | VERIFIED | File exists; 3 CLARIFICATION REPLY references |
| `apps/web/tests/jarvis-implicit-intent.fixtures.ts` | IMPLICIT_INTENT_FIXTURES array, length 20 | VERIFIED | Export confirmed; 20 entries |
| `apps/web/tests/jarvis-implicit-intent.test.ts` | runScriptedTurn + toolsStructurallyEqual not stubs; 20 fixture mocked mode passes | VERIFIED | 7 references; 234/235 pass (1 skipped = live mode) |
| `apps/web/tests/jarvis-clarification.test.tsx` | Inline question UI + reply correlation (9 tests) | VERIFIED | File exists; 6 chip-related assertions confirmed |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| Sidebar.tsx | TanStack Query | useQuery({ initialDataUpdatedAt: Date.now(), staleTime: Infinity }) | WIRED | Both patterns present at query config |
| executor.ts | validate-references.ts | resolveProjectIds helper with preValidatedProjectIds short-circuit | WIRED | resolveProjectIds at line 73; preValidatedProjectIds check at line 80 |
| jarvis-perf-budget.test.ts | lib/db | Drizzle logger spy | WIRED | logger hook in client.ts; test uses vi.mock("@/lib/db") |
| personality.ts TOOL_USE_RULES | Anthropic model | "leading text block" directive + "alone in the turn" prohibition | WIRED | Directives present in exported constant; consumed by buildSystemPrompt |
| route.ts | jarvis-stream-client.ts | new event: queued SSE wire | WIRED | sse("queued") in route.ts; onQueued? callback in stream client |
| JarvisScrollback.tsx | JarvisReceipt.tsx | variant="compact" passed when turn has prose | WIRED | `variant={turn.textDelta ? "compact" : "default"}` at line 98 |
| JarvisClarification.tsx | JarvisConsole.tsx | onReply callback prepends [CLARIFICATION REPLY] to next submit | WIRED | Console adds prefix at handleClarificationReply |
| route.ts | depth cap | if input.startsWith('[CLARIFICATION REPLY]') → system note | WIRED | 3 CLARIFICATION REPLY references in route.ts confirmed |
| prompt-builder.ts | facts block | buildFactsBlock(opts.facts!) inside buildSystemPrompt | WIRED | buildFactsBlock call confirmed in prompt-builder.ts |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| JarvisScrollback.tsx (textDelta) | turn.textDelta | SSE text event → JarvisConsole onText callback → setTurns | Real streaming text from Anthropic SDK | FLOWING |
| JarvisReceipt.tsx (action.result.receipt) | action.result | SSE action event → onAction → upgrades queued placeholder | Drizzle insert returning real DB-confirmed data | FLOWING |
| settings/memory/page.tsx (facts) | initialFacts | Server Component getJarvisFactsForUser Drizzle query | Real DB query (force-dynamic) | FLOWING |
| MemoryTable.tsx (facts queryFn) | facts | queryFn returns initialFacts (no refetch action yet) | Static pass-through — known stub per Plan 03 SUMMARY | WARNING — MemoryTable queryFn returns initialFacts instead of re-fetching; live updates require page reload. Flagged in Plan 03 SUMMARY as MVP-acceptable: hard refresh always gets fresh data |
| jarvis-facts-injection.test.ts (remember_fact executor) | factId | onConflictDoUpdate upsert returning { id } | Real executor path (mocked DB in tests) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| apps/web full test suite passes | `pnpm test -- --run` | 234 passed, 1 skipped (ANTHROPIC_LIVE gate), 36 test files | PASS |
| jarvis-core full test suite passes | `cd packages/jarvis-core && pnpm test -- --run` | 209 passed, 10 test files | PASS |
| apps/web typecheck | `pnpm typecheck` | exit 0 | PASS |
| jarvis-core typecheck | `cd packages/jarvis-core && pnpm typecheck` | exit 0 | PASS |
| "no incidental refetch" assertion in sidebar test | `grep -c "no incidental refetch" apps/web/tests/sidebar-no-refetch.test.tsx` | 2 | PASS |
| ≤2 DB roundtrips assertion in perf test | `grep -c "toBeLessThanOrEqual(2)" apps/web/tests/jarvis-perf-budget.test.ts` | 1 | PASS |
| 20 implicit-intent fixtures | `grep -c '  {$' apps/web/tests/jarvis-implicit-intent.fixtures.ts` | 20 | PASS |
| 5 adversarial memory fixtures | `grep -c 'it(' apps/web/tests/jarvis-facts-injection.test.ts` | 5 | PASS |
| Anthropic SDK ≥0.96 | `grep "anthropic" apps/web/package.json` | "^0.96.0" | PASS |
| create-only tool boundary preserved | No delete/update fields in remember_fact or ask_clarification schemas | 0 hits | PASS |
| Purity test (jarvis-core zero React/Next imports) | 91 purity assertions in jarvis-core test suite | 91/91 pass | PASS |
| getClaims still used in route.ts (JARVIS-12) | `grep -c "getClaims" apps/web/app/api/jarvis/route.ts` | 2 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| JARVIS-18 | 05.1-03 | Persistent memory — jarvis_facts table, remember_fact tool, whole-blob fact injection, /settings/memory | SATISFIED | Migration 0011, remember-fact.ts, buildFactsBlock, settings page all present and wired |
| JARVIS-19 | 05.1-04 | ask_clarification tool — inline question, chip options, [CLARIFICATION REPLY] depth cap | SATISFIED | ask-clarification.ts, JarvisClarification.tsx, depth cap in route.ts all confirmed |
| JARVIS-20 | 05.1-02 | Prose-first — leading text block above compact receipts, personality inverted | SATISFIED | Personality rewrite confirmed; scrollback gate removed; compact variant wired; queued SSE added |
| JARVIS-21 | 05.1-01 | Per-turn pipeline budget — ≤2 DB roundtrips, no sidebar refetches, validateTurnReferences | SATISFIED | perf-budget test passes; sidebar-no-refetch test passes; validateTurnReferences confirmed |
| JARVIS-22 | 05.1-04 | Implicit-intent fidelity — 20 paired fixtures, mocked-mode structural equivalence | SATISFIED | 20 fixtures confirmed; mocked-mode 234/235 pass; live-mode gated on ANTHROPIC_LIVE env flag |

**Phase 5 Baseline (JARVIS-01..17):**

All 17 Phase 5 requirements remain marked Complete in REQUIREMENTS.md traceability table. Phase 5 test count was 177/177 (apps/web) + 152/152 (jarvis-core) at baseline. Phase 5.1 grows these to 234/235 (+57) and 209/209 (+57) respectively. No tests removed or skipped that were previously passing. getClaims usage, capture-first default, and create-only tool boundary all confirmed intact.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| apps/web/components/settings/memory/MemoryTable.tsx | 61-66 | queryFn returns initialFacts instead of re-fetching from DB | WARNING | Facts written by JARVIS during a session do not appear in the Settings > Memory page without a page reload. Documented in Plan 03 SUMMARY as MVP-acceptable — hard refresh always shows current state; full read action deferred to Phase 6 |

No blocker anti-patterns found. One known warning-level stub (MemoryTable.queryFn) is documented, non-blocking for MVP, and does not prevent the goal (the D-M6 settings page with edit/delete is functional; data freshness without reload is the only gap).

### Human Verification Required

#### 1. Live Prose Register Smoke

**Test:** Open the dev server with a real ANTHROPIC_API_KEY. Type "dinner 8pm saturday with sam + remind me to pick up groceries friday" into the JARVIS console.
**Expected:** A leading prose block appears ABOVE two compact receipts. Prose opens with a JARVIS-register acknowledgment ("Handled, sir." or "Very good." or similar), states both actions in natural language, optionally includes a dry observational aside. Receipts render with thinner border (border-l vs border-l-2) and smaller padding than pre-5.1. A brief queued animation is visible before each receipt resolves.
**Why human:** Prose register, personality voice calibration, and queued animation are model-behavior + CSS-visual — not verifiable by static analysis or unit tests.

#### 2. Memory Round-Trip Smoke

**Test:** Type "Sam is my partner" into the JARVIS console. Observe the receipt. Then navigate to Settings > Memory.
**Expected:** JARVIS emits a remember_fact tool call (visible as a receipt with label "MEMORY" or similar). The receipt shows key="Sam", value="my partner", source="user_explicit" with a 5s undo countdown. After the countdown or after confirming, navigate to /settings/memory — the fact appears under the "entity" group. Refresh the page — fact persists.
**Why human:** Requires a live Supabase session with the jarvis_facts table provisioned (migration applied). Persistence across refresh requires actual Postgres write.

#### 3. /ask Mode Memory Read-Back Smoke

**Test:** After creating at least one fact (from test 2 above), type "/ask what do you remember about me?" into the JARVIS console.
**Expected:** No tool_use blocks emitted. JARVIS replies in prose only, referencing the stored fact from the JARVIS MEMORY block injected in the system prompt ("I know that Sam is your partner, sir." or similar). If no facts exist, a graceful "I haven't recorded anything about you yet, sir." response.
**Why human:** Requires live Supabase session with facts, live Anthropic API call, and model correctly reading from the JARVIS MEMORY block. D-R4 /ask mode memory reference is a model-behavior assertion.

#### 4. ask_clarification Live Trigger Smoke

**Test:** With multiple projects in the system, type an intentionally ambiguous project-linked request like "add a task to the project." Alternatively try a genuinely ambiguous date: "schedule something for next time."
**Expected:** JARVIS emits an ask_clarification receipt (violet border, HelpCircle badge, "QUESTION" label). The receipt shows the question with optional chip options. Clicking a chip or typing a reply in the free-text input and pressing Enter submits "[CLARIFICATION REPLY] ..." as the next turn. JARVIS then executes the action without asking again.
**Why human:** ask_clarification model routing depends on live project list + real model confidence. The chip UI, reply input, and answered-state rendering require visual inspection of the live Console.

### Gaps Summary

No automated gaps. All 22 must-haves pass programmatic verification. The 4 items above are exclusively human-verification concerns that cannot be checked without a live Supabase session + Anthropic API key. The one known warning-level stub (MemoryTable.queryFn) is non-blocking and documented.

---

_Verified: 2026-05-18T02:15:00Z_
_Verifier: Claude (gsd-verifier)_
