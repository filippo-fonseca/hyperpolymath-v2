# Phase 05.1: jarvis-agentic-refactor - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-17
**Phase:** 05.1-jarvis-agentic-refactor
**Areas discussed:** Response surface model, Persistent memory layer, Ambiguity & clarifying questions, Pipeline efficiency budget

---

## Response Surface Model

| Option | Description | Selected |
|--------|-------------|----------|
| Prose + small receipts (Recommended) | One conversational sentence summarises ALL actions. Receipt blocks render BELOW with resolved fields + Undo. | (basis) |
| Prose-only | One sentence, no badged receipts. Undo lives on the sentence. Resolved fields shown on expand. | |
| Receipts only, tighter copy | Keep current receipt-first UI but rewrite text. Smallest change. | |
| **User custom answer** | Witty Tony-Stark-JARVIS prose + quick summary. Example: *"Handled, sir. Lunch with Sam is set for Saturday at 8 PM, and I'll remind you Friday to pick up flowers. I'd recommend not showing up empty-handed unless your plan is to rely entirely on charm again."* Quick and efficient. | ✓ |

**User's choice:** Prose-first with witty observational register, supplementary receipts. Logged as D-R1 + D-R2 + D-R3.
**Notes:** Receipt cards stay but visually de-emphasized vs Phase 5; personality calibrates to allow 1-3 sentences per turn and optional dry wit. Phase 5's "tool calls only, no narrative prefix" rule reverses.

---

## Persistent Memory Layer

| Option | Description | Selected |
|--------|-------------|----------|
| jarvis_facts table, model-suggested (Recommended) | New table, one row per fact, typed (preference / rule / entity / workflow). Editable in Settings → Memory. Written via user-explicit OR model-suggested with visible receipt. | ✓ |
| users.jarvis_memory JSONB | All facts in one JSONB blob on users. Fast prompt injection, no edit history. | |
| Both — JSONB cache + facts table | Facts table source of truth, JSONB compiled blob for prompt cache. Most flexible, more upfront work. | |

**User's choice:** jarvis_facts table, model-suggested.
**Notes:** Whole-blob prompt injection (single-user scale, <200 facts lifetime). No pgvector / no retrieval. /ask mode allowed to read facts only. Logged as D-M1..D-M6.

---

## Ambiguity & Clarifying Questions

| Option | Description | Selected |
|--------|-------------|----------|
| ask_clarification tool (Recommended) | New 4th tool emits question + optional preset chips. UI renders inline; user reply continues same turn. Capture-first remains fallback. | ✓ |
| Capture + prose nudge | No new tool. Capture + follow-up prose ("Filed for now — make it an event?"). | |
| Server-side risk gate | Server inspects emission, gates risky actions with one-tap confirm. | |

**User's choice:** ask_clarification tool.
**Notes:** Model self-decides confidence; system-prompt rule scopes WHEN to ask (capture-first preserves info loss; ambiguous reference has multiple resolutions). Adversarial fixtures still demand injection prompts → Capture. Logged as D-A1..D-A4.

---

## Pipeline Efficiency Budget

| Option | Description | Selected |
|--------|-------------|----------|
| Audit + concrete budget (Recommended) | Fix Sidebar bug + audit all per-turn DB queries. Target ≤2 DB roundtrips + 1 LLM call per single-action turn. p50 TTFA <800ms warm, <3s cold. | ✓ |
| Bug fix only | Just stop Sidebar areas refetch. Ship in an hour. | |
| Action-graph route rewrite | Full SSE route restructure around explicit intent→entity→graph→exec stages. | |

**User's choice:** Audit + concrete budget.
**Notes:** Action-graph mental model still adopted (D-P3) but without route rewrite — uses Anthropic's native parallel_tool_use as the action graph; executor parallelizes via Promise.all. Implicit-intent fixture suite (~20 paired prompts) verifies fragmented language matches explicit fidelity. Logged as D-P1..D-P4.

---

## Claude's Discretion

- Exact wording of revised personality system prompt (target: D-R1 canonical example)
- `remember_fact` UI styling (thinner receipt variant)
- Fact taxonomy edge cases (preference for `rule` over `entity` when both apply)
- Perf-test mechanism (Drizzle query-log spy vs explain plan vs raw timing)
- Whether parallel-tool-use needs an upper bound (cap at 5 concurrent)
- ask_clarification reply correlation (server IDs vs conversation order)
- Migration ordering relative to in-flight schema work

## Deferred Ideas

- Action-graph DAG with explicit dependency edges (deferred for perf-budget reachability)
- Speculative execution / pre-emptive UI placeholders
- Cross-fact reasoning ("partner events" composed from two facts)
- Fact merge / conflict resolution UI (last-write-wins for now)
- Memory analytics (most-used / stale-fact warnings)
- Voice-aware prose ceiling (Phase 7 territory)
- General read-back tools (`list_tasks`, etc.) — backlog 999.3
