---
phase: 32-in-document-jarvis-inline-ux
plan: 01
type: execute
wave: 1
depends_on: [31-in-document-jarvis-engine]
files_modified:
  - apps/web/lib/jarvis/receipt-summary.ts
  - apps/web/lib/jarvis/at-trigger.ts
  - apps/web/components/pages/JarvisReceiptInline.tsx
  - apps/web/components/pages/PageBlockEditor.tsx
  - apps/web/components/pages/PageDetailClient.tsx
  - apps/web/lib/pages/markdown-export.ts
  - apps/web/tests/receipt-summary.test.ts
  - apps/web/tests/at-trigger.test.ts
  - apps/web/tests/strip-receipts-pill.test.ts
autonomous: true
requirements: [JDOC-UX-01, JDOC-UX-02, JDOC-UX-03, JDOC-UX-04, JDOC-UX-05, JDOC-UX-06]
must_haves:
  truths:
    - "Typing @ in a page opens a JARVIS suggestion menu; selecting it commits an @Jarvis prompt pill"
    - "The prompt pill renders neumorphic/outlined in a mono / Space-Grotesk font; Cmd+Enter submits it"
    - "On submit the pill shows a loading state then transforms into a receipt summary string"
    - "Hovering the receipt pill shows the original prompt text as a tooltip (title attr)"
    - "/Jarvis appears in the slash menu with the JARVIS logo"
    - "The nav-bar hide-receipts toggle hides receipt pills in-doc; markdown export ALWAYS strips receipts"
    - "formatReceiptSummary is pure + unit-tested; the pill markdown serialization is stripped by stripReceipts"
  artifacts:
    - path: "apps/web/lib/jarvis/receipt-summary.ts"
      provides: "Pure formatter: actions[] -> 'Created 1 task, Created 2 events, Edited 3 captures'"
      contains: "export function formatReceiptSummary"
    - path: "apps/web/lib/jarvis/at-trigger.ts"
      provides: "Pure @-trigger helpers (completion target, normalize prompt)"
      contains: "export"
    - path: "apps/web/components/pages/JarvisReceiptInline.tsx"
      provides: "BlockNote custom inline content spec for the @Jarvis pill (prompt|receipt) + markdown stripping"
      contains: "createReactInlineContentSpec"
  key_links:
    - from: "apps/web/components/pages/PageBlockEditor.tsx"
      to: "apps/web/lib/jarvis/invoke-in-document.ts"
      via: "pill submit calls invokeInDocumentJarvis (the Phase 31 seam)"
      pattern: "invokeInDocumentJarvis"
    - from: "apps/web/components/pages/JarvisReceiptInline.tsx"
      to: "apps/web/lib/pages/markdown-export.ts"
      via: "pill serializes to a <!-- jarvis:receipt --> region stripReceipts removes"
      pattern: "jarvis:receipt"
---

<objective>
Render the in-editor UX for in-document @JARVIS on top of the Phase 31 seam: an @ autocomplete, a neumorphic prompt pill that submits on Cmd+Enter, a loading->receipt transformation, a hover tooltip with the original prompt, a /Jarvis slash entry, a nav-bar receipt-hide toggle, and guaranteed receipt exclusion from markdown exports.

Build ONLY the UI + toggle + export exclusion. Do not POST the route or duplicate scope/serialization; call invokeInDocumentJarvis. Do not touch run-turn/executor/undo/jarvis-core.
</objective>

<tasks>
  <task id="1" requirement="JDOC-UX-03">
    <action>Add pure formatReceiptSummary(actions) in lib/jarvis/receipt-summary.ts mapping tool names (create_task/create_event/edit_capture/...) to verb+noun counts, pluralized, comma-joined.</action>
    <verify>vitest tests/receipt-summary.test.ts</verify>
    <acceptance_criteria>"Created 1 task, Created 2 events, Edited 3 captures" for the documented action mix; empty -> "No changes".</acceptance_criteria>
  </task>
  <task id="2" requirement="JDOC-UX-01">
    <action>Add pure at-trigger helpers (completion text "Jarvis", normalizePrompt) in lib/jarvis/at-trigger.ts.</action>
    <verify>vitest tests/at-trigger.test.ts</verify>
    <acceptance_criteria>"@J" + Enter -> "@Jarvis"; prompt normalization trims/strips the leading @Jarvis token.</acceptance_criteria>
  </task>
  <task id="3" requirement="JDOC-UX-06">
    <action>Define the jarvisReceipt inline content spec; its markdown serialization is a <!-- jarvis:receipt -->...<!-- /jarvis:receipt --> region. Confirm stripReceipts removes it (export exclusion).</action>
    <verify>vitest tests/strip-receipts-pill.test.ts; pnpm --filter web typecheck</verify>
    <acceptance_criteria>stripReceipts(pillMarkdown) === "" for the pill's serialized form.</acceptance_criteria>
  </task>
  <task id="4" requirement="JDOC-UX-01,02,03,04,05">
    <action>Wire the inline spec + @ suggestion menu + /Jarvis slash item + Cmd+Enter submit + loading/receipt transform into PageBlockEditor, calling invokeInDocumentJarvis. Pass pageId through props.</action>
    <verify>pnpm --filter web typecheck; pnpm --filter web build</verify>
    <acceptance_criteria>Typecheck + build pass (no new errors beyond the 6 known TTS ones); browser behavior deferred to human.</acceptance_criteria>
  </task>
  <task id="5" requirement="JDOC-UX-06">
    <action>Drive hideReceipts from PageDetailClient into PageBlockEditor (CSS hide of receipt pills); pass pageId.</action>
    <verify>pnpm --filter web typecheck</verify>
    <acceptance_criteria>Toggle hides pills in-doc only; export path unaffected (always stripped).</acceptance_criteria>
  </task>
</tasks>

<threat_model>
  - Receipt leaks into export: mitigated by serializing the pill to the fenced receipt region stripReceipts already removes + a unit test asserting empty output.
  - Pill prompt text loss on reload: prompt + summary live in the inline content props (persisted in content_json), so they survive reload.
  - Strict-grammar/route regressions: out of scope; we only call the existing seam.
  - hideReceipts confused with export: hideReceipts is in-doc CSS only; export always strips regardless of toggle (JDOC-UX-06 split).
</threat_model>

<success_criteria>
  - pnpm --filter web typecheck and build pass with no NEW errors (6 pre-existing TTS errors ignorable).
  - New unit tests pass: receipt-summary, at-trigger, strip-receipts-pill.
  - All six JDOC-UX requirements wired; browser end-to-end verification explicitly deferred to the human.
</success_criteria>
