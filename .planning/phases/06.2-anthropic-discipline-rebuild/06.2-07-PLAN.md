---
phase: 06.2-anthropic-discipline-rebuild
plan: 07
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - apps/web/app/template.tsx
  - apps/web/app/error.tsx
  - apps/web/app/global-error.tsx
  - .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md
autonomous: false
requirements: [AES-02, AES-03, AES-04, AES-06, AES-07]
must_haves:
  truths:
    - "template.tsx page transition uses --ease-out-quart curve + 150ms duration per §6b"
    - "error.tsx and global-error.tsx use only on-scale type sizes (no off-scale) and no deleted tokens"
    - "Automated VERIFICATION.md sweep passes the Phase 6.2 anti-pattern audit (cyan-scope, motion-vocab, off-scale, carry-forward, anti-pattern absence)"
    - "User-verified manual walkthrough lands `approved` — the gate two previous phases failed"
  artifacts:
    - path: ".planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md"
      provides: "Comprehensive verification report covering every UI-SPEC anti-pattern + cyan-scope audit + motion-vocab audit + manual walkthrough outcome"
      min_lines: 200
  key_links:
    - from: "Final integration verification"
      to: "User approval gate"
      via: "checkpoint:human-verify"
      pattern: "human-verify"
---

<objective>
Wave 6 — Final integration + manual walkthrough. This is the gate two previous phases (06 + 06.1) failed. The plan combines (a) a small file-level audit of template.tsx + error boundaries (where Phase 6.1 carry-forward direction was correct but values may have drifted from the new token set), (b) a comprehensive automated VERIFICATION.md sweep against every UI-SPEC §12 anti-pattern + §13 carry-forward + §14 revert checklist + §15 validation checklist, and (c) a human-verify checkpoint that walks the Anthropic-discipline scenarios live.

Purpose: Catch any drift before user sees the build. Then run the user-verify walkthrough. The user has explicitly rejected the prior two visual surfaces with the SAME words. This walkthrough is the only gate that matters.

Output: VERIFICATION.md report + an approved (or rejected with diagnosed gaps) manual walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-UI-SPEC.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-01-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-02-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-03-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-04-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-05-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-06-SUMMARY.md

<interfaces>
<!-- Phase 6.1 06.1-06 SUMMARY: template.tsx + error.tsx + global-error.tsx ship clean from 6.1. -->
<!-- This plan AUDITS them against the new token set + new motion vocabulary. -->

From apps/web/app/template.tsx (Phase 6.1 06.1-06 Task 1 carry-forward):
- Per-navigation page transition via motion/react opacity fade
- useReducedMotion()  → 0ms
- Duration was 150ms in 6.1 — UI-SPEC §6b confirms this stays correct
- Easing: --ease-out-quart (confirmed survives Wave 2)

From apps/web/app/error.tsx + apps/web/app/global-error.tsx (Phase 6.1 06.1-06 Task 1 carry-forward):
- error.tsx uses AlertTriangle + serif H1 + mono error block + canvas bg + Try again CTA
- global-error.tsx ships own <html><body> + inline styles + system serif fallback
- Direction was correct per UI-SPEC §13d
- Audit needed: no off-scale sizes, no references to deleted Phase 6.1 tokens (e.g., --ease-in-out-circ)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit template.tsx + error.tsx + global-error.tsx against new tokens + motion vocab</name>
  <files>
    apps/web/app/template.tsx,
    apps/web/app/error.tsx,
    apps/web/app/global-error.tsx
  </files>
  <read_first>
    - UI-SPEC §6b motion table (page transition row: 150ms ease-out-quart)
    - UI-SPEC §6e (template.tsx carry-forward)
    - UI-SPEC §11d (error state copy — `Something broke.` heading + `{error.message}` body + `Try again` CTA)
    - UI-SPEC §13b (motion + state carry-forward mechanisms)
    - apps/web/app/template.tsx — read current; verify motion/react usage + duration + easing
    - apps/web/app/error.tsx — read current; verify H1 + body + CTA copy + no off-scale sizes
    - apps/web/app/global-error.tsx — read current; verify inline styles + system serif fallback
  </read_first>
  <action>
    Step 1 — `apps/web/app/template.tsx`. Audit:
    - Verify the motion.div uses `transition={{ duration: 0.15, ease: [0.25, 1, 0.5, 1] }}` OR uses `var(--ease-out-quart)` via a CSS bridge — either is acceptable.
    - If the existing code references deleted easing tokens (`--ease-in-out-circ`, `--ease-out-back`, `--ease-in-fast`), update to `--ease-out-quart` literal `cubic-bezier(0.25, 1, 0.5, 1)`.
    - Verify `useReducedMotion()` guard reduces duration to 0 (or omits the motion wrapper).
    - The opacity fade should be 0 → 1 (no y-offset per Phase 6 polish 06-03 SUMMARY decision).

    Concrete expected template.tsx body (approximate; preserve actual exported function name + signature):
    ```tsx
    'use client';

    import { motion, useReducedMotion } from 'motion/react';

    export default function Template({ children }: { children: React.ReactNode }) {
      const reducedMotion = useReducedMotion();
      const duration = reducedMotion ? 0 : 0.15;
      return (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ duration, ease: [0.25, 1, 0.5, 1] }}
        >
          {children}
        </motion.div>
      );
    }
    ```

    Step 2 — `apps/web/app/error.tsx`. Audit per UI-SPEC §13d:
    - H1 uses `text-[32px] font-semibold tracking-tight font-serif` (Display H1 per §4a; NOT a responsive ladder).
    - H1 copy `Something broke.` per §11d.
    - Body renders `{error.message}` in serif body + ink-muted.
    - CTA button uses `Try again` label.
    - Page wrapper uses `bg-[var(--canvas)]`.
    - AlertTriangle icon (Lucide) with `strokeWidth={1.5}` per §8.
    - Mono error-detail block (digest, etc.) uses `font-mono text-[12px]` per §4a Caption role.
    - NO `var(--hud-cyan)` references (error pages forbidden cyan per §3b).
    - NO references to deleted easing tokens.
    - NO off-scale type sizes.

    If any of those drift, fix concretely:
    - Responsive H1 → fixed 32px.
    - Off-scale body or caption → 16px / 12px.
    - Removed token references → use surviving tokens.

    Step 3 — `apps/web/app/global-error.tsx`. Audit:
    - File ships its own `<html>` + `<body>` per Phase 6 polish 06-02 SUMMARY (root layout failure fallback).
    - Inline styles use system serif fallback (Georgia, "Times New Roman") because globals.css may not have loaded.
    - No reference to deleted tokens (since inline styles are hard-coded, this is unlikely; verify by reading the file).
    - Copy aligns with `Something broke.` / `Try again` per §11d.
    - The clipboard payload code-fence + execCommand fallback (carry-forward from Phase 6 polish 06-02) is preserved.

    Step 4 — Build + tests:
    - `pnpm --filter web build` exits 0.
    - `pnpm --filter web test` passes.

    Interaction-state coverage:
    - template.tsx: motion-only enter; no interactive states.
    - error.tsx Try Again CTA: hover + focus-visible + disabled coverage (inherits from shadcn Button updated in Plan 05 Task 3).
    - global-error.tsx Try Again CTA: inline styles include hover + focus styles (Phase 6 polish carry-forward) — verify no off-scale sizes.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      ! grep -q "ease-in-out-circ\|ease-out-back\|ease-in-fast" apps/web/app/template.tsx &&
      ! grep -q "ease-in-out-circ\|ease-out-back\|ease-in-fast" apps/web/app/error.tsx &&
      ! grep -q "ease-in-out-circ\|ease-out-back\|ease-in-fast" apps/web/app/global-error.tsx &&
      grep -q "Something broke\." apps/web/app/error.tsx &&
      grep -q "Try again" apps/web/app/error.tsx &&
      ! grep -E "text-\[(13|14|15|18|20|24|28|36)px\]" apps/web/app/error.tsx &&
      ! grep -q "hud-cyan" apps/web/app/template.tsx &&
      ! grep -q "hud-cyan" apps/web/app/error.tsx &&
      ! grep -q "hud-cyan" apps/web/app/global-error.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    template.tsx:
    - Contains NO references to deleted easing tokens (`ease-in-out-circ`, `ease-out-back`, `ease-in-fast`)
    - Uses motion/react `useReducedMotion` (carry-forward)
    - Transition duration is 150ms (0.15s) or 0s when reduced-motion
    - Easing curve is `[0.25, 1, 0.5, 1]` or `cubic-bezier(0.25, 1, 0.5, 1)`
    - Contains NO `hud-cyan` references

    error.tsx:
    - H1 copy `Something broke.` (UI-SPEC §11d)
    - CTA copy `Try again` (UI-SPEC §11d)
    - H1 uses `text-[32px]` (fixed Display H1 per §4a)
    - Contains NO off-scale type sizes (grep returns 0 for 13/14/15/18/20/24/28/36)
    - Contains NO `hud-cyan` references
    - Contains NO references to deleted easing tokens

    global-error.tsx:
    - Contains `Something broke.` heading
    - Contains `Try again` CTA
    - Contains NO references to deleted easing tokens
    - Contains NO `hud-cyan` references
    - Inline styles preserve system serif fallback (Georgia / Times New Roman)

    Build + tests green
  </acceptance_criteria>
  <done>template.tsx + error.tsx + global-error.tsx audited and aligned with the new token set. No references to deleted easing tokens. No off-scale type sizes. No cyan. Build + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Automated VERIFICATION.md sweep — token purge audit + cyan-scope audit + motion-vocab audit + carry-forward + anti-pattern absence</name>
  <files>.planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md</files>
  <read_first>
    - UI-SPEC §12 (the FULL anti-pattern catalog)
    - UI-SPEC §13 (carry-forward inventory)
    - UI-SPEC §14 (revert checklist + grep recipes)
    - UI-SPEC §15 (the 20 validation questions answered concretely)
    - All six prior SUMMARYs (06.2-01 through 06.2-06) to know what each plan claimed to deliver
  </read_first>
  <action>
    Step 1 — Create `.planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md`. The file is a comprehensive audit report. Structure:

    ```markdown
    # Phase 6.2 — Verification Report

    Generated: {timestamp}
    Build status: {pass/fail}
    Test status: {pass/fail}

    ## §1 Revert Checklist (UI-SPEC §14g)

    | Grep | Expected | Actual | Pass? |
    |------|----------|--------|-------|
    | `grep -rln "HudCoreBubble" apps/web/` | 0 | {actual} | {y/n} |
    | `grep -rln "HudCornerCrops" apps/web/` | 0 | {actual} | {y/n} |
    | `grep -rln "HudThinkingRing" apps/web/` | 0 | {actual} | {y/n} |
    | `grep -rln "HudStatusPill" apps/web/` | 0 | {actual} | {y/n} |
    | `grep -rln "HudEdgeInstrumentation" apps/web/` | 0 (renamed) | {actual} | {y/n} |
    | `grep -rln "hud-corner-crop\|hud-focus-breathe\|...\|hud-core-" apps/web/` | 0 | {actual} | {y/n} |
    | `grep -rln "agent-mode-scope" "apps/web/app/(app)/insights" "apps/web/app/(app)/health" "apps/web/app/(app)/settings/memory"` | 0 | {actual} | {y/n} |
    | `grep -rln "agent-mode-scope" "apps/web/app/(app)/today"` | ≥1 (Console keeps it) | {actual} | {y/n} |

    ## §2 Cyan-scope audit (UI-SPEC §3b allowlist)

    Cyan is permitted in EXACTLY three locations:
    1. JARVIS Console composer border at focus (apps/web/components/jarvis/JarvisInput.tsx)
    2. JARVIS Console streaming caret (apps/web/components/jarvis/JarvisInput.tsx OR JarvisConsole.tsx)
    3. /insights chart series strokes (apps/web/components/insights/InsightsCharts.tsx — sRGB hex literals)

    Files where cyan IS allowed:
    | File | Expected references | Actual |
    |------|---------------------|--------|
    | apps/web/components/jarvis/JarvisInput.tsx | ≥1 (focus rule + caret) | {actual} |
    | apps/web/components/insights/InsightsCharts.tsx | ≥1 (hex literals: #22d3ee, #0891b2, #0e7490) | {actual} |
    | apps/web/app/globals.css | ≥1 (--hud-cyan family token definitions in @theme + .dark blocks) | {actual} |

    Files where cyan is FORBIDDEN (grep should return 0):
    | File / directory | Grep |
    |------|------|
    | apps/web/components/jarvis/JarvisReceipt.tsx | `hud-cyan` |
    | apps/web/components/jarvis/JarvisScrollback.tsx | `hud-cyan` |
    | apps/web/components/jarvis/JarvisClarification.tsx | `hud-cyan` |
    | apps/web/components/jarvis/SlashCommandPopover.tsx | `hud-cyan` |
    | apps/web/components/jarvis/ConsoleMetaStrip.tsx | `hud-cyan` |
    | apps/web/components/shell/* | `hud-cyan` |
    | apps/web/components/tasks/* | `hud-cyan` |
    | apps/web/components/captures/* | `hud-cyan` |
    | apps/web/components/projects/* | `hud-cyan` |
    | apps/web/components/calendar/* | `hud-cyan` |
    | apps/web/components/ui/* (button/card/input/textarea/dialog/popover/sheet/tooltip) | `hud-cyan` |
    | apps/web/components/shared/* | `hud-cyan` |
    | apps/web/app/(app)/health/page.tsx | `hud-cyan` |
    | apps/web/app/(app)/settings/memory/page.tsx | `hud-cyan` |
    | apps/web/app/(app)/insights/page.tsx (the page wrapper, NOT InsightsCharts.tsx) | `hud-cyan` |
    | apps/web/app/(app)/settings/page.tsx | `hud-cyan` |
    | apps/web/app/template.tsx | `hud-cyan` |
    | apps/web/app/error.tsx | `hud-cyan` |
    | apps/web/app/global-error.tsx | `hud-cyan` |

    ## §3 Motion vocabulary audit (UI-SPEC §6)

    | Item | Expected | Actual | Pass? |
    |---|---|---|---|
    | `--ease-out-quart` in globals.css | exists | {y/n} | {y/n} |
    | `--ease-in-out-circ` in globals.css | absent | {y/n} | {y/n} |
    | `--ease-out-back` in globals.css | absent | {y/n} | {y/n} |
    | `--ease-in-fast` in globals.css | absent | {y/n} | {y/n} |
    | `@keyframes shimmer` in globals.css | exists (skeleton screen) | {y/n} | {y/n} |
    | `@keyframes hud-corner-breathe` in globals.css | absent | {y/n} | {y/n} |
    | (... all 13 deleted keyframes from §14c) | absent each | {summary} | {y/n} |
    | `animation:.*infinite` outside `.skeleton` rule | 0 occurrences | {actual} | {y/n} |

    ## §4 Off-scale type-size audit (UI-SPEC §4a + §12b)

    Run the following grep across `apps/web/`:
    ```
    grep -rnE "text-\\[(13|14|15|18|20|24|28|36)px\\]" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next"
    ```
    Expected: 0 occurrences.
    Actual: {paste output here; 0 = pass}

    ## §5 Carry-forward inventory (UI-SPEC §13)

    | Mechanism | Expected | Actual | Pass? |
    |---|---|---|---|
    | `app/providers.tsx` next-themes wiring | unchanged | {y/n} | {y/n} |
    | `apps/web/app/globals.css` `.font-mono-stats` utility | survives | {y/n} | {y/n} |
    | `.custom-scrollbar` + `.scrollbar-hidden` | survives | {y/n} | {y/n} |
    | `.theme-color-scheme` | survives | {y/n} | {y/n} |
    | `@custom-variant pointer-fine` declaration | survives | {y/n} | {y/n} |
    | `app/template.tsx` motion mechanism | survives | {y/n} | {y/n} |
    | `useReducedMotion()` guards | survive | {y/n} | {y/n} |
    | `lib/jarvis/focus.ts` Cmd+K singleton | survives | {y/n} | {y/n} |
    | `components/shell/GlobalHotkeys.tsx` Cmd+K | survives | {y/n} | {y/n} |
    | `components/shell/CommandMenu.tsx` Cmd+Shift+K binding | survives | {y/n} | {y/n} |
    | `components/shared/use-undo-toast.ts` | survives | {y/n} | {y/n} |
    | JARVIS Console slash-command popover (5 commands) | survives | {y/n} | {y/n} |
    | Thinking-word indicator | survives | {y/n} | {y/n} |
    | `/api/health` route handler | unchanged | {y/n} | {y/n} |
    | `getInsightsData` query | unchanged | {y/n} | {y/n} |
    | Drizzle schema + migrations | unchanged | {y/n} | {y/n} |
    | Supabase RLS policies | unchanged | {y/n} | {y/n} |
    | All Server Actions | unchanged | {y/n} | {y/n} |

    ## §6 Anti-pattern absence (UI-SPEC §12)

    | Anti-pattern | Grep recipe | Expected | Actual |
    |---|---|---|---|
    | Neumorphic shadows | `grep -rn "shadow-nm-" apps/web --include="*.tsx" --include="*.css"` | 0 | {actual} |
    | iOS Liquid Glass / refraction | `grep -rn "backdrop-filter\|backdrop-blur" apps/web --include="*.tsx" --include="*.css"` | only in modal/popover/sheet primitives | {actual} |
    | Hardcoded hex colors in JSX | `grep -rnE "bg-\\[#[0-9a-fA-F]" apps/web --include="*.tsx"` | only #22d3ee / #0891b2 / #0e7490 in InsightsCharts.tsx (chart series exception) | {actual} |
    | Bold EB Garamond at large sizes | `grep -rnE "font-bold.*text-(2xl|3xl|4xl|5xl|6xl)" apps/web --include="*.tsx"` | 0 | {actual} |
    | `font-medium` on EB Garamond | `grep -rn "font-serif.*font-medium\|font-medium.*font-serif" apps/web --include="*.tsx"` | 0 | {actual} |
    | Mono on agent-surface H1 | `grep -rnE "font-mono.*text-\\[3" apps/web --include="*.tsx"` | 0 | {actual} |
    | Always-on infinite animations outside skeleton | `grep -rnE "animation:.*infinite" apps/web --include="*.css"` | only `@keyframes shimmer` (skeleton) | {actual} |
    | Spinner imports for inline content | `grep -rn "Loader2" apps/web --include="*.tsx"` | (allowlist: thinking-word indicator only; document any other usage) | {actual} |
    | "AI-powered" / "magical" / "intuitive" copy | `grep -rniE "AI-powered\|magical\|intuitive\|revolutionary\|stunning\|seamless\|effortless" apps/web --include="*.tsx"` | 0 in chrome (allowlist domain code if it's an interface name) | {actual} |

    ## §7 Validation Checklist (UI-SPEC §15 — 20 questions)

    Verify each of the 20 numbered items from UI-SPEC §15. For each, state PASS / FAIL with one-line evidence.

    1. Exact cyan accent value? — PASS / FAIL: {evidence}
    2. The ONE focal element carrying cyan glow? — PASS / FAIL: {evidence}
    ...
    20. Focus state across surface tiers? — PASS / FAIL: {evidence}

    ## §8 Build + Test status

    - `pnpm --filter web build`: PASS / FAIL
    - `pnpm --filter web test`: PASS / FAIL

    ## §9 Summary

    Total checks: {N}
    Passing: {N}
    Failing: {N}

    {If any failing, list each as a gap with file + line + UI-SPEC reference. These become Plan 06.2-08 gap closure tasks.}
    ```

    Step 2 — Run every grep listed in the report. Fill in the actual values. Mark each row PASS or FAIL.

    Step 3 — If ALL checks pass, the document concludes with: `Status: ready for human-verify checkpoint (Task 3).`

    If ANY check fails, the document concludes with a list of gaps + file + line + UI-SPEC reference. These are NOT fixed in this task — they go to a follow-up `plan-phase --gaps` run.

    Step 4 — Commit the file:
    ```
    node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs commit "docs(06.2): VERIFICATION.md automated sweep" --files .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md
    ```
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      test -f .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      [ $(wc -l < .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md) -gt 100 ] &&
      grep -q "Revert Checklist" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      grep -q "Cyan-scope audit" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      grep -q "Motion vocabulary audit" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      grep -q "Off-scale type-size audit" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      grep -q "Carry-forward inventory" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md &&
      grep -q "Anti-pattern absence" .planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    VERIFICATION.md exists at `.planning/phases/06.2-anthropic-discipline-rebuild/06.2-VERIFICATION.md`
    File has at least 100 lines
    File contains sections for: Revert Checklist, Cyan-scope audit, Motion vocabulary audit, Off-scale type-size audit, Carry-forward inventory, Anti-pattern absence, Validation Checklist (UI-SPEC §15), Build + Test status, Summary
    Each grep recipe in the file has an actual measured result (not a `{actual}` placeholder)
    Either ALL checks pass (status: ready for human-verify) OR all failing checks are itemized with file + line + UI-SPEC reference
    File is committed to git
  </acceptance_criteria>
  <done>VERIFICATION.md is a comprehensive automated audit. Every grep in UI-SPEC §12/§13/§14/§15 has been run and its result recorded. Either everything passes or gaps are enumerated. File is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual walkthrough — the user-verify gate two previous phases failed</name>
  <what-built>
    The complete Phase 6.2 visual surface. The build is now ready for the user to walk through scenarios live. The walkthrough is scoped to Anthropic-discipline scenarios (not the Phase 6.1 22-scenario HUD list — that was scoped for a different visual contract).
  </what-built>
  <how-to-verify>
    Start the dev server: `pnpm --filter web dev`. Visit `http://localhost:3000` and walk through the following scenarios. For each scenario, the user should be able to verify the listed expectations from a quick visual scan.

    ### Scenario 1 — JARVIS Console at idle (`/today`)
    - Page renders with subtle vertical gradient bg (canvas at top, canvas/95 at bottom).
    - "Good {morning|afternoon|evening}, sir." greeting at 22px serif semibold tracking-tight.
    - Composer below the greeting at `rounded-2xl`, half-pixel hairline border.
    - NO arc-reactor, NO radial dial, NO floating logo, NO corner crops, NO hex-grid pattern.
    - Bottom-right: a single mono caption `latency: —ms` at 11px (the declared exception).
    - **Verify**: page feels Anthropic-restrained. The composer is the focal element by virtue of being the only interactive element on the page, NOT by virtue of chrome density.

    ### Scenario 2 — JARVIS Console composer at focus
    - Click into the composer (or press ⌘K).
    - Border transitions ~200ms to cyan (`var(--hud-cyan)`).
    - 12px outward cyan glow fades in (`var(--hud-cyan-glow-soft)`).
    - **THIS IS focal cyan moment #1 — verify it feels intentional, not theatrical.**

    ### Scenario 3 — JARVIS Console streaming response
    - Type a simple query (`hi` / `/capture testing`).
    - Submit. The thinking-word indicator should render during model latency.
    - Once tokens stream, a static cyan caret bar (2px wide, with cyan halo) should track the last character.
    - **NO pulse, NO light-trail, NO animation on the caret itself — the bar is static, only its position updates.**
    - **THIS IS focal cyan moment #2 — verify it feels like a typewriter cursor, NOT like a sparkle.**

    ### Scenario 4 — Receipts after a filed action
    - Use `/capture testing this` or `/task buy milk` to file an action.
    - The receipt card appears with a 200ms opacity fade-in.
    - 6px intent dot before the title (amber for task, sage for capture).
    - Mono metadata row below (timestamp, target table, id joined by `·`).
    - **NO cyan border, NO outline-trace animation, NO shimmer loop, NO corner-flash.**

    ### Scenario 5 — /insights
    - Visit /insights.
    - H1 "Insights" at 32px serif semibold tracking-tight.
    - Chart panels render as plain card surfaces with 1px hairline borders.
    - Chart series render in cyan family colors.
    - **NO corner crops on the panels. NO edge data overlays. NO bottom rail.**
    - The cyan should signal "agent telemetry" — clearly scoped to the chart strokes only.

    ### Scenario 6 — /health
    - Visit /health.
    - H1 "System Health" at 32px serif semibold tracking-tight.
    - Three rows below: service / status / latency in a clean data table.
    - Status text colored sage (ok), coral (down), muted (n/a).
    - **NO JSON braces, NO mono italic H1, NO corner crops, NO cyan.**
    - **THIS WAS THE PUREST CHROME REJECTION FROM 6.1 — verify it now reads as plain Anthropic data table.**

    ### Scenario 7 — /settings/memory
    - Visit /settings/memory.
    - H1 "Memory" at 32px serif semibold tracking-tight.
    - Fact cards as plain raised cards (no left-edge accent, no cyan glow).
    - Hover a fact card: bg tone shifts subtly; the "Forget" button fades in (mono uppercase 12px tracking-[0.06em]).
    - Hover the Forget button: text shifts to coral.

    ### Scenario 8 — Sidebar
    - Sidebar width is 224px (Notion exact). Visually compare against Notion's sidebar if any reference is available.
    - Section labels (Areas, Projects) are mono 12px uppercase tracking-[0.06em] in `--ink-muted`.
    - Hover an Area section header: a `+` icon fades in on the right.
    - Click an Area: nav-item active state shows `bg-[var(--surface-raised)]` tint — NO left-edge accent, NO cyan dot.
    - The JARVIS link (top of sidebar) has NO cyan dot at its active state — same bg-tone treatment as every other nav link.

    ### Scenario 9 — Sonner toast
    - Trigger a toast (e.g., complete a task → see the undo toast).
    - Toast renders with a 6px inline dot at the start of the message (sage / coral / muted per type).
    - Border is a 1px hairline at 20% alpha. No 3px left-edge stripe.
    - "Undo" button in mono uppercase 12px tracking-[0.06em].

    ### Scenario 10 — shadcn Dialog
    - Trigger a modal (e.g., delete a capture confirmation).
    - Dialog renders with `rounded-lg` + 1px hairline + scrim with subtle backdrop-blur.
    - Enter animation ~200ms opacity + zoom-in 0.98→1.0; exit ~150ms opacity-only.
    - Dialog title at 22px serif semibold tracking-tight (Subhead H2).
    - **NO 10px corner L-brackets.**

    ### Scenario 11 — /tasks
    - Visit /tasks.
    - Task rows show a 6px intent dot before the title (amber for P1, etc.).
    - Hover a row: bg tone shifts; an action bar fades in on the right (edit/complete/delete icons at 14px Lucide stroke 1.5px).
    - Completed (lesno) tasks render `line-through` with opacity-60.
    - **NO 4px sage left-edge on completed rows. NO 1px hairline left-edge on hover.**

    ### Scenario 12 — Calendar
    - Visit /calendar.
    - Today's column has a subtle amber tint (5% alpha, or cut entirely if it was deemed too loud).
    - Event tiles render with per-event gcal colors (carry-forward — unchanged).
    - **NO cyan anywhere on the calendar.**

    ### Scenario 13 — Theme toggle
    - Click the theme toggle (sidebar footer or top-right of pages without sidebar).
    - Hover the toggle button: tooltip says "Switch to dark" (or "Switch to light", depending on current theme).
    - Theme transition is instant (no animated theme swap chrome).
    - **Both light + dark modes should pass the same scrutiny — Anthropic discipline at parity.**

    ### Scenario 14 — Light + Dark mode parity
    - Switch to dark mode. Walk through Scenarios 1-13 again. Every element should render at parity.
    - Composer focus glow may read slightly different on dark canvas (the cyan-on-graphite pairing); verify it feels intentional, not muddy.

    ### Scenario 15 — Reduced-motion
    - In OS settings, enable "Reduce motion" (System Preferences → Accessibility → Display → Reduce Motion on macOS).
    - Reload the app. Every motion gate should respect reduced-motion:
      - Page transitions: instant (no opacity fade)
      - Composer focus: border change happens instantly (no 200ms transition)
      - Streaming caret: text appears instantly; caret may or may not render (verify code path)
      - Modal enter/exit: instant
      - Sonner toasts: sonner's own reduced-motion handling
    - **NO ambient motion anywhere even at default; explicitly verify in reduced-motion mode there are NO breathing rings, NO ambient scans, NO infinite animations.**

    ### Scenario 16 — Mobile (< md viewport)
    - Resize browser to ~600px wide.
    - Sidebar collapses to icon strip.
    - JARVIS Console composer takes ~90% of viewport width; padding tightens.
    - Bottom-right ConsoleMetaStrip hides.
    - ⌘K hint chip hides.
    - Type sizes do NOT shrink — H1 stays 32px.

    ### The "would Anthropic ship this?" test
    For each scenario above, answer in your head: "Would Anthropic ship this?" If you say yes to all 16, the surface passes the gate.

    ### What to report
    On approval: type `approved`.
    On rejection: describe each specific issue (which scenario, which element, what feels wrong, ideally with a reference or counter-example). The orchestrator will route to `/gsd:plan-phase 06.2 --gaps` to address specific failures rather than re-doing the entire phase.
  </how-to-verify>
  <resume-signal>Type `approved` to close Phase 6.2 OR describe issues + which scenarios failed for gap-closure routing.</resume-signal>
</task>

</tasks>

<verification>
After all three tasks complete:

1. template.tsx + error.tsx + global-error.tsx audited (no deleted token refs, no off-scale sizes, no cyan)
2. VERIFICATION.md exists with comprehensive audit results
3. Human walkthrough either approves or returns diagnosed gaps for follow-up

If the walkthrough returns `approved`:
- Phase 6.2 closes.
- AES-01, AES-02, AES-03, AES-04, AES-06, AES-07 are all re-verified.
- Move to Phase 7 (JARVIS Voice + Ambient).

If the walkthrough returns specific gaps:
- Run `/gsd:plan-phase 06.2 --gaps` to generate `06.2-08-PLAN.md` (or similar) addressing the gaps.
- DO NOT close Phase 6.2 until the user types `approved`.
</verification>

<success_criteria>
- AES-02 + AES-03 + AES-06 + AES-07 satisfied: final integration confirms full UI-SPEC §7/§3/§6/§10 contract
- AES-04 satisfied: brand voice copy verified on error states + theme toggle tooltip
- VERIFICATION.md produced
- Human walkthrough either approves or diagnoses specific gaps
- THE GATE THAT TWO PREVIOUS PHASES FAILED: pass or diagnose
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-anthropic-discipline-rebuild/06.2-07-SUMMARY.md` documenting:
- template.tsx + error.tsx + global-error.tsx audit results
- VERIFICATION.md summary (pass/fail count, total checks run)
- Manual walkthrough outcome (approved OR list of diagnosed gaps with scenario references)
- If approved: close phase; update STATE.md; reference next phase (07).
- If gaps: do NOT close phase; recommend `/gsd:plan-phase 06.2 --gaps`.
</output>
