---
phase: 06.2-anthropic-discipline-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/shared/HudCoreBubble.tsx
  - apps/web/components/shared/HudCornerCrops.tsx
  - apps/web/components/shared/HudThinkingRing.tsx
  - apps/web/components/shared/HudStatusPill.tsx
  - apps/web/components/shared/HudEdgeInstrumentation.tsx
  - apps/web/components/jarvis/ConsoleMetaStrip.tsx
  - apps/web/components/jarvis/JarvisConsole.tsx
  - apps/web/components/jarvis/JarvisInput.tsx
  - apps/web/app/globals.css
  - apps/web/app/(app)/insights/page.tsx
  - apps/web/app/(app)/health/page.tsx
  - apps/web/app/(app)/settings/memory/page.tsx
autonomous: true
requirements: [AES-02, AES-03]
must_haves:
  truths:
    - "All four deletable HUD primitives no longer exist in the codebase"
    - "agent-mode-scope class is mounted only on the JARVIS Console route, not on insights/health/settings/memory"
    - "Layered ambient HUD background pseudo-elements (hex grid + corner glows) are gone from globals.css"
    - "13 deleted HUD keyframes + their utility classes no longer appear in globals.css"
    - "JarvisConsole renders no deleted HUD primitives; build completes green after the revert"
    - "JARVIS state machine collapses to 3 states (idle / streaming / done) plus optional error"
  artifacts:
    - path: "apps/web/components/jarvis/ConsoleMetaStrip.tsx"
      provides: "Single mono 11px latency caption, JARVIS Console bottom-right only"
      contains: "ConsoleMetaStrip"
    - path: "apps/web/components/shared/HudCoreBubble.tsx"
      provides: "DELETED — must not exist"
    - path: "apps/web/components/shared/HudCornerCrops.tsx"
      provides: "DELETED — must not exist"
    - path: "apps/web/components/shared/HudThinkingRing.tsx"
      provides: "DELETED — must not exist"
    - path: "apps/web/components/shared/HudStatusPill.tsx"
      provides: "DELETED — must not exist"
    - path: "apps/web/components/shared/HudEdgeInstrumentation.tsx"
      provides: "DELETED — must not exist (refactored into ConsoleMetaStrip)"
  key_links:
    - from: "apps/web/components/jarvis/JarvisConsole.tsx"
      to: "apps/web/components/jarvis/ConsoleMetaStrip.tsx"
      via: "single import + mount, JARVIS Console viewport only"
      pattern: "ConsoleMetaStrip"
    - from: "apps/web/app/(app)/insights/page.tsx"
      to: "apps/web/app/globals.css"
      via: "no longer wraps content in .agent-mode-scope"
      pattern: "agent-mode-scope"
---

<objective>
Wave 1 — REVERT Phase 6.1's HUD-heavy chrome to create a clean slate before the rebuild.

Purpose: Phase 6.1 shipped HUD vocabulary across every agent-mode surface (`HudCoreBubble`, `HudCornerCrops`, `HudThinkingRing`, `HudStatusPill`, `HudEdgeInstrumentation`, 14 `.hud-*` keyframes, hex-grid background, corner-glow pseudo-elements, 7-state motion machine). The user rejected the cumulative effect ("clunky and blah"). Per UI-SPEC §14 the revert list is explicit: 4 components deleted, 1 refactored, 13 keyframes deleted, 2 pseudo-elements deleted, `.agent-mode-scope` narrowed to JARVIS Console only, motion machine collapsed to 3 states.

Output: A codebase where every grep in UI-SPEC §14g returns 0, the build is green, and Wave 2 can install Anthropic-discipline tokens onto an empty surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-UI-SPEC.md
@.planning/phases/06.1-visual-redesign-jarvis-notion/06.1-VERIFICATION.md
@.planning/phases/06.1-visual-redesign-jarvis-notion/06.1-06-SUMMARY.md

<interfaces>
<!-- Phase 6.1 introduced the deleted primitives. Their exports are no longer needed. -->
<!-- Confirm the only consumer of HudEdgeInstrumentation today is JarvisConsole; verify before file rename. -->

From apps/web/components/shared/HudEdgeInstrumentation.tsx (will be refactored away):
- Existing props supported `latency`, `cacheHit`, `lastAction` style multi-data; replacement keeps ONLY `latency` and renders bottom-right.

From apps/web/components/jarvis/JarvisConsole.tsx (will be edited):
- Currently mounts HudCornerCrops + HudStatusPill + HudThinkingRing (verify by grep before edit).
- Owns the assistant-turn streaming lifecycle; do NOT touch streaming logic in this plan (interface stays).

From apps/web/components/jarvis/JarvisInput.tsx (will be edited):
- Currently encodes a 7-state machine (`idle / focused-idle / typing / submitting-ignite / thinking-radial / streaming-with-light-trail / done-scan / error-glitch`). Wave 1 collapses to 3 + optional error — Wave 3 rebuilds the composer chrome.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the four extinct HUD primitives + scrub all imports/mounts</name>
  <files>
    apps/web/components/shared/HudCoreBubble.tsx,
    apps/web/components/shared/HudCornerCrops.tsx,
    apps/web/components/shared/HudThinkingRing.tsx,
    apps/web/components/shared/HudStatusPill.tsx,
    apps/web/components/jarvis/JarvisConsole.tsx,
    apps/web/components/jarvis/JarvisInput.tsx,
    apps/web/app/(app)/insights/page.tsx,
    apps/web/app/(app)/health/page.tsx,
    apps/web/app/(app)/settings/memory/page.tsx
  </files>
  <read_first>
    - UI-SPEC §14a (components to DELETE entirely) and §14g (grep checklist)
    - apps/web/components/shared/HudCoreBubble.tsx — read once to confirm no surprise side-effects (no module-level subscriptions, no provider registrations)
    - apps/web/components/shared/HudCornerCrops.tsx — same
    - apps/web/components/shared/HudThinkingRing.tsx — same
    - apps/web/components/shared/HudStatusPill.tsx — same
    - apps/web/components/jarvis/JarvisConsole.tsx — identify all imports + JSX mounts of the four primitives BEFORE editing
    - apps/web/components/jarvis/JarvisInput.tsx — identify any class/state references to the deleted primitives or their hud-* utility classes
    - apps/web/app/(app)/{insights,health,settings/memory}/page.tsx — identify all imports of the deleted primitives + `.agent-mode-scope` wraps to remove
  </read_first>
  <action>
    Step 1 — file deletion (concrete commands):
    - `rm apps/web/components/shared/HudCoreBubble.tsx`
    - `rm apps/web/components/shared/HudCornerCrops.tsx`
    - `rm apps/web/components/shared/HudThinkingRing.tsx`
    - `rm apps/web/components/shared/HudStatusPill.tsx`

    Step 2 — scrub all consumer imports + JSX mounts. For each of the following files, remove the import line AND any JSX usage of the deleted component:
    - apps/web/components/jarvis/JarvisConsole.tsx — remove `<HudCornerCrops />`, `<HudStatusPill />`, `<HudThinkingRing />`, and any conditional render branches that fed them props. The thinking-word indicator (`ThinkingWord.tsx`) is the SURVIVING agent-working UX — keep it.
    - apps/web/components/jarvis/JarvisInput.tsx — remove any JSX referencing the deleted primitives.
    - apps/web/app/(app)/insights/page.tsx — remove all `<HudCornerCrops />` mounts (chart panel level AND viewport level). Delete the `.agent-mode-scope` className from the outer wrapper (UI-SPEC §7b: insights drops the wrap). The outer wrapper becomes: `<div className="relative min-h-screen bg-[var(--canvas)] px-6 py-12">`.
    - apps/web/app/(app)/health/page.tsx — remove all HUD primitive mounts. Delete the `.agent-mode-scope` className. The outer wrapper becomes: `<div className="relative min-h-screen bg-[var(--canvas)] px-6 py-12">`. (Wave 4 rebuilds the body as a clean data table — this plan only removes chrome.)
    - apps/web/app/(app)/settings/memory/page.tsx — remove all HUD primitive mounts. Delete the `.agent-mode-scope` className. Same canvas wrapper as above.

    Step 3 — `agent-mode-scope` MUST survive on the JARVIS Console route only. Verify `apps/web/app/(app)/today/page.tsx` (the JARVIS Console route) STILL has `.agent-mode-scope` on its outermost wrapper (KEEP per UI-SPEC §7a — the class scopes the cyan focus ring to the composer). Do NOT remove it from there.

    Step 4 — build the project to confirm no dangling references:
    - `pnpm --filter web build` (or `npm run --workspace=apps/web build`) must complete without TypeScript or import errors.

    NOTE: This task does NOT yet refactor HudEdgeInstrumentation (that is Task 2) and does NOT yet collapse the motion state machine (Task 4). Stale `.hud-*` className references in JarvisInput.tsx may still be present after this task — Task 3 deletes the CSS, Task 4 cleans the classNames. The build MAY still produce visual breakage (intended) but MUST NOT produce compile errors.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      test ! -f apps/web/components/shared/HudCoreBubble.tsx &&
      test ! -f apps/web/components/shared/HudCornerCrops.tsx &&
      test ! -f apps/web/components/shared/HudThinkingRing.tsx &&
      test ! -f apps/web/components/shared/HudStatusPill.tsx &&
      [ $(grep -rln "HudCoreBubble\|HudCornerCrops\|HudThinkingRing\|HudStatusPill" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next" | wc -l) -eq 0 ] &&
      [ $(grep -rln "agent-mode-scope" "apps/web/app/(app)/insights" "apps/web/app/(app)/health" "apps/web/app/(app)/settings/memory" --include="*.tsx" | grep -v ".next" | wc -l) -eq 0 ] &&
      grep -q "agent-mode-scope" "apps/web/app/(app)/today/page.tsx"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f apps/web/components/shared/HudCoreBubble.tsx` → true
    - `test ! -f apps/web/components/shared/HudCornerCrops.tsx` → true
    - `test ! -f apps/web/components/shared/HudThinkingRing.tsx` → true
    - `test ! -f apps/web/components/shared/HudStatusPill.tsx` → true
    - `grep -rln "HudCoreBubble\|HudCornerCrops\|HudThinkingRing\|HudStatusPill" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next" | wc -l` → 0
    - `grep -c "agent-mode-scope" apps/web/app/\(app\)/insights/page.tsx` → 0
    - `grep -c "agent-mode-scope" apps/web/app/\(app\)/health/page.tsx` → 0
    - `grep -c "agent-mode-scope" apps/web/app/\(app\)/settings/memory/page.tsx` → 0
    - `grep -c "agent-mode-scope" apps/web/app/\(app\)/today/page.tsx` ≥ 1 (Console KEEPS the scope)
    - Build succeeds: `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Four HUD components no longer exist on disk. All non-Console pages no longer wrap content in `.agent-mode-scope`. Build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor HudEdgeInstrumentation → ConsoleMetaStrip + retire the source file</name>
  <files>
    apps/web/components/jarvis/ConsoleMetaStrip.tsx,
    apps/web/components/shared/HudEdgeInstrumentation.tsx,
    apps/web/components/jarvis/JarvisConsole.tsx
  </files>
  <read_first>
    - UI-SPEC §7a (the bottom-right meta strip block with the exact 11px tracking-[0.06em] JSX)
    - UI-SPEC §14b (the refactor entry) and §16a (new component declaration)
    - apps/web/components/shared/HudEdgeInstrumentation.tsx — read once to inventory existing prop surface (latency, cacheHit, lastAction, etc.) BEFORE rewriting; the new component carries ONLY latency
    - apps/web/components/jarvis/JarvisConsole.tsx — find the existing HudEdgeInstrumentation import + mount; that mount becomes the single ConsoleMetaStrip mount
  </read_first>
  <action>
    Step 1 — create the new file `apps/web/components/jarvis/ConsoleMetaStrip.tsx` with this exact JSX scaffold (values come straight from UI-SPEC §7a; the `font-mono-stats` utility class survives from Phase 6.1 per UI-SPEC §13e and provides `font-feature-settings: "tnum" on, "lnum" on`):

    ```tsx
    'use client';

    interface ConsoleMetaStripProps {
      latencyMs: number | null;
    }

    export function ConsoleMetaStrip({ latencyMs }: ConsoleMetaStripProps) {
      return (
        <div
          className="fixed bottom-4 right-6 font-mono-stats text-[11px] tracking-[0.06em] text-[var(--ink-muted)] pointer-events-none hidden md:block"
          aria-hidden="true"
        >
          latency: {latencyMs ?? '—'}ms
        </div>
      );
    }
    ```

    Why each value (do NOT change):
    - `bottom-4 right-6` — UI-SPEC §7a literal placement.
    - `text-[11px]` — the declared exception in §4a (the single 11px usage app-wide).
    - `tracking-[0.06em]` — engineering-legend register per §4a.
    - `font-mono-stats` — carry-forward utility, provides tnum + lnum.
    - `text-[var(--ink-muted)]` — Anthropic-restrained, never `--hud-cyan` (§3g forbids cyan here).
    - `pointer-events-none` — telemetry is passive readout, not interactive.
    - `hidden md:block` — UI-SPEC §9 mobile gracefully hides the strip.
    - `aria-hidden="true"` — purely decorative engineering-legend; the `latencyMs` prop value also surfaces in dev tools, no screen reader value.

    Step 2 — replace the existing import + mount in apps/web/components/jarvis/JarvisConsole.tsx:
    - Remove `import { HudEdgeInstrumentation } from '@/components/shared/HudEdgeInstrumentation'` (or whatever the existing path is).
    - Remove `<HudEdgeInstrumentation {...props} />` from JSX.
    - Add `import { ConsoleMetaStrip } from '@/components/jarvis/ConsoleMetaStrip'`.
    - Add `<ConsoleMetaStrip latencyMs={lastLatencyMs} />` (where `lastLatencyMs` is the existing latency state on JarvisConsole — preserve whatever variable already feeds the latency value; if no such state exists, pass `null`).

    Step 3 — delete the old file:
    - `rm apps/web/components/shared/HudEdgeInstrumentation.tsx`

    Step 4 — verify no residual import:
    - `grep -rln "HudEdgeInstrumentation" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next"` returns 0
    - `grep -rln "ConsoleMetaStrip" apps/web/components/jarvis` returns at least 1 (the import line in JarvisConsole.tsx).

    Step 5 — build to confirm:
    - `pnpm --filter web build` exits 0.

    Interaction-state coverage: the meta strip is decorative chrome with NO interaction states (no hover, no focus, no active). Verify by grep that no `:hover` / `:focus` / `:active` selectors target the strip. Acceptance criterion below grep-verifies absence.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      test -f apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      test ! -f apps/web/components/shared/HudEdgeInstrumentation.tsx &&
      grep -q "ConsoleMetaStrip" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      grep -q "text-\[11px\]" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      grep -q "tracking-\[0.06em\]" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      grep -q "font-mono-stats" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      grep -q "hidden md:block" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      ! grep -q "hud-cyan" apps/web/components/jarvis/ConsoleMetaStrip.tsx &&
      grep -q "ConsoleMetaStrip" apps/web/components/jarvis/JarvisConsole.tsx &&
      [ $(grep -rln "HudEdgeInstrumentation" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next" | wc -l) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists: `apps/web/components/jarvis/ConsoleMetaStrip.tsx`
    - File contains `text-[11px]`, `tracking-[0.06em]`, `font-mono-stats`, `hidden md:block` (each grep ≥ 1)
    - File contains NO `hud-cyan`, NO `var(--hud-cyan)`, NO `--hud-cyan` (cyan-bleed prevention; grep returns 0)
    - File contains NO `:hover`, `:focus`, `:active`, `hover:`, `focus:`, `active:` (decorative chrome has no interaction states; grep returns 0)
    - File `apps/web/components/shared/HudEdgeInstrumentation.tsx` does NOT exist
    - `grep -rln "HudEdgeInstrumentation" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next" | wc -l` → 0
    - `apps/web/components/jarvis/JarvisConsole.tsx` imports `ConsoleMetaStrip`
    - Build succeeds: `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>HudEdgeInstrumentation file deleted, ConsoleMetaStrip replaces it with the 11px mono caption, JarvisConsole mounts the new component, build green.</done>
</task>

<task type="auto">
  <name>Task 3: Delete 13 HUD keyframes + utility classes + ambient pseudo-elements from globals.css; collapse 3-state machine on JarvisInput</name>
  <files>
    apps/web/app/globals.css,
    apps/web/components/jarvis/JarvisInput.tsx,
    apps/web/components/jarvis/JarvisConsole.tsx
  </files>
  <read_first>
    - UI-SPEC §14c (the keyframe + utility class deletion table — exact line ranges)
    - UI-SPEC §14d (CSS scoping changes — the two pseudo-elements + reduced-motion + sonner)
    - UI-SPEC §6c (the collapsed 3-state machine: idle / streaming / done + optional error)
    - apps/web/app/globals.css — read the @theme block (lines 30-200) and the @keyframes blocks (lines 92-275) BEFORE editing; verify the line numbers still match (the file may have shifted slightly since UI-SPEC was authored)
    - apps/web/components/jarvis/JarvisInput.tsx — read the current state machine; identify every reference to deleted `.hud-*` utility classes
  </read_first>
  <action>
    Step 1 — `apps/web/app/globals.css`: delete every entry in the table below. Use Read first to confirm current line numbers (file may have shifted), then Edit.

    | DELETE | What |
    |---|---|
    | `@keyframes hud-corner-breathe` block | the keyframe + the `.hud-corner-crop` utility class |
    | `@keyframes hud-focus-breathe` block | the keyframe + the `.hud-focus-breathe` utility class |
    | `@keyframes hud-thinking-sweep` block | the keyframe + the `.hud-thinking-sweep` utility class |
    | `@keyframes hud-streaming-caret-pulse` block | the keyframe + the `.hud-streaming-caret` utility class |
    | `@keyframes hud-submit-ignite-border` block | (no utility class) |
    | `@keyframes hud-submit-scan-drop` block | (no utility class) |
    | `@keyframes hud-scan-reveal` block | the keyframe + the `.hud-scan-line` utility class |
    | `@keyframes hud-receipt-outline-trace` block | the keyframe + the `.hud-receipt-outline-trace` utility class |
    | `@keyframes hud-receipt-shimmer` block | the keyframe + the `.hud-receipt-shimmer` utility class |
    | `@keyframes hud-edge-tick` block | the keyframe + the `.hud-edge-tick` utility class |
    | `@keyframes hud-undo-wipe` block | the keyframe + the `.hud-undo-wipe` utility class |
    | `@keyframes hud-error-glitch` block | the keyframe + the `.hud-error-glitch` utility class |
    | `@keyframes hud-core-rotate-slow` block | the keyframe + the `.hud-core-rotate-slow` utility class |
    | `@keyframes hud-core-rotate-fast` block | the keyframe + the `.hud-core-rotate-fast` utility class |
    | `@keyframes hud-core-breathe` block | the keyframe + the `.hud-core-breathe` utility class |
    | `.agent-mode-scope::before` (hex-grid background) | entire pseudo-element block (UI-SPEC §14d line 1279) |
    | `.agent-mode-scope::after` (4 corner glows + center vignette) | entire pseudo-element block (UI-SPEC §14d line 1280) |

    KEEP:
    - `@keyframes shimmer` (lines around 92-95) — used by skeleton screens, the surviving motion pattern per UI-SPEC §14c footnote.
    - `--hud-cyan` family tokens (Wave 2 audits values; do NOT delete the OKLCH definitions here).
    - `--ring-hud` token (Wave 2 simplifies its value).
    - Token retention rules — do not touch tokens in this task; Task 2 of Plan 02 owns token edits.

    Step 2 — simplify the `prefers-reduced-motion` media block per UI-SPEC §6b. Replace the per-class `.hud-*` overrides with the universal block:

    ```css
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0s !important;
        transition-duration: 0s !important;
      }
      ::view-transition-old(*),
      ::view-transition-new(*),
      ::view-transition-group(*) {
        animation-duration: 0s !important;
        animation-delay: 0s !important;
      }
    }
    ```

    Step 3 — `apps/web/components/jarvis/JarvisInput.tsx`: collapse the existing 7-state machine to 3 states (idle / streaming / done) + optional `error`. Concrete action:
    - Remove all references to states named `focused-idle`, `typing`, `submitting-ignite`, `thinking-radial`, `streaming-with-light-trail`, `done-scan`, `error-glitch` (and any matching enum / union literal).
    - The canonical state union becomes: `type ConsoleState = 'idle' | 'streaming' | 'done' | 'error'`.
    - Remove ALL `.hud-*` className references from JSX (the matching CSS no longer exists). Search-and-delete: `hud-corner-crop`, `hud-focus-breathe`, `hud-thinking-sweep`, `hud-streaming-caret`, `hud-scan-line`, `hud-receipt-outline-trace`, `hud-receipt-shimmer`, `hud-edge-tick`, `hud-undo-wipe`, `hud-error-glitch`, `hud-core-rotate-slow`, `hud-core-rotate-fast`, `hud-core-breathe`.
    - At this point JarvisInput will look broken (no border styling, no caret animation, no focus glow). That is INTENDED — Wave 3 rebuilds the composer with Anthropic-discipline values. This plan only does the cleanup pass.

    Step 4 — `apps/web/components/jarvis/JarvisConsole.tsx`: do a final sweep for any `.hud-*` className strings (e.g., on receipt-render branches) and remove them. Same logic: Wave 3 rebuilds, Wave 1 only cleans.

    Step 5 — build:
    - `pnpm --filter web build` exits 0.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      ! grep -q "@keyframes hud-corner-breathe" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-focus-breathe" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-thinking-sweep" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-streaming-caret-pulse" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-submit-ignite-border" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-submit-scan-drop" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-scan-reveal" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-receipt-outline-trace" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-receipt-shimmer" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-edge-tick" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-undo-wipe" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-error-glitch" apps/web/app/globals.css &&
      ! grep -q "@keyframes hud-core" apps/web/app/globals.css &&
      grep -q "@keyframes shimmer" apps/web/app/globals.css &&
      ! grep -q "agent-mode-scope::before" apps/web/app/globals.css &&
      ! grep -q "agent-mode-scope::after" apps/web/app/globals.css &&
      [ $(grep -c "hud-corner-crop\|hud-focus-breathe\|hud-thinking-sweep\|hud-streaming-caret\|hud-scan-line\|hud-receipt-outline-trace\|hud-receipt-shimmer\|hud-edge-tick\|hud-undo-wipe\|hud-error-glitch\|hud-core-" apps/web/app/globals.css) -eq 0 ] &&
      [ $(grep -rln "hud-corner-crop\|hud-focus-breathe\|hud-thinking-sweep\|hud-streaming-caret\|hud-scan-line\|hud-receipt-outline-trace\|hud-receipt-shimmer\|hud-edge-tick\|hud-undo-wipe\|hud-error-glitch\|hud-core-" apps/web --include="*.tsx" --include="*.ts" | grep -v ".next" | wc -l) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - In `apps/web/app/globals.css`, each of the 13 `@keyframes hud-*` blocks listed in UI-SPEC §14c grep-returns 0 occurrences
    - `@keyframes shimmer` STILL exists (skeleton screen carry-forward)
    - `agent-mode-scope::before` no longer exists in globals.css
    - `agent-mode-scope::after` no longer exists in globals.css
    - Reduced-motion media block uses the universal `*` selector pattern (`grep -A4 "prefers-reduced-motion" apps/web/app/globals.css | grep -q "\\*, \\*::before"` → match)
    - No `.hud-*` utility class (any of the 13 deleted) referenced anywhere in `apps/web/**/*.{ts,tsx}`
    - JarvisInput.tsx state union contains exactly: `'idle' | 'streaming' | 'done'` (plus optional `'error'`)
    - Build succeeds: `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>13 HUD keyframes + their utility classes deleted from globals.css. Layered ambient pseudo-elements deleted. Reduced-motion block simplified. JarvisInput state machine collapsed to 3 states. Build green.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, the following must hold:

1. UI-SPEC §14g grep checklist passes:
   - `grep -rln "HudCoreBubble" apps/web/` → 0
   - `grep -rln "HudCornerCrops" apps/web/` → 0
   - `grep -rln "HudThinkingRing" apps/web/` → 0
   - `grep -rln "HudStatusPill" apps/web/` → 0
   - `grep -rln "HudEdgeInstrumentation" apps/web/` → 0 (renamed to ConsoleMetaStrip)
   - `grep -rln "hud-corner-crop\|hud-focus-breathe\|hud-thinking-sweep\|hud-streaming-caret\|hud-scan-line\|hud-receipt-outline-trace\|hud-receipt-shimmer\|hud-edge-tick\|hud-undo-wipe\|hud-error-glitch\|hud-core-" apps/web/` → 0
   - `grep -rln "agent-mode-scope" "apps/web/app/(app)/insights" "apps/web/app/(app)/health" "apps/web/app/(app)/settings/memory"` → 0

2. `agent-mode-scope` survives ONLY on the JARVIS Console route (`apps/web/app/(app)/today/page.tsx`).

3. Build is green: `pnpm --filter web build` exits 0.

4. The codebase looks visually broken (composer has no border, no glow, no chrome) — this is INTENDED. Wave 2 installs tokens, Wave 3 rebuilds the composer.
</verification>

<success_criteria>
- All four deletable HUD primitive files no longer exist on disk
- HudEdgeInstrumentation refactored into ConsoleMetaStrip (new file at `apps/web/components/jarvis/ConsoleMetaStrip.tsx`, old file deleted)
- 13 HUD keyframes + their utility classes deleted from globals.css; `@keyframes shimmer` preserved
- `.agent-mode-scope::before` and `::after` pseudo-elements deleted from globals.css
- `.agent-mode-scope` scope narrowed to JARVIS Console route only (`/today`)
- Reduced-motion media block uses universal `*` selector
- JARVIS state machine collapsed from 7 to 3 states (plus optional error)
- `pnpm --filter web build` exits 0 (no TypeScript/import errors, even if the UI looks visually broken)
- AES-02 (visual style — revert phase): the 6.1 chrome is no longer in the codebase
- AES-03 (motion vocabulary): always-on motion is gone
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-anthropic-discipline-rebuild/06.2-01-SUMMARY.md` documenting:
- The 4 deleted files (with paths)
- The 13 deleted keyframes (by name)
- The pseudo-elements deleted
- The Console state-machine collapse
- Any deviation from the plan (with rationale)
</output>
