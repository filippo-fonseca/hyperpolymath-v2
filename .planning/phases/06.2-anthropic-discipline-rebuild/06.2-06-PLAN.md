---
phase: 06.2-anthropic-discipline-rebuild
plan: 06
type: execute
wave: 5
depends_on: [01, 02]
files_modified:
  - apps/web/components/shell/Sidebar.tsx
  - apps/web/components/shell/SidebarTree.tsx
  - apps/web/components/shell/AppShell.tsx
  - apps/web/components/shell/PersistentNav.tsx
  - apps/web/components/shell/CommandMenu.tsx
  - apps/web/components/shell/CommandMenuContent.tsx
  - apps/web/components/calendar/CalendarClient.tsx
  - apps/web/components/calendar/CalendarGrid.tsx
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/popover.tsx
  - apps/web/components/ui/sheet.tsx
  - apps/web/components/ui/tooltip.tsx
  - apps/web/app/globals.css
autonomous: true
requirements: [AES-02, AES-03, AES-04, AES-06]
must_haves:
  truths:
    - "Sidebar width is 224px (down from 240px — Notion exact)"
    - "Sidebar Areas header has hover-reveal + button per Notion pattern"
    - "Sidebar JARVIS nav-link has NO cyan dot (the active state uses bg-tone shift only)"
    - "Sidebar active nav state is bg-[var(--surface-raised)] tint (no left-edge accent)"
    - "Sidebar nav labels use EB Garamond 400 16px Body (no off-scale sizes; the 14/16 distinction is collapsed)"
    - "Sidebar section labels (Areas/Projects) use JetBrains Mono 12px uppercase tracking-[0.06em]"
    - "shadcn Dialog corner L-brackets dropped; plain 1px hairline + scrim per §7k"
    - "Sonner toast left-edge 3px stripe REPLACED with 6px inline ::before dot per §7l"
    - "CalendarClient today-column amber wash evaluated against new restraint (dropped to 5% or cut)"
    - "CommandMenu chrome matches Anthropic-restraint (no HUD vocabulary)"
  artifacts:
    - path: "apps/web/components/shell/Sidebar.tsx"
      provides: "224px Notion-exact sidebar with hover-reveal + and no cyan"
      contains: "w-\\[224px\\]"
    - path: "apps/web/components/ui/dialog.tsx"
      provides: "Anthropic-restrained modal — no corner brackets"
      contains: "rounded-lg"
    - path: "apps/web/app/globals.css"
      provides: "Sonner ::before dot pattern (replaces 3px left-edge stripe)"
      contains: "data-sonner-toast"
  key_links:
    - from: "apps/web/components/shell/Sidebar.tsx"
      to: "Notion 8px-grid hover-reveal + pattern"
      via: "group/section + opacity-0 group-hover/section:opacity-100"
      pattern: "group/section"
    - from: "apps/web/app/globals.css [data-sonner-toast]"
      to: "all sonner toasts in the app"
      via: "::before inline 6px dot"
      pattern: "data-sonner-toast.*::before"
---

<objective>
Wave 5B — Diplomatic surfaces. Sidebar / AppShell / Calendar / CommandMenu / shadcn Dialog/Popover/Sheet/Tooltip / sonner toasts. Apply Anthropic + Notion restraint per UI-SPEC §7e/§7f/§7k/§7l + §13 carry-forward.

Purpose: The diplomatic surfaces frame every page. Phase 6.1's chrome here was largely direction-correct, but three specific drifts need fixing: (1) sidebar JARVIS nav link still carries a 4px cyan dot (UI-SPEC §14f removes), (2) sidebar width is 240px not Notion's 224px (UI-SPEC §7e), (3) shadcn DialogContent has 10px corner L-brackets (UI-SPEC §7k removes), (4) sonner toasts use a 3px intent left-edge stripe (UI-SPEC §7l replaces with 6px ::before dot).

This plan runs file-disjoint from Plan 05 (Wave 5A): Plan 05 touches components/{tasks,captures,projects,ui/button,ui/card,ui/input,ui/textarea,shared/EmptyState} + (app)/settings/page.tsx; this plan touches components/{shell,calendar,ui/dialog,ui/popover,ui/sheet,ui/tooltip} + globals.css for sonner. They can run in parallel safely.

Output: Diplomatic chrome that frames the app without competing for attention. Sidebar at 224px Notion-exact. shadcn modals as plain hairline cards. Sonner toasts with inline intent dots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-UI-SPEC.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-01-SUMMARY.md
@.planning/phases/06.2-anthropic-discipline-rebuild/06.2-02-SUMMARY.md

<interfaces>
<!-- These chrome surfaces have existing data contracts unchanged in 6.2. -->

From Phase 6.1 06.1-05 SUMMARY:
- Sidebar.tsx: workspace title + Areas section + Projects section + footer (theme toggle + settings link)
- SidebarTree.tsx: tree node renderer (areas + project rows)
- Phase 6.1 used 240px width; UI-SPEC §7e collapses to Notion-exact 224px
- Phase 6.1 had a 4px --hud-cyan dot on JARVIS nav-link active state; UI-SPEC §14f removes
- PersistentNav.tsx: JARVIS link with active indicator; UI-SPEC §14 carry-forward — href stays `/today`

From Phase 6.1 06.1-05 SUMMARY (shadcn primitives):
- Dialog.tsx: DialogContent had 10px --edge-hud corner L-brackets (UI-SPEC §7k removes); backdrop-blur-md(8px) scrim
- Popover.tsx, Sheet.tsx, Tooltip.tsx: direction was correct per §13f; verify no off-scale sizes

From Phase 6.1 sonner CSS in globals.css lines 668-688:
- 3px left-edge intent stripe for [data-sonner-toast][data-type=success|error|info]
- UI-SPEC §14d REPLACE with the ::before 6px dot pattern (§7l)

From CommandMenu (06-03 SUMMARY):
- Cmd+Shift+K binding (rebind committed e95f6e9; carry-forward per §13c)
- CommandMenuContent renders shadcn Command primitive
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar — 224px width, hover-reveal +, drop JARVIS cyan dot, drop left-edge active accents</name>
  <files>
    apps/web/components/shell/Sidebar.tsx,
    apps/web/components/shell/SidebarTree.tsx,
    apps/web/components/shell/PersistentNav.tsx
  </files>
  <read_first>
    - UI-SPEC §7e (the FULL sidebar treatment with literal JSX for section labels + hover-reveal + active nav indicator)
    - UI-SPEC §14f (sidebar revert items — width 240 → 224, drop cyan dot)
    - apps/web/components/shell/Sidebar.tsx — read current; identify width className + footer
    - apps/web/components/shell/SidebarTree.tsx — read current; identify Areas section header + nav-item active state
    - apps/web/components/shell/PersistentNav.tsx — read current; identify the JARVIS nav-link active dot
  </read_first>
  <action>
    Step 1 — `apps/web/components/shell/Sidebar.tsx`. Change width:

    - Find the existing width className. Likely `w-60` (240px) or `w-[240px]`.
    - Replace with `w-[224px]` (Notion exact per UI-SPEC §7e).
    - The outer container becomes: `<aside className="w-[224px] shrink-0 bg-[var(--surface)] border-r border-[rgb(20_37_32_/_0.15)] dark:border-[rgb(255_255_255_/_0.15)] min-h-screen flex flex-col">`

    Why:
    - `w-[224px]` — Notion exact (§7e).
    - `bg-[var(--surface)]` — sidebar surface tone per §7e.
    - `border-r border-[rgb(20_37_32_/_0.15)]` — 1px hairline at 15% alpha per §3d.

    Workspace title (top of sidebar):
    ```tsx
    <div className="px-3 py-3 font-serif text-base font-semibold text-[var(--ink)]">
      Hyperpolymath
    </div>
    ```

    Why:
    - `font-serif text-base font-semibold` — EB Garamond 600 16px Body per §7e.
    - Copy `Hyperpolymath` (no tagline per §11d "Anthropic's claude.ai doesn't show a tagline next to the logo").

    Step 2 — `apps/web/components/shell/SidebarTree.tsx`. Areas section header with hover-reveal `+`. Per UI-SPEC §7e literal:

    ```tsx
    <div className="group/section flex items-center justify-between px-3 py-1 h-[30px]">
      <span className="font-mono text-[12px] uppercase tracking-[0.06em] text-[var(--ink-muted)]">Areas</span>
      <button
        type="button"
        onClick={onCreateArea}
        className="opacity-0 group-hover/section:opacity-100 focus-visible:opacity-100 pointer-fine:inline-flex hidden transition-opacity duration-150 ease-out outline-none rounded p-0.5 hover:bg-[var(--surface-raised)]"
        aria-label="New area"
      >
        <Plus className="h-3.5 w-3.5 text-[var(--ink-muted)] hover:text-[var(--ink)]" strokeWidth={1.5} />
      </button>
    </div>
    ```

    Why each value:
    - `group/section` — named Tailwind group for the hover scope.
    - `h-[30px]` — section header height per UI-SPEC §5 (Notion exact).
    - Section label `font-mono text-[12px] uppercase tracking-[0.06em] text-[var(--ink-muted)]` — chrome label register per §7e + §4a.
    - Hover-reveal `+`: opacity-0 → group-hover/section:opacity-100; pointer-fine:inline-flex; focus-visible:opacity-100 (keyboard fallback).
    - `transition-opacity duration-150 ease-out` — 150ms per §6b.
    - Lucide Plus, `h-3.5 w-3.5` (14px stroke asset), `strokeWidth={1.5}` per §8.

    Apply the same group/section + hover-reveal pattern to the Projects section header (with onCreateProject handler).

    Nav-item row (Area, Project, top-level nav links):

    ```tsx
    <Link
      href={item.href}
      className={cn(
        'flex items-center gap-2 px-3 py-1.5 rounded-md font-serif text-base transition-colors duration-150 ease-out',
        isActive
          ? 'bg-[var(--surface-raised)] text-[var(--ink)]'
          : 'text-[var(--ink-muted)] hover:text-[var(--ink)] hover:bg-[var(--surface-raised)]/50',
      )}
    >
      <ItemIcon className="h-4 w-4" strokeWidth={1.5} />
      <span>{item.label}</span>
    </Link>
    ```

    Why each value:
    - `font-serif text-base` — EB Garamond 400 16px (the §7e "Note" about collapsing the 14/16 distinction to a single 16px Body line).
    - `rounded-md` — 8px.
    - Active state: `bg-[var(--surface-raised)] text-[var(--ink)]` (subtle bg tone shift per §7e — NO left-edge accent, NO cyan dot).
    - Hover state: `hover:text-[var(--ink)] hover:bg-[var(--surface-raised)]/50` (interaction state: hover; 150ms transition).
    - Lucide icon: `h-4 w-4` (16px stroke asset), `strokeWidth={1.5}` per §8.
    - `transition-colors duration-150 ease-out` — 150ms hover per §6b.
    - Full-row click target via `<Link>` filling the width.

    Step 3 — `apps/web/components/shell/PersistentNav.tsx`. The JARVIS nav-link active state. Per UI-SPEC §14f: drop the 4px --hud-cyan dot. Concrete action:
    - Find the existing JARVIS link's active indicator (likely a small cyan circle or 4px dot).
    - REMOVE the cyan dot element entirely.
    - The JARVIS link active state uses the SAME pattern as every other nav link: bg-[var(--surface-raised)] + text-[var(--ink)] (NO cyan).
    - href stays `/today` per UI-SPEC §14 carry-forward.
    - Label: per Phase 6.1 06.1-05 SUMMARY the label flips to JARVIS register (`JARVIS`); preserve unless current label drifts from §11d.

    Step 4 — Footer (theme toggle + settings + sign-out). Audit existing chrome:
    - Theme toggle: carry-forward Lucide Sun/Moon icon with `strokeWidth={1.5}` per §8.
    - Settings + sign-out: same nav-link pattern as Step 2.
    - No `var(--hud-cyan)` anywhere in the footer.

    Interaction-state coverage (felt-quality mandate):
    - **Nav link idle (inactive)**: text-ink-muted; no bg.
    - **Nav link hover (inactive)**: text-ink + bg-surface-raised/50; 150ms transition.
    - **Nav link focus-visible (inactive)**: same; global --ring-doc focus ring.
    - **Nav link active**: text-ink + bg-surface-raised (no transition needed — the active state is a static tone shift).
    - **Nav link active hover**: stays active (subtle additional opacity shift not required; the active bg is the dominant signal).
    - **Section header hover**: + button fades in via group-hover; 150ms.
    - **Section header + button hover**: bg-surface-raised tint on the button.
    - **Section header + button focus-visible**: keyboard appearance via focus-visible:opacity-100.

    Step 5 — Build + tests:
    - `pnpm --filter web build` exits 0.
    - `pnpm --filter web test` passes.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      grep -q "w-\[224px\]" apps/web/components/shell/Sidebar.tsx &&
      ! grep -q "w-60\|w-\[240px\]" apps/web/components/shell/Sidebar.tsx &&
      grep -q "Hyperpolymath" apps/web/components/shell/Sidebar.tsx &&
      grep -q "font-mono text-\[12px\] uppercase tracking-\[0.06em\]" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "group/section" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "group-hover/section:opacity-100" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "pointer-fine:inline-flex" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "h-\[30px\]" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "font-serif text-base" apps/web/components/shell/SidebarTree.tsx &&
      grep -q "bg-\[var(--surface-raised)\]" apps/web/components/shell/SidebarTree.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/Sidebar.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/SidebarTree.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/PersistentNav.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    Sidebar.tsx:
    - Container has `w-[224px]` (NOT `w-60` and NOT `w-[240px]`)
    - Workspace title contains `Hyperpolymath` (no tagline)
    - Contains NO `hud-cyan`
    - Contains NO `border-l-*` left-edge accents on any nav row

    SidebarTree.tsx:
    - Areas section header uses `group/section`, `h-[30px]`, `font-mono text-[12px] uppercase tracking-[0.06em]`
    - Plus button uses `group-hover/section:opacity-100`, `pointer-fine:inline-flex hidden`, `focus-visible:opacity-100`, `transition-opacity duration-150 ease-out`
    - Plus icon has `strokeWidth={1.5}`
    - Nav-item links use `font-serif text-base` (NO `text-[14px]`, NO `text-sm`)
    - Active state uses `bg-[var(--surface-raised)] text-[var(--ink)]` (the tone shift pattern)
    - Hover state uses `hover:bg-[var(--surface-raised)]/50` (50% alpha tint per §7e)
    - Contains NO `hud-cyan`
    - Contains NO `border-l-` left-edge accents

    PersistentNav.tsx:
    - JARVIS link has NO cyan dot (grep `hud-cyan` returns 0)
    - JARVIS link active state uses the SAME bg-[var(--surface-raised)] pattern as other nav links

    Build + tests green
  </acceptance_criteria>
  <done>Sidebar at 224px Notion-exact. Hover-reveal + on Areas + Projects sections. JARVIS link drops cyan dot. Active state uses bg-tone shift only. No cyan anywhere in shell. Build + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: shadcn Dialog/Popover/Sheet/Tooltip — drop corner brackets; verify Anthropic-restraint</name>
  <files>
    apps/web/components/ui/dialog.tsx,
    apps/web/components/ui/popover.tsx,
    apps/web/components/ui/sheet.tsx,
    apps/web/components/ui/tooltip.tsx
  </files>
  <read_first>
    - UI-SPEC §7k (the FULL modals/dialogs/popovers/sheets/tooltips treatment — plain hairline + scrim, NO corner brackets, 200ms enter, 150ms exit)
    - UI-SPEC §6b motion table (modal enter / exit / list item rows)
    - UI-SPEC §14f (modals revert — drop 10px --edge-hud corner L-brackets)
    - apps/web/components/ui/dialog.tsx — read current; identify HudCornerCrops mount inside DialogContent + backdrop scrim treatment
    - apps/web/components/ui/popover.tsx — read; identify chrome
    - apps/web/components/ui/sheet.tsx — read; identify chrome
    - apps/web/components/ui/tooltip.tsx — read; carry-forward direction per §7k
  </read_first>
  <action>
    Step 1 — `apps/web/components/ui/dialog.tsx`. Per UI-SPEC §7k:
    - REMOVE the `<HudCornerCrops />` mount inside DialogContent if present (Wave 1 already deleted the file; this scrubs the JSX usage).
    - DialogContent container className becomes:
      ```tsx
      'rounded-lg border border-[rgb(20_37_32_/_0.20)] dark:border-[rgb(255_255_255_/_0.15)] bg-[var(--surface-raised)] p-6 shadow-none data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=open]:fade-in-0 data-[state=closed]:fade-out-0 data-[state=open]:zoom-in-[0.98] data-[state=closed]:zoom-out-[0.98] data-[state=open]:duration-200 data-[state=closed]:duration-150 ease-out'
      ```
    - DialogOverlay backdrop becomes:
      ```tsx
      'fixed inset-0 z-50 bg-black/30 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out data-[state=open]:fade-in-0 data-[state=closed]:fade-out-0 data-[state=open]:duration-200 data-[state=closed]:duration-150 ease-out'
      ```
    - DialogTitle uses Subhead H2 register per §7k:
      ```tsx
      'font-serif text-[22px] font-semibold tracking-tight text-[var(--ink)]'
      ```
    - DialogDescription uses Body register:
      ```tsx
      'font-serif text-base text-[var(--ink-muted)] mt-2'
      ```

    Why each value:
    - `rounded-lg` — 8px per §3d (modals/dialogs use 8px not 16px).
    - 20% alpha border per §7k.
    - `bg-[var(--surface-raised)]` — raised tone.
    - `shadow-none` — no ambient depth per §3d.
    - Enter: 200ms opacity + zoom-in `0.98 → 1.0` per §6b.
    - Exit: 150ms opacity-only per §6b.
    - Single ease-out-quart curve via `ease-out` (Tailwind animation `ease-out` = `cubic-bezier(0, 0, 0.2, 1)` ≈ our --ease-out-quart for short durations; acceptable per §6b discipline).
    - Backdrop: `bg-black/30 backdrop-blur-sm` per §7k. The `backdrop-blur-sm` (4px) is the carve-out exception to §12a's "NO backdrop-filter outside the modal backdrop scrim".
    - Dialog title at 22px Subhead per §7k explicit decision (modals are NOT page H1).
    - NO `<HudCornerCrops />` mount.
    - NO `var(--hud-cyan)` references.
    - NO `--edge-hud` references (the cyan-tinted edge token is now dormant per §14e).

    Step 2 — `apps/web/components/ui/popover.tsx`. Same treatment:
    - `rounded-lg border border-[rgb(20_37_32_/_0.20)] dark:border-[rgb(255_255_255_/_0.15)] bg-[var(--surface-raised)]`
    - `backdrop-blur-sm` (allowed for the popover floating surface)
    - 200ms enter / 150ms exit
    - NO corner brackets, NO cyan

    Step 3 — `apps/web/components/ui/sheet.tsx`. Same treatment as Dialog but slide-in from edge:
    - Container `bg-[var(--surface-raised)] border-[rgb(20_37_32_/_0.20)]`
    - Slide enter/exit per shadcn default + 200ms/150ms timing per §6b
    - NO cyan
    - NO corner brackets

    Step 4 — `apps/web/components/ui/tooltip.tsx`. Per UI-SPEC §7k tooltip carry-forward:
    - `rounded-md px-2 py-1 bg-[var(--ink)] text-[var(--canvas)] font-mono text-[12px]` (caption, inverted tones — Anthropic-restrained)
    - Enter/exit per shadcn default
    - NO cyan, NO HUD chrome

    Step 5 — Audit consumers. Some consumers may have wrapped DialogContent with custom corner crops manually (not via the primitive). Grep:
    ```
    grep -rln "HudCornerCrops" apps/web --include="*.tsx" | grep -v ".next"
    ```
    Should return 0 (Wave 1 already cleaned this). If anything remains, scrub.

    Step 6 — Build + tests:
    - `pnpm --filter web build` exits 0.
    - `pnpm --filter web test` passes.

    Interaction-state coverage:
    - Dialog/Sheet enter: 200ms fade + zoom; reduced-motion → 0s.
    - Dialog/Sheet exit: 150ms fade; reduced-motion → 0s.
    - Popover enter: 200ms fade + zoom (Step 2); reduced-motion respected.
    - Tooltip: shadcn default delays + transitions; reduced-motion respected via globals.css universal `*` selector.
    - Modal focus: shadcn handles focus trap (preserve); first focusable element receives focus on open.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      grep -q "rounded-lg" apps/web/components/ui/dialog.tsx &&
      grep -q "bg-\[var(--surface-raised)\]" apps/web/components/ui/dialog.tsx &&
      grep -q "border-\[rgb(20_37_32_/_0.20)\]" apps/web/components/ui/dialog.tsx &&
      grep -q "backdrop-blur" apps/web/components/ui/dialog.tsx &&
      grep -q "duration-200" apps/web/components/ui/dialog.tsx &&
      grep -q "duration-150" apps/web/components/ui/dialog.tsx &&
      ! grep -q "HudCornerCrops" apps/web/components/ui/dialog.tsx &&
      ! grep -q "edge-hud" apps/web/components/ui/dialog.tsx &&
      ! grep -q "hud-cyan" apps/web/components/ui/dialog.tsx &&
      ! grep -q "hud-cyan" apps/web/components/ui/popover.tsx &&
      ! grep -q "hud-cyan" apps/web/components/ui/sheet.tsx &&
      ! grep -q "hud-cyan" apps/web/components/ui/tooltip.tsx &&
      [ $(grep -rln "HudCornerCrops" apps/web --include="*.tsx" | grep -v ".next" | wc -l) -eq 0 ]
    </automated>
  </verify>
  <acceptance_criteria>
    dialog.tsx:
    - DialogContent has `rounded-lg`, `bg-[var(--surface-raised)]`, `border border-[rgb(20_37_32_/_0.20)]`
    - DialogContent has `backdrop-blur-sm` on overlay (or similar small scrim)
    - DialogContent has 200ms enter (`data-[state=open]:duration-200`)
    - DialogContent has 150ms exit (`data-[state=closed]:duration-150`)
    - Contains NO `HudCornerCrops` mount
    - Contains NO `edge-hud` references
    - Contains NO `hud-cyan` references
    - DialogTitle uses `text-[22px]` (Subhead per §7k)

    popover.tsx, sheet.tsx, tooltip.tsx:
    - Each contains NO `hud-cyan` references
    - Each contains NO `HudCornerCrops` mounts
    - Each has `border` + `bg-[var(--surface-raised)]` (popover, sheet) or `bg-[var(--ink)]` (tooltip)
    - Each has explicit duration (200ms enter / 150ms exit pattern or shadcn default for tooltip)

    Cross-file:
    - `grep -rln "HudCornerCrops" apps/web --include="*.tsx" | grep -v ".next" | wc -l` → 0 (consistent with Wave 1 cleanup)

    Build + tests green
  </acceptance_criteria>
  <done>Dialog/Popover/Sheet/Tooltip strip corner brackets and any cyan; render with 1px hairline + scrim per §7k. Build + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Sonner toast CSS — replace 3px left-edge stripe with 6px ::before inline dot per §7l</name>
  <files>apps/web/app/globals.css</files>
  <read_first>
    - UI-SPEC §7l (the FULL sonner CSS replacement block — exact CSS literal)
    - UI-SPEC §14d (sonner left-edge stripe REPLACE entry)
    - apps/web/app/globals.css — read the existing sonner block (lines around 668-688 per UI-SPEC reference; confirm via Read since file may have shifted from Wave 1+2 edits)
  </read_first>
  <action>
    Step 1 — In `apps/web/app/globals.css`, locate the existing sonner toast CSS block. It currently uses `border-left: 3px solid` for intent indication. REPLACE the entire block with UI-SPEC §7l literal:

    ```css
    [data-sonner-toast] {
      background: var(--surface-raised) !important;
      color: var(--ink) !important;
      border: 1px solid rgb(20 37 32 / 0.20) !important;
      border-radius: 6px !important;
      font-family: var(--font-serif) !important;
      padding: 12px 16px !important;
    }

    .dark [data-sonner-toast] {
      border-color: rgb(255 255 255 / 0.15) !important;
    }

    /* Intent indication: small inline dot via ::before, color per type */
    [data-sonner-toast][data-type='success']::before {
      content: '';
      display: inline-block;
      width: 6px;
      height: 6px;
      border-radius: 9999px;
      background: var(--ink-sage);
      margin-right: 8px;
      vertical-align: middle;
    }
    [data-sonner-toast][data-type='error']::before {
      content: '';
      display: inline-block;
      width: 6px;
      height: 6px;
      border-radius: 9999px;
      background: var(--ink-coral);
      margin-right: 8px;
      vertical-align: middle;
    }
    [data-sonner-toast][data-type='info']::before {
      content: '';
      display: inline-block;
      width: 6px;
      height: 6px;
      border-radius: 9999px;
      background: var(--ink-muted);
      margin-right: 8px;
      vertical-align: middle;
    }

    /* Undo action button: mono uppercase chrome label */
    [data-sonner-toast] [data-button] {
      font-family: var(--font-mono) !important;
      font-size: 12px !important;
      font-weight: 400 !important;
      text-transform: uppercase !important;
      letter-spacing: 0.06em !important;
      color: var(--ink) !important;
    }
    ```

    Why each value (do NOT change):
    - `background: var(--surface-raised)` — Anthropic raised tone.
    - `border: 1px solid rgb(20 37 32 / 0.20)` — 1px hairline at 20% alpha per §7l.
    - `border-radius: 6px` — toast radius per §7l.
    - `font-family: var(--font-serif)` — EB Garamond body label per §7l.
    - `padding: 12px 16px` — toast padding per §7l (not 8px grid because sonner has tight content; aligns with §5 default-card pattern).
    - `!important` necessity: sonner injects inline styles at runtime that would otherwise win specificity per Phase 6.1 06.1-05 SUMMARY note.
    - Intent dots: 6px circles via `::before`, color per type (sage/coral/muted per §3c).
    - `margin-right: 8px; vertical-align: middle` — inline alignment per §7l literal.
    - Undo button: mono uppercase 12px tracking-[0.06em] per §11d Undo chrome label.
    - NO `border-left: 3px solid` (the rejected chrome).
    - NO `var(--hud-cyan)` ANYWHERE (toasts forbidden cyan per §3b row "All toast intent edges").

    Step 2 — Verify the block placement in globals.css. The sonner block should live AFTER the @theme block and the .dark { ... } block, in a normal CSS rule section. If the existing block is wrapped in a `@layer components` directive, preserve that layer (do not change layer placement).

    Step 3 — Build:
    - `pnpm --filter web build` exits 0.
    - Smoke: trigger a toast in dev (e.g., `toast.success('test')`) and verify the 6px ::before dot renders, NOT a 3px left-edge stripe.

    Interaction-state coverage:
    - Sonner manages toast enter / exit via its own state machine (~250ms enter / ~200ms exit per §6b sonner-managed row).
    - The CSS does not need explicit hover/focus rules — the Undo button inherits from the global focus-visible system + the inline `[data-button]` styles above.
    - reduced-motion: sonner has its own internal reduction; the globals.css universal `*` selector additionally ensures transition-duration → 0.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      ! grep -E "data-sonner-toast.*border-left:.*3px" apps/web/app/globals.css &&
      grep -q "data-sonner-toast\]\[data-type='success'\]::before" apps/web/app/globals.css &&
      grep -q "data-sonner-toast\]\[data-type='error'\]::before" apps/web/app/globals.css &&
      grep -q "data-sonner-toast\]\[data-type='info'\]::before" apps/web/app/globals.css &&
      grep -q "background: var(--ink-sage)" apps/web/app/globals.css &&
      grep -q "background: var(--ink-coral)" apps/web/app/globals.css &&
      grep -q "background: var(--ink-muted)" apps/web/app/globals.css &&
      grep -q "width: 6px" apps/web/app/globals.css &&
      grep -q "height: 6px" apps/web/app/globals.css &&
      grep -q "data-button\]" apps/web/app/globals.css &&
      grep -q "letter-spacing: 0.06em" apps/web/app/globals.css
    </automated>
  </verify>
  <acceptance_criteria>
    globals.css sonner block:
    - Contains `[data-sonner-toast][data-type='success']::before` rule
    - Contains `[data-sonner-toast][data-type='error']::before` rule
    - Contains `[data-sonner-toast][data-type='info']::before` rule
    - Each `::before` rule has `width: 6px`, `height: 6px`, `border-radius: 9999px`, `margin-right: 8px`, `vertical-align: middle`
    - Success dot bg = `var(--ink-sage)`
    - Error dot bg = `var(--ink-coral)`
    - Info dot bg = `var(--ink-muted)`
    - NO `border-left: 3px solid` rule on `[data-sonner-toast]` (the rejected chrome — grep returns 0)
    - `[data-button]` rule uses mono + uppercase + `letter-spacing: 0.06em` (the Undo chrome label)
    - Contains NO `var(--hud-cyan)` references anywhere in the sonner block

    Build succeeds: `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>Sonner toast left-edge 3px stripe REPLACED with 6px ::before inline dot. Undo button mono uppercase 12px tracking-[0.06em]. No cyan. Build green.</done>
</task>

<task type="auto">
  <name>Task 4: CalendarClient today-column wash audit; CommandMenu + AppShell verification</name>
  <files>
    apps/web/components/calendar/CalendarClient.tsx,
    apps/web/components/calendar/CalendarGrid.tsx,
    apps/web/components/shell/CommandMenu.tsx,
    apps/web/components/shell/CommandMenuContent.tsx,
    apps/web/components/shell/AppShell.tsx
  </files>
  <read_first>
    - UI-SPEC §7f (calendar carry-forward + today-column amber wash evaluation)
    - UI-SPEC §7m (AppShell global chrome — no edge instrumentation rail, no corner crops)
    - UI-SPEC §11d (theme toggle tooltip copy `Switch to dark` / `Switch to light`)
    - apps/web/components/calendar/CalendarClient.tsx — read; identify the today-column amber wash className (likely `bg-[rgb(217_119_6_/_0.08)]` or similar 8% alpha)
    - apps/web/components/calendar/CalendarGrid.tsx — read; verify event tile colors (per-event gcal color via inline style — carry-forward from Phase 4)
    - apps/web/components/shell/CommandMenu.tsx + CommandMenuContent.tsx — read; verify shadcn Command primitive chrome
    - apps/web/components/shell/AppShell.tsx — read; verify no global HUD chrome (no edge rail, no corner crops)
  </read_first>
  <action>
    Step 1 — `apps/web/components/calendar/CalendarClient.tsx` + `apps/web/components/calendar/CalendarGrid.tsx`. Audit per UI-SPEC §7f:
    - Find the today-column amber wash. If it's `rgb(217 119 6 / 0.08)` (8% alpha), evaluate against the new Anthropic-restraint floor:
      - **Recommendation**: drop to `rgb(217 119 6 / 0.05)` (5% alpha) per UI-SPEC §7f explicit guidance. The 5% alpha is barely-visible and matches Anthropic's discretion.
      - **Alternative**: cut entirely and rely on subtle date-cell bg tone shift (use `bg-[var(--surface)]` on today's column).
    - Pick the 5% alpha option for this plan (preserves the indicator, just dials restraint).
    - Concrete change: `bg-[rgb(217_119_6_/_0.08)]` → `bg-[rgb(217_119_6_/_0.05)]`.

    Event tiles carry forward per Phase 4 Plan 04-03 Pitfall 8 — per-event gcal color via inline `style={{ backgroundColor: gcalColor }}`. Do NOT change this; carry-forward.

    Verify CalendarClient has NO `var(--hud-cyan)` references (calendar is NOT in the cyan allowlist).

    Step 2 — `apps/web/components/shell/CommandMenu.tsx` + `apps/web/components/shell/CommandMenuContent.tsx`. Audit per UI-SPEC §7k (CommandMenu is built on shadcn Command primitive which uses Popover/Dialog primitives — Task 2 already updated those):
    - Verify NO HUD chrome (no `<HudCornerCrops />` mount; Wave 1 cleaned this).
    - Verify NO `var(--hud-cyan)` references.
    - Verify the item rows match the Anthropic-restraint pattern from JarvisInput's slash popover (Plan 03 Task 2 reference). The CommandMenu items should look like:
      ```tsx
      <CommandItem className="font-serif text-base text-[var(--ink)] hover:bg-[var(--surface)] aria-selected:bg-[var(--surface)] focus-visible:bg-[var(--surface)] transition-colors duration-150 ease-out">
        ...
      </CommandItem>
      ```
    - If the existing CommandItem chrome drifts (e.g., uses font-mono uppercase like the revoked agent-variant), update to serif body label.
    - Kbd shortcut chips (e.g., `⌘K`, `⌘N`): `font-mono text-[12px] tracking-[0.04em] text-[var(--ink-muted)]` per §4a Caption role.

    Step 3 — `apps/web/components/shell/AppShell.tsx`. Audit per UI-SPEC §7m:
    - Container: `flex min-h-screen bg-[var(--canvas)]`
    - Sidebar slot uses the 224px Sidebar (Task 1).
    - Content area: `flex-1 min-h-screen` (the page wrapper handles bg).
    - NO global HUD chrome — verify no `<HudEdgeInstrumentation />` mount (Wave 1 deleted file; this is a sanity check).
    - NO `<HudCornerCrops />` mount.
    - NO `var(--hud-cyan)` references.

    Step 4 — Theme toggle tooltip copy. Find the existing ThemeToggle.tsx (likely `apps/web/components/shell/ThemeToggle.tsx`). Audit:
    - Tooltip text: `Switch to dark` (when in light mode) / `Switch to light` (when in dark mode) per §11d.
    - If the existing tooltip says `Toggle theme` or similar generic phrase, update.

    Step 5 — Build + tests:
    - `pnpm --filter web build` exits 0.
    - `pnpm --filter web test` passes.

    Interaction-state coverage:
    - CommandMenu items: hover + focus-visible + aria-selected (the three interaction states for a listbox).
    - CalendarClient today-column: static visual indicator; no interaction state per se (it's a date-cell bg, not a button).
    - Event tiles: existing interaction states from Phase 4 (drag-resize, click-to-edit) preserved — no changes here.
    - Theme toggle: hover + focus-visible per shadcn button defaults.

    Step 6 — Build and verify:
    - All grep checks below pass.
    - Build is green.
  </action>
  <verify>
    <automated>
      cd /Users/filippofonseca/Developer/Projects/hyperpolymath-v2 &&
      ! grep -q "hud-cyan" apps/web/components/calendar/CalendarClient.tsx &&
      ! grep -q "hud-cyan" apps/web/components/calendar/CalendarGrid.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/CommandMenu.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/CommandMenuContent.tsx &&
      ! grep -q "hud-cyan" apps/web/components/shell/AppShell.tsx &&
      ! grep -q "HudCornerCrops" apps/web/components/shell/AppShell.tsx &&
      ! grep -q "HudEdgeInstrumentation" apps/web/components/shell/AppShell.tsx &&
      ! grep -q "rgb(217_119_6_/_0.08)\|rgb(217 119 6 / 0.08)" apps/web/components/calendar/CalendarClient.tsx &&
      ! grep -E "text-\[(13|14|15|18|20|24|28|36)px\]" apps/web/components/shell/CommandMenuContent.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    CalendarClient.tsx + CalendarGrid.tsx:
    - Today-column wash uses `rgb(217 119 6 / 0.05)` (5% alpha) OR is cut entirely (no 8% wash remains)
    - Contains NO `hud-cyan` references
    - Event tile per-event color via inline style preserved (carry-forward)

    CommandMenu.tsx + CommandMenuContent.tsx:
    - Item rows use `font-serif text-base` (NOT font-mono uppercase)
    - Item hover uses `hover:bg-[var(--surface)]` + `transition-colors duration-150 ease-out`
    - Item focus-visible covered
    - Item selected (`aria-selected`) covered
    - Kbd shortcut chips use `font-mono text-[12px] tracking-[0.04em]`
    - Contains NO `hud-cyan`
    - Contains NO off-scale type sizes

    AppShell.tsx:
    - Contains `flex min-h-screen` + `bg-[var(--canvas)]`
    - Contains NO `HudCornerCrops` mount
    - Contains NO `HudEdgeInstrumentation` mount
    - Contains NO `hud-cyan` references

    ThemeToggle tooltip copy:
    - Contains `Switch to dark` OR `Switch to light` (or a conditional based on current theme that produces these literals) — verify via grep on the tooltip text strings

    Build + tests green
  </acceptance_criteria>
  <done>Calendar today-column wash at 5% alpha (restraint dialed). CommandMenu chrome at Anthropic-restraint. AppShell free of HUD chrome. Theme toggle tooltip copy per §11d. Build + tests green.</done>
</task>

</tasks>

<verification>
After all four tasks complete:

1. UI-SPEC §7e (sidebar) — width 224px, hover-reveal +, no cyan dot, bg-tone active state
2. UI-SPEC §7k (modals) — no corner brackets, 1px hairline + scrim, 200/150ms timing
3. UI-SPEC §7l (sonner) — 6px ::before dot replaces 3px left-edge stripe
4. UI-SPEC §7f (calendar) — today wash at 5% or cut
5. UI-SPEC §7m (AppShell) — no global HUD chrome

6. Cyan-bleed prevention across diplomatic surfaces:
   ```
   grep -rln "hud-cyan" apps/web/components/{shell,calendar,ui/dialog,ui/popover,ui/sheet,ui/tooltip}.tsx | wc -l
   ```
   → 0

7. Build + tests green.
</verification>

<success_criteria>
- AES-02 satisfied: diplomatic chrome matches Anthropic + Notion restraint per §7e/§7f/§7k/§7l/§7m
- AES-03 satisfied: modal motion respects 200ms enter / 150ms exit timing; sonner managed; reduced-motion honored
- AES-04 satisfied: theme toggle tooltip copy aligns with §11d Anthropic register
- AES-06 satisfied: all chrome surfaces token-driven; parity in both modes
- Cyan-application discipline: zero unauthorized cyan in any diplomatic surface
- Felt-quality mandate: every interactive chrome element has hover + focus + active coverage
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-anthropic-discipline-rebuild/06.2-06-SUMMARY.md` documenting:
- Sidebar 240 → 224 width change + JARVIS cyan-dot removal
- Section header hover-reveal + pattern (Notion)
- Active nav-link bg-tone shift (no left-edge accents)
- Dialog/Popover/Sheet/Tooltip corner-bracket removal + 200/150ms enter/exit timing
- Sonner CSS block replacement (3px stripe → 6px ::before dot)
- Calendar today-column wash adjustment (5% or cut)
- CommandMenu + AppShell audit pass
- Theme toggle tooltip copy alignment
- Any deviation from plan with rationale
</output>
