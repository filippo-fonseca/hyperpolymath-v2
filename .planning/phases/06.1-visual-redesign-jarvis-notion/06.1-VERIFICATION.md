---
phase: 06.1-visual-redesign-jarvis-notion
verified: 2026-05-19T20:50:00Z
status: passed_with_deferrals
score: infrastructure shipped; cumulative visual contract rejected by user; AES-* deferred to Phase 6.2
re_verification: no — initial verification, scoped narrowly per user decision (2026-05-19 evening)
deferred_to: 06.2-anthropic-discipline-rebuild
deferral_reason: |
  User reviewed the live Phase 6.1 visual surface at the 06.1-06 Task 3 manual-walkthrough checkpoint on 2026-05-19 and rejected the cumulative result with the verbatim words: "i need you to do a massive refactor. research properly the UI of claude code / anthropic + notion paired with something like what jarvis from tony stark has. do another phase. i still do not like how it looks and everything feels clunky and blah." This is the SECOND visual contract rejection in a row (Phase 6's neumorphic was rejected earlier the same day). The pattern is unmistakable: the user wants Anthropic-level interaction polish + Notion document discipline + JARVIS as atmospheric mood only, NOT literal HUD vocabulary. AES-01..AES-07 are deferred to Phase 6.2 (`anthropic-discipline-rebuild`) for rebuild against the new contract. Infrastructure work that's direction-independent (token cleanup, motion library substrate, focus-visible system, shadcn primitive restyles, utility-class adoption from intentionality.io) remains in main as foundation Phase 6.2 can consume.
---

# Phase 6.1: Visual Redesign — JARVIS × Notion — Verification Report

**Phase Goal:** Rebuild the visual surface to "as if the UI for JARVIS from Tony Stark had a baby with Notion" — holographic-AI surface details anchored to clean-document discipline. Replace the rejected Phase 6 visual contract.

**Verified:** 2026-05-19T20:50:00Z
**Status:** **passed_with_deferrals** — infrastructure shipped and useful; the cumulative visual contract was rejected by user; AES-* requirements deferred to Phase 6.2 against a new contract.

## Scoping Decision (2026-05-19 evening)

Phase 6.1 is the second visual rebuild in this milestone that did not deliver felt-quality. Both Phase 6 (neumorphic + JARVIS-blue) and Phase 6.1 (Stark HUD vocabulary) were rejected by user live-walkthrough with the same disposition ("clunky and blah" / "don't like the UI"). This is a pattern — and the pattern says the *discipline pole* of the brief was wrong both times. Phase 6 leaned on Material/neumorphism; Phase 6.1 leaned on Linear/Vercel/Stark for HUD vocabulary. Neither hit the felt-quality bar.

User's directional reset 2026-05-19 evening: *"research properly the UI of claude code / anthropic + notion paired with something like what jarvis from tony stark has."* Translation: **Anthropic (claude.ai + claude.com + Claude Code CLI + console.anthropic.com) becomes the new discipline pole**, Notion remains content frame, JARVIS contributes mood (cyan accent + subtle depth) only.

Phase 6.1 closes here on what's direction-independent + foundational. Visual surface rebuild moves to Phase 6.2.

## In-Scope Infrastructure (Verified)

| Plan | Outcome | Status |
|------|---------|--------|
| 06.1-01 Token foundation | Complete — neumorphic shadows retired; OKLCH base palette installed (warm parchment / soft graphite); HUD cyan family scoped to .agent-mode-scope; 3 intent inks (amber/sage/coral); motion @keyframes library (12 keyframes); focus-visible ring system with `--ring-doc` + `--ring-hud`; JetBrains Mono 500 italic for JARVIS prose; HudCornerCrops + HudEdgeInstrumentation shared primitives | VERIFIED |
| 06.1-02 JARVIS Console choreography | Complete — 7-state interaction machine + receipt materialize choreography + HudStatusPill + HudThinkingRing. **Likely scoped down or replaced in 6.2** per Anthropic-restraint pivot (the 7-state cumulative feel was rejected). | SHIPPED but deferred for likely replacement |
| 06.1-03 Agent surfaces (/insights, /health, /memory) | Complete — agent-mode-scope wrap, cyan chart strokes, mono italic 24px /health H1, JSON-styled readout, fact-card chrome. **Likely simplified in 6.2** (mono-italic-24px H1 stays direction-independent; HUD chrome around fact cards is part of the rejected aesthetic) | SHIPPED but deferred for likely simplification |
| 06.1-04 Document surfaces | Complete — shadcn Button/Card/Input/EmptyState restyled to document tier; Tasks/Captures/Projects/Settings rebuilt with Notion-leaning treatment + Motion 12 interaction states. **Largely carries forward to 6.2** since direction was correctly Notion-restrained | VERIFIED (carries forward) |
| 06.1-05 Diplomatic surfaces + AES-04 copy | Complete — Sidebar (mono AREAS/JARVIS/NAVIGATE labels), AppShell, Calendar grid, CommandMenu, Dialog/Popover/Sheet/Tooltip, sonner intent edges. AES-04 brand-voice copy pass complete (Sidebar/AppShell/CalendarClient + button labels). **Mostly carries forward to 6.2** with cyan chrome possibly dialed down | VERIFIED (carries forward) |
| 06.1-06 Final integration (Tasks 1-2) | Complete — template.tsx --ease-out-quart confirmation; error.tsx + global-error.tsx restyle; 06.1-06-VERIFICATION.md (all 7 automated audits PASS); HudCoreBubble (added per user feedback, deferred for likely removal in 6.2); intentionality.io pattern adoption (utility classes + custom variant + layered bg + glow tokens) | VERIFIED |
| 06.1-06 Task 3 (manual walkthrough) | NOT EXECUTED — gate failed implicitly by user direction; replaced by Phase 6.2 verification pass against new contract | DEFERRED |

## Deferred to Phase 6.2

| Requirement | Reason |
|----|---|
| AES-01 (typography values) | Type scale + weights stay direction-independent but specific deployment per surface needs Anthropic-restraint re-anchoring |
| AES-02 (visual style — the JARVIS × Notion target) | The TARGET itself is being replaced — new target: Anthropic-discipline + Notion-frame + JARVIS-atmospheric-mood |
| AES-03 (motion language) | The 7-state JARVIS Console machine + always-on motion (scan ambient, breathing focus, corner-crop breathe) were rejected as cumulative-overwhelming; Phase 6.2 will define an Anthropic-restrained motion vocabulary |
| AES-04 (brand voice copy — finish pass) | Sidebar/AppShell/CalendarClient + button labels done in 06.1-05 (direction-independent); remaining tone polish + alignment with new Anthropic-leaning voice happens in 6.2 |
| AES-06 (theme feel) | Light + dark mechanism intact; "feel" claim deferred with the visual contract |
| AES-07 (responsive ≥768px) | Code-level checks pass; live walkthrough verification against new contract happens in 6.2 |

AES-05 (Cmd+K JARVIS focus) was already verified complete in Phase 6.

## Automated Quality Gates

- `pnpm --filter web typecheck` exits 0
- `pnpm --filter web build` succeeds (all 20 routes register)
- `pnpm --filter web test` 237 passed, 1 skipped (no regressions from Phase 5.1 / Phase 6 baselines)
- All 6 plan SUMMARY.md files exist (06.1-01 through 06.1-06)
- All commits present in `main`

## What Carries Forward to Phase 6.2 (direction-independent)

**Definitely keeping:**
- Token foundation cleanup (neumorphic shadows retired from globals.css)
- Light + dark mode mechanism (next-themes + Tailwind 4 `@variant dark`)
- Focus-visible ring SYSTEM (mechanism intact; shadow values may re-tune)
- Motion @keyframes library (substrate for any motion vocabulary)
- shadcn primitive restyles (Button/Card/Input/Dialog/Popover/Sheet/Tooltip — document-tier was correct direction)
- Document surfaces (Tasks/Captures/Projects/Settings) — Notion-leaning was correct
- AES-04 brand-voice copy pass (direction-independent)
- Error boundary restyle (mostly direction-independent)
- Page transition template.tsx mechanism (only motion VOCABULARY may change)
- intentionality.io utility classes: `.font-mono-stats`, `.custom-scrollbar`, `.scrollbar-hidden`, `.theme-color-scheme`, `@custom-variant pointer-fine`
- `--hud-cyan-rgb` triplet token
- `--glow-hud-subtle/medium/strong` tokens (as opt-in only)
- HudStatusPill, HudThinkingRing primitives (likely kept as quiet single-moment indicators)

**Likely scoped down or removed in 6.2:**
- HudCoreBubble.tsx (arc-reactor centerpiece) — likely removed per Anthropic-restraint
- HudCornerCrops on every agent surface — likely limited to ONE focal moment or removed
- HudEdgeInstrumentation as always-visible rail — likely removed or scoped to one quiet appearance
- Layered ambient HUD background (`.agent-mode-scope::before + ::after`) — likely removed or dramatically simplified
- 7-state JARVIS Console motion machine — likely collapsed to 3 quiet states (idle / thinking / done)
- Always-on motion (scan ambient, breathing focus, corner-crop breathe) — likely removed

**Per UI-SPEC §14 carry-forward (unchanged across both visual rebuilds):**
- `app/providers.tsx` (next-themes wiring mechanism)
- `lib/jarvis/focus.ts` (Cmd+K singleton)
- `components/shell/GlobalHotkeys.tsx`
- `components/shell/CommandMenu.tsx` Cmd+Shift+K binding
- `components/shared/use-undo-toast.ts`
- `lib/db/queries/insights.ts`
- `app/api/health/route.ts`
- All backend code, Drizzle, Server Actions, RLS, migrations
- recharts library choice
- `.agent-mode-scope` wrapper class (mechanism; values change)

## Decision Log

- **2026-05-19 evening — User rejected cumulative Phase 6.1 visual surface at 06.1-06 Task 3 checkpoint.** Verbatim: *"i need you to do a massive refactor. research properly the UI of claude code / anthropic + notion paired with something like what jarvis from tony stark has. do another phase. i still do not like how it looks and everything feels clunky and blah."*
- **2026-05-19 evening — User reinforced cyan-canonical signature via second image reference** (`/Users/filippofonseca/.claude/image-cache/35b72e63-d948-4491-ace6-9ec5b74e0b7d/2.png`) and the message "for the accent color, remember... we are doing it jarvis-esque like in the tony stark movie." Cyan stays; the PROBLEM was OVERWHELMING APPLICATION of cyan + supporting HUD chrome, not the color choice.
- **2026-05-19 evening — Closed Phase 6.1 with `passed_with_deferrals` rather than `gaps_found`.** Two visual contracts in a row failing means the brief discipline pole was wrong. Cleaner to defer AES-* to a research-first 6.2 with the new Anthropic + Notion + Claude Code triumvirate than to plan point fixes against a contract that 6.2 will replace wholesale.
- **2026-05-19 evening — Inserting Phase 6.2 (`anthropic-discipline-rebuild`)** as decimal between 6.1 and 7. Will absorb deferred AES-01..AES-07 plus the 06.1-06 Task 3 walkthrough scope, re-anchored against the new contract.

## Next

`/gsd:insert-phase 06.1` → create Phase 6.2 → `/gsd:research-phase 06.2` (deep research on Anthropic UI + Claude Code + Notion + JARVIS atmospheric mood) → `/gsd:ui-phase 06.2` (research-grounded UI-SPEC) → `/gsd:plan-phase 06.2` → `/gsd:execute-phase 06.2`.
