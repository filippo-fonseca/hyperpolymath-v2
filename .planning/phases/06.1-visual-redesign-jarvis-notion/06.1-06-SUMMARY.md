---
phase: 06.1-visual-redesign-jarvis-notion
plan: 06
subsystem: final-integration
tags: [page-transition, error-boundary, verification-sweep, manual-walkthrough, partial-execution, deferred-to-6.2]
status: partial
execution_outcome: tasks 1+2 shipped + bonus arc-reactor + intentionality.io patterns; task 3 (manual walkthrough) deferred to Phase 6.2 manual walkthrough

# Dependency graph
requires:
  - phase: 06.1-visual-redesign-jarvis-notion
    provides: token foundation + shared HUD primitives (06.1-01-SUMMARY.md)
  - phase: 06.1-visual-redesign-jarvis-notion
    provides: 7-state JARVIS Console motion machine (06.1-02-SUMMARY.md)
  - phase: 06.1-visual-redesign-jarvis-notion
    provides: agent surfaces rebuild — /insights /health /memory (06.1-03-SUMMARY.md)
  - phase: 06.1-visual-redesign-jarvis-notion
    provides: document surfaces rebuild (06.1-04-SUMMARY.md)
  - phase: 06.1-visual-redesign-jarvis-notion
    provides: diplomatic surfaces rebuild + AES-04 copy pass (06.1-05-SUMMARY.md)
provides:
  - template.tsx motion variant updated to consume --ease-out-quart cubic-bezier (mechanism unchanged per UI-SPEC §14)
  - (app)/error.tsx visually restyled — --canvas bg, AlertTriangle in --ink-coral, serif H1, mono code block, Button variants, brand-voice copy preserved per §12d
  - global-error.tsx visually restyled — hex literals updated to match new tokens; system serif fallback preserved per UI-SPEC §14
  - 06.1-06-VERIFICATION.md — automated sweep (token purge, anti-patterns, prefers-reduced-motion, focus-visible, carry-forward, build/test, responsive code-level) — all 7 audits PASS
  - HudCoreBubble.tsx — arc-reactor centerpiece (added in response to user feedback during walkthrough)
  - Layered ambient HUD background scoped to .agent-mode-scope (intentionality.io pattern adoption)
  - --hud-cyan-rgb triplet + --glow-hud-subtle/medium/strong tokens (intentionality.io pattern)
  - .font-mono-stats / .custom-scrollbar / .scrollbar-hidden / .theme-color-scheme utility classes (intentionality.io)
  - @custom-variant pointer-fine (intentionality.io)
deferred:
  - Task 3 (22-scenario manual walkthrough). User rejected the cumulative Phase 6.1 visual surface during the live walkthrough on 2026-05-19 with the verbatim words: "i still do not like how it looks and everything feels clunky and blah" and requested a fresh research-first phase (6.2) anchored on Anthropic + Notion + Claude Code as the discipline pole with JARVIS as atmospheric mood only. Running the 22-scenario walkthrough now would only confirm what the user already said: the cumulative HUD-heavy direction is not landing. Walkthrough scenarios + the felt-quality verification are reframed in Phase 6.2's verification pass against the new contract.
affects: [06.2-anthropic-discipline-rebuild]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inline pseudo-element layered background via .agent-mode-scope::before + ::after — scoped to specific surface classes so ambient HUD chrome never bleeds onto document surfaces (Notion-clean integrity preserved)"
    - "Font-feature-settings tabular + lining numerals via .font-mono-stats — direction-independent polish for stat-heavy surfaces"
    - "RGB triplet alongside OKLCH (--hud-cyan-rgb) — enables rgba(var()) pattern for component glows without color-mix()"
    - "@custom-variant for pointer-fine media query — Tailwind 4 native variant for hover-reveal patterns that adapt to input device"

key-files:
  modified:
    - apps/web/app/(app)/template.tsx
    - apps/web/app/(app)/error.tsx
    - apps/web/app/global-error.tsx
    - apps/web/app/globals.css
    - apps/web/components/shared/HudEdgeInstrumentation.tsx
    - apps/web/app/(app)/health/page.tsx
    - apps/web/components/jarvis/JarvisConsole.tsx
  created:
    - apps/web/components/shared/HudCoreBubble.tsx (added during walkthrough in response to user "need a proper central jarvis bubble thingy" — deferred to 6.2 for likely removal/replacement per the Anthropic-restraint pivot)
    - .planning/phases/06.1-visual-redesign-jarvis-notion/06.1-06-VERIFICATION.md
    - .planning/phases/06.1-visual-redesign-jarvis-notion/06.1-06-SUMMARY.md

key-decisions:
  - "Task 3 deferred to 06.2 (not failed) — running the walkthrough against a contract the user has already rejected wholesale produces no signal, only confirmation"
  - "HudCoreBubble (arc-reactor) shipped as user-requested mid-walkthrough addition; likely removed/scoped down in 06.2 per the Anthropic-restraint pivot"
  - "intentionality.io pattern adoption stays in main as foundational utility infrastructure (pointer-fine, font-mono-stats, scrollbar utilities, theme-color-scheme); the layered HUD background (.agent-mode-scope::before/::after) is scoped to agent surfaces and may be replaced or removed in 06.2"
  - "Phase 6 plumbing carry-forward preserved per UI-SPEC §14: next-themes, motion/react template.tsx, focus.ts singleton, GlobalHotkeys (Cmd+K), CommandMenu (Cmd+Shift+K), use-undo-toast, /api/health route, /insights query, recharts, focus-visible SYSTEM, .agent-mode-scope wrapper class — mechanisms intact, only values changed"

requirements-completed:
  - "Partial — AES-02 (visual style): structurally implemented per Phase 6.1 UI-SPEC; user rejected the cumulative result; deferred to 6.2 against new contract"
  - "Partial — AES-03 (motion language): 7-state machine technically shipped but rejected as cumulative-overwhelming; deferred to 6.2 for Anthropic-restrained motion"
  - "Partial — AES-04 (brand voice copy): Sidebar/AppShell/CalendarClient + button labels done in 06.1-05 (direction-independent); remaining tone polish deferred to 6.2"
  - "Partial — AES-06 (theme feel): light + dark mechanism intact; 'feel' claim deferred with the rejected visual contract"
  - "Partial — AES-07 (responsive ≥768px): code-level checks pass; live walkthrough verification deferred to 6.2"

verification:
  automated: All 7 automated audits pass (06.1-06-VERIFICATION.md). typecheck + build green. 237/237 tests pass.
  manual: deferred — see Phase 6.2 manual walkthrough when ready

# Commits
- a56b597 — feat(06.1-06): restyle page transition + error boundaries with new tokens (Task 1)
- 1e12f92 — test(06.1-06): automated verification sweep — all 7 audits pass (Task 2)
- df1b850 — feat(06.1-06): add HudCoreBubble arc-reactor centerpiece (user-requested addition; deferred for likely removal in 6.2)
- 97573d7 — feat(06.1-06): adopt intentionality.io HUD patterns (utilities + layered bg + glow tokens)
- 21cdef4 — chore(06.1-06): apply font-mono-stats to high-impact number surfaces
---

# Plan 06.1-06 Summary — partial close

## What shipped

**Task 1 — page transition + error boundary restyle (commit `a56b597`)**
template.tsx motion variant now consumes `--ease-out-quart` cubic-bezier `[0.25, 1, 0.5, 1]` per UI-SPEC §6g; mechanism unchanged per §14. (app)/error.tsx restyled: --canvas bg, AlertTriangle 48px in --ink-coral with 1.5px stroke, serif H1 "Something went wrong.", mono code-fenced JSON error report, shadcn Button default + secondary variants. global-error.tsx hex literals updated to match new tokens (#f5f3ec canvas, #2a2520 ink, #dc2626 coral, #766f64 muted, #ece8df surface, #d4cfc4 edge); system serif fallback preserved per §14.

**Task 2 — automated verification sweep (commit `1e12f92`)**
06.1-06-VERIFICATION.md created with grep audit results across 7 dimensions: token purge (zero retired tokens), anti-pattern compliance (no Liquid Glass / hue-rotate / EB-Garamond-500), prefers-reduced-motion coverage (3 CSS blocks + 9 useReducedMotion guards + 12 .hud-* keyframes), focus-visible system (14 refs + 6 .agent-mode-scope refs), carry-forward inventory (focus.ts / GlobalHotkeys / CommandMenu / use-undo-toast / /api/health / /insights / recharts / template.tsx all preserved), build + 237/237 tests pass, code-level responsive ≥768px (grid + breakpoint utilities verified). All 7 audits PASS.

**Bonus 1 — HudCoreBubble arc-reactor (commit `df1b850`)**
Created in response to user feedback during the manual walkthrough ("need a proper central jarvis bubble thingy"). 280px SVG composition: outer instrument-scale ring with 24 tick marks (slow 80s rotation), middle dashed ring (faster 6s rotation when active), inner glow disc + central triangle (4s breathing). Reactive state prop; opacity dims 0.7 → 0.18 once scrollback has turns. prefers-reduced-motion compliant. **Likely removed or scoped down in Phase 6.2** per the Anthropic-restraint pivot — kept here as the historical record of the iteration.

**Bonus 2 — intentionality.io pattern adoption (commits `97573d7` + `21cdef4`)**
Stolen from github.com/Alienware2000/intentionality (Iron Man HUD-inspired open-source reference): `--hud-cyan-rgb` triplet, `--glow-hud-subtle/medium/strong` tokens, `@custom-variant pointer-fine`, layered ambient HUD background scoped to `.agent-mode-scope::before + ::after`, `.font-mono-stats` (tabular + lining figures), `.custom-scrollbar`, `.scrollbar-hidden`, `.theme-color-scheme` utilities. Applied `.font-mono-stats` to HudEdgeInstrumentation + /health JSON readout rows. **The utility classes + custom variant stay in Phase 6.2 as foundational infrastructure; the layered HUD background may be removed or replaced.**

## What was deferred (and why)

**Task 3 — End-to-end human verification (22 scenarios).**

User reviewed the live UI at the Task 3 checkpoint on 2026-05-19 and rejected the cumulative Phase 6.1 visual surface with: *"i need you to do a massive refactor. research properly the UI of claude code / anthropic + notion paired with something like what jarvis from tony stark has. do another phase. i still do not like how it looks and everything feels clunky and blah."*

This is the **second visual contract rejection in a row** (Phase 6's neumorphic + JARVIS-blue rejected on 2026-05-19 morning; Phase 6.1's HUD-heavy rejected on 2026-05-19 evening). The pattern is unmistakable: this user wants Anthropic-level interaction polish + Notion document discipline + JARVIS as atmospheric mood only, NOT literal HUD vocabulary. Running the 22-scenario walkthrough against a contract the user has already rejected wholesale produces no signal — only confirmation of what they already said.

Task 3 is **deferred to Phase 6.2's verification pass** against the new contract. The walkthrough scenarios + felt-quality criteria + responsive verification get re-anchored against 6.2's UI-SPEC (Anthropic + Notion + Claude Code reference triumvirate; JARVIS atmospheric mood only).

## Phase 6.1 outcome (this is the second visual rebuild that did not land)

Phase 6.1 shipped its non-visual deliverables — token foundation cleanup, focus-visible system mechanism, motion library substrate, shared HUD primitive scaffolding, AES-04 brand-voice copy pass, document surface restyle (Tasks/Captures/Projects/Settings — Notion-leaning was correct direction), shadcn primitive restyles (Button/Card/Input/Dialog/Popover/Sheet/Tooltip), error boundary restyle, page transition vocabulary, automated verification infrastructure, **plus** the universally-useful intentionality.io utility-class infrastructure.

What did NOT land: the cumulative HUD-heavy visual surface — corner crops on every agent surface, hex-grid layered background, edge instrumentation rail, arc-reactor centerpiece, 7-state JARVIS Console motion machine. These executed technically correctly per the Phase 6.1 UI-SPEC but together produced "clunky and blah" felt-quality. The discipline pole was wrong (Linear/Vercel/Stark) and the cumulative effect was overwhelming.

Phase 6.2 inserts after 6.1 to rebuild the visual surface with Anthropic (claude.ai, claude.com, Claude Code CLI, console.anthropic.com) as the new discipline pole, Notion as content-frame, and JARVIS as atmospheric mood only.

## Deviations

None on the mechanical Task 1 + Task 2 work. The bonus HudCoreBubble + intentionality patterns were direct response to user feedback during the walkthrough, not plan deviations.

## Quality gates

- `pnpm --filter web typecheck` exits 0
- `pnpm --filter web build` succeeds (all 20 routes register)
- `pnpm --filter web test` 237 passed, 1 skipped (no regressions from Phase 6 baseline)
- All 5 commits present in `main` (a56b597 + 1e12f92 + df1b850 + 97573d7 + 21cdef4)

## Next

Phase 6.1 closes here. Phase 6.2 (`anthropic-discipline-rebuild`) inserts via `/gsd:insert-phase 06.1` for a fresh research-first rebuild with the new triumvirate reference set. Run `/gsd:research-phase 06.2` then `/gsd:ui-phase 06.2` then `/gsd:plan-phase 06.2` then `/gsd:execute-phase 06.2`.
