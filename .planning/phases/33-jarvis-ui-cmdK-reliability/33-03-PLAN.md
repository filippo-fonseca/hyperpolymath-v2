---
phase: 33-jarvis-ui-cmdK-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
files_modified:
  - apps/web/components/jarvis/JarvisScrollback.tsx
  - apps/web/components/jarvis/ThinkingWord.tsx
  - apps/web/components/shell/AppShell.tsx
autonomous: false
requirements:
  - JAR-UI-04
  - JAR-UI-05

must_haves:
  truths:
    - "ThinkingWord.tsx is deleted — no import references remain in the codebase"
    - "JarvisSidePanel aside in AppShell.tsx has agent-mode-scope so the ambient background renders in split-screen"
    - "Full cmd+K → /today path works end-to-end: send → navigate → streaming visible → receipt + 5s undo → retry on error"
  artifacts:
    - path: "apps/web/components/shell/AppShell.tsx"
      provides: "agent-mode-scope on the JARVIS side-panel aside"
      contains: "agent-mode-scope"
  key_links:
    - from: "AppShell.tsx aside"
      to: "agent-mode-scope class"
      via: "className on the aside that contains JarvisSidePanel"
      pattern: "agent-mode-scope"
---

<objective>
Final integration wave: dead-code cleanup, split-screen ambient fix, and a full end-to-end smoke checkpoint covering all Phase 33 success criteria.

Three targeted changes:

1. Delete ThinkingWord.tsx. The component was retired in Phase 6.1 (replaced by HudThinkingRing). It is only referenced in a comment inside JarvisScrollback.tsx (a mention of the old Phase 5 indicator), not imported anywhere. Remove the comment reference in JarvisScrollback.tsx, then delete the file.

2. Fix JarvisSidePanel ambient background. AppShell.tsx renders the JARVIS split-screen panel in an `aside` element (the `splitOn && !onJarvis` branch). That aside currently uses `bg-[var(--canvas)]` but lacks `agent-mode-scope`, so the hex dot grid + corner cyan radial glows don't render in split-screen mode. Add `agent-mode-scope` to the aside className so the ambient background is present in split-screen.

3. Full-path checkpoint: exercise the complete scenario from CONTEXT.md §Deliverable B — cmd+K → navigate → stream → undo — plus verify the visual deliverables from §Deliverable A.

Purpose: After this plan, all JAR-UI-04 (streaming indicator placement), JAR-UI-05 (split-screen compatibility), and JAR-REL-01 through JAR-REL-04 (reliability) are satisfied and verified by the human checkpoint.

Output: One deleted file, one minor AppShell change, one comment removal in JarvisScrollback, and a final smoke checkpoint. Commit each change separately.
</objective>

<execution_context>
@/Users/filippofonseca/.claude/gsd-core/workflows/execute-plan.md
@/Users/filippofonseca/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-jarvis-ui-cmdK-reliability/33-CONTEXT.md
@.planning/phases/33-jarvis-ui-cmdK-reliability/33-RESEARCH-ui.md
@.planning/phases/33-jarvis-ui-cmdK-reliability/33-01-SUMMARY.md
@.planning/phases/33-jarvis-ui-cmdK-reliability/33-02-SUMMARY.md
@apps/web/components/shell/AppShell.tsx
@apps/web/components/jarvis/JarvisScrollback.tsx
@apps/web/components/jarvis/ThinkingWord.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ThinkingWord dead code + fix split-screen ambient background</name>
  <files>apps/web/components/jarvis/ThinkingWord.tsx, apps/web/components/jarvis/JarvisScrollback.tsx, apps/web/components/shell/AppShell.tsx</files>
  <action>
    STEP A — ThinkingWord cleanup:
    Confirm ThinkingWord is not imported anywhere by running:
      grep -rn "ThinkingWord" apps/web/
    The only references should be: the file itself, and a comment in JarvisScrollback.tsx (Phase 5 indicator mention). Verify there are zero import statements.

    In JarvisScrollback.tsx, find the comment block that references `<ThinkingWord />` (around line 385-388 in the Phase 6.1 annotation). Remove or update the comment so it no longer references the deleted component — change the comment to just say "HudThinkingRing replaced the Phase 5 thinking indicator" (one line is fine).

    Delete the file:
      rm apps/web/components/jarvis/ThinkingWord.tsx

    Commit: chore(jarvis): delete retired ThinkingWord component (replaced by HudThinkingRing in Phase 6.1)

    STEP B — Split-screen ambient background:
    Open AppShell.tsx. Find the aside that contains JarvisSidePanel — look for the className that includes `bg-[var(--canvas)]` in the split-screen branch (when `splitOn && !onJarvis`).

    Add `agent-mode-scope` to that aside's className alongside the existing classes. Do not remove any existing classes. The `agent-mode-scope` pseudo-element rules in globals.css will then activate the hex dot grid and corner cyan radial glows inside the side panel, matching the full-page /today experience.

    Verify globals.css contains `.agent-mode-scope::before` and `.agent-mode-scope::after` selectors (they do — confirmed in research). No CSS change is needed.

    Commit: fix(shell): add agent-mode-scope to JarvisSidePanel aside for split-screen ambient background
  </action>
  <verify>
    <automated>grep -rn "import.*ThinkingWord\|from.*ThinkingWord" apps/web/ 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <done>ThinkingWord.tsx is deleted. No import of ThinkingWord exists in the codebase. AppShell.tsx aside for the JARVIS split panel contains agent-mode-scope.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 33 deliverables: iMessage-style bubble layout, cyan JARVIS glow, glass composer, cmd+K reliability fix, split-screen ambient background, ThinkingWord deleted.
  </what-built>
  <how-to-verify>
    Run `pnpm --filter web dev` and verify each scenario below:

    SCENARIO 1 — cmd+K full path (JAR-REL-01 through JAR-REL-04):
    1. Navigate to /captures (any non-/today route)
    2. Press ⌘K — the dialog opens
    3. Type "add task: test the cmd+K reliability fix" and press ⌘⏎
    4. The dialog closes and the browser navigates to /today
    5. The composer auto-submits the text; you see the user message bubble RIGHT-ALIGNED
    6. The JARVIS label in cyan appears above a left-aligned glass bubble
    7. HudThinkingRing appears inside the bubble while JARVIS thinks
    8. Streaming text appears inside the bubble
    9. A receipt card appears inside the bubble column after streaming
    10. The receipt has a 5s "Undo (N)" countdown button — click it within 5s and verify the action undoes

    SCENARIO 2 — Navigation during stream (JAR-REL-01):
    1. On /captures, press ⌘K and type a longer message (something that takes 3-4 seconds to respond)
    2. Press ⌘⏎ — dialog closes, router pushes to /today
    3. Verify the user bubble appears IMMEDIATELY on /today
    4. Verify you see the stream arriving (JARVIS bubble + thinking ring → streaming text)
    5. Verify the turn completes normally with receipt + undo

    SCENARIO 3 — Visual audit (JAR-UI-01 through JAR-UI-05):
    1. On /today with an existing conversation: user bubbles RIGHT-ALIGNED in glass (no > prefix)
    2. JARVIS bubbles LEFT-ALIGNED with "JARVIS" label in cyan above, subtle ambient glow around the bubble
    3. Composer strip is visually translucent / glassy (not flat gray bg-card)
    4. Click composer with no text: focus ring pulses with the hud-focus-breathe animation
    5. Enable split-screen view: the JARVIS side panel should show the ambient hex grid + corner cyan glows (previously it was plain dark background)
    6. Date dividers, "Older messages", and empty state are visually unchanged

    SCENARIO 4 — Error and retry (JAR-REL-04):
    If you can trigger an error turn (e.g. disconnect network mid-stream), verify:
    - The failed JARVIS bubble shows a coral left border + hud-error-glitch jitter
    - A "↺ Retry" button is visible inside the bubble
    - Clicking Retry re-submits the message and a new turn begins

    TypeScript check: `pnpm --filter web build` passes with zero type errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass. Describe any failures in detail for a targeted fix before merging.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppShell aside className | CSS-only change; agent-mode-scope only activates pseudo-element backgrounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03-01 | Information Disclosure | agent-mode-scope ambient in split-screen | accept | Decorative CSS pseudo-elements only; no data exposure |
| T-33-03-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
All four scenarios in the checkpoint task above must pass before this plan is marked complete. TypeScript build (`pnpm --filter web build`) must exit 0.
</verification>

<success_criteria>
- JAR-UI-04: Streaming indicator (HudThinkingRing) renders cleanly inside the JARVIS bubble, not outside it
- JAR-UI-05: Split-screen JarvisSidePanel shows agent-mode-scope ambient background
- ThinkingWord.tsx is deleted with zero remaining import references
- End-to-end: cmd+K → navigate → streaming → receipt → 5s undo → retry all work
- pnpm --filter web build exits 0
- Human checkpoint approved before branch is considered merge-ready
</success_criteria>

<output>
Create `.planning/phases/33-jarvis-ui-cmdK-reliability/33-03-SUMMARY.md` when done.
</output>
