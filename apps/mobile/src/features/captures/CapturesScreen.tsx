import React from "react";

import { EmptyState, Screen, ScreenHeader } from "@/ui";

/** Placeholder — the captures lane replaces this file wholesale. */
export default function CapturesScreen() {
  return (
    <Screen>
      <ScreenHeader title="Captures" />
      <EmptyState title="Coming together…" caption="Quick thoughts land here." />
    </Screen>
  );
}
