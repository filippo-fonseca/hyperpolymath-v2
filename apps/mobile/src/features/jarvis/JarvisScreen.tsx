import React from "react";

import { EmptyState, Screen, ScreenHeader } from "@/ui";

/** Placeholder — the jarvis lane replaces this file wholesale. */
export default function JarvisScreen() {
  return (
    <Screen>
      <ScreenHeader eyebrow="Hyperpolymath" title="Jarvis" />
      <EmptyState title="Coming together…" caption="The Jarvis console lands here." />
    </Screen>
  );
}
