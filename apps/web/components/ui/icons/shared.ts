/**
 * Re-export shim for the shared recipe, now in `@hyperpolymath/ui-icons`.
 * Web icons authored outside this directory (CalendarIcon, NutritionIcon) import
 * the recipe from `@/components/ui/icons/shared`; this keeps that path valid.
 */
export * from "@hyperpolymath/ui-icons/shared";
