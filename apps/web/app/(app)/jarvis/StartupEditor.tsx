"use client";

import type { StartupConfig } from "@hyperpolymath/jarvis-core";

interface Props {
  initial: StartupConfig;
}

// Filled in by the Startup & Briefing editor commit.
export function StartupEditor(_props: Props) {
  return null;
}
