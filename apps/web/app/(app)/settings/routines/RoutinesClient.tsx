"use client";

import type { Routine } from "@hyperpolymath/jarvis-core";

interface Props {
  userId: string;
  initialRoutines: Routine[];
}

// Shell — the list, editor, and query wiring land in subsequent commits.
export function RoutinesClient(_props: Props) {
  return null;
}
