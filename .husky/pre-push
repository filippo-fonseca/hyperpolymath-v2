#!/usr/bin/env sh
# >>> kiwi-autodev pre-push guard >>>
# Installed by tools/kiwi-autodev/install-hook.sh. Delegates to the sidecar
# guard. Safe to remove this whole file if you no longer want the guard.
# Resolve relative to this hook so cloud/CI agents (and any non-Mac clone)
# work — the previous absolute /Users/filippofonseca/... path only ran locally.
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$DIR/kiwi-autodev-pre-push" "$@"
# <<< kiwi-autodev pre-push guard <<<
