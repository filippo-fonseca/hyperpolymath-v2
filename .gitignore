resources/

# Node / pnpm
node_modules/
.pnpm-store/

# Next.js
.next/
.turbo/
next-env.d.ts

# Build outputs
dist/
build/

# Vercel
.vercel/

# Env files (NEVER commit — D-08, FOUND-05, Pitfall 11)
.env
.env.local
.env.*.local
.env.development
.env.production
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Testing
coverage/
.vitest-cache/

# Local-only deployment notes (keys, URL flips, etc. — not for sharing)
DEPLOYMENT.md

# Supabase CLI local scratch state (generated by `supabase` CLI at repo root)
/supabase/

# Local Claude Code runtime state (lock files, ephemeral scratch)
.claude/scheduled_tasks.lock
.claude/*.lock

# v1 reference checkout — kept locally for cross-reference, not for the repo
/old-v1/
